# Technology Stack

**Analysis Date:** 2026-02-26

## Languages

**Primary:**
- **R** 4.5.1 - Application logic, data processing, statistical analysis
  - Location: `C:\Program Files\R\R-4.5.1\bin\`
  - Used for: Shiny app, file I/O, data detection algorithms

**Secondary:**
- **Python** - Data cleaning scripts (historical/utility)
  - Files: `clean_chems.py`, `clean_chems_2.R`
  - Status: Utility scripts, not part of main application flow

## Runtime

**Environment:**
- R 4.5.1 (critical - security requirement per CLAUDE.md)
- Shiny runtime (embedded in R packages)

**Package Manager:**
- **pak** 0.x - Efficient package installation and dependency management
  - Auto-installed if missing (`load_packages.R` lines 4-15)
  - Handles OS-specific binary repositories for faster installation

- **remotes** - GitHub package installation
  - Used for ComptoxR installation from GitHub

**Lockfile:**
- Not present (uses dynamic pak-based installation)
- No renv or packrat lock system in place

## Frameworks

**Core UI/Web:**
- **Shiny** - Interactive web application framework for R
  - Used for: File upload, data preview, header detection interface
  - Entry point: `app.R`
  - Configuration: `bslib` for Bootstrap 5 theming (line 50-53)
  - File upload limit: 50MB (configurable at `app.R` line 40)

**UI Components:**
- **bslib** 0.5+ - Bootstrap 5 theming and layouts
  - Theme: Flatly bootswatch theme
  - Primary color: #007bff
  - Layout: page_sidebar with tabs

- **bsicons** - Icon library for UI elements
  - Provides Icons for buttons and UI components

- **DT** - DataTables for HTML table rendering
  - Used for displaying preview tables and detection results

- **shinyjs** - JavaScript helpers for Shiny
  - DOM manipulation and client-side interactions
  - Loaded with `useShinyjs()` in UI (`app.R` line 47)

**Data Processing:**
- **tidyverse** ecosystem - Data transformation and manipulation
  - **dplyr** - Data frame operations, filtering, mutating
  - **tidyr** - Data reshaping (fill, pivot operations)
  - **tibble** - Modern data frame implementation
  - **purrr** - Functional programming, safe operations with `safely()`
  - **stringr** - String manipulation for pattern detection
  - **readr** - CSV reading with multiple encoding strategies

- **janitor** - Data cleaning utilities
  - Header name standardization (`clean_names()`)
  - Empty row/column removal (`remove_empty()`)

**File I/O:**
- **rio** - Universal import/export for tabular data
  - Primary fallback reader for multiple formats

- **readxl** - Excel file reading (XLSX, XLS)
  - Fallback strategy for Excel files
  - Used by `read_excel_robust()` in `R/file_handlers.R`

- **writexl** - Excel file writing
  - Used in tests for creating test data

**Utilities:**
- **here** - Relative path management
  - All file references use `here::here()`

- **fs** - Cross-platform file system operations
  - File checking, directory creation

- **digest** - File and object hashing
  - MD5 hashing in checkpoint system (`checkpoint.R`)

- **rlang** - Low-level R utilities
  - Symbol manipulation for non-standard evaluation in `curate_chemical_data()` (`R/curation.R` line 110)

- **tidyselect** - Utilities for working with column selection
  - Column selection operations in curation

**Testing:**
- **testthat** - Unit testing framework
  - Config: Tests located in `tests/` directory
  - Test file: `tests/test_data_detection.R` (11 test cases)
  - Run: `testthat::test_dir("tests")`

**External Integration:**
- **ComptoxR** - Custom R package for EPA Comptox chemistry API
  - GitHub source: `seanthimons/ComptoxR`
  - Installation: `remotes::install_github("seanthimons/ComptoxR@*release")`
  - Purpose: Chemical name lookup and CAS number validation
  - Functions: `ComptoxR::ct_search()`, `ComptoxR::is_cas()`, `ComptoxR::as_cas()`
  - Requires: `ctx_api_key` environment variable (checked at `app.R` line 927)

**Build/Dev Tools:**
- **air** - R code formatter
  - Config file: `air.toml` (line width: 120, indent: 2 spaces)
  - Auto-generated by `load_packages.R` if missing
  - Uses persistent line breaks and UTF-8 line endings

- **devtools** - Development utilities
  - Package installed but not actively used in current implementation

## Key Dependencies

**Critical (Application cannot function without):**
- `shiny` - Web framework
- `tidyverse` (core: dplyr, purrr, readr, tibble, stringr, tidyr)
- `readxl` - Excel support
- `janitor` - Data cleaning
- `ComptoxR` - Chemical data validation and lookup

**Infrastructure:**
- `here` - Path management (all file paths use this)
- `rio` - File I/O fallback
- `bslib`, `bsicons`, `DT`, `shinyjs` - UI components
- `testthat` - Testing
- `digest` - File checksums
- `fs` - File operations
- `rlang` - Programming utilities

**Recommended but optional:**
- `pak` - Package management efficiency
- `remotes` - GitHub package installation
- `devtools` - Development utilities
- `parallel`, `mirai` - Parallel processing (commented out, not used)
- `nanoparquet` - Parquet support (commented out, not used)

## Configuration

**Environment:**
- **ctx_api_key** - ComptoxR API authentication key (required for curation features)
  - Location: System environment variable
  - Checked at: `app.R` line 927
  - Missing key triggers error notification

**Build Configuration:**
- **air.toml** - R code formatting rules
  - Line width: 120 characters
  - Indentation: 2 spaces
  - Line endings: auto-detect
  - Persistent line breaks enabled

**Application Configuration:**
- **shiny.maxRequestSize** - File upload limit
  - Location: `app.R` line 40
  - Default: 50MB (50 * 1024^2 bytes)
  - Configurable via `options(shiny.maxRequestSize = ...)`

**Detection Algorithm Parameters** (in `R/data_detection.R`):
- `min_filled_ratio = 0.7` (line 11) - Minimum fill ratio for heuristic detection
- `min_cols = 3` (line 11) - Minimum filled columns required
- `scan_rows = 50` (line 185) - Type consistency scan depth

**Preview Defaults** (in `R/file_handlers.R`):
- Smart preview rows calculation (lines 139-149):
  - ≤100 rows → preview 50 rows
  - ≤1000 rows → preview 25 rows
  - >1000 rows → preview 10 rows

**R Language Settings:**
- **Encoding**: UTF-8 (CSV reading: `locale = readr::locale(encoding = "UTF-8")`)
- **Latin-1 fallback** enabled for legacy files
- **Delimiter detection**: Comma (,), semicolon (;), tab (\t)

## Platform Requirements

**Development:**
- **OS**: Windows 11 (primary), supports Linux via pak binary repos
- **R Version**: 4.5.1 minimum (earlier versions have security issues)
- **Disk Space**: ~500MB for package installation + data directory
- **Network**: Required for:
  - Package installation from r-universe and CRAN
  - ComptoxR API calls (Comptox dashboard)
  - GitHub package downloads (remotes)

**Production:**
- **Deployment Target**: Shiny Server or Cloud platforms (Shinyapps.io, Posit Cloud)
- **R Runtime**: 4.5.1+
- **Memory**: 2GB+ recommended for large file processing (up to 50MB files)
- **Port**: Configurable (default: 3838 for Shiny Server)

**Package Repository Configuration:**
- **Primary**: r-universe (ropensci, cran universes)
- **Fallback**: CRAN mirrors
- **GitHub**: For ComptoxR custom package
- **Linux Binary Path** (lines 17-22 in `load_packages.R`):
  - URL pattern: `https://{universe}.r-universe.dev/bin/linux/noble-{arch}/{r_version}/`
  - Automatically detected based on R version and architecture

---

*Stack analysis: 2026-02-26*
