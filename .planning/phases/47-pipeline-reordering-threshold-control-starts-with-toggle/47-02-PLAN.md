---
phase: 47-pipeline-reordering-threshold-control-starts-with-toggle
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - R/mod_clean_data.R
  - R/mod_run_curation.R
autonomous: false
requirements:
  - CONF-01
  - TOG-01

must_haves:
  truths:
    - "Pre-flight modal contains a WQX fuzzy threshold slider (0.50-1.00, default 0.85) with a companion numeric input"
    - "Pre-flight modal contains a starts-with toggle switch that is off by default"
    - "Slider and numeric input stay synchronized when either is changed"
    - "Run All Steps reads the current slider/toggle values (does not force starts-with on)"
    - "mod_run_curation.R passes wqx_threshold and starts_with from data_store to run_curation_pipeline()"
    - "Notification text includes WQX match count in the tier breakdown"
  artifacts:
    - path: "R/mod_clean_data.R"
      provides: "Search Settings accordion panel with slider, numeric input, checkbox; extended mask; sync observers"
      contains: "Search Settings"
    - path: "R/mod_run_curation.R"
      provides: "Pipeline call with wqx_threshold + starts_with from data_store; updated notification"
      contains: "n_wqx"
  key_links:
    - from: "R/mod_clean_data.R"
      to: "data_store$wqx_threshold"
      via: "mask assignment in execute_pipeline or direct data_store write"
      pattern: "data_store\\$wqx_threshold"
    - from: "R/mod_run_curation.R"
      to: "R/curation.R"
      via: "run_curation_pipeline(wqx_threshold=, starts_with=)"
      pattern: "wqx_threshold\\s*=\\s*data_store\\$wqx_threshold"
---

<objective>
Add the "Search Settings" accordion panel to the pre-flight modal with WQX threshold slider+numeric and starts-with toggle, wire the values through to run_curation_pipeline() in both the cleaning path (mod_clean_data.R) and the curation path (mod_run_curation.R), and update the notification string.

Purpose: Users can configure fuzzy match sensitivity and opt into starts-with search directly from the pre-flight UI. The settings persist in data_store so the curation module uses them when executing separately.
Output: Modified R/mod_clean_data.R and R/mod_run_curation.R with full UI+wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-CONTEXT.md
@.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-PATTERNS.md
@.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — the new signatures the UI must target -->

From R/curation.R (after Plan 01):
```r
run_curation_pipeline <- function(
  clean_data,
  column_tags,
  progress_callback = NULL,
  dedup_only = FALSE,
  wqx_threshold = 0.85,
  starts_with = FALSE
) {
```

From R/mod_clean_data.R (current accordion pattern, lines 294-300):
```r
bslib::accordion(
  id = session$ns("preflight_accordion"),
  open = TRUE,
  multiple = TRUE,
  bslib::accordion_panel(title = "Cleaning Steps", value = "cleaning", cleaning_rows),
  bslib::accordion_panel(title = "Harmonization Steps", value = "harmonization", harmonize_rows)
)
```

From R/mod_clean_data.R (current build_mask_from_inputs, lines 304-318):
```r
build_mask_from_inputs <- function() {
  list(
    unicode = isTRUE(input$step_unicode),
    whitespace = isTRUE(input$step_whitespace),
    cas = isTRUE(input$step_cas),
    names = isTRUE(input$step_names),
    isotopes = isTRUE(input$step_isotopes),
    multi = isTRUE(input$step_multi),
    chiral = isTRUE(input$step_chiral),
    units = isTRUE(input$step_units),
    duration = isTRUE(input$step_duration),
    dates = isTRUE(input$step_dates),
    media = isTRUE(input$step_media)
  )
}
```

From R/mod_clean_data.R (current run_all observer, lines 631-645):
```r
observeEvent(input$run_all, {
  mask <- list(
    unicode = TRUE, whitespace = TRUE, cas = TRUE, names = TRUE,
    isotopes = TRUE, multi = TRUE, chiral = TRUE,
    units = TRUE, duration = TRUE, dates = TRUE, media = TRUE
  )
  execute_pipeline(mask)
})
```

From R/mod_run_curation.R (current pipeline call, lines 161-166):
```r
pipeline_result <- run_curation_pipeline(
  clean_data = input_data,
  column_tags = data_store$column_tags,
  progress_callback = progress_callback,
  dedup_only = FALSE
)
```

From R/mod_run_curation.R (current notification, lines 258-264):
```r
notification_msg <- sprintf(
  "Search complete: %d exact, %d CAS, %d starts-with, %d no match",
  pipeline_result$search_summary$n_exact,
  pipeline_result$search_summary$n_cas_valid,
  pipeline_result$search_summary$n_starts_with,
  pipeline_result$search_summary$n_miss
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Search Settings accordion panel, extend mask, add sync observers</name>
  <files>R/mod_clean_data.R</files>
  <read_first>
    - R/mod_clean_data.R (full file — lines 254-318 for modal renderUI + build_mask; lines 631-645 for run_all observer)
    - .planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-PATTERNS.md (exact code blocks for accordion panel, mask extension, sync observers)
  </read_first>
  <action>
Five changes to R/mod_clean_data.R:

**Change 1: Add "Search Settings" accordion panel (per D-03, D-04, D-05)**

In the `output$preflight_checklist` renderUI block, inside the `bslib::accordion(...)` call (around line 294-300), add a THIRD `bslib::accordion_panel` after the "Harmonization Steps" panel:

```r
bslib::accordion(
  id = session$ns("preflight_accordion"),
  open = TRUE,
  multiple = TRUE,
  bslib::accordion_panel(title = "Cleaning Steps", value = "cleaning", cleaning_rows),
  bslib::accordion_panel(title = "Harmonization Steps", value = "harmonization", harmonize_rows),
  bslib::accordion_panel(
    title = "Search Settings",
    value = "search_settings",
    div(
      class = "mb-3",
      tags$label(class = "form-label fw-semibold", "WQX Fuzzy Match Threshold"),
      tags$small(
        class = "text-muted d-block mb-2",
        "Minimum similarity score for fuzzy WQX matches (0.50 = permissive, 1.00 = exact only)"
      ),
      bslib::layout_columns(
        col_widths = c(8, 4),
        sliderInput(
          session$ns("wqx_threshold"),
          label = NULL,
          min = 0.50, max = 1.00, step = 0.01, value = 0.85,
          ticks = FALSE
        ),
        numericInput(
          session$ns("wqx_threshold_num"),
          label = NULL,
          value = 0.85, min = 0.50, max = 1.00, step = 0.01
        )
      )
    ),
    div(
      checkboxInput(
        session$ns("starts_with_enabled"),
        label = "Enable CompTox starts-with search",
        value = FALSE
      ),
      tags$small(
        class = "text-muted",
        "Off by default. Enable for datasets where exact + CAS + WQX resolution is insufficient."
      )
    )
  )
)
```

CRITICAL: All input IDs use `session$ns(...)` because they are inside `renderUI({})`. Without namespacing, `input$wqx_threshold` will be NULL.

**Change 2: Extend `build_mask_from_inputs()` (per D-07)**

Append two fields to the list returned by `build_mask_from_inputs()` (around line 304-318):

```r
build_mask_from_inputs <- function() {
  list(
    unicode = isTRUE(input$step_unicode),
    whitespace = isTRUE(input$step_whitespace),
    cas = isTRUE(input$step_cas),
    names = isTRUE(input$step_names),
    isotopes = isTRUE(input$step_isotopes),
    multi = isTRUE(input$step_multi),
    chiral = isTRUE(input$step_chiral),
    units = isTRUE(input$step_units),
    duration = isTRUE(input$step_duration),
    dates = isTRUE(input$step_dates),
    media = isTRUE(input$step_media),
    wqx_threshold = input$wqx_threshold,
    starts_with = isTRUE(input$starts_with_enabled)
  )
}
```

**Change 3: Extend `run_all` observer mask (per D-07, Pitfall 4)**

In the `observeEvent(input$run_all, {...})` block (around line 631-645), add the search settings to the hardcoded mask. These READ the current input values (do NOT hardcode TRUE/FALSE for starts_with — honor the toggle per RESEARCH.md Open Question 1 recommendation):

```r
observeEvent(input$run_all, {
  mask <- list(
    unicode = TRUE,
    whitespace = TRUE,
    cas = TRUE,
    names = TRUE,
    isotopes = TRUE,
    multi = TRUE,
    chiral = TRUE,
    units = TRUE,
    duration = TRUE,
    dates = TRUE,
    media = TRUE,
    wqx_threshold = input$wqx_threshold,
    starts_with = isTRUE(input$starts_with_enabled)
  )
  execute_pipeline(mask)
})
```

**Change 4: Add slider/numeric sync observers**

Place these two `observeEvent` blocks AFTER the `build_mask_from_inputs` function definition (around line 319), inside the module server function:

```r
# Sync slider -> numeric
observeEvent(input$wqx_threshold, {
  updateNumericInput(session, "wqx_threshold_num", value = input$wqx_threshold)
}, ignoreInit = TRUE)

# Sync numeric -> slider (with bounds guard)
observeEvent(input$wqx_threshold_num, {
  val <- input$wqx_threshold_num
  if (!is.null(val) && !is.na(val) && val >= 0.50 && val <= 1.00) {
    updateSliderInput(session, "wqx_threshold", value = val)
  }
}, ignoreInit = TRUE)
```

`ignoreInit = TRUE` prevents the observers from firing when the modal first renders. The bounds check on the numeric path prevents invalid out-of-range values from propagating.

**Change 5: Store search settings in data_store for mod_run_curation.R**

Inside `execute_pipeline(mask)` function, BEFORE the `removeModal()` call or at the beginning of the tryCatch block (around line 322-326), store the search settings into data_store:

```r
execute_pipeline <- function(mask) {
  # Store search settings for mod_run_curation.R to use
  data_store$wqx_threshold <- mask$wqx_threshold %||% 0.85
  data_store$starts_with <- isTRUE(mask$starts_with)

  removeModal()
  ...
```

The `%||%` operator (from rlang, already a dependency) provides a safe fallback if wqx_threshold is somehow NULL.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('mod_clean_data.R loads OK\n')"</automated>
  </verify>
  <done>
    - Pre-flight modal accordion has three panels: Cleaning Steps, Harmonization Steps, Search Settings
    - Search Settings panel contains sliderInput "wqx_threshold" (0.50-1.00, default 0.85) and numericInput "wqx_threshold_num"
    - Search Settings panel contains checkboxInput "starts_with_enabled" (default FALSE)
    - build_mask_from_inputs() returns wqx_threshold and starts_with fields
    - run_all mask reads input$wqx_threshold and input$starts_with_enabled
    - Two sync observers keep slider and numeric in sync
    - execute_pipeline stores wqx_threshold and starts_with into data_store
  </done>
  <acceptance_criteria>
    - R/mod_clean_data.R contains the string "Search Settings" (accordion panel title)
    - R/mod_clean_data.R contains 'session$ns("wqx_threshold")' (namespaced slider ID)
    - R/mod_clean_data.R contains 'session$ns("wqx_threshold_num")' (namespaced numeric ID)
    - R/mod_clean_data.R contains 'session$ns("starts_with_enabled")' (namespaced checkbox ID)
    - R/mod_clean_data.R contains "min = 0.50, max = 1.00, step = 0.01, value = 0.85" (slider config)
    - R/mod_clean_data.R contains "value = FALSE" in the starts_with_enabled checkboxInput
    - R/mod_clean_data.R build_mask_from_inputs contains "wqx_threshold = input$wqx_threshold"
    - R/mod_clean_data.R build_mask_from_inputs contains "starts_with = isTRUE(input$starts_with_enabled)"
    - R/mod_clean_data.R run_all observer mask contains "wqx_threshold = input$wqx_threshold"
    - R/mod_clean_data.R contains "observeEvent(input$wqx_threshold" (sync observer)
    - R/mod_clean_data.R contains "observeEvent(input$wqx_threshold_num" (sync observer)
    - R/mod_clean_data.R contains "data_store$wqx_threshold" (storage for curation module)
    - devtools::load_all() succeeds without error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire parameters through mod_run_curation.R and update notification</name>
  <files>R/mod_run_curation.R</files>
  <read_first>
    - R/mod_run_curation.R (full file — lines 155-166 for pipeline call, lines 258-264 for notification)
    - .planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-PATTERNS.md (exact replacement patterns for this file)
  </read_first>
  <action>
Two changes to R/mod_run_curation.R:

**Change 1: Pass wqx_threshold and starts_with from data_store to run_curation_pipeline()**

Replace the pipeline call at lines 161-166:

```r
pipeline_result <- run_curation_pipeline(
  clean_data = input_data,
  column_tags = data_store$column_tags,
  progress_callback = progress_callback,
  dedup_only = FALSE,
  wqx_threshold = data_store$wqx_threshold %||% 0.85,
  starts_with = isTRUE(data_store$starts_with)
)
```

The `%||% 0.85` fallback handles the case where `data_store$wqx_threshold` is NULL (e.g., user clicked "Start Curation" without going through the pre-flight modal first). `isTRUE()` safely converts NULL to FALSE for `starts_with`.

**Change 2: Update notification string to include WQX count**

Replace the notification_msg at lines 258-264:

```r
notification_msg <- sprintf(
  "Search complete: %d exact, %d CAS, %d WQX, %d starts-with, %d no match",
  pipeline_result$search_summary$n_exact,
  pipeline_result$search_summary$n_cas_valid,
  pipeline_result$search_summary$n_wqx,
  pipeline_result$search_summary$n_starts_with,
  pipeline_result$search_summary$n_miss
)
```

This adds `%d WQX` with `pipeline_result$search_summary$n_wqx` (already populated by run_curation_pipeline since Phase 45).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('mod_run_curation.R loads OK\n')"</automated>
  </verify>
  <done>
    - run_curation_pipeline() call in mod_run_curation.R includes wqx_threshold and starts_with args
    - wqx_threshold reads from data_store$wqx_threshold with 0.85 fallback
    - starts_with reads from data_store$starts_with wrapped in isTRUE()
    - Notification string includes "%d WQX" between CAS and starts-with counts
  </done>
  <acceptance_criteria>
    - R/mod_run_curation.R contains "wqx_threshold = data_store$wqx_threshold" in the run_curation_pipeline call
    - R/mod_run_curation.R contains "starts_with = isTRUE(data_store$starts_with)" in the run_curation_pipeline call
    - R/mod_run_curation.R contains the string "%d WQX" in the notification sprintf
    - R/mod_run_curation.R contains "pipeline_result$search_summary$n_wqx" in the sprintf arguments
    - devtools::load_all() succeeds without error
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Shiny app cold boot and modal rendering</name>
  <files>R/mod_clean_data.R, R/mod_run_curation.R</files>
  <action>
Run Shiny cold boot smoke test and verify the pre-flight modal renders correctly with the new Search Settings accordion panel. Confirm slider/numeric sync works, and the pipeline runs end-to-end with WQX count in the notification.

Verification steps:
1. Start the app: run `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)"` in background
2. Open http://localhost:3838 in browser
3. Upload a test file (e.g., sswqs.xlsx or any chemical data file)
4. Tag columns and click "Run Pipeline" to open the pre-flight modal
5. Verify the modal shows three accordion sections: Cleaning Steps, Harmonization Steps, Search Settings
6. Verify the Search Settings section contains:
   - A slider labeled "WQX Fuzzy Match Threshold" at 0.85
   - A numeric input showing 0.85
   - A checkbox "Enable CompTox starts-with search" that is UNCHECKED
7. Move the slider to 0.70 — verify the numeric input updates to 0.70
8. Type 0.90 into the numeric input — verify the slider moves to 0.90
9. Click "Run All Steps" and observe that curation runs successfully
10. Check the notification includes "WQX" in the tier breakdown string
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)"</automated>
  </verify>
  <done>App starts without error. Modal renders Search Settings accordion with slider, numeric input, and toggle. Pipeline runs end-to-end with WQX in notification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Shiny server | Slider/numeric/checkbox values cross from client to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Tampering | wqx_threshold slider input | accept | Server-side Shiny slider enforces min/max bounds (0.50-1.00). Even a crafted value outside bounds would only affect match sensitivity -- no code injection or data corruption path. The numeric input has an additional bounds guard in the observeEvent handler. |
| T-47-04 | Information Disclosure | Notification text | accept | Notification shows only aggregate counts (n_exact, n_wqx, etc.), no PII or sensitive chemical identity data. |
</threat_model>

<verification>
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)"
```
App starts without error. Pre-flight modal renders Search Settings accordion with slider at 0.85 and toggle off. Full pipeline runs successfully with WQX count in notification.
</verification>

<success_criteria>
- Pre-flight modal has "Search Settings" accordion panel with slider (0.50-1.00, default 0.85) and starts-with toggle (default OFF)
- Slider and numeric input stay synchronized
- Run All Steps honors current toggle position (does not force starts-with on)
- mod_run_curation.R passes data_store values to run_curation_pipeline()
- Notification string includes WQX match count
- Shiny app cold boots without error
</success_criteria>

<output>
After completion, create `.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-02-SUMMARY.md`
</output>
