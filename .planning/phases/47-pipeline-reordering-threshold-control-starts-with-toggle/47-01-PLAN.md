---
phase: 47-pipeline-reordering-threshold-control-starts-with-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/curation.R
  - R/curate_headless.R
  - tests/testthat/test-pipeline-reorder-toggle.R
autonomous: true
requirements:
  - ORD-01
  - ORD-02
  - CONF-02
  - TOG-02

must_haves:
  truths:
    - "WQX matching tier runs before CompTox starts-with in run_curation_pipeline()"
    - "CompTox starts-with fires only on names still unresolved after WQX matching"
    - "Pipeline passes user-configured threshold to match_wqx() instead of hardcoded default"
    - "Pipeline skips starts-with search when starts_with = FALSE"
    - "curate_headless() exposes wqx_threshold and starts_with arguments and threads them to run_curation_pipeline()"
  artifacts:
    - path: "R/curation.R"
      provides: "Reordered pipeline: WQX at Tier 3, starts-with at Tier 4 gated by flag"
      contains: "threshold = wqx_threshold"
    - path: "R/curate_headless.R"
      provides: "Headless API with wqx_threshold and starts_with params"
      contains: "wqx_threshold = 0.85"
    - path: "tests/testthat/test-pipeline-reorder-toggle.R"
      provides: "Unit tests for tier ordering, threshold passthrough, and toggle"
      min_lines: 50
  key_links:
    - from: "R/curate_headless.R"
      to: "R/curation.R"
      via: "run_curation_pipeline(wqx_threshold=, starts_with=)"
      pattern: "run_curation_pipeline.*wqx_threshold"
    - from: "R/curation.R"
      to: "R/wqx_matching.R"
      via: "match_wqx(threshold = wqx_threshold)"
      pattern: "match_wqx.*threshold\\s*=\\s*wqx_threshold"
---

<objective>
Reorder the curation pipeline so WQX matching fires as Tier 3 (before starts-with), add wqx_threshold and starts_with parameters to run_curation_pipeline() and curate_headless(), and gate starts-with execution on the boolean flag.

Purpose: Ensures WQX dictionary matches resolve names before the expensive/noisy CompTox starts-with tier runs, gives users programmatic control over fuzzy threshold, and makes starts-with opt-in.
Output: Modified R/curation.R and R/curate_headless.R with new parameter signatures, reordered tiers, and a new test file validating all behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-CONTEXT.md
@.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-PATTERNS.md

<interfaces>
<!-- Key signatures the executor needs -->

From R/curation.R (current signature, line 629):
```r
run_curation_pipeline <- function(clean_data, column_tags, progress_callback = NULL, dedup_only = FALSE) {
```

From R/wqx_matching.R (line 21 — threshold param already exists):
```r
match_wqx <- function(names_vec, wqx_dict, threshold = 0.85, verbose = TRUE) {
```

From R/curate_headless.R (current signature, lines 63-76):
```r
curate_headless <- function(
  input_path, output_path, tag_map,
  skip_flags = NULL, header_row = NULL, reference_lists = NULL,
  verbose = TRUE, harmonize = FALSE, format = "parquet",
  unit_map = NULL, corrections = NULL, media = NULL
) {
```

From R/curation.R (current call to match_wqx, line 763):
```r
wqx_raw <- match_wqx(final_missed, wqx_dict, verbose = FALSE)
```

From R/curate_headless.R (current pipeline call, line 176):
```r
pipeline_result <- run_curation_pipeline(cleaning_result$cleaned_data, merged_tags)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test file for pipeline reorder, threshold passthrough, and toggle</name>
  <files>tests/testthat/test-pipeline-reorder-toggle.R</files>
  <read_first>
    - R/curation.R (lines 629-801 — current pipeline signature and tier blocks)
    - R/wqx_matching.R (lines 1-30 — match_wqx signature with threshold param)
    - tests/testthat/test-wqx-pipeline-integration.R (existing test patterns for mocking)
  </read_first>
  <behavior>
    - Test 1 (ORD-01): When run_curation_pipeline() is called with names that WQX can resolve, the result contains source_tier starting with "wqx_" for those names (not "starts_with")
    - Test 2 (ORD-02): When starts_with=TRUE and WQX resolves a name, the starts-with tier does NOT receive that name (it gets only post-WQX misses)
    - Test 3 (CONF-02): When wqx_threshold=0.60 is passed, match_wqx() receives threshold=0.60 (not the hardcoded 0.85)
    - Test 4 (TOG-02): When starts_with=FALSE, no rows have source_tier=="starts_with" even if unresolved names remain after WQX
    - Test 5 (TOG-02): When starts_with=TRUE and names remain after WQX, those names DO get starts_with tier treatment
  </behavior>
  <action>
Create `tests/testthat/test-pipeline-reorder-toggle.R` with the following test structure:

1. A helper function at the top that builds minimal test data compatible with `run_curation_pipeline()`:
```r
make_test_data <- function(names_vec) {
  df <- data.frame(Chemical = names_vec, stringsAsFactors = FALSE)
  tags <- c(Chemical = "Name")
  list(df = df, tags = tags)
}
```

2. Use `testthat::local_mocked_bindings()` to mock:
   - `search_exact()` -> returns 0-row tibble (forces fallback)
   - `validate_and_lookup_cas()` -> returns 0-row tibble (forces fallback)
   - `search_starts_with()` -> records what names it receives + returns 0-row tibble
   - `load_wqx_dictionary()` -> returns a minimal data.frame with known names
   - `match_wqx()` -> returns a data.frame with `match_tier = "exact"` for known names and `match_tier = "none"` for unknown names; captures `threshold` argument for assertion

3. Five test_that blocks:
   - "WQX tier resolves names before starts-with (ORD-01)"
   - "Names resolved by WQX never reach starts-with (ORD-02)"
   - "wqx_threshold parameter passes through to match_wqx (CONF-02)"
   - "starts_with=FALSE skips starts-with entirely (TOG-02)"
   - "starts_with=TRUE allows starts-with on post-WQX misses (TOG-02)"

Tests will initially FAIL (RED) because `run_curation_pipeline()` does not yet accept `wqx_threshold` or `starts_with` params. This is expected — Task 2 implements the changes.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); tryCatch(testthat::test_file('tests/testthat/test-pipeline-reorder-toggle.R'), error = function(e) cat('Tests exist but fail (expected RED):', e$message))"</automated>
  </verify>
  <done>File tests/testthat/test-pipeline-reorder-toggle.R exists with 5 test_that blocks covering ORD-01, ORD-02, CONF-02, TOG-02</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reorder pipeline tiers and add wqx_threshold + starts_with parameters</name>
  <files>R/curation.R, R/curate_headless.R</files>
  <read_first>
    - R/curation.R (full file — especially lines 629, 737-801 for current tier blocks)
    - R/curate_headless.R (full file — lines 63-76 for signature, line 176 for pipeline call)
    - .planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-PATTERNS.md (exact replacement code blocks)
  </read_first>
  <behavior>
    - All 5 tests from Task 1 pass (GREEN)
    - run_curation_pipeline(wqx_threshold=0.60, starts_with=TRUE) threads threshold to match_wqx and gates starts-with
    - curate_headless(wqx_threshold=0.75, starts_with=TRUE) threads both params to run_curation_pipeline
  </behavior>
  <action>
**R/curation.R changes:**

1. Update function signature at line 629 to add two new parameters:
```r
run_curation_pipeline <- function(
  clean_data,
  column_tags,
  progress_callback = NULL,
  dedup_only = FALSE,
  wqx_threshold = 0.85,
  starts_with = FALSE
) {
```

2. Replace the entire block from line 737 through line 801 (current Tier 3: Starts-with + Tier 3b: WQX) with the reordered block. The new structure is:

```r
      # Tier 3: WQX — no character minimum (D-01: local dictionary, no API cost)
      if (length(still_missed) > 0) {
        cache_dir <- system.file("extdata", "reference_cache", package = "chemreg")
        wqx_dict <- load_wqx_dictionary(cache_dir)
        wqx_raw <- match_wqx(still_missed, wqx_dict, threshold = wqx_threshold, verbose = FALSE)

        wqx_resolved <- wqx_raw[wqx_raw$match_tier != "none", ]
        n_wqx <- nrow(wqx_resolved)

        if (n_wqx > 0) {
          wqx_rows <- tibble::tibble(
            searchValue = wqx_resolved$input_name,
            dtxsid = NA_character_,
            preferredName = wqx_resolved$wqx_name,
            searchName = NA_character_,
            rank = NA_integer_,
            source_tier = paste0("wqx_", wqx_resolved$match_tier)
          )
          all_results[[length(all_results) + 1]] <- wqx_rows
        }

        wqx_matched_names <- wqx_resolved$input_name
        final_missed <- setdiff(still_missed, wqx_matched_names)

        if (!is.null(progress_callback)) {
          progress_callback("wqx", sprintf("WQX match: %d more found...", n_wqx))
        }

        # Tier 4: Starts-with — only when enabled AND names remain (D-02, D-05)
        if (starts_with && length(final_missed) > 0) {
          sw_candidates <- final_missed[nchar(final_missed) >= 3]
          if (length(sw_candidates) > 0) {
            sw_results <- search_starts_with(sw_candidates)
            if (nrow(sw_results) > 0) {
              sw_results$source_tier <- "starts_with"
              all_results[[length(all_results) + 1]] <- sw_results
              n_starts_with <- sum(!is.na(sw_results$dtxsid))
            }
            if (!is.null(progress_callback)) {
              progress_callback("starts_with", sprintf("Starts-with: %d more found...", n_starts_with))
            }
            sw_matched <- sw_results$searchValue[!is.na(sw_results$dtxsid)]
            final_missed <- setdiff(final_missed, sw_matched)
          }
        }

        n_miss <- length(final_missed)

        if (length(final_missed) > 0) {
          miss_rows <- tibble::tibble(
            searchValue = final_missed,
            dtxsid = NA_character_,
            preferredName = NA_character_,
            searchName = NA_character_,
            rank = NA_integer_,
            source_tier = "miss"
          )
          all_results[[length(all_results) + 1]] <- miss_rows
        }
      }
```

Key differences from current code:
- WQX receives `still_missed` directly (NOT `final_missed` which was post-starts-with)
- WQX calls `match_wqx(..., threshold = wqx_threshold, ...)` instead of no threshold arg
- Starts-with is nested INSIDE the WQX block and gated by `if (starts_with && ...)`
- Starts-with receives `final_missed` (post-WQX remainder, NOT `still_missed`)
- The 3-char minimum filter stays ONLY on starts-with, not WQX

**R/curate_headless.R changes:**

1. Update function signature (lines 63-76) to append two new arguments after `media = NULL`:
```r
  media = NULL,
  wqx_threshold = 0.85,
  starts_with = FALSE
```

2. Update the `run_curation_pipeline()` call at line 176 to pass both params:
```r
    pipeline_result <- run_curation_pipeline(
      cleaning_result$cleaned_data,
      merged_tags,
      wqx_threshold = wqx_threshold,
      starts_with = starts_with
    )
```

After both modifications, run the test file to confirm GREEN.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-pipeline-reorder-toggle.R')"</automated>
  </verify>
  <done>
    - run_curation_pipeline() signature includes wqx_threshold=0.85 and starts_with=FALSE
    - WQX tier block appears BEFORE starts-with block in R/curation.R
    - match_wqx() call uses threshold = wqx_threshold (not hardcoded)
    - starts-with block is wrapped in if (starts_with && ...)
    - curate_headless() signature includes wqx_threshold=0.85 and starts_with=FALSE
    - curate_headless() passes both to run_curation_pipeline()
    - All 5 tests pass
  </done>
  <acceptance_criteria>
    - R/curation.R contains "wqx_threshold = 0.85" in function formals
    - R/curation.R contains "starts_with = FALSE" in function formals
    - R/curation.R contains "threshold = wqx_threshold" in the match_wqx() call
    - R/curation.R contains "if (starts_with && length(final_missed)" gating starts-with
    - R/curation.R has "Tier 3: WQX" comment BEFORE "Tier 4: Starts-with" comment
    - R/curate_headless.R contains "wqx_threshold = 0.85" in function formals
    - R/curate_headless.R contains "starts_with = FALSE" in function formals
    - R/curate_headless.R contains "wqx_threshold = wqx_threshold" in run_curation_pipeline call
    - R/curate_headless.R contains "starts_with = starts_with" in run_curation_pipeline call
    - testthat::test_file('tests/testthat/test-pipeline-reorder-toggle.R') exits 0 with all tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> pipeline params | wqx_threshold (numeric 0.50-1.00) and starts_with (boolean) enter from UI slider/checkbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | wqx_threshold value | accept | Server-side slider enforces min=0.50, max=1.00; even out-of-range values only affect match sensitivity, no injection surface. match_wqx() uses it in a numeric comparison only. |
| T-47-02 | Denial of Service | starts_with=TRUE with large name list | accept | Pre-existing behavior unchanged; toggle merely gates existing functionality. API rate limits already in place for CompTox. |
</threat_model>

<verification>
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-pipeline-reorder-toggle.R')"
```
All 5 tests pass. `run_curation_pipeline()` and `curate_headless()` accept and thread the new parameters correctly.
</verification>

<success_criteria>
- run_curation_pipeline() has WQX at Tier 3 and starts-with at Tier 4 (gated)
- wqx_threshold flows from function argument to match_wqx(threshold=)
- starts_with=FALSE prevents starts-with from executing
- curate_headless() exposes both parameters and threads them
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/47-pipeline-reordering-threshold-control-starts-with-toggle/47-01-SUMMARY.md`
</output>
