---
phase: 48-wqx-resolution-ui
plan: 04
type: execute
wave: 1
depends_on: [48-03]
files_modified:
  - R/mod_review_results.R
  - tests/testthat/test-mod-review-helpers.R
autonomous: true
gap_closure: true
requirements:
  - CONF-03
  - RES-01
  - RES-02
  - RES-03

must_haves:
  truths:
    - "User can click Review on a WQX row and see a modal with the correct input name from the tagged Name column"
    - "Review Results table shows a formatted WQX Conf. column in multi-tag mode (e.g., wqx_confidence_Chemical)"
    - "WQX Review modal displays the confidence score for fuzzy rows in multi-tag datasets"
    - "Rejecting a WQX match initializes the needs_review column before setting TRUE"
  artifacts:
    - path: "R/mod_review_results.R"
      provides: "Fixed wqx_review_click observer, grep-based colDef guard, needs_review init guard, em-dash fix"
      contains: "column_tags.*Name"
    - path: "tests/testthat/test-mod-review-helpers.R"
      provides: "Regression tests for multi-tag wqx_confidence column name detection"
  key_links:
    - from: "R/mod_review_results.R (wqx_review_click observer)"
      to: "data_store$column_tags"
      via: "Name column lookup replaces row$searchValue"
      pattern: 'names\\(data_store\\$column_tags\\)\\[data_store\\$column_tags == "Name"\\]'
    - from: "R/mod_review_results.R (colDef guard)"
      to: "df_display column names"
      via: "grep-based lookup for wqx_confidence prefix"
      pattern: 'grep\\("\\^wqx_confidence", names\\(df_display\\)'
---

<objective>
Fix two BLOCKER gaps preventing the WQX Review modal and confidence column from working in production:

1. CR-01: `row$searchValue` references a column that does not exist on `resolution_state`, causing the WQX Review modal to crash on click with "argument is of length zero". Replace with a lookup against the tagged Name column(s) from `data_store$column_tags`.

2. WR-02: In multi-tag mode (common case: Name + CASRN tagged), `map_results_to_rows` produces `wqx_confidence_Chemical` (suffixed), but the colDef guard and modal confidence lookup check for the unsuffixed `wqx_confidence`. Replace with `grep("^wqx_confidence", ...)` pattern.

Also addresses two low-severity issues from code review: WR-01 (needs_review init guard) and IN-02 (em-dash consistency).

Purpose: Unblocks all four phase requirements (CONF-03, RES-01, RES-02, RES-03) by fixing the two runtime defects that prevent the modal from opening and the confidence column from displaying.
Output: Patched R/mod_review_results.R, regression tests
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-wqx-resolution-ui/48-03-SUMMARY.md
@.planning/phases/48-wqx-resolution-ui/48-VERIFICATION.md
@.planning/phases/48-wqx-resolution-ui/48-REVIEW.md

<interfaces>
<!-- Key patterns the executor needs from the existing codebase. -->

From R/mod_review_results.R line 678 (established pattern for reading tagged Name columns):
```r
name_cols <- names(data_store$column_tags)[data_store$column_tags == "Name"]
```

From R/mod_review_results.R line 767-778 (current broken colDef guard -- unsuffixed):
```r
if ("wqx_confidence" %in% names(df_display)) {
  col_defs[["wqx_confidence"]] <- reactable::colDef(
    name = "WQX Conf.",
    minWidth = 80,
    align = "right",
    cell = function(value, index) {
      if (is.na(value)) return("")
      formatC(value, digits = 2, format = "f")
    }
  )
}
```

From R/mod_review_results.R line 1755 (broken line -- column does not exist):
```r
input_name <- row$searchValue
```

From R/mod_review_results.R line 1788 (broken confidence lookup -- unsuffixed):
```r
confidence <- if ("wqx_confidence" %in% names(row)) row$wqx_confidence else NA_real_
```

From R/mod_review_results.R line 1935-1937 (missing init guard):
```r
for (r in group_rows) {
  updated_df$consensus_status[r] <- "unresolvable"
  updated_df$needs_review[r] <- TRUE
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01, WR-02, WR-01, IN-02 in mod_review_results.R</name>
  <files>R/mod_review_results.R</files>
  <read_first>
    - R/mod_review_results.R (full file -- read lines 760-780 for colDef guard, lines 1743-1800 for wqx_review_click observer, lines 1924-1948 for wqx_reject_click observer)
    - .planning/phases/48-wqx-resolution-ui/48-REVIEW.md (code review findings with exact fixes)
  </read_first>
  <action>
Apply four targeted fixes to R/mod_review_results.R. Each fix addresses a specific finding from the code review and verification reports.

**Fix 1 — CR-01 (BLOCKER): Replace `row$searchValue` at line 1755**

Replace this single line:
```r
input_name <- row$searchValue
```

With this block (uses the established `column_tags` pattern from line 678):
```r
name_cols <- names(data_store$column_tags)[data_store$column_tags == "Name"]
input_name <- NA_character_
for (nc in name_cols) {
  if (nc %in% names(row) && !is.na(row[[nc]])) {
    input_name <- row[[nc]]
    break
  }
}
```

This reads the user's original input value from whichever column is tagged as "Name" in the current dataset, which is the correct semantic for "Input Name" in the modal context card. The `$searchValue` column does not exist on `resolution_state` -- it was never propagated by `map_results_to_rows()`.

**Fix 2 — WR-02a (BLOCKER): Replace colDef guard at lines 767-779**

Replace:
```r
if ("wqx_confidence" %in% names(df_display)) {
  col_defs[["wqx_confidence"]] <- reactable::colDef(
    name = "WQX Conf.",
    minWidth = 80,
    align = "right",
    cell = function(value, index) {
      if (is.na(value)) {
        return("")
      }
      formatC(value, digits = 2, format = "f")
    }
  )
}
```

With:
```r
wqx_conf_cols <- grep("^wqx_confidence", names(df_display), value = TRUE)
for (wcc in wqx_conf_cols) {
  col_defs[[wcc]] <- reactable::colDef(
    name = "WQX Conf.",
    minWidth = 80,
    align = "right",
    cell = function(value, index) {
      if (is.na(value)) {
        return("")
      }
      formatC(value, digits = 2, format = "f")
    }
  )
}
```

This handles both `wqx_confidence` (single-tag mode) and `wqx_confidence_Chemical` (multi-tag mode) using the same `grep("^wqx_confidence", ...)` pattern already used throughout the file for `preferredName_*` and `source_tier_*`.

**Fix 3 — WR-02b (BLOCKER): Replace modal confidence lookup at line 1788**

Replace:
```r
confidence <- if ("wqx_confidence" %in% names(row)) row$wqx_confidence else NA_real_
```

With:
```r
wqx_conf_col <- grep("^wqx_confidence", names(row), value = TRUE)
confidence <- if (length(wqx_conf_col) > 0) row[[wqx_conf_col[1]]] else NA_real_
```

This mirrors Fix 2, using grep-based lookup to find the confidence column regardless of suffix.

**Fix 4 — WR-01 (WARNING): Add needs_review init guard in wqx_reject_click observer**

In the `wqx_reject_click` observer (around line 1934), add an initialization guard BEFORE the for loop. The current code is:
```r
updated_df <- data_store$resolution_state

for (r in group_rows) {
  updated_df$consensus_status[r] <- "unresolvable"
  updated_df$needs_review[r] <- TRUE
}
```

Change to:
```r
updated_df <- data_store$resolution_state

if (!"needs_review" %in% names(updated_df)) {
  updated_df$needs_review <- FALSE
}
for (r in group_rows) {
  updated_df$consensus_status[r] <- "unresolvable"
  updated_df$needs_review[r] <- TRUE
}
```

**Fix 5 — IN-02 (INFO): Replace double hyphen with em-dash in reject notification**

In the `wqx_reject_click` observer notification (around line 1944), replace:
```r
sprintf("WQX match rejected for %d row(s) -- marked unresolvable", length(group_rows))
```

With:
```r
sprintf("WQX match rejected for %d row(s) \u2014 marked unresolvable", length(group_rows))
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('Load OK\n')"</automated>
    Then verify all fixes with grep:
    - grep "row\$searchValue" R/mod_review_results.R should return NO matches (CR-01 fixed)
    - grep "column_tags.*Name" R/mod_review_results.R should match in the wqx_review_click observer
    - grep 'grep.*wqx_confidence.*names' R/mod_review_results.R should match at least 2 times (colDef guard + modal lookup)
    - grep "needs_review.*FALSE" R/mod_review_results.R should match the init guard
    - grep '\\\\u2014' R/mod_review_results.R should match in the reject notification (replaces --)
  </verify>
  <acceptance_criteria>
    - ZERO matches for `row$searchValue` anywhere in R/mod_review_results.R
    - The string `data_store$column_tags)[data_store$column_tags == "Name"]` appears in the wqx_review_click observer block (lines 1743-1874)
    - The string `grep("^wqx_confidence", names(df_display), value = TRUE)` appears in the renderReactable colDef section (lines 766-779)
    - The string `grep("^wqx_confidence", names(row), value = TRUE)` appears in the wqx_review_click observer (lines 1786-1790)
    - The string `if (!"needs_review" %in% names(updated_df))` appears in the wqx_reject_click observer (lines 1924-1948)
    - The string `\u2014` appears in the reject notification sprintf (replaces `--`)
    - `devtools::load_all()` completes without errors
  </acceptance_criteria>
  <done>All four code review findings (CR-01, WR-02a, WR-02b, WR-01) and cosmetic fix (IN-02) applied. Package loads cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for multi-tag wqx_confidence and input name lookup</name>
  <files>tests/testthat/test-mod-review-helpers.R</files>
  <read_first>
    - tests/testthat/test-mod-review-helpers.R (full file -- understand existing test structure, Test Groups 1-6)
    - R/mod_review_results.R (the patched colDef guard and confidence lookup from Task 1)
  </read_first>
  <action>
Add Test Group 7 to `tests/testthat/test-mod-review-helpers.R` with regression tests for the gap closure fixes.

**Test 7a: grep-based wqx_confidence column detection handles suffixed names**

This test verifies that the grep pattern `^wqx_confidence` correctly matches both unsuffixed and suffixed column names -- the core logic behind Fix 2 (WR-02). The test does NOT call the full renderReactable; instead it tests the grep pattern in isolation since the colDef application itself is a Shiny server-side operation.

```r
test_that("wqx_confidence grep pattern matches both suffixed and unsuffixed column names", {
  # Single-tag mode: unsuffixed
  single_tag_names <- c("consensus_status", "wqx_confidence", "preferredName_Chemical")
  expect_length(grep("^wqx_confidence", single_tag_names, value = TRUE), 1)
  expect_equal(grep("^wqx_confidence", single_tag_names, value = TRUE), "wqx_confidence")

  # Multi-tag mode: suffixed
  multi_tag_names <- c("consensus_status", "wqx_confidence_Chemical", "preferredName_Chemical")
  expect_length(grep("^wqx_confidence", multi_tag_names, value = TRUE), 1)
  expect_equal(grep("^wqx_confidence", multi_tag_names, value = TRUE), "wqx_confidence_Chemical")

  # Both present (edge case): matches both
  both_names <- c("wqx_confidence", "wqx_confidence_Chemical")
  expect_length(grep("^wqx_confidence", both_names, value = TRUE), 2)

  # No match
  no_match_names <- c("consensus_status", "preferredName_Chemical")
  expect_length(grep("^wqx_confidence", no_match_names, value = TRUE), 0)
})
```

**Test 7b: grep-based confidence lookup extracts value from suffixed column**

This test verifies that the modal confidence lookup pattern (Fix 3, WR-02b) correctly retrieves the confidence value from a suffixed column.

```r
test_that("grep-based wqx_confidence lookup extracts value from suffixed column", {
  # Simulate a single-row slice of resolution_state with suffixed column (multi-tag mode)
  row <- data.frame(
    consensus_status = "wqx",
    wqx_confidence_Chemical = 0.87,
    stringsAsFactors = FALSE
  )

  wqx_conf_col <- grep("^wqx_confidence", names(row), value = TRUE)
  confidence <- if (length(wqx_conf_col) > 0) row[[wqx_conf_col[1]]] else NA_real_
  expect_equal(confidence, 0.87)

  # Single-tag mode (unsuffixed)
  row2 <- data.frame(
    consensus_status = "wqx",
    wqx_confidence = 0.92,
    stringsAsFactors = FALSE
  )
  wqx_conf_col2 <- grep("^wqx_confidence", names(row2), value = TRUE)
  confidence2 <- if (length(wqx_conf_col2) > 0) row2[[wqx_conf_col2[1]]] else NA_real_
  expect_equal(confidence2, 0.92)

  # No confidence column at all
  row3 <- data.frame(consensus_status = "wqx", stringsAsFactors = FALSE)
  wqx_conf_col3 <- grep("^wqx_confidence", names(row3), value = TRUE)
  confidence3 <- if (length(wqx_conf_col3) > 0) row3[[wqx_conf_col3[1]]] else NA_real_
  expect_true(is.na(confidence3))
})
```

**Test 7c: input name lookup from tagged Name column**

This test verifies the column_tags-based lookup logic that replaced `row$searchValue` (Fix 1, CR-01). It tests the lookup pattern in isolation.

```r
test_that("input name lookup reads from tagged Name column, not searchValue", {
  # Simulate resolution_state row with tagged Name column
  row <- data.frame(
    Chemical = "Dissolved oxygen",
    consensus_status = "wqx",
    stringsAsFactors = FALSE
  )

  # Simulate column_tags (list mapping column names to tag types)
  column_tags <- c("Chemical" = "Name", "cas_number" = "CASRN")

  name_cols <- names(column_tags)[column_tags == "Name"]
  input_name <- NA_character_
  for (nc in name_cols) {
    if (nc %in% names(row) && !is.na(row[[nc]])) {
      input_name <- row[[nc]]
      break
    }
  }
  expect_equal(input_name, "Dissolved oxygen")

  # Row where the Name column is NA (falls through to NA_character_)
  row2 <- data.frame(
    Chemical = NA_character_,
    consensus_status = "wqx",
    stringsAsFactors = FALSE
  )
  input_name2 <- NA_character_
  for (nc in name_cols) {
    if (nc %in% names(row2) && !is.na(row2[[nc]])) {
      input_name2 <- row2[[nc]]
      break
    }
  }
  expect_true(is.na(input_name2))
})
```

Add all three tests under a comment header `# Test Group 7: Gap closure regression tests (Plan 04 -- CR-01, WR-02)` at the end of the file.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-mod-review-helpers.R')"</automated>
  </verify>
  <acceptance_criteria>
    - The string `Test Group 7: Gap closure regression tests` appears in tests/testthat/test-mod-review-helpers.R
    - Three new test_that blocks exist: "wqx_confidence grep pattern matches both suffixed and unsuffixed column names", "grep-based wqx_confidence lookup extracts value from suffixed column", "input name lookup reads from tagged Name column, not searchValue"
    - All tests pass (0 failures) -- expect 28 total (25 existing + 3 new)
    - No test uses `row$searchValue` (the broken pattern)
  </acceptance_criteria>
  <done>Three regression tests pass, covering the grep-based column lookup pattern (WR-02) and the column_tags-based input name lookup (CR-01). Total test count is 28.</done>
</task>

<task type="auto">
  <name>Task 3: Shiny cold boot smoke test</name>
  <files>R/mod_review_results.R</files>
  <read_first>
    - R/mod_review_results.R (verify Task 1 changes are in place)
  </read_first>
  <action>
Run the mandatory Shiny cold boot smoke test per CLAUDE.md requirements. The app must start cleanly after the mod_review_results.R changes.

Execute:
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)"
```

Run in background, wait for "Listening on" message or an error. If the app starts successfully, kill the process. If it crashes, diagnose and fix immediately.

This catches missing imports, syntax errors, broken module wiring, and other issues that unit tests do not surface.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)" (run in background, wait for Listening on, then stop)</automated>
  </verify>
  <acceptance_criteria>
    - App prints "Listening on http://127.0.0.1:3838" (or similar) without errors
    - No R errors or warnings related to mod_review_results, wqx_confidence, column_tags, or searchValue
    - Process exits cleanly when stopped
  </acceptance_criteria>
  <done>Shiny app starts cleanly with all gap closure fixes applied. No startup errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click -> modal observer | data-row attribute from browser JS crosses into R server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04-01 | T (Tampering) | wqx_review_click data-row attribute | accept | Existing bounds check at line 1747 validates row_idx range; pattern unchanged from Plan 02 |
| T-48-04-02 | I (Info Disclosure) | column_tags Name column lookup | accept | column_tags is server-side only; no user data exposed beyond what is already in the table |
</threat_model>

<verification>
1. `grep -c "row\\$searchValue" R/mod_review_results.R` returns 0 (CR-01 fixed)
2. `grep -c 'grep.*wqx_confidence.*names' R/mod_review_results.R` returns >= 2 (WR-02 fixed at both sites)
3. `grep -c "needs_review.*FALSE" R/mod_review_results.R` returns >= 1 (WR-01 fixed)
4. All 28 tests pass in test-mod-review-helpers.R
5. Shiny app boots cleanly
</verification>

<success_criteria>
- WQX Review modal no longer crashes on click (CR-01 closed)
- WQX Conf. column appears formatted in multi-tag datasets (WR-02 closed)
- Modal shows correct confidence score in multi-tag mode (WR-02 closed)
- needs_review column initialized before reject mutation (WR-01 closed)
- Em-dash consistency in reject notification (IN-02 closed)
- All 28 unit/integration tests pass
- Shiny cold boot succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/48-wqx-resolution-ui/48-04-SUMMARY.md`
</output>
