---
phase: 12-name-cleaning
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - R/modules/mod_clean_data.R
  - app.R
autonomous: false
requirements: [UIUX-03]

must_haves:
  truths:
    - "User can see before/after data comparison via audit trail accordion"
    - "User can see value boxes for name cleaning statistics (parentheticals, synonyms, adjectives)"
    - "User can see step-by-step progress for name cleaning stages"
    - "App starts without error after all UI changes"
  artifacts:
    - path: "R/modules/mod_clean_data.R"
      provides: "Audit trail accordion, name cleaning value boxes, extended progress"
      contains: "accordion"
    - path: "app.R"
      provides: "App orchestration unchanged (module handles all)"
  key_links:
    - from: "R/modules/mod_clean_data.R"
      to: "R/cleaning_pipeline.R"
      via: "run_cleaning_pipeline called with tag_map including Name columns"
      pattern: "run_cleaning_pipeline"
    - from: "R/modules/mod_clean_data.R"
      to: "data_store$cleaning_audit"
      via: "Audit trail rendered in accordion DT table"
      pattern: "cleaning_audit"
---

<objective>
Integrate name cleaning into the Clean Data tab UI: add audit trail accordion (UIUX-03), extend value box dashboard with name cleaning statistics, and extend progress indicator with name cleaning steps.

Purpose: Users can see what name cleaning transformations were applied and inspect before/after changes in a collapsible audit trail.
Output: Updated mod_clean_data.R with accordion UI, extended value boxes, extended progress steps.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-name-cleaning/12-CONTEXT.md
@.planning/phases/12-name-cleaning/12-RESEARCH.md
@.planning/phases/12-name-cleaning/12-01-SUMMARY.md

<interfaces>
<!-- Executor needs these interfaces from Plan 01 output -->

From R/cleaning_pipeline.R — new name cleaning functions:
```r
strip_terminal_enclosures <- function(df, name_cols)
# Returns: list(cleaned_data, audit_trail, new_tags)
# audit_trail step_name: "strip_parentheticals"

strip_quality_adjectives <- function(df, name_cols)
# Returns: list(cleaned_data, audit_trail)
# audit_trail step_name: "strip_adjectives"

strip_salt_references <- function(df, name_cols)
# Returns: list(cleaned_data, audit_trail)
# audit_trail step_name: "strip_salts"

strip_terminal_unspecified <- function(df, name_cols)
# Returns: list(cleaned_data, audit_trail)
# audit_trail step_name: "strip_unspecified"

split_synonyms <- function(df, name_cols, tag_map)
# Returns: list(cleaned_data, audit_trail)
# audit_trail step_name: "split_synonyms"
# Adds synonym_count, synonym_index columns to df
```

From R/modules/mod_clean_data.R — existing UI structure:
```r
# Value boxes rendered in output$cleaning_summary (renderUI)
# Uses bslib::layout_columns with col_widths = c(4, 4, 4) for rows of 3
# Existing boxes: CAS Rescued, CAS Normalized, CAS Invalid, Multi-CAS Flagged, Unicode Cleaned, Fields Trimmed

# Progress uses withProgress() with incProgress() calls:
# unicode (0.15) -> trim (0.15) -> normalize CAS (0.20) -> rescue CAS (0.20) -> detect multi-CAS (0.10) -> finalize (0.10)

# data_store$cleaning_audit holds the combined audit trail tibble
# Columns: row_id, field, original_value, new_value, step, reason
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit trail accordion and extend value boxes and progress</name>
  <files>R/modules/mod_clean_data.R</files>
  <action>
Modify R/modules/mod_clean_data.R to add three features:

**1. Audit Trail Accordion (UIUX-03) — add below the cleaned data table:**

Add to UI function (mod_clean_data_ui):
- uiOutput(ns("audit_section")) after the cleaned data table output

Add to server function (mod_clean_data_server):
- output$audit_section <- renderUI({...}) — bslib::accordion, collapsed by default (open = FALSE)
  - Single accordion_panel titled "Cleaning Audit Trail -- {N} Changes"
  - Icon: bsicons::bs_icon("journal-text")
  - Contains DT::dataTableOutput(ns("audit_table"))
- output$audit_table <- DT::renderDataTable({...})
  - Renders data_store$cleaning_audit
  - Options: pageLength=25, scrollX=TRUE, order by row_id asc
  - filter="top" for column filtering (step, field)
  - rownames=FALSE

**2. Extend Value Box Dashboard — add name cleaning boxes:**

In output$cleaning_summary renderUI, add a third row of value boxes after the existing 2 rows:
- "Parentheticals Stripped" — count audit entries where step == "strip_parentheticals", theme="info", icon=bsicons::bs_icon("scissors")
- "Synonyms Split" — count audit entries where step == "split_synonyms", theme="primary", icon=bsicons::bs_icon("diagram-3")
- "Adjectives Removed" — count audit entries where step %in% c("strip_adjectives", "strip_salts", "strip_unspecified"), theme="info", icon=bsicons::bs_icon("eraser")

Use bslib::layout_columns(col_widths = c(4, 4, 4), ...) matching existing pattern. Only show this row when name cleaning steps have entries (conditional on audit having name cleaning steps).

**3. Extend Progress Indicator:**

In the observeEvent(input$run_cleaning, {...}) handler, update the withProgress block:
- Redistribute progress weights to accommodate name cleaning steps:
  - unicode: 0.10, trim: 0.10, normalize CAS: 0.15, rescue CAS: 0.15, detect multi-CAS: 0.10
  - strip parentheticals: 0.10, strip adjectives: 0.05, strip salts: 0.05, strip unspecified: 0.05, split synonyms: 0.10, finalize: 0.05
- Add incProgress() calls between each name cleaning step with descriptive detail messages:
  - "Stripping parentheticals..."
  - "Removing quality adjectives..."
  - "Removing salt references..."
  - "Removing unspecified suffixes..."
  - "Splitting synonyms..."

NOTE: The actual name cleaning logic is handled by run_cleaning_pipeline() from Plan 01. The progress messages here just wrap the pipeline call. If run_cleaning_pipeline handles all steps internally, add incProgress messages around the single pipeline call or restructure the server to call pipeline steps individually for granular progress. Follow whichever approach Phase 11 used for CAS steps.

IMPORTANT: Verify icon names exist in bsicons before using them. If "diagram-3" or "eraser" don't exist, use alternatives like "list-nested" or "x-circle".
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" &amp; SHINY_PID=$!; sleep 8; kill $SHINY_PID 2>/dev/null; wait $SHINY_PID 2>/dev/null</automated>
  </verify>
  <done>App starts without error (smoke test passes). Audit trail accordion renders below cleaned data table. Value boxes include name cleaning statistics. Progress indicator shows name cleaning steps. All existing functionality preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Name cleaning pipeline with before/after audit trail UI. Full pipeline: parenthetical stripping, quality adjective removal, salt reference stripping, unspecified suffix removal, IUPAC-aware synonym splitting — all with audit trail visible in collapsible accordion.</what-built>
  <how-to-verify>
    1. Start app: run shiny::runApp() or use the running instance
    2. Upload data/chemical_validation_test.csv (or a test file with synonym and parenthetical examples)
    3. Go to Tag Columns tab, tag a column as "Name" and optionally one as "CASRN"
    4. Go to Clean Data tab, click "Run Cleaning"
    5. Verify value boxes show: existing CAS boxes + new Parentheticals Stripped, Synonyms Split, Adjectives Removed boxes
    6. Verify progress bar shows name cleaning steps (Stripping parentheticals, Removing adjectives, Splitting synonyms, etc.)
    7. Scroll below cleaned data table — verify "Cleaning Audit Trail" accordion exists, collapsed by default
    8. Click accordion to expand — verify DT table with columns: row_id, field, original_value, new_value, step, reason
    9. Verify column filters work (filter by step to see only "split_synonyms" entries)
    10. If synonym splitting occurred, verify new rows appear in cleaned data table with proper original_row_id tracking
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Shiny smoke test: app starts without error
2. Full test suite passes: "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"
3. Visual verification of audit accordion, value boxes, and progress indicator
</verification>

<success_criteria>
- Audit trail accordion renders below cleaned data table, collapsed by default
- Value boxes show parentheticals stripped, synonyms split, and adjectives removed counts
- Progress indicator shows name cleaning step names
- App starts without crash (smoke test)
- All existing functionality (CAS pipeline, tag columns, curation) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/12-name-cleaning/12-02-SUMMARY.md`
</output>
