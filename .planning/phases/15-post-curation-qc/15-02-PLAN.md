---
phase: 15-post-curation-qc
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - R/modules/mod_review_results.R
  - app.R
autonomous: false
requirements:
  - POST-02

must_haves:
  truths:
    - "User can see QC value boxes showing non-ASCII row count and unhandled character count after curation completes"
    - "User can see rows with non-ASCII characters highlighted yellow with 'QC: non-ASCII' flag in Review Results table"
    - "User can see a QC summary card listing specific unhandled characters with codepoints and row counts"
    - "User can click Re-run QC button to refresh QC results after manual resolutions"
    - "QC runs automatically when curation completes without user action"
    - "QC does not gate the export button — user can export with warnings present"
  artifacts:
    - path: "R/modules/mod_review_results.R"
      provides: "QC value boxes, DT qc_flag column, summary card, re-run button"
      contains: "rerun_qc"
    - path: "app.R"
      provides: "Auto-run QC observer after curation completes"
      contains: "qc_results"
  key_links:
    - from: "app.R"
      to: "R/cleaning_pipeline.R"
      via: "perform_unicode_qc() call on curation complete"
      pattern: "perform_unicode_qc"
    - from: "R/modules/mod_review_results.R"
      to: "data_store$qc_results"
      via: "reactive read for value boxes and DT flags"
      pattern: "qc_results"
---

<objective>
Integrate post-curation QC results into the Review Results tab with value boxes, inline DT flags, summary card, and re-run button.

Purpose: Users need visibility into remaining non-ASCII characters after curation before exporting. This surfaces data quality issues as advisory warnings without blocking the workflow.
Output: QC UI elements in Review Results, auto-run wiring in app.R.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-post-curation-qc/15-CONTEXT.md
@.planning/phases/15-post-curation-qc/15-RESEARCH.md
@.planning/phases/15-post-curation-qc/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 15-01 output -->
From R/cleaning_pipeline.R (new function):
```r
perform_unicode_qc <- function(df) {
  # Returns:
  # list(
  #   rows_with_non_ascii = integer,      # count of affected rows
  #   row_indices = integer vector,        # which rows
  #   unhandled_chars = named list         # keyed by "U+XXXX", each with: char, codepoint, count
  # )
}
```

From R/modules/mod_review_results.R (existing patterns):
```r
# Value box pattern (lines 133-159):
layout_columns(
  col_widths = c(3, 3, 3, 3),
  value_box(title = "Resolved", value = resolved, showcase = bsicons::bs_icon("check-circle-fill"), theme = "success"),
  value_box(title = "Disagree", value = summary$n_disagree, showcase = bsicons::bs_icon("x-circle-fill"), theme = "danger"),
  value_box(title = "Errors", value = errors, showcase = bsicons::bs_icon("exclamation-triangle-fill"), theme = "warning"),
  value_box(title = "Match Rate", value = paste0(match_rate, "%"), showcase = bsicons::bs_icon("percent"), theme = "info")
)

# Download button (line 85-89):
downloadButton(ns("download_curated"), "Download Excel", class = "btn-primary")

# DT table output (line 93):
DTOutput(ns("curation_table"))
```

From app.R (existing data_store fields, line 133-134):
```r
data_store <- reactiveValues(
  ..., consensus_summary = NULL, resolution_state = NULL, ...
)
```

From app.R (curation complete callback, line 336):
```r
on_curation_complete = function() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QC UI elements to Review Results module</name>
  <files>R/modules/mod_review_results.R</files>
  <action>
    **UI changes (mod_review_results_ui):**

    1. Add a second row of value boxes below existing curation_stats output. Create a new `uiOutput(ns("qc_stats"))` placed directly after the existing `uiOutput(ns("curation_stats"))` line (line 44).

    2. Add QC summary card output: `uiOutput(ns("qc_summary_card"))` placed ABOVE the header row that contains "Curated Results" and the download button (before the div at line 57).

    3. Add "Re-run QC" button in the button group next to "Download Excel" (inside the d-flex gap-2 div, line 62). Use: `actionButton(ns("rerun_qc"), "Re-run QC", icon = bsicons::bs_icon("arrow-repeat"), class = "btn-sm btn-outline-secondary")`. Place it immediately before the downloadButton.

    **Server changes (mod_review_results_server):**

    4. Add `output$qc_stats` renderUI:
       - `req(data_store$qc_results)` to only show when QC has run
       - Two value boxes in `layout_columns(col_widths = c(6, 6))`:
         - "Rows with Non-ASCII": value = qc_results$rows_with_non_ascii, icon = "exclamation-circle", theme = "success" if 0, "warning" if > 0
         - "Unhandled Characters": value = length(qc_results$unhandled_chars), icon = "question-circle", theme = "success" if 0, "info" if > 0

    5. Add `output$qc_summary_card` renderUI:
       - `req(data_store$qc_results)` — return NULL if no unhandled chars (length == 0)
       - Bootstrap card with border-warning class
       - Card header with exclamation-triangle icon: "QC Warning: Unmapped Unicode Characters"
       - Card body with ul listing each unhandled char: "U+XXXX (char) found in N rows"
       - Muted footer text: "These characters will remain in your exported data."

    6. Modify the `output$curation_table` renderDT to inject a `qc_flag` column:
       - If `data_store$qc_results` is not NULL and has row_indices, add column `qc_flag` to the display df
       - Rows in qc_results$row_indices get "WARN: non-ASCII", others get NA
       - Apply DT::formatStyle on qc_flag column with yellow background (#fff3cd) for WARN rows
       - Use the same JavaScript callback pattern from Phase 13 for row-level styling

    7. Add `observeEvent(input$rerun_qc)`:
       - `req(data_store$resolution_state)`
       - Wrap in `withProgress(message = "Running QC checks...", value = 0)`
       - Call `perform_unicode_qc(data_store$resolution_state)`
       - Store result in `data_store$qc_results`
       - Show notification with result summary (0 issues = "message" type, issues = "warning" type)

    IMPORTANT: The qc_flag column must be EXCLUDED from the export. The existing downloadHandler builds sheets via build_export_sheets() which uses data_store$resolution_state directly — since qc_flag is only added in the DT render (not stored in resolution_state), it won't leak to export. Verify this is the case.

    IMPORTANT: Do NOT gate the export button on QC results. User decision: "advisory only".
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 15 && curl -s http://localhost:3838 > /dev/null && echo "APP_STARTED" || echo "APP_FAILED"; kill %1 2>/dev/null</automated>
  </verify>
  <done>QC value boxes render when qc_results populated, summary card shows unhandled characters, re-run button triggers QC, DT shows yellow WARN rows for non-ASCII</done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-run QC in app.R after curation completes</name>
  <files>app.R</files>
  <action>
    1. Add `qc_results = NULL` to the data_store reactiveValues initialization (around line 133-134).

    2. Find the `on_curation_complete` callback (line 336). Inside it, after existing logic, add the QC auto-run:
       ```r
       # Auto-run post-curation QC
       qc_results <- perform_unicode_qc(data_store$resolution_state)
       data_store$qc_results <- qc_results
       if (qc_results$rows_with_non_ascii > 0) {
         showNotification(
           sprintf("QC: %d rows contain non-ASCII characters", qc_results$rows_with_non_ascii),
           type = "warning", duration = 5
         )
       }
       ```

    3. In the reset_all_downstream function (or wherever data_store fields are cleared on re-upload), add `data_store$qc_results <- NULL` to clear QC state.

    IMPORTANT: Use `ignoreInit = TRUE` if wrapping in observeEvent. The on_curation_complete callback approach is preferred since it already fires once at the right time.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 15 && curl -s http://localhost:3838 > /dev/null && echo "APP_STARTED" || echo "APP_FAILED"; kill %1 2>/dev/null</automated>
  </verify>
  <done>App starts without error, QC auto-runs when curation completes, qc_results cleared on re-upload</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Post-curation QC integration: value boxes, DT flags, summary card, re-run button, auto-run on curation complete</what-built>
  <how-to-verify>
    1. Start app: run `shiny::runApp('app.R')` in R console
    2. Upload a test file and complete the full workflow through curation
    3. After curation completes, verify:
       a. QC value boxes appear below the existing consensus stats (showing row/character counts)
       b. If non-ASCII characters exist, rows are highlighted yellow with "WARN: non-ASCII" in qc_flag column
       c. QC summary card appears above the results table listing specific characters
    4. Click "Re-run QC" button — verify it refreshes the QC results with a progress indicator
    5. Click "Download Excel" — verify export works and does NOT contain a qc_flag column
    6. Re-upload a file — verify QC stats are cleared
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. App smoke test passes (starts without error)
2. Full test suite green: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_dir('tests')"`
3. QC value boxes, summary card, and re-run button render in Review Results
4. Export does not contain qc_flag column
</verification>

<success_criteria>
- QC auto-runs when curation completes showing value boxes with counts
- Affected rows highlighted yellow with WARN flag in DT table
- Summary card lists specific unhandled characters with codepoints
- Re-run QC button refreshes results without re-running curation
- Export button works regardless of QC warnings (advisory only)
- App starts cleanly with no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-post-curation-qc/15-02-SUMMARY.md`
</output>
