---
phase: 15-post-curation-qc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/cleaning_pipeline.R
  - R/modules/mod_clean_data.R
  - tests/test_cleaning_pipeline.R
  - tests/test_unicode_qc.R
autonomous: true
requirements:
  - POST-02

must_haves:
  truths:
    - "ComptoxR::clean_unicode() is used for all unicode cleaning instead of custom clean_unicode_field()"
    - "Greek letters are converted to dot-notation (.alpha., .beta.) not single letters (a, b)"
    - "perform_unicode_qc() detects rows with non-ASCII characters without modifying data"
    - "perform_unicode_qc() reports specific unhandled unicode characters with codepoints and counts"
    - "All existing cleaning pipeline tests pass with updated expectations"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "perform_unicode_qc() function, ComptoxR::clean_unicode() calls replacing clean_unicode_field()"
      contains: "perform_unicode_qc"
    - path: "tests/test_unicode_qc.R"
      provides: "Tests for post-curation QC detection"
      min_lines: 30
    - path: "tests/test_cleaning_pipeline.R"
      provides: "Updated unicode test expectations (.alpha. not a)"
      contains: ".alpha.-tocopherol"
  key_links:
    - from: "R/cleaning_pipeline.R"
      to: "ComptoxR::clean_unicode"
      via: "direct function call in pipeline step 1"
      pattern: "ComptoxR::clean_unicode"
    - from: "R/modules/mod_clean_data.R"
      to: "ComptoxR::clean_unicode"
      via: "inline cleaning call"
      pattern: "ComptoxR::clean_unicode"
---

<objective>
Replace custom unicode cleaning with ComptoxR's chemistry-specific implementation and create post-curation QC detection function.

Purpose: ComptoxR::clean_unicode() has 157 chemistry-specific mappings (Greek letters to dot-notation, trademark removal) vs our generic stringi transliteration. The QC function enables post-curation flagging without modifying data.
Output: Updated cleaning pipeline using ComptoxR, new perform_unicode_qc() function, updated and new tests.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-post-curation-qc/15-CONTEXT.md
@.planning/phases/15-post-curation-qc/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From R/cleaning_pipeline.R (lines 28-32, DELETE):
```r
clean_unicode_field <- function(x) {
  result <- ifelse(is.na(x), NA_character_, stringi::stri_trans_general(x, "Any-Latin; Latin-ASCII"))
  return(result)
}
```

From R/cleaning_pipeline.R (line 1133, REPLACE):
```r
dplyr::mutate(dplyr::across(where(is.character), clean_unicode_field))
# BECOMES:
dplyr::mutate(dplyr::across(where(is.character), ComptoxR::clean_unicode))
```

From R/modules/mod_clean_data.R (line 98, REPLACE):
```r
df <- dplyr::mutate(df, dplyr::across(where(is.character), clean_unicode_field))
# BECOMES:
df <- dplyr::mutate(df, dplyr::across(where(is.character), ComptoxR::clean_unicode))
```

From tests/test_cleaning_pipeline.R (lines 10-20, UPDATE expectations):
```r
test_that("clean_unicode_field converts unicode to ASCII", {
  expect_equal(clean_unicode_field("cafe\u0301"), "cafe")
  expect_equal(clean_unicode_field("\u03B1-tocopherol"), "a-tocopherol")  # CHANGES to ".alpha.-tocopherol"
  expect_equal(clean_unicode_field(NA_character_), NA_character_)
  expect_true(is.na(clean_unicode_field(NA)))
})
```

ComptoxR::clean_unicode() behavior:
- S3 generic with character method
- Greek alpha -> ".alpha.", beta -> ".beta." (chemistry dot-notation)
- Trademark/registered/copyright symbols removed (not expanded)
- NA passthrough
- 157 mapped entries in internal unicode_map
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace clean_unicode_field with ComptoxR::clean_unicode and update tests</name>
  <files>R/cleaning_pipeline.R, R/modules/mod_clean_data.R, tests/test_cleaning_pipeline.R</files>
  <behavior>
    - Test: ComptoxR::clean_unicode("\u03B1-tocopherol") returns ".alpha.-tocopherol" (chemistry dot-notation)
    - Test: ComptoxR::clean_unicode("cafe\u0301") returns "cafe" (accent stripping still works)
    - Test: ComptoxR::clean_unicode(NA) returns NA (passthrough)
    - Test: run_cleaning_pipeline() with Greek letters produces .alpha. notation in output
  </behavior>
  <action>
    1. In R/cleaning_pipeline.R:
       - DELETE the `clean_unicode_field` function definition (lines 15-32, the function and its roxygen docs)
       - UPDATE the file header comment to remove reference to clean_unicode_field, mention ComptoxR::clean_unicode instead
       - On line 1133 (inside run_cleaning_pipeline), REPLACE `clean_unicode_field` with `ComptoxR::clean_unicode`
       - The `library(stringi)` import can stay (used elsewhere) but is no longer needed for unicode cleaning

    2. In R/modules/mod_clean_data.R:
       - On line 98, REPLACE `clean_unicode_field` with `ComptoxR::clean_unicode`

    3. In tests/test_cleaning_pipeline.R:
       - RENAME test from "clean_unicode_field converts unicode to ASCII" to "ComptoxR::clean_unicode handles chemistry-specific unicode"
       - REPLACE `clean_unicode_field(...)` calls with `ComptoxR::clean_unicode(...)`
       - CHANGE Greek alpha expectation: `expect_equal(ComptoxR::clean_unicode("\u03B1-tocopherol"), ".alpha.-tocopherol")`
       - Keep cafe accent test (should still return "cafe")
       - Keep NA passthrough tests

    IMPORTANT: Do NOT use ComptoxR:::check_unhandled() (triple-colon internal function). Replicate detection logic instead (see Task 2).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_file('tests/test_cleaning_pipeline.R')"</automated>
  </verify>
  <done>clean_unicode_field() deleted from codebase, all references replaced with ComptoxR::clean_unicode(), all tests pass with updated expectations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create perform_unicode_qc() function and tests</name>
  <files>R/cleaning_pipeline.R, tests/test_unicode_qc.R</files>
  <behavior>
    - Test: perform_unicode_qc() on data with no non-ASCII returns rows_with_non_ascii = 0, empty row_indices, empty unhandled_chars
    - Test: perform_unicode_qc() on data with Greek alpha detects the row and reports the character
    - Test: perform_unicode_qc() on data with NA values does not crash
    - Test: perform_unicode_qc() reports correct codepoint (U+03B1 for alpha) and row count
    - Test: perform_unicode_qc() does NOT modify the input dataframe (read-only QC)
    - Test: perform_unicode_qc() handles dataframes with no character columns gracefully
  </behavior>
  <action>
    Add `perform_unicode_qc(df)` function to R/cleaning_pipeline.R (at the end, before run_cleaning_pipeline or in a logical grouping with QC functions).

    The function:
    1. Takes a dataframe (post-curation resolution_state)
    2. Identifies all character columns
    3. For each cell, checks if it contains non-ASCII characters using `grepl("[^\x01-\x7F]", x)` on each value
    4. Tracks which rows have non-ASCII characters
    5. For non-ASCII characters found, extracts the specific characters, their Unicode codepoints (via `utf8ToInt()`), and counts per unique character
    6. Returns a list with:
       - `rows_with_non_ascii`: integer count
       - `row_indices`: integer vector of affected row numbers
       - `unhandled_chars`: named list keyed by "U+XXXX" with elements: char, codepoint, count

    IMPORTANT: Do NOT call ComptoxR:::check_unhandled() — replicate the detection by scanning for non-ASCII bytes directly. This avoids depending on an internal unexported function.

    Also add a helper `detect_non_ascii_chars(x)` that takes a character vector and returns a list of unique non-ASCII characters with their codepoints and occurrence counts. This helper is used by perform_unicode_qc internally.

    Create tests/test_unicode_qc.R:
    - Source load_packages.R and R/cleaning_pipeline.R
    - Test clean data (all ASCII) returns zero issues
    - Test data with Greek letters detects them
    - Test data with mixed ASCII/non-ASCII reports correct row indices
    - Test NA handling (no crash)
    - Test empty dataframe and no-character-column dataframe
    - Test that input df is not modified (compare before/after)
    - Test specific codepoint reporting (U+03B1 for alpha)
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_file('tests/test_unicode_qc.R')"</automated>
  </verify>
  <done>perform_unicode_qc() returns correct detection results for all test cases, input data is never modified, all tests green</done>
</task>

</tasks>

<verification>
1. All existing tests pass: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_dir('tests')"`
2. No references to `clean_unicode_field` remain in R/ directory (grep confirms)
3. perform_unicode_qc() tested with both clean and dirty data
</verification>

<success_criteria>
- clean_unicode_field() completely removed from codebase
- ComptoxR::clean_unicode() used in all unicode cleaning locations
- perform_unicode_qc() function exists and passes all tests
- Greek alpha produces ".alpha." notation (not "a")
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/15-post-curation-qc/15-01-SUMMARY.md`
</output>