---
phase: 40-date-parser
plan: 03
type: execute
wave: 2
depends_on:
  - 40-01
  - 40-02
files_modified:
  - R/mod_harmonize.R
  - R/curate_headless.R
autonomous: false
requirements:
  - DATE-05
  - DATE-06

must_haves:
  truths:
    - "A column tagged StudyDate in the Harmonize tab routes through parse_dates() during the pipeline run"
    - "The date_year output populates expanded_curated$year before map_to_toxval_schema() is called"
    - "original_year in ToxVal schema output contains the parsed year values"
    - "curate_headless() with StudyDate-tagged columns produces year column in input_df before ToxVal mapping"
    - "QC dashboard shows date-specific value boxes (Dates Parsed, Partial Dates, Ambiguous Dates, Unparseable Dates) when StudyDate results exist"
    - "The harmonize button enables when only StudyDate columns are tagged (no numeric tags required)"
    - "Shiny app cold boots without error after all changes"
  artifacts:
    - path: "R/mod_harmonize.R"
      provides: "Stage 4.6 date parsing, date merge into expanded_curated, QC dashboard date boxes, updated has_numeric_tags gate"
      contains: "Stage 4.6"
    - path: "R/curate_headless.R"
      provides: "Stage 3c date parsing, year column merge into input_df"
      contains: "Stage 3c"
  key_links:
    - from: "R/mod_harmonize.R"
      to: "R/date_parser.R"
      via: "parse_dates() call in Stage 4.6"
      pattern: "parse_dates\\("
    - from: "R/mod_harmonize.R"
      to: "expanded_curated$year"
      via: "date_year merge before ToxVal mapping"
      pattern: "expanded_curated\\$year"
    - from: "R/curate_headless.R"
      to: "R/date_parser.R"
      via: "parse_dates() call in Stage 3c"
      pattern: "parse_dates\\("
    - from: "R/curate_headless.R"
      to: "input_df$year"
      via: "date_year merge before ToxVal mapping"
      pattern: "input_df\\$year"
---

<objective>
Wire `parse_dates()` into both the Shiny harmonization pipeline (mod_harmonize.R) and the headless pipeline (curate_headless.R), merge date_year into the curated data as `year` for ToxVal schema `original_year` population, add date-specific QC dashboard value boxes, and update the empty-state gate so StudyDate-tagged columns enable the harmonize button.

Purpose: This connects the date parsing engine (Plan 01) and tag system (Plan 02) to the production pipelines, completing the end-to-end flow from column tagging to ToxVal export.
Output: Updated `R/mod_harmonize.R`, `R/curate_headless.R`
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-date-parser/40-CONTEXT.md
@.planning/phases/40-date-parser/40-RESEARCH.md
@.planning/phases/40-date-parser/40-PATTERNS.md
@.planning/phases/40-date-parser/40-UI-SPEC.md
@.planning/phases/39-duration-conversion/39-02-SUMMARY.md

<interfaces>
<!-- Key integration points the executor needs. All line numbers from current codebase. -->

From R/date_parser.R (created by Plan 01):
```r
parse_dates <- function(raw_dates, orig_row_id = seq_along(raw_dates))
# Returns: tibble(orig_row_id, raw_date, parsed_date, date_year, date_flag)
# date_flag values: "" | "partial" | "inferred_format" | "ambiguous" | "unparseable"
```

From R/mod_harmonize.R -- Duration stage template (lines 387-406):
```r
# Stage 4.5: Duration harmonization (D-13, DUR-03)
incProgress(0.05, detail = "Harmonizing durations...")
duration_cols <- names(numeric_tags_vec)[numeric_tags_vec == "Duration"]
duration_unit_cols <- names(numeric_tags_vec)[numeric_tags_vec == "DurationUnit"]
data_store$duration_results <- NULL
if (length(duration_cols) > 0 && length(duration_unit_cols) > 0) {
  dur_tibble <- harmonize_units(...)
  data_store$duration_results <- tibble::tibble(...)
}
```

From R/mod_harmonize.R -- Duration merge template (lines 415-430):
```r
if (!is.null(data_store$duration_results)) {
  dur_values_expanded <- dur_for_merge$study_duration_value[harmonize_tibble$orig_row_id]
  expanded_curated$study_duration_value <- dur_values_expanded
  expanded_curated$study_duration_units <- dur_units_expanded
}
```

From R/mod_harmonize.R -- has_numeric_tags gate (lines 156-159):
```r
output$has_numeric_tags <- reactive({
  !is.null(data_store$numeric_tags) && length(data_store$numeric_tags) > 0
})
```

From R/mod_harmonize.R -- Button enable/disable (lines 163-170):
```r
observe({
  has_tags <- !is.null(data_store$numeric_tags) && length(data_store$numeric_tags) > 0
  if (has_tags) { shinyjs::enable("run_harmonization") }
  else { shinyjs::disable("run_harmonization") }
})
```

From R/mod_harmonize.R -- QC dashboard (lines 470-519):
4-box layout with col_widths = c(3, 3, 3, 3). Theme: primary, success, info, warning.

From R/mod_harmonize.R -- incProgress totals (FULL MODE):
0.15 + 0.30 + 0.25 + 0.15 + 0.05 + 0.10 = 1.00
Date stage needs 0.05 -- steal from Stage 3 (0.25 -> 0.20) to maintain total 1.00.

From R/curate_headless.R -- Duration stage template (lines 275-294):
```r
# Stage 3.5: Duration harmonization
message("[headless] Stage 3.5: Harmonizing durations...")
duration_cols <- names(tag_map)[tag_map == "Duration"]
if (length(duration_cols) > 0 && length(duration_unit_cols) > 0) {
  dur_tibble <- harmonize_units(...)
  input_df$study_duration_value <- dur_tibble$harmonized_value[
    match(seq_len(nrow(input_df)), dur_tibble$orig_row_id)
  ]
}
```

From R/toxval_mapper.R -- original_year wiring (line 153):
```r
original_year = safe_extract_num(curated_data, "year", n_rows)
```
No change needed in toxval_mapper.R -- it already reads curated_data$year.

From 40-UI-SPEC.md -- QC value box definitions:
| Box | Theme | Icon |
| Dates Parsed | success | check-circle |
| Partial Dates | info | calendar |
| Ambiguous Dates | warning | question-circle |
| Unparseable Dates | danger | exclamation-triangle |

From 40-UI-SPEC.md -- Copywriting Contract:
- Notification: "Date parsing complete. {n} dates parsed, {n_ambiguous} flagged as ambiguous."
- Notification: "Date parsing: no valid dates found in column '{col}'. All rows unparseable."
- Progress stage label: "Parsing dates..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire date parsing stage and QC dashboard in mod_harmonize.R</name>
  <files>R/mod_harmonize.R</files>
  <read_first>
    - R/mod_harmonize.R (full file -- Stage 4.5 at lines 387-406, duration merge at lines 415-430, expanded_curated at line 413, ToxVal map at line 432, has_numeric_tags at lines 156-159, button enable at lines 163-170, QC dashboard at lines 470-519, incProgress totals)
    - .planning/phases/40-date-parser/40-PATTERNS.md (mod_harmonize.R section with exact insertion code)
    - .planning/phases/40-date-parser/40-UI-SPEC.md (QC value box spec, copywriting contract)
    - .planning/phases/40-date-parser/40-CONTEXT.md (D-06 advisory-only ambiguity, D-07 no schema column, D-12 dedup-eligible, D-16 merge before mapper, D-17 new stage after duration)
  </read_first>
  <action>
    **Five modifications to R/mod_harmonize.R:**

    **1. Update has_numeric_tags reactive (lines 156-159) to also check study_type_tags:**

    Keep `has_numeric_tags` as the output name (avoids touching the UI conditionalPanel JavaScript conditions) but broaden the check:
    ```r
    output$has_numeric_tags <- reactive({
      has_numeric <- !is.null(data_store$numeric_tags) && length(data_store$numeric_tags) > 0
      has_study   <- !is.null(data_store$study_type_tags) && length(data_store$study_type_tags) > 0
      has_numeric || has_study
    })
    ```

    **2. Update button enable/disable observe block (lines 163-170):**
    ```r
    observe({
      has_numeric <- !is.null(data_store$numeric_tags) && length(data_store$numeric_tags) > 0
      has_study   <- !is.null(data_store$study_type_tags) && length(data_store$study_type_tags) > 0
      if (has_numeric || has_study) {
        shinyjs::enable("run_harmonization")
      } else {
        shinyjs::disable("run_harmonization")
      }
    })
    ```

    **3. Insert Stage 4.6 after Stage 4.5 duration block (after line 406, before Stage 5 at line 408):**

    Adjust incProgress budget: reduce Stage 3 from `incProgress(0.25, ...)` to `incProgress(0.20, ...)` (line 342) to free 0.05 for Stage 4.6. New total: 0.15 + 0.30 + 0.20 + 0.15 + 0.05 + 0.05 + 0.10 = 1.00.

    Insert after the duration stage block:
    ```r
    # Stage 4.6: Date parsing (DATE-01, DATE-05)
    incProgress(0.05, detail = "Parsing dates...")
    study_type_tags_vec <- unlist(data_store$study_type_tags)
    date_cols <- if (!is.null(study_type_tags_vec)) {
      names(study_type_tags_vec)[study_type_tags_vec == "StudyDate"]
    } else {
      character(0)
    }
    data_store$date_results <- NULL

    if (length(date_cols) > 0) {
      date_tibble <- tryCatch(
        parse_dates(
          raw_dates = as.character(input_df[[date_cols[1]]]),
          orig_row_id = seq_len(nrow(input_df))
        ),
        error = function(e) {
          showNotification(
            paste0("Date parsing failed for column '", date_cols[1], "': ", conditionMessage(e), ". Column skipped."),
            type = "error",
            duration = 10
          )
          NULL
        }
      )

      if (!is.null(date_tibble)) {
        data_store$date_results <- date_tibble

        n_parsed <- sum(date_tibble$date_flag != "unparseable", na.rm = TRUE)
        n_ambiguous <- sum(date_tibble$date_flag == "ambiguous", na.rm = TRUE)

        if (n_parsed > 0) {
          showNotification(
            sprintf("Date parsing complete. %d dates parsed, %d flagged as ambiguous.", n_parsed, n_ambiguous),
            type = "message",
            duration = 5
          )
        } else {
          showNotification(
            sprintf("Date parsing: no valid dates found in column '%s'. All rows unparseable.", date_cols[1]),
            type = "warning",
            duration = 8
          )
        }
      }
    }
    ```

    **4. Insert date_year merge into expanded_curated (after the duration merge block, before the map_to_toxval_schema call at line 432):**

    Insert after the `if (!is.null(data_store$duration_results)) { ... }` block:
    ```r
    # Merge date_year into expanded_curated for original_year ToxVal mapping (DATE-06, D-16)
    if (!is.null(data_store$date_results)) {
      year_expanded <- data_store$date_results$date_year[harmonize_tibble$orig_row_id]
      expanded_curated$year <- year_expanded
    }
    ```
    This uses the same `harmonize_tibble$orig_row_id` subscript indexing as the duration merge. `safe_extract_num(curated_data, "year", n_rows)` in toxval_mapper.R (line 153) will pick up `expanded_curated$year` automatically.

    **5. Add date QC value boxes to the qc_dashboard renderUI (after existing 4-box layout, lines 490-519):**

    After the existing `bslib::layout_columns(col_widths = c(3, 3, 3, 3), ...)` block, add a conditional second row for date boxes:

    ```r
    # Date parsing QC boxes (conditional -- only when date results exist)
    date_qc_row <- NULL
    if (!is.null(data_store$date_results)) {
      dr <- data_store$date_results
      n_date_parsed  <- sum(dr$date_flag != "unparseable", na.rm = TRUE)
      n_partial      <- sum(dr$date_flag == "partial", na.rm = TRUE)
      n_ambiguous    <- sum(dr$date_flag == "ambiguous", na.rm = TRUE)
      n_unparseable  <- sum(dr$date_flag == "unparseable", na.rm = TRUE)

      date_qc_row <- bslib::layout_columns(
        col_widths = c(3, 3, 3, 3),
        bslib::value_box(
          title = "Dates Parsed",
          value = n_date_parsed,
          showcase = bsicons::bs_icon("check-circle"),
          theme = "success"
        ),
        bslib::value_box(
          title = "Partial Dates",
          value = n_partial,
          showcase = bsicons::bs_icon("calendar"),
          theme = "info"
        ),
        bslib::value_box(
          title = "Ambiguous Dates",
          value = n_ambiguous,
          showcase = bsicons::bs_icon("question-circle"),
          theme = "warning"
        ),
        bslib::value_box(
          title = "Unparseable Dates",
          value = n_unparseable,
          showcase = bsicons::bs_icon("exclamation-triangle"),
          theme = "danger"
        )
      )
    }
    ```

    Then update the final `div()` wrapper in `output$qc_dashboard` to include both rows:
    ```r
    div(
      class = stale_class,
      # Existing numeric QC boxes
      bslib::layout_columns(
        col_widths = c(3, 3, 3, 3),
        # ... existing 4 boxes ...
      ),
      # Date QC boxes (conditional)
      date_qc_row
    )
    ```

    Run `air format R/mod_harmonize.R` and `jarl check R/mod_harmonize.R` after all edits.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all('.'); testthat::test_dir('tests/testthat')" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_harmonize.R contains `# Stage 4.6: Date parsing`
    - R/mod_harmonize.R contains `parse_dates(` call
    - R/mod_harmonize.R contains `data_store$date_results <- date_tibble`
    - R/mod_harmonize.R contains `expanded_curated$year <- year_expanded`
    - R/mod_harmonize.R contains `has_study` in the has_numeric_tags reactive (broadened gate)
    - R/mod_harmonize.R contains `"Dates Parsed"` value box title
    - R/mod_harmonize.R contains `"Ambiguous Dates"` value box title
    - R/mod_harmonize.R contains `"Partial Dates"` value box title
    - R/mod_harmonize.R contains `"Unparseable Dates"` value box title
    - incProgress fractions in FULL MODE sum to 1.00
    - Full test suite runs with no new failures
  </acceptance_criteria>
  <done>Date parsing wired into Shiny harmonization pipeline as Stage 4.6; date_year merged into expanded_curated$year for ToxVal original_year; QC dashboard shows 4 date value boxes; harmonize button enables for StudyDate-only columns.</done>
</task>

<task type="auto">
  <name>Task 2: Wire date parsing in curate_headless.R</name>
  <files>R/curate_headless.R</files>
  <read_first>
    - R/curate_headless.R (full file -- Stage 3.5 duration at lines 275-294, Stage 4 ToxVal map at lines 296-303, tag_map usage at line 277)
    - .planning/phases/40-date-parser/40-PATTERNS.md (curate_headless.R section with exact insertion code)
    - .planning/phases/40-date-parser/40-CONTEXT.md (D-18: Stage 3c conditional call)
  </read_first>
  <action>
    **Insert Stage 3c after Stage 3.5 duration block (after line 294, before Stage 4 ToxVal map at line 296):**

    ```r
    # Stage 3c: Date parsing (DATE-05, DATE-06)
    message("[headless] Stage 3c: Parsing dates...")
    date_cols <- names(tag_map)[tag_map == "StudyDate"]

    if (length(date_cols) > 0) {
      date_tibble <- parse_dates(
        raw_dates = as.character(input_df[[date_cols[1]]]),
        orig_row_id = seq_len(nrow(input_df))
      )
      # Join by position via match() -- same pattern as duration (lines 288-293)
      input_df$year <- date_tibble$date_year[
        match(seq_len(nrow(input_df)), date_tibble$orig_row_id)
      ]
    }
    ```

    This uses `tag_map` (not `merged_tags`) for `StudyDate` lookup, consistent with the Duration/DurationUnit lookup pattern at line 277. The `match()` join ensures correct alignment even if future changes introduce reordering.

    The existing `map_to_toxval_schema(curated_data = input_df, ...)` call at line 298 reads `input_df$year` via `safe_extract_num(curated_data, "year", n_rows)` -- no change needed in toxval_mapper.R.

    Run `air format R/curate_headless.R` and `jarl check R/curate_headless.R` after editing.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all('.'); testthat::test_dir('tests/testthat')" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - R/curate_headless.R contains `# Stage 3c: Date parsing (DATE-05, DATE-06)`
    - R/curate_headless.R contains `parse_dates(` call
    - R/curate_headless.R contains `input_df$year <- date_tibble$date_year[`
    - R/curate_headless.R uses `tag_map` (not `merged_tags`) for StudyDate lookup
    - Full test suite runs with no new failures
  </acceptance_criteria>
  <done>Date parsing wired into headless pipeline as Stage 3c; input_df$year populated before ToxVal mapping; curate_headless() produces identical date output to Shiny path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Shiny cold boot and visual verification</name>
  <files>R/mod_harmonize.R, R/curate_headless.R</files>
  <action>
    Run the Shiny cold boot test to verify the app starts without error after all Phase 40 changes. The executor starts the app in the background, waits for "Listening on" output, then stops the process. This is the mandatory smoke test per project CLAUDE.md.

    Command: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('.', port=3838, launch.browser=FALSE)"`

    If the app fails to start, diagnose and fix the issue before presenting for human verification.
  </action>
  <what-built>
    Date parsing pipeline wired end-to-end in both Shiny and headless paths:
    - parse_dates() in R/date_parser.R (Plan 01)
    - StudyDate tag in classify_tags() and dropdown (Plan 02)
    - Stage 4.6 in mod_harmonize.R with QC dashboard date boxes
    - Stage 3c in curate_headless.R
    - has_numeric_tags gate broadened for StudyDate-only tagging
  </what-built>
  <how-to-verify>
    1. Start the app: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('.', port=3838, launch.browser=FALSE)"`
    2. Confirm app starts without error (shows "Listening on http://127.0.0.1:3838")
    3. Open http://127.0.0.1:3838 in browser
    4. Upload a test CSV/XLSX file that has a date column with mixed formats
    5. Go to "Tag Columns" tab -- verify the tag dropdown shows "Study / Contextual" optgroup with "Study Date" entry
    6. Tag a column as "Study Date" and click "Apply Tags"
    7. Go to "Harmonize" tab -- verify the "Run Harmonization" button is enabled (even without numeric tags)
    8. Click "Run Harmonization" -- verify progress bar shows "Parsing dates..." stage
    9. After completion, verify QC dashboard shows date-specific value boxes below the existing boxes
    10. If any dates were ambiguous, the "Ambiguous Dates" box should show a non-zero count in warning theme
  </how-to-verify>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('.', port=3838, launch.browser=FALSE)" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - App starts and prints "Listening on http://127.0.0.1:3838" without error
    - No missing icon errors, no source() failures, no module wiring crashes
  </acceptance_criteria>
  <done>Shiny app cold boots successfully; user verifies tag dropdown, harmonize button enable, date parsing stage, and QC dashboard value boxes.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data_store -> expanded_curated$year | Parsed year values enter the ToxVal schema via expanded_curated |
| data_store -> QC dashboard | date_flag values drive conditional UI rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-06 | Tampering | R/mod_harmonize.R | accept | date_results stored in server-side reactiveValues, not client-accessible. Year values are integers extracted by lubridate -- no injection vector. |
| T-40-07 | Denial of Service | R/mod_harmonize.R | mitigate | parse_dates() wrapped in tryCatch with showNotification on failure -- pipeline continues even if date stage errors. Error does not abort unit harmonization or ToxVal mapping. |
| T-40-08 | Information Disclosure | R/mod_harmonize.R QC dashboard | accept | Date counts (parsed, ambiguous, unparseable) are aggregate metrics, not individual data values. No PII exposure. |
</threat_model>

<verification>
1. Shiny cold boot passes without error
2. R/mod_harmonize.R contains Stage 4.6, parse_dates call, expanded_curated$year merge, 4 date QC value boxes
3. R/curate_headless.R contains Stage 3c, parse_dates call, input_df$year assignment
4. has_numeric_tags reactive checks both numeric_tags and study_type_tags
5. incProgress fractions sum to 1.00
6. Full test suite has no new failures
</verification>

<success_criteria>
- StudyDate-tagged columns route through parse_dates() in both Shiny and headless paths per DATE-04/DATE-05
- date_year populates expanded_curated$year -> ToxVal original_year per DATE-06
- QC dashboard shows date-specific value boxes per D-06
- Harmonize button enables for StudyDate-only columns
- App cold boots without error
</success_criteria>

<output>
After completion, create `.planning/phases/40-date-parser/40-03-SUMMARY.md`
</output>
