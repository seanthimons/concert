---
phase: 49-conflict-scoring-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/curation.R
  - R/consensus.R
  - tests/testthat/test-enrichment.R
  - tests/testthat/test-consensus.R
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
must_haves:
  truths:
    - "enrich_synonyms() fetches synonym tiers from CompTox API and stores a pipe-joined string in the enrichment cache"
    - "compute_similarity_scores() returns resolution_state with a numeric similarity_score column for disagree rows and NA for non-disagree rows"
    - "Score formula implements max(JW(input, preferredName, synonym_1, ..., synonym_N)) with +0.05 rank bonus for rank <= 3, clamped to [0, 1]"
    - "score_one_candidate() is a standalone helper that computes a single candidate's score for reuse in the modal"
  artifacts:
    - path: "R/curation.R"
      provides: "enrich_synonyms() and flatten_synonym_tiers() functions"
      contains: "enrich_synonyms"
    - path: "R/consensus.R"
      provides: "compute_similarity_scores() and score_one_candidate() functions"
      contains: "compute_similarity_scores"
    - path: "tests/testthat/test-enrichment.R"
      provides: "enrich_synonyms() unit tests with mocked ComptoxR"
      contains: "enrich_synonyms"
    - path: "tests/testthat/test-consensus.R"
      provides: "compute_similarity_scores() unit tests with synthetic data"
      contains: "compute_similarity_scores"
  key_links:
    - from: "R/consensus.R"
      to: "R/curation.R"
      via: "enrichment_cache$synonyms column consumed by compute_similarity_scores()"
      pattern: "enrichment_cache\\$synonyms"
    - from: "R/consensus.R"
      to: "stringdist"
      via: "stringdist::stringdist(..., method = 'jw') for JW similarity"
      pattern: "stringdist::stringdist"
---

<objective>
Implement the backend scoring infrastructure for Phase 49: Conflict Scoring Engine.

Purpose: Create the synonym caching function and similarity scoring function that form the computation layer for conflict scores. These pure functions have no Shiny dependency and are independently testable.

Output: `enrich_synonyms()` in R/curation.R, `compute_similarity_scores()` and `score_one_candidate()` in R/consensus.R, plus unit tests in existing test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-conflict-scoring-engine/49-CONTEXT.md
@.planning/phases/49-conflict-scoring-engine/49-RESEARCH.md
@.planning/phases/49-conflict-scoring-engine/49-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/curation.R (enrich_candidates pattern, lines 960-1064):
```r
# Signature and return contract:
enrich_candidates <- function(dtxsids, existing_cache = NULL)
# Returns: list(cache = tibble(dtxsid, casrn, molecular_formula, molecular_weight),
#               failed_dtxsids = character())

# Empty cache schema:
empty_cache <- tibble::tibble(
  dtxsid = character(0),
  casrn = character(0),
  molecular_formula = character(0),
  molecular_weight = numeric(0)
)

# Incremental caching: filters already-cached IDs, fetches only new
# API call: ComptoxR::ct_chemical_detail_search_bulk(dtxsids_to_fetch)
# Error handling: tryCatch with api_error <<- pattern
# Partial response: NA rows for missing DTXSIDs
# Final: dplyr::bind_rows(existing_cache, new_cache)
```

From R/consensus.R (get_resolution_options pattern, lines 190-258):
```r
# Column name derivation from dtxsid_cols:
pref_col <- sub("^dtxsid_", "preferredName_", col)
rank_col <- sub("^dtxsid_", "rank_", col)
tier_col <- sub("^dtxsid_", "source_tier_", col)

# Enrichment cache lookup:
match_idx <- which(enrichment_cache$dtxsid == val)
if (length(match_idx) > 0) {
  enrich_casrn <- enrichment_cache$casrn[match_idx[1]]
}
```

From R/wqx_matching.R (JW similarity pattern, line 112):
```r
# stringdist returns DISTANCE (0=identical, 1=maximally different)
# Similarity = 1 - distance
dist_matrix <- stringdist::stringdistmatrix(
  names_clean[still_unresolved_idx],
  tolower(canonical_name_vec),
  method = "jw"
)
```

From tests/testthat/test-enrichment.R (mock pattern, lines 14-17):
```r
testthat::local_mocked_bindings(
  ct_chemical_detail_search_bulk = function(...) mock_response,
  .package = "ComptoxR"
)
```

From tests/testthat/test-consensus.R (synthetic df pattern, lines 29-44):
```r
df <- data.frame(
  Chemical = c("Toluene"),
  CAS = c("108-88-3"),
  dtxsid_Chemical = c("DTXSID7021360"),
  dtxsid_CAS = c("DTXSID7021360"),
  stringsAsFactors = FALSE
)
dtxsid_cols <- c("dtxsid_Chemical", "dtxsid_CAS")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enrich_synonyms() and flatten_synonym_tiers() to R/curation.R</name>
  <files>R/curation.R, tests/testthat/test-enrichment.R</files>

  <read_first>
    - R/curation.R (full file -- especially lines 960-1064 for the enrich_candidates() pattern to mirror)
    - tests/testthat/test-enrichment.R (full file -- for the mock and assertion patterns)
    - .planning/phases/49-conflict-scoring-engine/49-PATTERNS.md (Pattern Assignment for R/curation.R)
    - .planning/phases/49-conflict-scoring-engine/49-RESEARCH.md (Pattern 1: Synonym Cache Extension)
  </read_first>

  <action>
Add two new exported functions to R/curation.R, placed immediately after the closing brace of `enrich_candidates()` (after line 1064):

**1. `flatten_synonym_tiers(row)` helper (not exported):**

```r
#' Flatten all synonym tiers from a single API response row into a pipe-joined string
#' @param row A single-row list/tibble from ct_chemical_synonym_search_bulk output
#' @return Character string of pipe-joined unique synonyms, or NA_character_ if none
#' @noRd
flatten_synonym_tiers <- function(row) {
  tier_cols <- c("valid", "good", "other", "deleted", "beilstein", "alternate", "pcCode")
  syns <- unlist(lapply(tier_cols, function(col) {
    vals <- row[[col]]
    if (!is.null(vals)) as.character(unlist(vals)) else character(0)
  }))
  syns <- unique(syns[!is.na(syns) & nchar(syns) > 0])
  if (length(syns) == 0) NA_character_ else paste(syns, collapse = "|")
}
```

**2. `enrich_synonyms(dtxsids, existing_cache = NULL)` (exported):**

Mirror the exact incremental-caching pattern from `enrich_candidates()` (lines 960-1064). Key differences:

- Empty cache schema: `tibble::tibble(dtxsid = character(0), synonyms = character(0))`
- API call: `ComptoxR::ct_chemical_synonym_search_bulk(dtxsids_to_fetch)` instead of `ct_chemical_detail_search_bulk`
- Incremental filtering: check `existing_cache$dtxsid` AND `"synonyms" %in% names(existing_cache)`. If the existing_cache lacks a `synonyms` column, treat ALL DTXSIDs as needing fetch.
- After API returns, flatten each row's tiers using `flatten_synonym_tiers()`. Build a `new_syn_cache` tibble with columns `dtxsid` and `synonyms` (pipe-joined string, NA_character_ for DTXSIDs with no synonym data).
- For DTXSIDs not in the API response, create rows with `synonyms = NA_character_`.
- If the existing_cache already has a `synonyms` column: do `dplyr::left_join()` to merge new synonyms into the existing cache by `dtxsid`. If existing_cache does NOT have a `synonyms` column: add it via `dplyr::left_join(existing_cache, new_syn_cache, by = "dtxsid")`.
- Return `list(cache = combined, failed_dtxsids = character(0))` on success, or `list(cache = existing_cache %||% empty_enrichment_cache_with_synonyms, failed_dtxsids = dtxsids_to_fetch)` on API failure.
- All message() calls prefixed with `[synonyms]` (e.g., `message("[synonyms] Fetching synonyms for %d DTXSIDs...")`).

**Important design note per D-01:** `enrich_synonyms()` returns the FULL enrichment cache (with the existing casrn/formula/mw columns preserved plus the new `synonyms` column added/updated). It does NOT return a separate synonym-only cache. The caller assigns the result directly to `data_store$enrichment_cache`.

Add roxygen2 documentation block matching the `enrich_candidates()` doc style. Add `@export` tag.

**3. Tests in test-enrichment.R:**

Add a new test group section at the end of the file:

```r
# ============================================================================
# Test Group N: enrich_synonyms - synonym fetching and caching
# ============================================================================
```

Add these test cases (mock `ct_chemical_synonym_search_bulk` via `testthat::local_mocked_bindings(.package = "ComptoxR")`):

a. **"enrich_synonyms returns cache with synonyms column"** -- Mock returns a tibble with one DTXSID having `valid = list(c("Silica gel", "Quartz"))`, `good = list(c("Silicon dioxide"))`, other tiers NULL. Assert: result$cache has `synonyms` column, value is `"Silica gel|Quartz|Silicon dioxide"` (or similar unique pipe-joined string). Assert: `dtxsid` column present.

b. **"enrich_synonyms stores NA when API returns 0 rows"** -- Mock returns a 0-row tibble. Assert: result$cache has `synonyms = NA_character_` for the queried DTXSID.

c. **"enrich_synonyms merges into existing cache preserving other columns"** -- Pass an existing_cache with `dtxsid`, `casrn`, `molecular_formula`, `molecular_weight` (no `synonyms` column). Mock returns synonyms. Assert: result$cache has ALL original columns PLUS `synonyms`.

d. **"enrich_synonyms skips already-cached DTXSIDs with synonyms"** -- Pass existing_cache that already has a `synonyms` column for one DTXSID. Query two DTXSIDs. Assert: API mock is called with only the uncached DTXSID. Use a call_count counter like the existing test at line 43.

e. **"enrich_synonyms handles API failure gracefully"** -- Mock throws an error. Assert: returns existing_cache unchanged, `failed_dtxsids` contains the queried DTXSIDs.
  </action>

  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-enrichment.R')"</automated>
  </verify>

  <acceptance_criteria>
    - R/curation.R contains `enrich_synonyms <- function(dtxsids, existing_cache = NULL)`
    - R/curation.R contains `flatten_synonym_tiers <- function(row)`
    - R/curation.R contains `#' @export` before `enrich_synonyms`
    - tests/testthat/test-enrichment.R contains at least 5 new test_that blocks with "enrich_synonyms" in the description
    - test-enrichment.R contains `local_mocked_bindings` with `ct_chemical_synonym_search_bulk`
    - All tests in test-enrichment.R pass (exit code 0)
    - enrich_synonyms return value has structure: list(cache = tibble with dtxsid + synonyms columns, failed_dtxsids = character)
  </acceptance_criteria>

  <done>
    enrich_synonyms() fetches CompTox synonym tiers, flattens to pipe-joined string, stores in enrichment cache with incremental caching. flatten_synonym_tiers() correctly extracts from all tier columns. All 5+ new unit tests pass with mocked API calls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add compute_similarity_scores() and score_one_candidate() to R/consensus.R</name>
  <files>R/consensus.R, tests/testthat/test-consensus.R</files>

  <read_first>
    - R/consensus.R (full file -- especially lines 190-258 for get_resolution_options() column derivation pattern)
    - tests/testthat/test-consensus.R (full file -- for synthetic df construction pattern)
    - .planning/phases/49-conflict-scoring-engine/49-PATTERNS.md (Pattern Assignment for R/consensus.R)
    - .planning/phases/49-conflict-scoring-engine/49-RESEARCH.md (Pattern 2: Score Formula, Pattern 3: Input Name Column)
    - .planning/phases/49-conflict-scoring-engine/49-CONTEXT.md (D-03, D-04, D-07, D-08 locked decisions)
  </read_first>

  <action>
Add two new exported functions to R/consensus.R. Place them after `get_resolution_options()` (after line 258):

**1. `score_one_candidate(input_name, preferred_name, synonyms_str, rank)` (exported):**

This is a pure function that scores ONE candidate against ONE input string. It will be reused both by `compute_similarity_scores()` and by the modal card rendering in Plan 02.

```r
#' Compute similarity score for a single candidate against an input name
#'
#' Score formula (per D-03): max(JW(input, preferredName, synonym_1, ..., synonym_N)).
#' If candidate rank <= 3, add +0.05 bonus. Clamp final score to [0, 1].
#' All synonym tiers treated equally (per D-04).
#'
#' @param input_name Character scalar -- user's original chemical name (per D-07)
#' @param preferred_name Character scalar -- candidate's CompTox preferred name (NA allowed)
#' @param synonyms_str Character scalar -- pipe-joined synonyms from enrichment cache (NA allowed)
#' @param rank Numeric scalar -- candidate's rank value (NA allowed; bonus only if <= 3)
#' @return Numeric scalar in [0, 1], or NA_real_ if no valid comparison possible
#' @export
score_one_candidate <- function(input_name, preferred_name, synonyms_str, rank) {
  if (is.na(input_name) || nchar(trimws(input_name)) == 0) return(NA_real_)

  syns <- if (!is.na(synonyms_str)) strsplit(synonyms_str, "|", fixed = TRUE)[[1]] else character(0)
  all_names <- c(preferred_name, syns)
  all_names <- all_names[!is.na(all_names) & nchar(trimws(all_names)) > 0]

  if (length(all_names) == 0) return(NA_real_)

  sims <- 1 - stringdist::stringdist(tolower(input_name), tolower(all_names), method = "jw")
  base_score <- max(sims, na.rm = TRUE)

  bonus <- if (!is.na(rank) && rank <= 3) 0.05 else 0.0
  min(1.0, base_score + bonus)
}
```

**2. `compute_similarity_scores(resolution_state, enrichment_cache, dtxsid_cols, column_tags)` (exported):**

This function iterates over disagree rows, computes the best candidate score for each, and adds a `similarity_score` column.

```r
#' Compute similarity scores for all disagree rows in resolution_state
#'
#' For each disagree row, scores each candidate DTXSID by comparing the user's
#' original Name-tagged column value against the candidate's preferredName and
#' all cached synonyms. Returns the best (max) candidate score per row.
#' Non-disagree rows get NA_real_.
#'
#' @param resolution_state Data frame with consensus_status, dtxsid_*, preferredName_*, rank_* columns
#' @param enrichment_cache Tibble with dtxsid and (optionally) synonyms columns
#' @param dtxsid_cols Character vector of dtxsid column names (e.g., c("dtxsid_Chemical", "dtxsid_CAS"))
#' @param column_tags Named list (col_name -> "Name"|"CASRN"|"Other") -- used to find input name column
#' @return resolution_state with similarity_score column added
#' @export
compute_similarity_scores <- function(resolution_state, enrichment_cache, dtxsid_cols, column_tags) {
  n <- nrow(resolution_state)
  scores <- rep(NA_real_, n)

  # Identify the Name-tagged column (per D-07: input is always user's original chemical name)
  name_cols <- names(column_tags)[column_tags == "Name"]
  if (length(name_cols) == 0) {
    message("[scoring] No Name-tagged column found -- skipping similarity scoring")
    resolution_state$similarity_score <- scores
    return(resolution_state)
  }
  name_col <- name_cols[1]  # Use first Name-tagged column

  disagree_idx <- which(resolution_state$consensus_status == "disagree")

  if (length(disagree_idx) == 0) {
    resolution_state$similarity_score <- scores
    return(resolution_state)
  }

  has_synonyms <- !is.null(enrichment_cache) &&
    nrow(enrichment_cache) > 0 &&
    "synonyms" %in% names(enrichment_cache)

  for (i in disagree_idx) {
    input_name <- as.character(resolution_state[[name_col]][i])
    if (is.na(input_name) || nchar(trimws(input_name)) == 0) next

    candidate_scores <- numeric(0)

    for (col in dtxsid_cols) {
      dtxsid_val <- resolution_state[[col]][i]
      if (is.na(dtxsid_val)) next

      pref_col <- sub("^dtxsid_", "preferredName_", col)
      rank_col <- sub("^dtxsid_", "rank_", col)
      pref_name <- if (pref_col %in% names(resolution_state)) resolution_state[[pref_col]][i] else NA_character_
      rank_val <- if (rank_col %in% names(resolution_state)) resolution_state[[rank_col]][i] else NA_real_

      # Get synonyms from cache
      synonyms_str <- NA_character_
      if (has_synonyms) {
        match_idx <- which(enrichment_cache$dtxsid == dtxsid_val)
        if (length(match_idx) > 0) {
          synonyms_str <- enrichment_cache$synonyms[match_idx[1]]
        }
      }

      cs <- score_one_candidate(input_name, pref_name, synonyms_str, rank_val)
      if (!is.na(cs)) candidate_scores <- c(candidate_scores, cs)
    }

    if (length(candidate_scores) > 0) {
      scores[i] <- max(candidate_scores)
    }
  }

  resolution_state$similarity_score <- scores
  resolution_state
}
```

**3. Tests in test-consensus.R:**

Add a new test group section at the end of the file:

```r
# ============================================================================
# Test Group N: score_one_candidate - single candidate scoring
# ============================================================================
```

And:

```r
# ============================================================================
# Test Group N+1: compute_similarity_scores - batch scoring
# ============================================================================
```

Add these test cases:

**score_one_candidate tests:**

a. **"score_one_candidate: exact synonym match scores near 1.0"** -- `input_name = "Silica"`, `preferred_name = "Silicon Dioxide"`, `synonyms_str = "Silica gel|Silica|Quartz"`, `rank = 1`. Assert: result >= 0.95 (exact JW match on "Silica" + rank bonus = 1.0, clamped).

b. **"score_one_candidate: dissimilar name scores low"** -- `input_name = "Silica"`, `preferred_name = "Estradiol"`, `synonyms_str = NA_character_`, `rank = 5`. Assert: result < 0.5.

c. **"score_one_candidate: rank bonus adds 0.05 for rank <= 3"** -- Same input but rank = 2 vs rank = 5. Assert: rank-2 result is exactly 0.05 higher than rank-5 result (both with same names).

d. **"score_one_candidate: clamped to 1.0"** -- `input_name = "Toluene"`, `preferred_name = "Toluene"`, `synonyms_str = NA_character_`, `rank = 1`. JW("toluene","toluene") = 1.0, + 0.05 = 1.05, clamped to 1.0. Assert: result == 1.0.

e. **"score_one_candidate: NA input returns NA"** -- `input_name = NA_character_`. Assert: result is NA_real_.

f. **"score_one_candidate: no valid names returns NA"** -- `preferred_name = NA_character_`, `synonyms_str = NA_character_`. Assert: result is NA_real_.

**compute_similarity_scores tests:**

g. **"compute_similarity_scores: disagree rows get numeric score, others NA"** -- Build a 3-row resolution_state: row 1 = "agree", row 2 = "disagree", row 3 = "single". Add dtxsid_Chemical, preferredName_Chemical, rank_Chemical columns. Build enrichment_cache with synonyms column. column_tags = list(Chemical = "Name"). Assert: result$similarity_score[1] is NA, result$similarity_score[2] is numeric and not NA, result$similarity_score[3] is NA.

h. **"compute_similarity_scores: best candidate score is selected"** -- 1 disagree row with 2 candidates: one high-similarity (Silica synonym match), one low (Estradiol). Assert: similarity_score equals the higher of the two candidate scores.

i. **"compute_similarity_scores: missing synonyms column falls back to preferredName-only"** -- Pass enrichment_cache WITHOUT a `synonyms` column. Assert: scoring still works (based on preferredName comparison only), no error.

j. **"compute_similarity_scores: no Name-tagged column returns all NA"** -- Pass `column_tags = list(CAS_Number = "CASRN")` with no "Name" tag. Assert: all similarity_score values are NA.
  </action>

  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-consensus.R')"</automated>
  </verify>

  <acceptance_criteria>
    - R/consensus.R contains `score_one_candidate <- function(input_name, preferred_name, synonyms_str, rank)`
    - R/consensus.R contains `compute_similarity_scores <- function(resolution_state, enrichment_cache, dtxsid_cols, column_tags)`
    - R/consensus.R contains `#' @export` before both functions
    - R/consensus.R contains `stringdist::stringdist(` with `method = "jw"` (not stringsim)
    - R/consensus.R contains `min(1.0, base_score + bonus)` clamping logic
    - tests/testthat/test-consensus.R contains at least 10 new test_that blocks (6 for score_one_candidate + 4 for compute_similarity_scores)
    - test-consensus.R contains `score_one_candidate` in test descriptions
    - test-consensus.R contains `compute_similarity_scores` in test descriptions
    - All tests in test-consensus.R pass (exit code 0)
  </acceptance_criteria>

  <done>
    score_one_candidate() implements D-03 formula: max JW similarity across preferredName + all synonyms, +0.05 rank bonus for rank <= 3, clamped to [0, 1]. compute_similarity_scores() iterates disagree rows, finds best candidate score, writes similarity_score column. All 10+ unit tests pass. Functions are exported and documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompTox API -> enrichment cache | Synonym strings from external API stored as display-only data |
| enrichment_cache -> score computation | Cache data consumed by stringdist comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | flatten_synonym_tiers() | mitigate | Guard with `nchar(syns) > 0` and `!is.na(syns)` filters before pipe-joining; malformed strings produce NA, not crashes |
| T-49-02 | Information Disclosure | compute_similarity_scores() NA propagation | mitigate | Explicit `na.rm = TRUE` in `max(sims)` call; NA scores render as blank in table (no error leakage) |
| T-49-03 | Denial of Service | enrich_synonyms() API call | accept | ComptoxR handles internal batching (default 100/batch); same pattern as existing enrich_candidates() which handles 1000+ DTXSIDs |
</threat_model>

<verification>
1. `testthat::test_file('tests/testthat/test-enrichment.R')` -- all tests pass including new enrich_synonyms tests
2. `testthat::test_file('tests/testthat/test-consensus.R')` -- all tests pass including new scoring tests
3. `devtools::load_all()` succeeds with no namespace errors
4. `grep("enrich_synonyms", "R/curation.R")` finds the function
5. `grep("compute_similarity_scores", "R/consensus.R")` finds the function
6. `grep("score_one_candidate", "R/consensus.R")` finds the function
</verification>

<success_criteria>
- enrich_synonyms() correctly fetches, flattens, and caches synonym data with the incremental caching pattern
- compute_similarity_scores() produces a numeric similarity_score column on resolution_state for disagree rows
- score_one_candidate() implements the D-03 formula exactly (max JW + rank bonus + clamp)
- All existing tests continue to pass (no regressions)
- All new tests pass with mocked API calls (no live API needed)
</success_criteria>

<output>
After completion, create `.planning/phases/49-conflict-scoring-engine/49-01-SUMMARY.md`
</output>
