---
phase: 49-conflict-scoring-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - R/mod_run_curation.R
  - R/mod_review_results.R
  - R/export_helpers.R
autonomous: false
requirements:
  - SCORE-01
  - SCORE-02
must_haves:
  truths:
    - "Synonym fetch and score computation are wired into the curation pipeline after enrichment"
    - "Review Results table shows a Sim. Score column for disagree rows, blank for non-disagree"
    - "Comparison modal cards show per-candidate similarity scores as badges"
    - "Exported Curated Data sheet includes similarity_score column"
    - "Shiny app starts without errors after all changes"
  artifacts:
    - path: "R/mod_run_curation.R"
      provides: "Synonym fetch + score computation wiring after enrichment"
      contains: "enrich_synonyms"
    - path: "R/mod_review_results.R"
      provides: "Sim. Score colDef and modal per-candidate score badges"
      contains: "similarity_score"
    - path: "R/export_helpers.R"
      provides: "Verification that similarity_score flows through export"
      contains: "similarity_score"
  key_links:
    - from: "R/mod_run_curation.R"
      to: "R/curation.R"
      via: "enrich_synonyms() call after enrich_candidates()"
      pattern: "enrich_synonyms"
    - from: "R/mod_run_curation.R"
      to: "R/consensus.R"
      via: "compute_similarity_scores() call after synonym fetch"
      pattern: "compute_similarity_scores"
    - from: "R/mod_review_results.R"
      to: "R/consensus.R"
      via: "score_one_candidate() called inline in modal card builder"
      pattern: "score_one_candidate"
---

<objective>
Wire the scoring infrastructure into the Shiny app: connect synonym fetch and score computation into the curation pipeline, display scores in the Review Results table and comparison modal, and verify export passthrough.

Purpose: Make similarity scores visible to users in the Review Results table (best score per row) and comparison modal (per-candidate scores), completing SCORE-01 and SCORE-02 requirements.

Output: Modified R/mod_run_curation.R (wiring), R/mod_review_results.R (display), verified R/export_helpers.R (passthrough), Shiny smoke test passing.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-conflict-scoring-engine/49-CONTEXT.md
@.planning/phases/49-conflict-scoring-engine/49-RESEARCH.md
@.planning/phases/49-conflict-scoring-engine/49-PATTERNS.md
@.planning/phases/49-conflict-scoring-engine/49-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 that this plan consumes -->

From R/curation.R (created in Plan 01):
```r
#' @export
enrich_synonyms <- function(dtxsids, existing_cache = NULL)
# Returns: list(cache = tibble(dtxsid, casrn, molecular_formula, molecular_weight, synonyms),
#               failed_dtxsids = character())
# The returned cache is the FULL enrichment cache with synonyms column added/updated.
# Caller assigns directly: data_store$enrichment_cache <- result$cache
```

From R/consensus.R (created in Plan 01):
```r
#' @export
score_one_candidate <- function(input_name, preferred_name, synonyms_str, rank)
# Returns: numeric scalar in [0, 1], or NA_real_ if no valid comparison
# input_name: user's original chemical name (D-07)
# preferred_name: candidate's CompTox preferred name
# synonyms_str: pipe-joined synonyms from enrichment_cache$synonyms
# rank: candidate's rank (bonus +0.05 if <= 3)

#' @export
compute_similarity_scores <- function(resolution_state, enrichment_cache, dtxsid_cols, column_tags)
# Returns: resolution_state with similarity_score column added
# NA for non-disagree rows, numeric [0,1] for disagree rows
# column_tags: named list (col_name -> "Name"|"CASRN"|"Other")
```

From R/mod_run_curation.R (existing enrichment wiring, lines 221-264):
```r
# Enrichment call site at line 229:
enrich_result <- enrich_candidates(
  dtxsids = all_unique_dtxsids,
  existing_cache = data_store$enrichment_cache
)
data_store$enrichment_cache <- enrich_result$cache
data_store$enrichment_failed <- enrich_result$failed_dtxsids
# ... notification logic ...
# tryCatch error handler at lines 256-264
```

From R/mod_review_results.R (WQX Conf. colDef, lines 769-788):
```r
wqx_conf_cols <- grep("^wqx_confidence", names(df_display), value = TRUE)
# ... visibility filter ...
for (wcc in wqx_conf_visible) {
  col_defs[[wcc]] <- reactable::colDef(
    name = "WQX Conf.",
    minWidth = 80,
    align = "right",
    cell = function(value, index) {
      if (is.na(value)) return("")
      formatC(value, digits = 2, format = "f")
    }
  )
}
```

From R/mod_review_results.R (modal card structure, lines 1213-1256):
```r
cards <- lapply(names(options), function(col) {
  opt <- options[[col]]
  div(
    class = "candidate-card card mb-2",
    ...
    div(
      class = "d-flex justify-content-between align-items-start",
      div(
        tags$h6(class = "mb-1 fw-bold", opt$dtxsid),
        if (!is.na(opt$preferredName)) tags$p(...) else NULL
      ),
      tags$button(class = "modal-select-btn btn btn-sm btn-outline-success", ...)
    ),
    tags$hr(class = "my-2"),
    div(class = "row small",
      div(class = "col-4", tags$strong("CASRN"), ...),
      div(class = "col-4", tags$strong("Formula"), ...),
      div(class = "col-4", tags$strong("Mol. Weight"), ...)
    ),
    div(class = "row small mt-2",
      div(class = "col-4", tags$strong("Source"), ...),
      div(class = "col-4", tags$strong("Match Type"), ...),
      div(class = "col-4", tags$strong("Rank"), ...)
    )
  )
})
```

From R/export_helpers.R (line 41-45):
```r
curated_data_sheet <- resolution_state %>%
  dplyr::mutate(needs_review = ...) %>%
  dplyr::select(-tidyselect::any_of(c(".pinned", ".manual_entry")))
# similarity_score is NOT in the exclusion list, so it flows through automatically
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire synonym fetch and score computation into curation pipeline</name>
  <files>R/mod_run_curation.R, R/export_helpers.R</files>

  <read_first>
    - R/mod_run_curation.R (full file -- especially lines 215-275 for the enrichment wiring block and tryCatch structure)
    - R/export_helpers.R (lines 35-60 -- verify similarity_score is not excluded by select(-any_of(...)))
    - .planning/phases/49-conflict-scoring-engine/49-PATTERNS.md (Pattern Assignment for R/mod_run_curation.R and R/export_helpers.R)
    - .planning/phases/49-conflict-scoring-engine/49-CONTEXT.md (D-01: synonym fetch at enrichment time, no API calls at score time)
  </read_first>

  <action>
**1. R/mod_run_curation.R -- Add synonym fetch + score computation:**

Locate the enrichment `tryCatch` block (approximately lines 220-264). The block structure is:

```r
tryCatch(
  {
    if (length(all_unique_dtxsids) > 0) {
      # ... enrichment notification ...
      enrich_result <- enrich_candidates(...)
      data_store$enrichment_cache <- enrich_result$cache
      data_store$enrichment_failed <- enrich_result$failed_dtxsids
      # ... enrichment success notifications ...
    }
  },
  error = function(e) { ... }
)
```

Insert the following INSIDE the `tryCatch` expression block, AFTER the existing enrichment notifications complete (after the `}` that closes `if (length(all_unique_dtxsids) > 0)`) but BEFORE the `error = function(e)` handler. This keeps synonym fetch and scoring inside the same non-fatal tryCatch:

```r
# --- Synonym fetch (per D-01: fetch at enrichment time, read from cache at score time) ---
if (length(all_unique_dtxsids) > 0) {
  synonym_result <- enrich_synonyms(
    dtxsids = all_unique_dtxsids,
    existing_cache = data_store$enrichment_cache
  )
  data_store$enrichment_cache <- synonym_result$cache
}

# --- Similarity scoring (per D-03: max JW + rank bonus, clamped [0,1]) ---
if (!is.null(data_store$resolution_state) &&
    !is.null(data_store$dtxsid_cols) &&
    !is.null(data_store$column_tags)) {
  data_store$resolution_state <- compute_similarity_scores(
    resolution_state = data_store$resolution_state,
    enrichment_cache = data_store$enrichment_cache,
    dtxsid_cols      = data_store$dtxsid_cols,
    column_tags      = data_store$column_tags
  )
}
```

The existing `error = function(e)` handler already logs and notifies the user without stopping curation -- synonym and scoring failures are non-fatal by being inside this same tryCatch.

**2. R/export_helpers.R -- Verify similarity_score passthrough:**

Read R/export_helpers.R lines 41-45 and confirm that `similarity_score` is NOT in the `any_of(c(".pinned", ".manual_entry"))` exclusion list. It should flow through automatically since `curated_data_sheet` starts from `resolution_state`.

If for any reason the column IS dropped (check the full pipeline), add it back. But based on current code, NO changes should be needed to export_helpers.R. Add a code comment near line 45 to make the passthrough explicit:

```r
# Note: similarity_score (from Phase 49) flows through automatically -- not excluded
```

This is a documentation-only change that makes the export path discoverable for future maintainers.
  </action>

  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n'); testthat::test_dir('tests/testthat')"</automated>
  </verify>

  <acceptance_criteria>
    - R/mod_run_curation.R contains `enrich_synonyms(` call with `dtxsids = all_unique_dtxsids`
    - R/mod_run_curation.R contains `compute_similarity_scores(` call with `resolution_state = data_store$resolution_state`
    - R/mod_run_curation.R contains `data_store$enrichment_cache <- synonym_result$cache`
    - R/mod_run_curation.R contains `data_store$resolution_state <- compute_similarity_scores(`
    - R/export_helpers.R contains a comment mentioning `similarity_score` near the select(-any_of(...)) line
    - `devtools::load_all()` succeeds without namespace errors
    - All existing tests in tests/testthat/ pass (no regressions from wiring changes)
  </acceptance_criteria>

  <done>
    Synonym fetch and score computation are wired into the curation pipeline inside the existing enrichment tryCatch block. Scoring runs after enrichment, reads from cache only (per D-01). Export passthrough is verified -- similarity_score column is not excluded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Sim. Score column to Review Results table and per-candidate badges to comparison modal</name>
  <files>R/mod_review_results.R</files>

  <read_first>
    - R/mod_review_results.R (full file -- especially lines 766-788 for WQX Conf. colDef pattern, lines 1170-1260 for modal card builder, lines 676-680 for column_tags usage)
    - .planning/phases/49-conflict-scoring-engine/49-PATTERNS.md (Pattern Assignment for R/mod_review_results.R)
    - .planning/phases/49-conflict-scoring-engine/49-CONTEXT.md (D-05: Sim. Score format, D-06: best score in table / per-candidate in modal)
  </read_first>

  <action>
**1. Add Sim. Score colDef to the Review Results reactable (per D-05):**

Locate the WQX Conf. colDef block (approximately lines 766-788). AFTER that block ends, add the Sim. Score colDef. Use the presence-gated pattern (per Pitfall 4 in RESEARCH.md):

```r
# Similarity score column (per D-05: 2-decimal right-aligned, blank for non-disagree)
if ("similarity_score" %in% names(df_display)) {
  col_defs[["similarity_score"]] <- reactable::colDef(
    name = "Sim. Score",
    minWidth = 80,
    align = "right",
    cell = function(value, index) {
      if (is.na(value)) return("")
      formatC(value, digits = 2, format = "f")
    }
  )
}
```

This mirrors the WQX Conf. colDef pattern exactly (same `minWidth = 80`, `align = "right"`, same NA-to-blank rendering, same `formatC(value, digits = 2, format = "f")` formatting). The column will only appear when `compute_similarity_scores()` has run and added the column to `resolution_state`.

**2. Add per-candidate score badges to comparison modal cards (per D-06):**

Locate the modal card builder `lapply(names(options), function(col) { ... })` block (approximately lines 1213-1257). Inside the `lapply` callback, BEFORE the card `div()` is returned, compute the per-candidate score.

The input name for scoring comes from the same Name-tagged column used elsewhere in the modal. Add this at the TOP of the `lapply` callback body (before the `div(class = "candidate-card card mb-2", ...)`):

```r
# Compute per-candidate similarity score for this candidate (per D-06)
candidate_sim_score <- NA_real_
if (!is.null(data_store$column_tags) && !is.null(data_store$enrichment_cache)) {
  name_cols <- names(data_store$column_tags)[data_store$column_tags == "Name"]
  if (length(name_cols) > 0 &&
      name_cols[1] %in% names(data_store$resolution_state)) {
    input_name <- as.character(data_store$resolution_state[[name_cols[1]]][row_idx])
    synonyms_str <- NA_character_
    if ("synonyms" %in% names(data_store$enrichment_cache)) {
      syn_idx <- which(data_store$enrichment_cache$dtxsid == opt$dtxsid)
      if (length(syn_idx) > 0) {
        synonyms_str <- data_store$enrichment_cache$synonyms[syn_idx[1]]
      }
    }
    candidate_sim_score <- score_one_candidate(
      input_name = input_name,
      preferred_name = opt$preferredName,
      synonyms_str = synonyms_str,
      rank = opt$rank
    )
  }
}
```

Then inject the score badge into the card header div. Locate the `div(class = "d-flex justify-content-between align-items-start", ...)` block. Between the left `div` (containing DTXSID + preferredName) and the right `tags$button` ("Select"), add the score badge:

```r
div(
  class = "d-flex justify-content-between align-items-start",
  div(
    tags$h6(class = "mb-1 fw-bold", opt$dtxsid),
    if (!is.na(opt$preferredName)) tags$p(class = "mb-1 text-muted", opt$preferredName) else NULL
  ),
  div(
    class = "d-flex align-items-center gap-2",
    if (!is.na(candidate_sim_score)) {
      tags$span(
        class = "badge bg-info",
        title = "Similarity score (Jaro-Winkler)",
        sprintf("%.2f", candidate_sim_score)
      )
    } else {
      NULL
    },
    tags$button(
      class = "modal-select-btn btn btn-sm btn-outline-success",
      `data-column` = col,
      "Select"
    )
  )
)
```

This wraps the badge + button in a flex container so they sit side-by-side. The badge uses Bootstrap's `bg-info` class (light blue) and a `title` tooltip explaining the metric. If score is NA (no scoring available), no badge is rendered.
  </action>

  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>

  <acceptance_criteria>
    - R/mod_review_results.R contains `col_defs[["similarity_score"]] <- reactable::colDef(`
    - R/mod_review_results.R contains `name = "Sim. Score"`
    - R/mod_review_results.R contains `minWidth = 80` in the Sim. Score colDef
    - R/mod_review_results.R contains `formatC(value, digits = 2, format = "f")` in the Sim. Score cell function
    - R/mod_review_results.R contains `if ("similarity_score" %in% names(df_display))` presence gate
    - R/mod_review_results.R contains `score_one_candidate(` call inside the modal card builder
    - R/mod_review_results.R contains `class = "badge bg-info"` for the score badge
    - R/mod_review_results.R contains `sprintf("%.2f", candidate_sim_score)` for badge formatting
    - `devtools::load_all()` succeeds without errors
  </acceptance_criteria>

  <done>
    Review Results table shows a "Sim. Score" column with 2-decimal right-aligned values for disagree rows and blank for non-disagree rows. Comparison modal cards show a blue badge with per-candidate similarity score next to the Select button.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Shiny smoke test and visual verification</name>
  <files>R/mod_run_curation.R, R/mod_review_results.R</files>

  <action>
Start the Shiny app and verify the full scoring pipeline visually. The executor runs the smoke test command, confirms the app starts without errors, then hands off to the user for visual verification.

1. Start the app in the background:
   ```
   cd C:/Users/sxthi/Documents/chemreg
   "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)"
   ```
   Wait for `Listening on http://0.0.0.0:3838` -- if an error occurs instead, fix it before proceeding.

2. Present the user with the following verification checklist:
   - Upload a test file that produces disagree rows
   - Run curation -- console shows `[synonyms]` and `[scoring]` messages
   - Review Results tab: "Sim. Score" column visible, 2-decimal scores for disagree rows, blank for non-disagree
   - Click a disagree row: modal cards show blue badge with per-candidate score
   - Export results: output file contains `similarity_score` column
  </action>

  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all(); chemreg::run_app(port=3838, launch.browser=FALSE)"</automated>
  </verify>

  <acceptance_criteria>
    - Shiny app starts without errors (Listening on http://0.0.0.0:3838 appears in console)
    - No R errors or warnings related to enrich_synonyms, compute_similarity_scores, or score_one_candidate during curation
    - User confirms: Sim. Score column visible in Review Results for disagree rows
    - User confirms: Per-candidate score badges visible in comparison modal
  </acceptance_criteria>

  <done>
    Shiny app starts cleanly. User has visually confirmed that similarity scores appear correctly in both the Review Results table and the comparison modal. Phase 49 is functionally complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompTox API -> enrichment cache | Synonym strings from external API flow into cache and are displayed in UI |
| enrichment_cache -> Shiny UI | Cached scores rendered in table colDef and modal badges |
| resolution_state -> export file | similarity_score column included in exported Excel/Parquet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Tampering | mod_review_results.R modal badge | mitigate | Score computed via score_one_candidate() from cached data; sprintf("%.2f", ...) ensures numeric-only display; no raw synonym strings rendered in badge |
| T-49-05 | Denial of Service | mod_run_curation.R synonym fetch | accept | Inside existing tryCatch; failure is non-fatal; curation results remain valid; same risk profile as existing enrich_candidates() |
| T-49-06 | Information Disclosure | export_helpers.R similarity_score | accept | Score is a numeric value [0,1] derived from public CompTox data; no sensitive information exposed |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds -- no namespace or syntax errors
2. `testthat::test_dir('tests/testthat')` -- all tests pass (no regressions)
3. Shiny app starts on port 3838 without errors -- `Listening on http://0.0.0.0:3838` appears
4. Manual verification: Sim. Score column visible for disagree rows in Review Results
5. Manual verification: Per-candidate badges visible in comparison modal
6. Manual verification: Export file contains similarity_score column
</verification>

<success_criteria>
- Synonym fetch and score computation run inside the curation pipeline tryCatch block
- Review Results table shows "Sim. Score" column: 2-decimal, right-aligned, blank for non-disagree
- Comparison modal shows per-candidate score badges (blue bg-info badge with 2-decimal value)
- Export file includes similarity_score column
- App starts cleanly with no console errors related to scoring
- All existing tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/49-conflict-scoring-engine/49-02-SUMMARY.md`
</output>
