---
phase: 03-prototype-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - scripts/run_prototype.R
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02

must_haves:
  truths:
    - "Script runs end-to-end against data/sample_messy.csv (7 rows) and produces lookup results"
    - "Script runs against first 100 rows of uncurated_chemicals_2023-05-16_12-43-41.csv and produces results"
    - "Output includes two tables: (1) lookup table keyed by unique input value, (2) joined-back table mapped to all original rows"
    - "Console output shows progress messages for each search tier"
  artifacts:
    - path: "scripts/run_prototype.R"
      provides: "Standalone script demonstrating full pipeline on real data"
      min_lines: 60
  key_links:
    - from: "scripts/run_prototype.R"
      to: "R/prototype_pipeline.R"
      via: "source() call"
      pattern: "source.*prototype_pipeline"
    - from: "scripts/run_prototype.R"
      to: "data/sample_messy.csv"
      via: "read_csv or rio::import"
      pattern: "sample_messy"
    - from: "scripts/run_prototype.R"
      to: "uncurated_chemicals_2023-05-16_12-43-41.csv"
      via: "read_csv"
      pattern: "uncurated_chemicals"
---

<objective>
Create a standalone runner script that demonstrates the full prototype pipeline against both test datasets, proving end-to-end functionality.

Purpose: Validates the pipeline functions from Plan 01 against real chemical data at two scales — 7 rows (sample_messy.csv) and 100 rows (uncurated_chemicals subset). Produces the two required outputs (lookup table + joined-back table).
Output: scripts/run_prototype.R that can be executed standalone.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-prototype-pipeline/03-CONTEXT.md
@.planning/phases/03-prototype-pipeline/03-01-SUMMARY.md

@R/prototype_pipeline.R
@data/sample_messy.csv

<interfaces>
<!-- From Plan 01 (R/prototype_pipeline.R): -->

deduplicate_tagged_columns(df, tag_map)
  - df: data.frame with chemical data
  - tag_map: named list e.g. list(Chemical = "Name", CAS = "CASRN")
  - Returns: list(unique_names, unique_cas, dedup_key_map)

run_tiered_search(dedup_result)
  - Input: output of deduplicate_tagged_columns
  - Returns: tibble of all lookup results with searchValue as primary key

map_results_to_rows(df, dedup_key_map, lookup_results)
  - Returns: original df with lookup columns joined back

<!-- Data structures: -->
<!-- sample_messy.csv: Chemical, CAS, Formula, MW, Hazard (after frontmatter detection) -->
<!--   Has 2 empty rows at top as frontmatter, data rows: Toluene, Chloroform, Acetic Acid, Phosphoric Acid -->
<!-- uncurated_chemicals CSV: id, raw_cas, raw_chem_name, datagroup_id -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone runner script</name>
  <files>scripts/run_prototype.R</files>
  <action>
Create scripts/ directory and scripts/run_prototype.R that demonstrates the full pipeline.

The script should:

1. **Setup:**
   - `source("R/prototype_pipeline.R")` to load pipeline functions
   - Load required packages: tidyverse, ComptoxR
   - Check API key: `stopifnot(nzchar(Sys.getenv("ctx_api_key")))` with helpful error message

2. **Dataset 1: sample_messy.csv (7 rows)**
   - Read data/sample_messy.csv using rio::import or readr::read_csv
   - Note: This file has 2 empty frontmatter rows then a header row. The actual data starts at row 3 (Chemical, CAS, Formula, MW, Hazard columns). Use skip parameter or read raw and detect.
   - Actually, looking at the file: row 1-2 are empty, row 3 is headers "Chemical,CAS,Formula,MW,Hazard,", rows 4-7 are data. Read with `read_csv("data/sample_messy.csv", skip = 2)` to skip empty rows.
   - Define tag_map: `list(Chemical = "Name", CAS = "CASRN")`
   - Run pipeline:
     ```r
     message("=== Dataset 1: sample_messy.csv ===")
     dedup1 <- deduplicate_tagged_columns(df1, tag_map1)
     message(sprintf("Deduplicated: %d unique names, %d unique CAS", length(dedup1$unique_names), length(dedup1$unique_cas)))
     results1 <- run_tiered_search(dedup1)
     joined1 <- map_results_to_rows(df1, dedup1$dedup_key_map, results1)
     ```
   - Print both outputs:
     - `message("\n--- Lookup Table (unique values) ---")`
     - `print(results1)`
     - `message("\n--- Joined Back Table (all rows) ---")`
     - `print(joined1)`

3. **Dataset 2: uncurated_chemicals (first 100 rows)**
   - Read: `read_csv("uncurated_chemicals_2023-05-16_12-43-41.csv", col_types = "ccc_", n_max = 100)`
     - Columns: id, raw_cas, raw_chem_name, datagroup_id — only need raw_cas and raw_chem_name
   - Define tag_map: `list(raw_chem_name = "Name", raw_cas = "CASRN")`
   - Run same pipeline:
     ```r
     message("\n=== Dataset 2: uncurated_chemicals (first 100 rows) ===")
     dedup2 <- deduplicate_tagged_columns(df2, tag_map2)
     message(sprintf("Deduplicated: %d unique names, %d unique CAS", length(dedup2$unique_names), length(dedup2$unique_cas)))
     results2 <- run_tiered_search(dedup2)
     joined2 <- map_results_to_rows(df2, dedup2$dedup_key_map, results2)
     ```
   - Print summary stats:
     - Total rows, unique names searched, unique CAS validated
     - Exact match hits, starts-with hits, CAS hits, total misses
     - `message(sprintf("Results: %d exact, %d starts-with, %d CAS, %d misses", ...))`

4. **Error handling:**
   - Wrap API calls in tryCatch at the script level
   - If CompTox API fails: print error message, stop execution (per user decision: fail fast, no checkpointing)

Run the script:
```
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" scripts/run_prototype.R
```

If API errors occur, debug and fix. The script must complete successfully against both datasets.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" scripts/run_prototype.R 2>&1 | tail -20</automated>
  </verify>
  <done>Script runs end-to-end against both datasets. Console shows progress messages for each tier. Both lookup table and joined-back table are printed. No errors.</done>
</task>

</tasks>

<verification>
- [ ] scripts/run_prototype.R exists and is executable
- [ ] Script sources R/prototype_pipeline.R (no copy-paste of functions)
- [ ] Dataset 1 (sample_messy.csv, 7 rows) produces lookup + joined tables
- [ ] Dataset 2 (uncurated first 100 rows) produces lookup + joined tables
- [ ] Console shows progress: dedup counts, search tier messages, result summaries
- [ ] Fail-fast on API errors (no partial saves)
- [ ] Two distinct outputs: (1) lookup table by unique value, (2) full joined table
</verification>

<success_criteria>
- Script completes without error on both datasets
- Lookup tables contain dtxsid, preferredName, searchName, searchValue, rank columns
- Joined tables have all original rows with lookup results mapped back
- Progress messages show dedup counts and search tier activity
- CAS validation correctly normalizes and validates CAS numbers
</success_criteria>

<output>
After completion, create `.planning/phases/03-prototype-pipeline/03-02-SUMMARY.md`
</output>
