---
phase: 09-modularization
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - app.R
  - tests/test_modules_render.R
autonomous: false
requirements:
  - MODL-01
  - MODL-02

must_haves:
  truths:
    - "App starts without error and all 6 tabs render correctly"
    - "User can upload files and all existing tabs work identically to current behavior"
    - "App.R is less than 500 lines containing only UI orchestration and module calls"
    - "All existing tests pass without modification"
  artifacts:
    - path: "app.R"
      provides: "Orchestration-only app entry point with module calls"
      min_lines: 80
      contains: "mod_file_upload_server"
    - path: "tests/test_modules_render.R"
      provides: "Module render validation tests"
      contains: "testServer"
  key_links:
    - from: "app.R"
      to: "R/modules/mod_file_upload.R"
      via: "mod_file_upload_ui('upload') in UI, mod_file_upload_server('upload', data_store) in server"
      pattern: "mod_file_upload_server"
    - from: "app.R"
      to: "R/modules/mod_data_preview.R"
      via: "mod_data_preview_ui('preview') in UI, mod_data_preview_server('preview', data_store) in server"
      pattern: "mod_data_preview_server"
    - from: "app.R"
      to: "R/modules/mod_review_results.R"
      via: "mod_review_results_ui('results') in UI, mod_review_results_server('results', data_store) in server"
      pattern: "mod_review_results_server"
---

<objective>
Rewrite app.R to orchestration-only code using the 7 modules created in Plan 01, and add module render tests.

Purpose: Complete the modularization by replacing all inline tab code in app.R with module calls. App.R becomes a thin orchestration layer (<500 lines) that creates the data_store, calls modules, wires return values, and manages gated navigation. Module render tests validate that each module's server function runs without error.

Output: Rewritten app.R (~150-300 lines) + tests/test_modules_render.R
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-modularization/09-CONTEXT.md
@.planning/phases/09-modularization/09-RESEARCH.md
@.planning/phases/09-modularization/09-01-SUMMARY.md
@app.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app.R to orchestration-only using modules</name>
  <files>app.R</files>
  <action>
Replace the entire app.R with a thin orchestration file. Structure:

**Section 1: Library loading** (keep existing lines 1-25 unchanged)

**Section 2: Custom operators** (keep existing lines 27-31)

**Section 3: Auto-source R files recursively** — Replace the 4 individual source() calls (lines 34-37) with:
```r
for (f in list.files("R", recursive = TRUE, pattern = "\\.R$", full.names = TRUE)) {
  source(f)
}
```

**Section 4: App configuration** (keep existing lines 39-42)

**Section 5: UI Definition** — Slim version:
```r
ui <- page_sidebar(
  shinyjs::useShinyjs(),

  # Tab pulse animation CSS (keep existing)
  tags$head(tags$style("...")),

  # Theme (keep existing)
  theme = bs_theme(version = 5, bootswatch = "flatly", primary = "#007bff"),
  title = "Chem-Janitor Data Upload & Preview",

  # Sidebar — delegated to upload module
  sidebar = sidebar(
    id = "main_sidebar",
    width = 300,
    mod_file_upload_ui("upload")
  ),

  # Main Content — module UIs in tabs
  navset_underline(
    id = "main_tabs",
    nav_panel("Data Preview", value = "data_preview",
      icon = bsicons::bs_icon("table"),
      mod_data_preview_ui("preview")
    ),
    nav_panel("Detection Info", value = "detection_info",
      icon = bsicons::bs_icon("search"),
      mod_detection_info_ui("detection")
    ),
    nav_panel("Raw Data", value = "raw_data",
      icon = bsicons::bs_icon("file-text"),
      mod_raw_data_ui("raw")
    ),
    nav_panel("Tag Columns", value = "tag_columns",
      icon = bsicons::bs_icon("tags"),
      mod_tag_columns_ui("tags")
    ),
    nav_panel("Run Curation", value = "run_curation_tab",
      icon = bsicons::bs_icon("play-circle"),
      mod_run_curation_ui("curation")
    ),
    nav_panel("Review Results", value = "review_results",
      icon = bsicons::bs_icon("clipboard-check"),
      mod_review_results_ui("results")
    )
  )
)
```

NOTE: Remove the resolution dropdown JavaScript from app.R (tags$script with resolve-select handler) — it now lives inside mod_review_results_ui().

**Section 6: Server** — Orchestration only:
```r
server <- function(input, output, session) {
  # Create shared data store
  data_store <- reactiveValues(
    raw = NULL, clean = NULL, detection = NULL, file_info = NULL,
    selected_columns = NULL, column_tags = NULL,
    curation_results = NULL, curation_report = NULL, curation_status = NULL,
    dedup_preview = NULL, consensus_data = NULL, consensus_summary = NULL,
    resolution_state = NULL, dtxsid_cols = NULL, priority_order = NULL,
    error_filter_active = FALSE, display_row_map = NULL,
    selected_error_rows = NULL, manual_queue = list()
  )

  # --- Gated Navigation ---
  session$onFlushed(function() {
    nav_hide("main_tabs", target = "detection_info")
    nav_hide("main_tabs", target = "raw_data")
    nav_hide("main_tabs", target = "tag_columns")
    nav_hide("main_tabs", target = "run_curation_tab")
    nav_hide("main_tabs", target = "review_results")
  }, once = TRUE)

  show_tab_with_pulse <- function(tab_value) {
    nav_show("main_tabs", target = tab_value)
    shinyjs::runjs(sprintf("
      var tab = document.querySelector('[data-value=\"%s\"]');
      if (tab) {
        var li = tab.closest('li');
        if (li) {
          li.classList.add('tab-pulse');
          setTimeout(function() { li.classList.remove('tab-pulse'); }, 1200);
        }
      }
    ", tab_value))
  }

  reset_all_downstream <- function() {
    data_store$column_tags <- NULL
    data_store$curation_results <- NULL
    # ... (keep existing reset logic)
    nav_hide("main_tabs", target = "detection_info")
    # ... (keep existing hide calls)
    nav_select("main_tabs", "data_preview")
  }

  # Show tabs when data is available
  observe({
    req(data_store$clean)
    show_tab_with_pulse("detection_info")
    show_tab_with_pulse("raw_data")
    show_tab_with_pulse("tag_columns")
  })

  # Sidebar visibility based on active tab
  observeEvent(input$main_tabs, {
    curation_tabs <- c("tag_columns", "run_curation_tab", "review_results")
    is_curation <- input$main_tabs %in% curation_tabs
    toggle_sidebar("main_sidebar", open = !is_curation)
  })

  # --- Initialize Modules ---
  upload_result <- mod_file_upload_server("upload", data_store)

  preview_rows <- upload_result$preview_rows
  mod_data_preview_server("preview", data_store, preview_rows)
  mod_detection_info_server("detection", data_store)
  mod_raw_data_server("raw", data_store)

  mod_tag_columns_server("tags", data_store,
    on_tags_applied = function() {
      show_tab_with_pulse("run_curation_tab")
      nav_hide("main_tabs", target = "review_results")
    }
  )

  mod_run_curation_server("curation", data_store,
    on_curation_complete = function() {
      show_tab_with_pulse("review_results")
    }
  )

  mod_review_results_server("results", data_store)
}

shinyApp(ui = ui, server = server)
```

**Critical details:**
- Keep ALL existing data_store fields (lines 391-415) — modules expect them
- Keep ALL gated navigation logic (hide on startup, show_tab_with_pulse, reset_all_downstream) in app.R — this IS orchestration
- The `filtered_data` reactive and `has_data` output that were in app.R server should now be in their respective modules (upload module for has_data, data_preview module for filtered_data)
- The Column Selection sidebar UI including select_all/deselect_all is part of mod_file_upload_ui
- Do NOT include any output$ renders or observeEvent handlers for tab-specific logic — those are all in modules now
- Final app.R should be 150-300 lines
- Verify line count with `wc -l app.R` — must be under 500
  </action>
  <verify>
    <automated>wc -l app.R && grep -c "mod_.*_server\|mod_.*_ui" app.R</automated>
  </verify>
  <done>app.R is under 500 lines. Contains only: library loading, auto-source, app config, UI with module calls in sidebar and tabs, server with data_store creation, gated navigation, and module server calls with wiring. No inline output renders or tab-specific observeEvent handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Create module render tests and verify existing tests pass</name>
  <files>tests/test_modules_render.R</files>
  <action>
Create tests/test_modules_render.R with testServer() tests for each of the 7 modules. These validate that module server functions initialize without error when given a mock data_store.

```r
library(testthat)
library(shiny)

# Source all R files (modules + utilities)
for (f in list.files(here::here("R"), recursive = TRUE, pattern = "\\.R$", full.names = TRUE)) {
  source(f)
}

# Create mock data_store matching app.R's reactiveValues structure
create_test_store <- function() {
  reactiveValues(
    raw = NULL, clean = NULL, detection = NULL, file_info = NULL,
    selected_columns = NULL, column_tags = NULL,
    curation_results = NULL, curation_report = NULL, curation_status = NULL,
    dedup_preview = NULL, consensus_data = NULL, consensus_summary = NULL,
    resolution_state = NULL, dtxsid_cols = NULL, priority_order = NULL,
    error_filter_active = FALSE, display_row_map = NULL,
    selected_error_rows = NULL, manual_queue = list()
  )
}

# Test each module server initializes without error
test_that("mod_file_upload_server initializes without error", {
  testServer(mod_file_upload_server, args = list(data_store = create_test_store()), {
    session$flushReact()
    expect_true(TRUE)  # Reaching here means no init error
  })
})

test_that("mod_data_preview_server initializes without error", {
  testServer(mod_data_preview_server, args = list(
    data_store = create_test_store(),
    preview_rows = reactive(10)
  ), {
    session$flushReact()
    expect_true(TRUE)
  })
})

# ... similar tests for detection_info, raw_data, tag_columns, run_curation, review_results
```

For each module:
1. Create test_store with create_test_store()
2. Call testServer() with module server function and required args
3. Call session$flushReact()
4. Assert no error occurred (expect_true(TRUE) — reaching this line proves no crash)
5. For modules with return values (upload, tag_columns, run_curation), also test that return value is accessible

After creating the test file, run:
1. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/test_modules_render.R')"` — new module tests pass
2. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"` — ALL existing tests still pass (MODL-01 regression check)

If any existing test fails, investigate — the modularization should NOT change behavior. Fix any issues in the module files or app.R wiring.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"</automated>
  </verify>
  <done>tests/test_modules_render.R exists with testServer() tests for all 7 modules. All 7 module tests pass (servers initialize without error). All existing tests (test_data_detection.R, test_consensus.R, test_prototype_pipeline.R) still pass without modification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke test of modularized app</name>
  <action>Human verifies the app works end-to-end after modularization.</action>
  <what-built>Complete modularization: app.R rewritten to orchestration-only code (~150-300 lines) using 7 Shiny modules in R/modules/. All existing functionality preserved.</what-built>
  <how-to-verify>
    1. Start the app: Run `shiny::runApp()` or `Rscript -e "shiny::runApp('app.R')"`
    2. Verify cold start: App loads without errors in the R console
    3. Upload a test file (e.g., data/chemical_validation_test.csv)
    4. Verify tabs appear: Detection Info, Raw Data, and Tag Columns tabs should pulse and become visible
    5. Click through each tab: Data Preview shows summary cards + table, Detection Info shows method details, Raw Data shows first 20 rows
    6. Tag some columns and click "Apply Tags" — Run Curation tab should appear
    7. Run curation — Review Results tab should appear with resolution table
    8. Verify no console errors throughout the workflow
    9. Check app.R line count: `wc -l app.R` should show <500 lines
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `wc -l app.R` < 500 lines (MODL-02)
2. `ls R/modules/mod_*.R | wc -l` = 7 files (MODL-02)
3. `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"` — all tests pass (MODL-01)
4. Manual smoke test: app starts, all 6 tabs work, no console errors (MODL-01)
</verification>

<success_criteria>
- app.R is under 500 lines with only orchestration code
- All 7 modules are called from app.R (both UI and server)
- All existing tests pass without modification
- New module render tests pass for all 7 modules
- Manual smoke test confirms identical behavior to pre-modularization
</success_criteria>

<output>
After completion, create `.planning/phases/09-modularization/09-02-SUMMARY.md`
</output>
