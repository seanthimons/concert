---
phase: 38-benchmark-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/cleaning_pipeline.R
  - R/unit_harmonizer.R
  - DESCRIPTION
  - .gitignore
autonomous: true
requirements:
  - BENCH-03
user_setup: []

must_haves:
  truths:
    - "run_cleaning_pipeline() accepts use_dedup parameter defaulting to TRUE"
    - "harmonize_units() accepts use_dedup parameter defaulting to TRUE"
    - "All existing tests pass unchanged (default TRUE preserves current behavior)"
    - "data/benchmark/ is gitignored so real regulatory data is never committed"
    - "bench package is declared in Suggests for benchmark script usage"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "use_dedup parameter on run_cleaning_pipeline()"
      contains: "use_dedup = TRUE"
    - path: "R/unit_harmonizer.R"
      provides: "use_dedup parameter on harmonize_units()"
      contains: "use_dedup = TRUE"
    - path: "DESCRIPTION"
      provides: "bench in Suggests"
      contains: "bench"
    - path: ".gitignore"
      provides: "data/benchmark/ exclusion"
      contains: "data/benchmark/"
  key_links:
    - from: "R/cleaning_pipeline.R"
      to: "dedup_step()"
      via: "use_dedup conditional"
      pattern: "use_dedup"
    - from: "R/unit_harmonizer.R"
      to: "unit-key dedup block"
      via: "use_dedup conditional"
      pattern: "use_dedup"
---

<objective>
Add `use_dedup` toggle parameter to both pipeline entry points and prepare package configuration for the benchmark script.

Purpose: Enables apples-to-apples before/after benchmark comparison by toggling dedup on/off in the same R session (per D-03). Also gates infrastructure: `bench` in Suggests and `data/benchmark/` gitignored.
Output: Modified `run_cleaning_pipeline()` and `harmonize_units()` with `use_dedup` parameter; updated DESCRIPTION and .gitignore.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-benchmark-harness/38-CONTEXT.md
@.planning/phases/38-benchmark-harness/38-PATTERNS.md
@.planning/phases/37-performance-architecture/37-01-SUMMARY.md
@.planning/phases/37-performance-architecture/37-03-SUMMARY.md

<interfaces>
<!-- Key signatures the executor needs. Extracted from codebase. -->

From R/cleaning_pipeline.R (current main branch, line 1727):
```r
run_cleaning_pipeline <- function(df, tag_map = NULL, reference_lists = NULL) {
```

From R/cleaning_pipeline.R (Phase 37 completed branch, line 1936):
```r
run_cleaning_pipeline <- function(df, tag_map = NULL, reference_lists = NULL) {
```
Note: The Phase 37 completed branch (worktree-agent-a73cb82e) has dedup_step() wired into the
orchestrator at lines 1941-2174. Calls to dedup_step() appear at lines 1956, 1976, 1996, 2085, 2160.
Pre-check calls appear at lines 1941, 1962, 1990, 2016, 2154, 2156, 2157.

From R/cleaning_pipeline.R (line 184):
```r
dedup_step <- function(step_fn, df, ..., dedup_cols, uniqueness_threshold = 0.5) {
```

From R/cleaning_pipeline.R (line 259, Phase 37 completed branch):
```r
build_skip_result <- function(df, step_name) {
```

From R/unit_harmonizer.R (line 261):
```r
harmonize_units <- function(values, units, unit_map,
                            media = NULL,
                            dtxsid = NULL,
                            molecular_weight = NULL) {
```
Note: Phase 37 completed branch has unit-key dedup at lines 356-573 with a
`if (n_unique < n / 2)` conditional. The use_dedup=FALSE path must bypass this entire block.

From DESCRIPTION (lines 43-45):
```
Suggests:
    testthat (>= 3.0.0),
    withr
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add use_dedup toggle to run_cleaning_pipeline() and harmonize_units()</name>
  <files>R/cleaning_pipeline.R, R/unit_harmonizer.R</files>
  <read_first>
    - R/cleaning_pipeline.R (read lines 1720-1900 for current run_cleaning_pipeline signature and body; also read lines 184-250 for dedup_step definition)
    - R/unit_harmonizer.R (read lines 255-280 for current harmonize_units signature)
    - .planning/phases/38-benchmark-harness/38-CONTEXT.md (D-03: use_dedup toggle)
    - .planning/phases/37-performance-architecture/37-03-SUMMARY.md (dedup wiring pattern context)
  </read_first>
  <action>
**R/cleaning_pipeline.R:**

1. Change the `run_cleaning_pipeline` signature at line 1727 from:
   ```r
   run_cleaning_pipeline <- function(df, tag_map = NULL, reference_lists = NULL) {
   ```
   to:
   ```r
   run_cleaning_pipeline <- function(df, tag_map = NULL, reference_lists = NULL,
                                     use_dedup = TRUE) {
   ```

2. Add roxygen parameter documentation above the function (before @export):
   ```r
   #' @param use_dedup Logical. When TRUE (default), uses dedup_step() wrappers
   #'   and pre-check predicates for performance optimization. Set to FALSE for
   #'   benchmark comparison against the non-dedup baseline path.
   ```

3. **IMPORTANT: The dedup wiring may or may not be present in the current state of the code** (Phase 37 plans 02-04 may still be pending merge). The toggle implementation must handle BOTH states:

   - If `dedup_step()` calls ARE already wired in: The `use_dedup = FALSE` path needs to bypass them. Add a conditional at the top of the function body, right after `df_after_lineage <- inject_row_lineage(df)`:
     ```r
     # Benchmark toggle: when FALSE, bypass dedup_step wrappers and pre-check gates
     # This allows before/after comparison per D-03
     ```
   - If `dedup_step()` calls are NOT yet wired in (the function still uses direct step calls): The toggle has no effect yet, which is correct -- adding the parameter is forward-compatible. The parameter exists but `use_dedup = TRUE` and `FALSE` produce identical behavior until Phase 37 wiring lands.

   The safest implementation: wrap EACH dedup_step() call site (if present) in `if (use_dedup) { ... dedup path ... } else { ... direct call path ... }`. If the current code does not yet have dedup_step() calls in run_cleaning_pipeline, just add the parameter to the signature -- the toggle becomes active when Phase 37 plans 02-04 merge.

**R/unit_harmonizer.R:**

1. Change the `harmonize_units` signature at line 261 from:
   ```r
   harmonize_units <- function(values, units, unit_map,
                               media = NULL,
                               dtxsid = NULL,
                               molecular_weight = NULL) {
   ```
   to:
   ```r
   harmonize_units <- function(values, units, unit_map,
                               media = NULL,
                               dtxsid = NULL,
                               molecular_weight = NULL,
                               use_dedup = TRUE) {
   ```

2. Add roxygen parameter documentation:
   ```r
   #' @param use_dedup Logical. When TRUE (default), applies unit-key dedup
   #'   optimization (Phase 37 D-07). Set to FALSE for benchmark baseline.
   ```

3. Find the unit-key dedup block (look for the comment "Phase 37: Unit-key dedup optimization" or the `if (n_unique < n / 2)` conditional). Wrap the dedup conditional so it only fires when `use_dedup` is TRUE:
   ```r
   if (use_dedup && n_unique < n / 2) {
   ```
   This single-character change makes the dedup path conditional. When `use_dedup = FALSE`, the code falls through to the else branch which processes all rows directly.

   If the unit-key dedup block is not yet present in the current codebase (Phase 37-04 not merged), just add the parameter to the signature. Same forward-compatibility approach as cleaning_pipeline.

**For both files:** Run `air format` after editing.
  </action>
  <verify>
    <automated>cd "C:/Users/sxthi/Documents/chemreg" && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::load_all('.'); args <- formals(run_cleaning_pipeline); cat('use_dedup found:', 'use_dedup' %in% names(args), '\n'); cat('default TRUE:', isTRUE(args$use_dedup), '\n'); args2 <- formals(harmonize_units); cat('harmonize use_dedup found:', 'use_dedup' %in% names(args2), '\n'); cat('harmonize default TRUE:', isTRUE(args2$use_dedup), '\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R contains the string `use_dedup = TRUE` in the `run_cleaning_pipeline` function signature
    - R/unit_harmonizer.R contains the string `use_dedup = TRUE` in the `harmonize_units` function signature
    - Both files contain roxygen `@param use_dedup` documentation
    - `devtools::load_all()` succeeds without errors
    - Existing test suite passes: `devtools::test()` produces no NEW failures (pre-existing failures in test-cleaning-reference.R and test-reference-provenance.R are acceptable)
  </acceptance_criteria>
  <done>
    run_cleaning_pipeline() and harmonize_units() both accept use_dedup parameter with TRUE default.
    All existing callers work unchanged. Existing test suite shows no regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bench to DESCRIPTION Suggests and data/benchmark/ to .gitignore</name>
  <files>DESCRIPTION, .gitignore</files>
  <read_first>
    - DESCRIPTION (read full file for current Suggests field at lines 43-45 and overall structure)
    - .gitignore (read full file for current entries)
  </read_first>
  <action>
**DESCRIPTION:**

Edit the `Suggests:` field (currently at line 43) to add `bench` in alphabetical order:
```
Suggests:
    bench,
    testthat (>= 3.0.0),
    withr
```
`bench` comes first alphabetically. Keep the same 4-space indentation as other entries.

**.gitignore:**

Add `data/benchmark/` to the .gitignore file. Place it after the existing `data/reference_cache/` entry (around line 17) to keep data-related ignores grouped:
```
data/reference_cache/
data/benchmark/
```

**Create the directory** (it must exist for the benchmark script to find data there):
```bash
mkdir -p data/benchmark
```

Create a `.gitkeep` file so the empty directory is tracked:
```bash
touch data/benchmark/.gitkeep
```

Wait -- `.gitkeep` would be ignored by the `data/benchmark/` gitignore rule. Instead, do NOT create a `.gitkeep`. The directory will be created by the benchmark script if needed, or the user creates it when they place their data file there. The script should create the directory if it does not exist via `dir.create()`.

So: just add the `.gitignore` entry. Do NOT create the directory.
  </action>
  <verify>
    <automated>cd "C:/Users/sxthi/Documents/chemreg" && grep -q "bench," DESCRIPTION && echo "DESCRIPTION: bench found" && grep -q "data/benchmark/" .gitignore && echo "GITIGNORE: data/benchmark/ found"</automated>
  </verify>
  <acceptance_criteria>
    - DESCRIPTION Suggests field contains `bench,` on its own indented line, before `testthat`
    - .gitignore contains the line `data/benchmark/`
    - `R CMD check` style validation: `devtools::load_all()` succeeds (DESCRIPTION is valid)
  </acceptance_criteria>
  <done>
    bench is declared as a Suggests dependency. data/benchmark/ directory is gitignored to prevent accidental commit of large regulatory datasets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan modifies function signatures and package config only; no new I/O, network, or user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | T (Tampering) | use_dedup parameter | accept | Parameter is a boolean with TRUE default; no security impact -- controls internal optimization path only |
| T-38-02 | I (Info Disclosure) | data/benchmark/ gitignore | mitigate | .gitignore entry prevents accidental commit of potentially sensitive regulatory dataset |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds
2. `formals(run_cleaning_pipeline)` includes `use_dedup` with default `TRUE`
3. `formals(harmonize_units)` includes `use_dedup` with default `TRUE`
4. Existing test suite: no new failures
5. `grep "bench" DESCRIPTION` returns a match
6. `grep "data/benchmark/" .gitignore` returns a match
</verification>

<success_criteria>
- Both pipeline entry points accept use_dedup toggle with TRUE default (per D-03)
- Zero regression in existing test suite
- Package configuration ready for benchmark script (bench in Suggests, data dir gitignored)
</success_criteria>

<output>
After completion, create `.planning/phases/38-benchmark-harness/38-01-SUMMARY.md`
</output>
