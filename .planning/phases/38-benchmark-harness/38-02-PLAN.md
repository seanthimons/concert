---
phase: 38-benchmark-harness
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - scripts/benchmark_pipeline.R
  - docs/benchmark_results.md
autonomous: false
requirements:
  - BENCH-01
  - BENCH-02
  - BENCH-03
user_setup:
  - service: local-data
    why: "Benchmark requires real regulatory dataset for authentic uniqueness rates"
    env_vars: []
    dashboard_config:
      - task: "Place a regulatory CSV/XLSX file in data/benchmark/"
        location: "Local filesystem: data/benchmark/<your_file>.csv"

must_haves:
  truths:
    - "scripts/benchmark_pipeline.R runs to completion when real data is present at data/benchmark/"
    - "Benchmark measures cleaning pipeline and harmonization pipeline separately (per D-05)"
    - "bench::press() grid covers n = 1K, 10K, 100K with use_dedup = TRUE and FALSE"
    - "Cold-start cost is measured separately from warm iterations"
    - "Uniqueness rate of benchmark data is computed and reported per subset size"
    - "Raw results saved to data/benchmark/results.csv (gitignored)"
    - "Markdown summary with speedup table committed to docs/benchmark_results.md"
    - "Speedup factor computed as median_no_dedup / median_dedup"
  artifacts:
    - path: "scripts/benchmark_pipeline.R"
      provides: "Standalone benchmark script"
      min_lines: 120
    - path: "docs/benchmark_results.md"
      provides: "Committed benchmark results template/summary"
      contains: "Speedup"
  key_links:
    - from: "scripts/benchmark_pipeline.R"
      to: "R/cleaning_pipeline.R"
      via: "source() and run_cleaning_pipeline(use_dedup=)"
      pattern: "run_cleaning_pipeline"
    - from: "scripts/benchmark_pipeline.R"
      to: "R/unit_harmonizer.R"
      via: "source() and harmonize_units(use_dedup=)"
      pattern: "harmonize_units"
    - from: "scripts/benchmark_pipeline.R"
      to: "data/benchmark/"
      via: "list.files() for input, write_csv for results"
      pattern: "data.*benchmark"
---

<objective>
Create the standalone benchmark script and committed results summary that prove the dedup architecture delivers measurable speedup at 100K rows.

Purpose: Fulfills BENCH-01 (bench::press grid), BENCH-02 (cold-start, uniqueness, median), and BENCH-03 (before/after speedup factor). The script runs both dedup and non-dedup paths in the same R session for clean comparison per D-03.
Output: `scripts/benchmark_pipeline.R` (executable benchmark) and `docs/benchmark_results.md` (committed results document).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-benchmark-harness/38-CONTEXT.md
@.planning/phases/38-benchmark-harness/38-RESEARCH.md
@.planning/phases/38-benchmark-harness/38-PATTERNS.md
@.planning/phases/38-benchmark-harness/38-01-SUMMARY.md

<interfaces>
<!-- Key signatures from Plan 01 that this plan depends on -->

From R/cleaning_pipeline.R (after Plan 01):
```r
run_cleaning_pipeline <- function(df, tag_map = NULL, reference_lists = NULL,
                                  use_dedup = TRUE) {
```

From R/unit_harmonizer.R (after Plan 01):
```r
harmonize_units <- function(values, units, unit_map,
                            media = NULL,
                            dtxsid = NULL,
                            molecular_weight = NULL,
                            use_dedup = TRUE) {
```

From scripts/curate_dataset.R (analog pattern):
```r
CHEMREG_ROOT <- here::here()
source(file.path(CHEMREG_ROOT, "R", "cleaning_pipeline.R"))
# ... stopifnot guards ...
# ... pipeline calls with message() progress ...
```

From R/cleaning_pipeline.R:
```r
dedup_step <- function(step_fn, df, ..., dedup_cols, uniqueness_threshold = 0.5) {
```

From R/cleaning_reference.R:
```r
load_all_reference_lists <- function(cache_dir = NULL) {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/benchmark_pipeline.R</name>
  <files>scripts/benchmark_pipeline.R</files>
  <read_first>
    - scripts/curate_dataset.R (analog script: header, sourcing, message() pattern, guard pattern)
    - R/cleaning_pipeline.R (lines 1720-1740 for run_cleaning_pipeline signature after Plan 01)
    - R/unit_harmonizer.R (lines 255-270 for harmonize_units signature after Plan 01)
    - R/cleaning_reference.R (find load_all_reference_lists signature)
    - .planning/phases/38-benchmark-harness/38-CONTEXT.md (D-01 through D-07)
    - .planning/phases/38-benchmark-harness/38-RESEARCH.md (code examples, anti-patterns, pitfall 1-6)
  </read_first>
  <action>
Create `scripts/benchmark_pipeline.R` following the `curate_dataset.R` structure (section headers, message() progress, stopifnot guards). Use numbered sections per the PATTERNS.md shared pattern convention.

The script must contain these sections in order:

**Section 0: CONFIGURATION**
```r
# benchmark_pipeline.R
# Standalone benchmark -- measures dedup architecture speedup
# No Shiny dependency -- runs in any R session with bench installed
#
# Prerequisites:
#   1. Place a regulatory CSV or XLSX file in data/benchmark/
#   2. Install bench: pak::pak("bench")
#   3. Source this file: source("scripts/benchmark_pipeline.R")
#
# Output:
#   - data/benchmark/results.csv (raw timing data, gitignored)
#   - docs/benchmark_results.md (summary with speedup table, committed)

library(dplyr)
library(bench)
library(readr)

CHEMREG_ROOT <- here::here()
```

Source the pipeline R files. Follow the curate_dataset.R pattern exactly:
```r
source(file.path(CHEMREG_ROOT, "R", "cleaning_pipeline.R"))
source(file.path(CHEMREG_ROOT, "R", "unit_harmonizer.R"))
source(file.path(CHEMREG_ROOT, "R", "cleaning_reference.R"))
```
Also source any other files needed for the pipeline to run (check what curate_dataset.R sources).

**Section 1: INPUT DATA**

Load benchmark data from `data/benchmark/` per D-01. Auto-detect the first CSV or XLSX file:
```r
bench_dir <- file.path(CHEMREG_ROOT, "data", "benchmark")
dir.create(bench_dir, showWarnings = FALSE, recursive = TRUE)

bench_files <- list.files(bench_dir, pattern = "\\.(csv|xlsx)$", full.names = TRUE)
stopifnot("No benchmark data found in data/benchmark/ -- place a CSV or XLSX file there" = length(bench_files) > 0)

message("=== LOADING BENCHMARK DATA ===")
file_ext <- tolower(tools::file_ext(bench_files[1]))
benchmark_df <- if (file_ext == "csv") {
  readr::read_csv(bench_files[1], show_col_types = FALSE)
} else {
  readxl::read_xlsx(bench_files[1])
}
message(sprintf("  Loaded: %s (%d rows x %d cols)", basename(bench_files[1]), nrow(benchmark_df), ncol(benchmark_df)))

stopifnot("Benchmark dataset must have >= 100000 rows for full grid" = nrow(benchmark_df) >= 100000L)
```

**Section 2: TAG MAP AND REFERENCE LISTS**

Build a tag_map from the benchmark dataset. Auto-detect columns by naming convention:
```r
# Build tag_map -- benchmark needs realistic tags for dedup to exercise name-chain
# Auto-detect: columns with "cas" in name -> CASRN, columns with "name"/"chemical" -> Name
col_names_lower <- tolower(names(benchmark_df))
tag_map <- c()
cas_candidates <- grep("cas", col_names_lower, value = TRUE)
name_candidates <- grep("name|chemical|compound|substance", col_names_lower, value = TRUE)
if (length(cas_candidates) > 0) tag_map <- c(tag_map, setNames(rep("CASRN", length(cas_candidates)), names(benchmark_df)[tolower(names(benchmark_df)) %in% cas_candidates]))
if (length(name_candidates) > 0) tag_map <- c(tag_map, setNames(rep("Name", length(name_candidates)), names(benchmark_df)[tolower(names(benchmark_df)) %in% name_candidates]))
if (length(tag_map) == 0) {
  # Fallback: tag first character column as Name
  char_cols <- names(benchmark_df)[vapply(benchmark_df, is.character, logical(1))]
  if (length(char_cols) > 0) tag_map <- c(tag_map, setNames("Name", char_cols[1]))
}
message(sprintf("  Tag map: %d columns tagged (%s)", length(tag_map), paste(names(tag_map), "=", tag_map, collapse = ", ")))
```

Load reference lists:
```r
ref_lists <- tryCatch(
  load_all_reference_lists(cache_dir = file.path(CHEMREG_ROOT, "data", "reference_cache")),
  error = function(e) { message("  Warning: reference lists not available, using NULL"); NULL }
)
```

**Section 3: PRE-GENERATE SUBSETS**

Per D-02, use `set.seed(42)` + `dplyr::slice_sample()` for reproducible subsets. Pre-generate OUTSIDE any bench call (Pitfall 2):
```r
message("=== GENERATING SUBSETS ===")
set.seed(42)
df_1k   <- dplyr::slice_sample(benchmark_df, n = 1000L)
df_10k  <- dplyr::slice_sample(benchmark_df, n = 10000L)
df_100k <- dplyr::slice_sample(benchmark_df, n = 100000L)
```

**Section 4: COMPUTE UNIQUENESS RATES**

Per BENCH-02, compute uniqueness rate of the test data per subset:
```r
compute_uniqueness <- function(df, tag_map) {
  name_cols <- names(tag_map)[tag_map == "Name"]
  if (length(name_cols) == 0) name_cols <- names(df)[vapply(df, is.character, logical(1))][1]
  if (length(name_cols) == 0 || is.na(name_cols[1])) return(NA_real_)
  key_vec <- do.call(paste0, df[, name_cols, drop = FALSE])
  dplyr::n_distinct(key_vec) / nrow(df)
}

uniq_1k   <- compute_uniqueness(df_1k, tag_map)
uniq_10k  <- compute_uniqueness(df_10k, tag_map)
uniq_100k <- compute_uniqueness(df_100k, tag_map)

message(sprintf("  Uniqueness: 1K=%.1f%%, 10K=%.1f%%, 100K=%.1f%%",
                uniq_1k * 100, uniq_10k * 100, uniq_100k * 100))
```

**Section 5: COLD-START MEASUREMENT**

Per BENCH-02, measure cold-start separately (Pitfall 5). Use a separate `bench::mark()` with `min_iterations=1, max_iterations=1`:
```r
message("=== COLD-START MEASUREMENT ===")
cold_result <- bench::mark(
  cleaning = run_cleaning_pipeline(df_1k, tag_map, ref_lists, use_dedup = TRUE),
  min_iterations = 1,
  max_iterations = 1,
  memory = TRUE,
  check = FALSE
)
message(sprintf("  Cold-start: %s (mem: %s)", format(cold_result$median), format(cold_result$mem_alloc)))
```

**Section 6: CLEANING PIPELINE BENCHMARK**

Per D-05 and BENCH-01, use `bench::press()` across the grid. Per D-07, `min_iterations = 3`. Per Pitfall 1, `check = FALSE`:
```r
message("=== CLEANING PIPELINE BENCHMARK ===")
cleaning_results <- bench::press(
  n = c(1000L, 10000L, 100000L),
  use_dedup = c(TRUE, FALSE),
  {
    df_sub <- switch(as.character(n),
      "1000"   = df_1k,
      "10000"  = df_10k,
      "100000" = df_100k
    )
    bench::mark(
      run_cleaning_pipeline(df_sub, tag_map, ref_lists, use_dedup = use_dedup),
      min_iterations = 3,
      memory = TRUE,
      check = FALSE
    )
  }
)
message("  Cleaning benchmark complete")
```

**Section 7: HARMONIZATION PIPELINE BENCHMARK**

Per D-05, harmonization is benchmarked separately. Build a realistic unit harmonization call:
```r
message("=== HARMONIZATION PIPELINE BENCHMARK ===")
unit_map <- tryCatch({
  unit_table_path <- system.file("extdata", "unit_table.csv", package = "chemreg")
  if (unit_table_path == "") unit_table_path <- file.path(CHEMREG_ROOT, "inst", "extdata", "unit_table.csv")
  readr::read_csv(unit_table_path, show_col_types = FALSE)
}, error = function(e) { message("  Warning: unit_table.csv not found; skipping harmonization benchmark"); NULL })

if (!is.null(unit_map)) {
  sample_units <- c("mg/L", "ug/L", "ppb", "ppm", "mg/kg", "ug/kg", "ng/L", "mg/L", "ug/L", "mg/L")
  
  harmonize_results <- bench::press(
    n = c(1000L, 10000L, 100000L),
    use_dedup = c(TRUE, FALSE),
    {
      set.seed(42)
      test_values <- runif(n, 0.001, 1000)
      test_units <- sample(sample_units, n, replace = TRUE)
      test_media <- sample(c("aqueous", "soil", "air"), n, replace = TRUE)
      bench::mark(
        harmonize_units(test_values, test_units, unit_map, media = test_media, use_dedup = use_dedup),
        min_iterations = 3,
        memory = TRUE,
        check = FALSE
      )
    }
  )
  message("  Harmonization benchmark complete")
} else {
  harmonize_results <- NULL
  message("  Harmonization benchmark skipped (no unit_table.csv)")
}
```

**Section 8: SAVE RAW RESULTS**

Per D-04, save raw results to `data/benchmark/results.csv` (gitignored):
```r
message("=== SAVING RESULTS ===")

cleaning_tidy <- cleaning_results %>%
  dplyr::mutate(
    pipeline = "cleaning",
    median_secs = as.numeric(median),
    mem_alloc_bytes = as.numeric(mem_alloc)
  ) %>%
  dplyr::select(pipeline, n, use_dedup, median_secs, mem_alloc_bytes, n_itr)

if (!is.null(harmonize_results)) {
  harmonize_tidy <- harmonize_results %>%
    dplyr::mutate(
      pipeline = "harmonization",
      median_secs = as.numeric(median),
      mem_alloc_bytes = as.numeric(mem_alloc)
    ) %>%
    dplyr::select(pipeline, n, use_dedup, median_secs, mem_alloc_bytes, n_itr)
  all_results <- dplyr::bind_rows(cleaning_tidy, harmonize_tidy)
} else {
  all_results <- cleaning_tidy
}

readr::write_csv(all_results, file.path(bench_dir, "results.csv"))
message(sprintf("  Raw results saved to data/benchmark/results.csv (%d rows)", nrow(all_results)))
```

**Section 9: GENERATE MARKDOWN SUMMARY**

Per D-04 and BENCH-03, compute speedup factors and write committed Markdown:
```r
message("=== GENERATING MARKDOWN SUMMARY ===")

compute_speedup <- function(results_df) {
  results_df %>%
    tidyr::pivot_wider(
      id_cols = c(pipeline, n),
      names_from = use_dedup,
      values_from = median_secs,
      names_prefix = "dedup_"
    ) %>%
    dplyr::mutate(speedup = dedup_FALSE / dedup_TRUE) %>%
    dplyr::select(pipeline, n, speedup)
}

speedup_table <- compute_speedup(all_results)

format_results_table <- function(results_df, speedup_df) {
  results_df %>%
    dplyr::left_join(speedup_df, by = c("pipeline", "n")) %>%
    dplyr::mutate(
      median_fmt = sprintf("%.3fs", median_secs),
      mem_fmt = sprintf("%.1fMB", mem_alloc_bytes / 1024^2),
      speedup_fmt = dplyr::if_else(use_dedup, sprintf("%.1fx", speedup), "-")
    )
}

formatted <- format_results_table(all_results, speedup_table)

docs_dir <- file.path(CHEMREG_ROOT, "docs")
dir.create(docs_dir, showWarnings = FALSE, recursive = TRUE)

# Assemble md_lines from results
cleaning_speedup_100k <- speedup_table %>% dplyr::filter(pipeline == "cleaning", n == 100000L)
harm_speedup_100k <- speedup_table %>% dplyr::filter(pipeline == "harmonization", n == 100000L)

md_lines <- c(
  "# Benchmark Results: Dedup Architecture", "",
  sprintf("**Date:** %s", Sys.Date()),
  sprintf("**R version:** %s", R.version.string),
  sprintf("**bench version:** %s", as.character(packageVersion("bench"))),
  sprintf("**Dataset:** %s (%d rows)", basename(bench_files[1]), nrow(benchmark_df)),
  "", "## Data Uniqueness Rates", "",
  "| Subset | Uniqueness Rate |", "|--------|----------------|",
  sprintf("| 1K     | %.1f%%           |", uniq_1k * 100),
  sprintf("| 10K    | %.1f%%           |", uniq_10k * 100),
  sprintf("| 100K   | %.1f%%           |", uniq_100k * 100),
  "", sprintf("## Cold-Start Cost: %s (mem: %s)", format(cold_result$median), format(cold_result$mem_alloc)),
  "", "## Cleaning Pipeline", "",
  "| Rows | Dedup | Median | Memory | Speedup |",
  "|------|-------|--------|--------|---------|"
)
cleaning_fmt <- formatted %>% dplyr::filter(pipeline == "cleaning")
for (i in seq_len(nrow(cleaning_fmt))) {
  r <- cleaning_fmt[i, ]
  md_lines <- c(md_lines, sprintf("| %s | %s | %s | %s | %s |", r$n, r$use_dedup, r$median_fmt, r$mem_fmt, r$speedup_fmt))
}
if (!is.null(harmonize_results)) {
  harm_fmt <- formatted %>% dplyr::filter(pipeline == "harmonization")
  md_lines <- c(md_lines, "", "## Harmonization Pipeline", "",
    "| Rows | Dedup | Median | Memory | Speedup |", "|------|-------|--------|--------|---------|")
  for (i in seq_len(nrow(harm_fmt))) {
    r <- harm_fmt[i, ]
    md_lines <- c(md_lines, sprintf("| %s | %s | %s | %s | %s |", r$n, r$use_dedup, r$median_fmt, r$mem_fmt, r$speedup_fmt))
  }
}
md_lines <- c(md_lines, "", "## Summary", "",
  sprintf("- **Cleaning speedup at 100K:** %.1fx", cleaning_speedup_100k$speedup),
  if (!is.null(harmonize_results) && nrow(harm_speedup_100k) > 0) sprintf("- **Harmonization speedup at 100K:** %.1fx", harm_speedup_100k$speedup) else NULL,
  "", "## Methodology", "",
  "- `bench::press()` grid: n x use_dedup (1K, 10K, 100K x TRUE, FALSE)",
  "- Minimum iterations: 3 (adaptive defaults from bench)",
  "- Cold-start measured separately with single iteration",
  "- Subsets pre-generated with `set.seed(42)` + `dplyr::slice_sample()`",
  "- No CompTox API calls (local-only benchmark)"
)
writeLines(md_lines, file.path(docs_dir, "benchmark_results.md"))
```

**Section 10: DONE**
```r
message("=== BENCHMARK COMPLETE ===")
message(sprintf("  Cleaning speedup at 100K: %.1fx", cleaning_speedup_100k$speedup))
message("  Raw data: data/benchmark/results.csv")
message("  Summary:  docs/benchmark_results.md")
```

**IMPORTANT implementation notes:**
- Per D-06: NO CompTox API calls. Do NOT source `R/curation.R` or call any curation functions. The benchmark covers cleaning and harmonization only.
- Per Pitfall 4: If `normalize_cas_fields` or any cleaning step makes network calls, pass a `tag_map` without CASRN entries OR verify `as_cas()` is local. Read the `normalize_cas_fields` source to confirm before including CASRN in tag_map. If network calls are detected, filter out CASRN from the tag_map before benchmarking.
- Per D-07: `min_iterations = 3` floor.
- Per Pitfall 1: ALL `bench::mark()` calls must use `check = FALSE`.
- Per Pitfall 2: Subsets are pre-generated in Section 3, never inside bench expressions.

Run `air format scripts/benchmark_pipeline.R` then `jarl check scripts/benchmark_pipeline.R` after creating.
  </action>
  <verify>
    <automated>cd "C:/Users/sxthi/Documents/chemreg" && test -f scripts/benchmark_pipeline.R && echo "SCRIPT EXISTS" && grep -c "bench::press" scripts/benchmark_pipeline.R && grep -c "bench::mark" scripts/benchmark_pipeline.R && grep -q "use_dedup" scripts/benchmark_pipeline.R && echo "USE_DEDUP FOUND" && grep -q "check = FALSE" scripts/benchmark_pipeline.R && echo "CHECK FALSE FOUND" && grep -q "set.seed" scripts/benchmark_pipeline.R && echo "SET SEED FOUND" && grep -q "min_iterations" scripts/benchmark_pipeline.R && echo "MIN_ITERATIONS FOUND" && air format scripts/benchmark_pipeline.R && jarl check scripts/benchmark_pipeline.R && echo "LINT PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/benchmark_pipeline.R exists and is > 120 lines
    - Script contains `bench::press(` (at least 2 calls -- cleaning and harmonization)
    - Script contains `bench::mark(` (at least 3 calls -- cold-start, cleaning, harmonization)
    - Script contains `check = FALSE` in every bench::mark call
    - Script contains `set.seed(42)` for reproducible subsets per D-02
    - Script contains `use_dedup` parameter usage in pipeline calls
    - Script contains `min_iterations = 3` per D-07
    - Script contains `slice_sample` for subset generation per D-02
    - Script does NOT source R/curation.R (per D-06, no API calls)
    - Script contains message() progress output following curate_dataset.R pattern
    - Script writes results to `data/benchmark/results.csv`
    - Script writes Markdown to `docs/benchmark_results.md`
    - Script contains uniqueness rate computation per BENCH-02
    - Script contains cold-start measurement (min_iterations = 1, max_iterations = 1) per BENCH-02
    - Script contains speedup computation as `median_no_dedup / median_dedup` per BENCH-03
  </acceptance_criteria>
  <done>
    scripts/benchmark_pipeline.R is a complete, executable benchmark script that exercises
    both pipeline entry points across 1K/10K/100K grids with dedup on/off comparison.
    The script is ready to run when the user places real data in data/benchmark/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify benchmark script and run with real data</name>
  <files>scripts/benchmark_pipeline.R, docs/benchmark_results.md</files>
  <action>
    Human reviews the benchmark script for correctness, places real regulatory data in
    data/benchmark/, runs the script, and verifies the output.
  </action>
  <what-built>
    Standalone benchmark script at scripts/benchmark_pipeline.R that:
    - Loads real regulatory data from data/benchmark/
    - Pre-generates 1K, 10K, 100K subsets with set.seed(42)
    - Measures cold-start cost separately
    - Runs bench::press() grids for cleaning and harmonization pipelines
    - Computes uniqueness rates and speedup factors
    - Saves raw results (gitignored) and Markdown summary (committed)
  </what-built>
  <how-to-verify>
    1. Open `scripts/benchmark_pipeline.R` and review the script structure:
       - Sections 0-10 should follow curate_dataset.R pattern
       - No CompTox API calls or curation references
       - check = FALSE on every bench::mark() call
    2. Verify `docs/benchmark_results.md` will be generated with speedup table
    3. To run the benchmark:
       a. Place a regulatory CSV with >= 100K rows in `data/benchmark/`
       b. Run: `source("scripts/benchmark_pipeline.R")`
       c. Check `data/benchmark/results.csv` and `docs/benchmark_results.md` are created
    4. Verify the Markdown summary shows speedup factors > 1.0x at 100K (if Phase 37 is fully merged)
  </how-to-verify>
  <verify>User confirms script runs successfully and produces correct output</verify>
  <done>Benchmark script approved by user; docs/benchmark_results.md contains real speedup data</done>
  <resume-signal>Type "approved" to proceed, or describe any issues with the script</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem read | Script reads user-provided data files from data/benchmark/ |
| Filesystem write | Script writes results.csv to gitignored path and benchmark_results.md to committed path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | T (Tampering) | benchmark data input | accept | Script is developer-only tool; data files are gitignored; no user-facing attack surface |
| T-38-04 | I (Info Disclosure) | results.csv | mitigate | Raw results with dataset details saved to gitignored path; Markdown summary contains only aggregate statistics |
| T-38-05 | D (Denial of Service) | 100K row benchmark | accept | Script is run manually by developer; long runtime is expected and desired |
</threat_model>

<verification>
1. `scripts/benchmark_pipeline.R` exists and has sections 0-10
2. Script sources R/cleaning_pipeline.R and R/unit_harmonizer.R (not R/curation.R)
3. Script uses `bench::press()` with `n = c(1000L, 10000L, 100000L)` and `use_dedup = c(TRUE, FALSE)`
4. Script uses `check = FALSE` in all bench::mark calls
5. Script computes uniqueness rate per subset
6. Script measures cold-start with separate bench::mark(min_iterations=1, max_iterations=1)
7. Script saves raw results to data/benchmark/results.csv
8. Script writes Markdown summary to docs/benchmark_results.md with speedup table
9. Speedup computed as median_no_dedup / median_dedup
</verification>

<success_criteria>
- BENCH-01: scripts/benchmark_pipeline.R uses bench::press() across n = 1K, 10K, 100K with memory tracking
- BENCH-02: Cold-start measured separately, uniqueness rates reported, median (not mean) used throughout
- BENCH-03: Before/after speedup factor computed and documented in committed Markdown
- D-01: Real data from data/benchmark/, not synthetic
- D-02: Reproducible subsets via set.seed(42) + slice_sample()
- D-03: use_dedup toggle for same-session comparison
- D-04: Raw CSV gitignored, Markdown committed
- D-05: Cleaning and harmonization benchmarked separately
- D-06: No CompTox API calls, no curation
- D-07: min_iterations = 3 with adaptive defaults
</success_criteria>

<output>
After completion, create `.planning/phases/38-benchmark-harness/38-02-SUMMARY.md`
</output>
