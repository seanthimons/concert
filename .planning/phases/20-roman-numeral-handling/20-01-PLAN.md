---
phase: 20-roman-numeral-handling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - R/cleaning_pipeline.R
  - tests/test_name_cleaning.R
  - data/chemical_validation_test.csv
autonomous: true
requirements: [ROMAN-01, ROMAN-02]

must_haves:
  truths:
    - "chromium (III) retains the roman numeral as part of the name after cleaning"
    - "chromium (iii) lowercase variant also retains the roman numeral"
    - "Iron(III) chloride with non-terminal roman numeral survives cleaning intact"
    - "Acetone (ACS reagent) still gets its parenthetical stripped (no false protection)"
    - "formula_extract column does not contain roman numeral content"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "Roman numeral protection in strip_terminal_enclosures"
      contains: "has_roman"
    - path: "tests/test_name_cleaning.R"
      provides: "Roman numeral unit tests"
      contains: "roman numeral"
    - path: "data/chemical_validation_test.csv"
      provides: "Roman numeral validation rows"
      contains: "chromium"
  key_links:
    - from: "R/cleaning_pipeline.R"
      to: "strip_terminal_enclosures should_strip"
      via: "has_roman condition added to both paren and bracket paths"
      pattern: "!has_roman"
---

<objective>
Add roman numeral protection to `strip_terminal_enclosures()` so that parenthesized oxidation states like "(III)" are not stripped from chemical names or misrouted to the formula_extract column.

Purpose: Chemical names like "chromium (III)" and "Iron(III) chloride" must retain their oxidation state indicators. Currently "(III)" is stripped because the should_strip logic only checks for "yl" content, exception words, and percentages.

Output: Modified `strip_terminal_enclosures()` with roman numeral detection, unit tests, and validation CSV rows.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-roman-numeral-handling/20-CONTEXT.md

<interfaces>
<!-- Key code the executor must understand before modifying -->

From R/cleaning_pipeline.R lines 344-477 — strip_terminal_enclosures():
- Parenthetical path (line 370-396): regex `^(.*)\\(([^)]*)\\)\\s*$` matches terminal parens
- should_strip logic (line 388): `should_strip <- (!has_yl || has_exception) && !has_percentage`
- Bracket path (line 398-430): parallel logic with regex `^(.*)\\[([^]]*)\\]\\s*$`
- should_strip logic (line 417): identical conditional for brackets
- Both paths: when should_strip is TRUE, content goes to formula_extract column

Module-level constants are defined at top of file (lines 1-14, currently just library loads).

Validation CSV columns: chemical_name, casrn, expected_validity, issue_type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add roman numeral protection to strip_terminal_enclosures and write unit tests</name>
  <files>R/cleaning_pipeline.R, tests/test_name_cleaning.R</files>
  <read_first>
    - R/cleaning_pipeline.R (lines 1-14 for module-level constants location; lines 344-477 for full strip_terminal_enclosures function)
    - tests/test_name_cleaning.R (full file — understand existing test patterns and structure)
    - .planning/phases/20-roman-numeral-handling/20-CONTEXT.md (decisions section for exact implementation spec)
  </read_first>
  <behavior>
    - Test: "chromium (III)" is NOT stripped — name stays "chromium (III)", formula_extract is NA
    - Test: "chromium (iii)" lowercase is NOT stripped — name stays "chromium (iii)", formula_extract is NA
    - Test: "Iron(III) chloride" is NOT stripped — name stays "Iron(III) chloride" (non-terminal, but verify no regression)
    - Test: "Chromium (Cr III) complex" with element+numeral is NOT stripped — name unchanged
    - Test: "chromium [III]" bracket form is NOT stripped — name stays "chromium [III]"
    - Test: "Acetone (ACS reagent)" IS still stripped — name becomes "Acetone" (negative regression)
    - Test: "ethanol (ethyl alcohol)" with yl content is still preserved — name unchanged
    - Test: "Sodium chloride (NaCl)" IS still stripped — name becomes "Sodium chloride"
    - Test: Content with mixed roman+other like "(Fe2O3)" IS still stripped — only pure roman numerals protected
  </behavior>
  <action>
    **Step 1: Define roman numeral regex constant (R/cleaning_pipeline.R, after line 14)**

    Add a module-level constant after the library() calls:

    ```r
    # Roman numeral pattern for oxidation states (I through XII, case-insensitive)
    # Matches: pure roman numeral like "III", or element symbol + roman numeral like "Cr III"
    ROMAN_NUMERAL_PATTERN <- "(?i)^\\s*([A-Z][a-z]?\\s+)?(I{1,3}|IV|V|VI{0,3}|IX|X|XI{0,2})\\s*$"
    ```

    This regex matches the ENTIRE parenthetical content (anchored with ^ and $) against:
    - Optional element symbol prefix: `[A-Z][a-z]?` followed by space
    - Roman numerals I through XII: built from standard subtractive notation pattern
    - Case-insensitive via `(?i)` flag
    - Whitespace tolerance with `\\s*` at boundaries

    **Step 2: Add has_roman check to parenthetical path (line ~388)**

    Before the existing `should_strip` line (line 388), add:

    ```r
    has_roman <- stringr::str_detect(content_trimmed, ROMAN_NUMERAL_PATTERN)
    ```

    Then modify line 388 from:
    ```r
    should_strip <- (!has_yl || has_exception) && !has_percentage
    ```
    to:
    ```r
    should_strip <- (!has_yl || has_exception) && !has_percentage && !has_roman
    ```

    **Step 3: Add has_roman check to bracket path (line ~417)**

    Same change — before the bracket `should_strip` line (line 417), add:

    ```r
    has_roman <- stringr::str_detect(content_trimmed, ROMAN_NUMERAL_PATTERN)
    ```

    Then modify line 417 from:
    ```r
    should_strip <- (!has_yl || has_exception) && !has_percentage
    ```
    to:
    ```r
    should_strip <- (!has_yl || has_exception) && !has_percentage && !has_roman
    ```

    **Step 4: Write unit tests in tests/test_name_cleaning.R**

    Add a new test section after the existing strip_terminal_enclosures tests (after the percentage preservation test). Use the heading pattern already in the file:

    ```r
    # ==============================================================================
    # ROMAN-01/ROMAN-02: Roman numeral oxidation state protection
    # ==============================================================================

    test_that("strip_terminal_enclosures preserves terminal roman numeral oxidation states", {
      df <- tibble::tibble(
        chemical_name = c(
          "chromium (III)",
          "chromium (iii)",
          "Copper (II)",
          "Iron (IV)",
          "Manganese (VII)"
        )
      )
      result <- strip_terminal_enclosures(df, "chemical_name")
      cleaned <- result$cleaned_data

      # All should be unchanged — roman numerals are chemical identity
      expect_equal(cleaned$chemical_name[1], "chromium (III)")
      expect_equal(cleaned$chemical_name[2], "chromium (iii)")
      expect_equal(cleaned$chemical_name[3], "Copper (II)")
      expect_equal(cleaned$chemical_name[4], "Iron (IV)")
      expect_equal(cleaned$chemical_name[5], "Manganese (VII)")

      # formula_extract should be NA for all (nothing extracted)
      expect_true(all(is.na(cleaned$formula_extract_chemical_name)))
    })

    test_that("strip_terminal_enclosures preserves element+numeral roman forms", {
      df <- tibble::tibble(
        chemical_name = c(
          "Chromium (Cr III) complex",  # non-terminal, won't match terminal regex anyway
          "Some compound (Fe II)"       # terminal element+numeral
        )
      )
      result <- strip_terminal_enclosures(df, "chemical_name")
      cleaned <- result$cleaned_data

      expect_equal(cleaned$chemical_name[1], "Chromium (Cr III) complex")
      expect_equal(cleaned$chemical_name[2], "Some compound (Fe II)")
    })

    test_that("strip_terminal_enclosures preserves roman numerals in brackets", {
      df <- tibble::tibble(
        chemical_name = c("chromium [III]", "Iron [VI]")
      )
      result <- strip_terminal_enclosures(df, "chemical_name")
      cleaned <- result$cleaned_data

      expect_equal(cleaned$chemical_name[1], "chromium [III]")
      expect_equal(cleaned$chemical_name[2], "Iron [VI]")
    })

    test_that("strip_terminal_enclosures still strips non-roman parentheticals (regression)", {
      df <- tibble::tibble(
        chemical_name = c(
          "Acetone (ACS reagent)",
          "Sodium chloride (NaCl)",
          "Benzene (analytical grade)"
        )
      )
      result <- strip_terminal_enclosures(df, "chemical_name")
      cleaned <- result$cleaned_data

      expect_equal(cleaned$chemical_name[1], "Acetone")
      expect_equal(cleaned$chemical_name[2], "Sodium chloride")
      expect_equal(cleaned$chemical_name[3], "Benzene")
    })
    ```

    Write tests FIRST (RED), run to confirm they fail on the roman numeral preservation cases, then apply the code changes (GREEN).
  </action>
  <verify>
    <automated>Write and run a temp R script that sources cleaning_pipeline.R and runs: testthat::test_file("tests/test_name_cleaning.R") — all tests pass including new roman numeral tests</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R contains `ROMAN_NUMERAL_PATTERN <-` near top of file (after library calls)
    - R/cleaning_pipeline.R contains `has_roman <- stringr::str_detect(content_trimmed, ROMAN_NUMERAL_PATTERN)` in parenthetical path
    - R/cleaning_pipeline.R contains `!has_roman` in should_strip conditional at BOTH paren path and bracket path
    - tests/test_name_cleaning.R contains `"roman numeral"` in test descriptions
    - tests/test_name_cleaning.R contains assertions for "chromium (III)", "chromium (iii)", "Copper (II)"
    - tests/test_name_cleaning.R contains regression assertion for "Acetone (ACS reagent)" stripping to "Acetone"
    - testthat::test_file("tests/test_name_cleaning.R") exits with 0 failures
  </acceptance_criteria>
  <done>Roman numeral oxidation states in parentheses and brackets are protected from stripping; non-roman parentheticals still stripped; all unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add roman numeral rows to validation CSV and run end-to-end validation</name>
  <files>data/chemical_validation_test.csv</files>
  <read_first>
    - data/chemical_validation_test.csv (full file — understand column structure and existing issue_type tags)
    - data/chemical_validation_test_README.md (understand validation dataset conventions)
    - tests/test_cleaning_pipeline_validation.R (understand how validation CSV is consumed in end-to-end tests)
  </read_first>
  <action>
    **Step 1: Append roman numeral test rows to data/chemical_validation_test.csv**

    Add the following rows at the end of the CSV file. Use `issue_type` tag `roman_numeral_oxidation` for all:

    ```csv
    chromium (III),16065-83-1,valid,roman_numeral_oxidation
    chromium (iii),16065-83-1,valid,roman_numeral_oxidation
    Iron(III) chloride,7705-08-0,valid,roman_numeral_oxidation
    Copper(II) sulfate,7758-98-7,valid,roman_numeral_oxidation
    Antimony(III) ethoxide,10433-06-0,valid,roman_numeral_oxidation
    Manganese(VII) oxide,12057-92-0,valid,roman_numeral_oxidation
    ```

    These cover:
    - Terminal uppercase roman numeral with space: "chromium (III)" — the primary bug case
    - Terminal lowercase variant: "chromium (iii)" — confirmed in production data
    - Non-terminal forms: "Iron(III) chloride", "Copper(II) sulfate" — regression proof
    - Higher numerals: "Antimony(III)", "Manganese(VII)" — range coverage

    **Step 2: Run the end-to-end validation test suite**

    Execute `tests/test_cleaning_pipeline_validation.R` to confirm the new rows pass through the full pipeline without errors. The validation test reads chemical_validation_test.csv and runs names through the cleaning pipeline, checking that valid entries remain valid.

    **Step 3: Run the Shiny smoke test**

    Start the app briefly to confirm no startup crashes from the cleaning_pipeline.R changes:
    ```
    "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)"
    ```
    Wait for "Listening on" message, then kill the process.
  </action>
  <verify>
    <automated>Write and run a temp R script: source("load_packages.R"); testthat::test_dir("tests") — all tests pass (0 failures)</automated>
  </verify>
  <acceptance_criteria>
    - data/chemical_validation_test.csv contains "chromium (III)" with issue_type "roman_numeral_oxidation"
    - data/chemical_validation_test.csv contains "chromium (iii)" lowercase variant
    - data/chemical_validation_test.csv contains "Iron(III) chloride" non-terminal form
    - data/chemical_validation_test.csv contains at least 6 rows with issue_type "roman_numeral_oxidation"
    - testthat::test_dir("tests") exits with 0 failures
    - Shiny app starts without crash (shows "Listening on" before being killed)
  </acceptance_criteria>
  <done>Validation CSV includes roman numeral test cases covering terminal, non-terminal, uppercase, and lowercase forms; full test suite passes; app starts cleanly</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `testthat::test_dir("tests")` — 0 failures (all unit + validation tests pass)
2. grep confirms `has_roman` appears in both paren and bracket paths of strip_terminal_enclosures
3. grep confirms `ROMAN_NUMERAL_PATTERN` defined as module-level constant
4. Shiny smoke test passes (app starts without crash)
</verification>

<success_criteria>
- "chromium (III)" passes through strip_terminal_enclosures unchanged (name retained, formula_extract is NA)
- "chromium (iii)" lowercase also retained
- "Iron(III) chloride" non-terminal form survives
- "Acetone (ACS reagent)" still stripped to "Acetone" (no false protection)
- Bracket forms like "chromium [III]" also protected
- All existing tests continue to pass (zero regressions)
- Validation CSV has roman numeral coverage
</success_criteria>

<output>
After completion, create `.planning/phases/20-roman-numeral-handling/20-01-SUMMARY.md`
</output>
