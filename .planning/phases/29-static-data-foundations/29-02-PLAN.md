---
phase: 29-static-data-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/extdata/toxval_schema.rds
  - R/cleaning_reference.R
  - tests/testthat/test_cleaning_reference.R
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - "ToxVal schema manifest exists as zero-row typed tibble"
    - "Schema includes all 56 ToxVal columns"
    - "All columns use typed NA values for parquet compatibility"
  artifacts:
    - path: "inst/extdata/toxval_schema.rds"
      provides: "ToxVal 56-column schema template"
      contains: "zero-row tibble with typed columns"
    - path: "R/cleaning_reference.R"
      provides: "load_toxval_schema() function"
      exports: ["load_toxval_schema"]
  key_links:
    - from: "R/cleaning_reference.R"
      to: "inst/extdata/toxval_schema.rds"
      via: "readRDS in load_toxval_schema()"
      pattern: "readRDS.*toxval_schema\\.rds"
---

# Plan 29-02: ToxVal Schema Manifest

<objective>
Create ToxVal schema manifest RDS file with zero-row typed tibble containing all 56 ToxVal columns, plus implement the `load_toxval_schema()` loader function.

Purpose: Provide schema template for Phase 32's ToxVal mapper to ensure correct column types and parquet compatibility.
Output: `inst/extdata/toxval_schema.rds` and `load_toxval_schema()` function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-static-data-foundations/29-CONTEXT.md
@R/cleaning_reference.R
</context>

<interfaces>
<!-- ToxVal Database Schema (56 columns) -->
<!-- Based on EPA ToxVal v9+ schema -->
<!-- Reference: https://comptox.epa.gov/dashboard/batch-search -->

**ToxVal Core Columns (required for all records):**
- source_hash (character) - Unique identifier
- dtxsid (character) - DSSTox Substance Identifier
- casrn (character) - CAS Registry Number
- name (character) - Chemical name

**Toxicity Values:**
- toxval_type (character) - e.g., "NOAEL", "LOAEL", "LD50"
- toxval_subtype (character) - Subcategory
- toxval_numeric (double) - Numeric value
- toxval_numeric_qualifier (character) - "<", ">", "~", etc.
- toxval_units (character) - Units of measurement

**Study Information:**
- study_type (character) - e.g., "acute", "chronic"
- study_duration_class (character) - Duration classification
- study_duration_value (double) - Numeric duration
- study_duration_units (character) - Duration units

**Species/Strain:**
- species_common (character) - Common name
- species_scientific (character) - Latin name
- strain (character) - Strain identifier
- sex (character) - Sex of test subjects

**Exposure:**
- exposure_route (character) - e.g., "oral", "inhalation"
- exposure_method (character) - Method details

**Source/Quality:**
- source (character) - Data source
- subsource (character) - Sub-source
- source_url (character) - URL
- quality (character) - Data quality flag
- qc_status (character) - QC status

**All columns use typed NA: NA_character_ for character, NA_real_ for double**
</interfaces>

<tasks>

<task id="1" type="auto">
<name>Task 1: Create ToxVal schema manifest RDS file</name>
<files>inst/extdata/toxval_schema.rds</files>
<read_first>
- .planning/phases/29-static-data-foundations/29-CONTEXT.md (D-04, D-05 decisions)
- R/cleaning_reference.R (tibble structure pattern)
</read_first>
<action>
Create R script to build and save toxval_schema.rds with zero-row typed tibble containing all 56 ToxVal columns.

**Per D-04:** Include all 56 ToxVal columns from day one.
**Per D-05:** Use typed NA values (`NA_character_`, `NA_real_`, `NA_integer_`) for parquet compatibility.

**Full 56-column schema:**

```r
toxval_schema <- tibble::tibble(
 # Identifiers (5 columns)
  source_hash = NA_character_,
  dtxsid = NA_character_,
  casrn = NA_character_,
  name = NA_character_,
  source_name = NA_character_,

  # Toxicity Value (7 columns)
  toxval_type = NA_character_,
  toxval_subtype = NA_character_,
  toxval_numeric = NA_real_,
  toxval_numeric_qualifier = NA_character_,
  toxval_units = NA_character_,
  risk_assessment_class = NA_character_,
  toxval_type_supercategory = NA_character_,

  # Study Design (8 columns)
  study_type = NA_character_,
  study_duration_class = NA_character_,
  study_duration_value = NA_real_,
  study_duration_units = NA_character_,
  generation = NA_character_,
  lifestage = NA_character_,
  exposure_form = NA_character_,
  media = NA_character_,

  # Species (6 columns)
  species_common = NA_character_,
  species_scientific = NA_character_,
  species_supercategory = NA_character_,
  strain = NA_character_,
  strain_group = NA_character_,
  sex = NA_character_,

  # Exposure Route (4 columns)
  exposure_route = NA_character_,
  exposure_method = NA_character_,
  exposure_route_description = NA_character_,
  critical_effect = NA_character_,

  # Source/Provenance (8 columns)
  source = NA_character_,
  subsource = NA_character_,
  source_url = NA_character_,
  subsource_url = NA_character_,
  details_text = NA_character_,
  year = NA_integer_,
  title = NA_character_,
  author = NA_character_,

  # Quality/Status (6 columns)
  quality = NA_character_,
  qc_status = NA_character_,
  priority_id = NA_integer_,
  human_eco = NA_character_,
  species_id = NA_integer_,
  document_type = NA_character_,

  # Audit Columns for Harmonization (12 columns)
  toxval_numeric_original = NA_real_,
  toxval_units_original = NA_character_,
  toxval_numeric_qualifier_original = NA_character_,
  study_duration_value_original = NA_real_,
  study_duration_units_original = NA_character_,
  conversion_factor = NA_real_,
  parse_flag = NA_character_,
  unit_flag = NA_character_,
  range_bin = NA_character_,
  harmonized_by = NA_character_,
  harmonized_at = NA_character_,
  curation_notes = NA_character_
)

# Verify it's zero-row
stopifnot(nrow(toxval_schema) == 0)

# Save
saveRDS(toxval_schema, "inst/extdata/toxval_schema.rds", compress = FALSE)
```

Write to temp R script and execute with Rscript to create the file.

**Column count verification:** 5 + 7 + 8 + 6 + 4 + 8 + 6 + 12 = 56 columns
</action>
<verify>
<automated>ls -la inst/extdata/toxval_schema.rds && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "x <- readRDS('inst/extdata/toxval_schema.rds'); cat('Rows:', nrow(x), '\\nCols:', ncol(x), '\\nColumn names:', paste(head(names(x), 10), collapse=', '), '...')"</automated>
</verify>
<acceptance_criteria>
- File `inst/extdata/toxval_schema.rds` exists and is readable
- Tibble has exactly 0 rows (zero-row template)
- Tibble has exactly 56 columns
- Contains required columns: dtxsid, casrn, name, toxval_type, toxval_numeric, toxval_units
- Contains audit columns: toxval_numeric_original, toxval_units_original, conversion_factor, parse_flag
- Column types are explicit: character columns have NA_character_, numeric have NA_real_
- Rscript `sapply(x, typeof)` returns "character" or "double" or "integer", never "logical" (which indicates bare NA)
</acceptance_criteria>
<done>ToxVal schema manifest exists at inst/extdata/toxval_schema.rds with zero-row 56-column typed tibble</done>
</task>

<task id="2" type="auto">
<name>Task 2: Implement load_toxval_schema() function</name>
<files>R/cleaning_reference.R</files>
<read_first>
- R/cleaning_reference.R (existing loader patterns)
- inst/extdata/toxval_schema.rds (created in Task 1)
</read_first>
<action>
Add `load_toxval_schema()` function to R/cleaning_reference.R, following the established pattern.

**Insert after `load_unit_map()` function (if exists from Plan 01, otherwise after `load_isotope_lookup()`), before `load_all_reference_lists()`:**

```r
#' Load ToxVal schema manifest
#'
#' Returns a zero-row tibble defining the 56-column ToxVal schema with proper
#' column types. Use this as a template for creating ToxVal-compatible output.
#'
#' All columns use typed NA values (NA_character_, NA_real_, NA_integer_) to
#' ensure parquet compatibility and proper column type inference.
#'
#' Schema includes:
#' - Identifiers: source_hash, dtxsid, casrn, name, source_name
#' - Toxicity values: toxval_type, toxval_numeric, toxval_units, etc.
#' - Study design: study_type, study_duration_*, generation, lifestage
#' - Species: species_common, species_scientific, strain, sex
#' - Exposure: exposure_route, exposure_method, critical_effect
#' - Source/provenance: source, subsource, year, title, author
#' - Quality: quality, qc_status, priority_id
#' - Audit columns: *_original fields for harmonization tracking
#'
#' @param cache_dir Directory for cache files (e.g., "inst/extdata")
#' @return Zero-row tibble with 56 typed columns
#' @export
load_toxval_schema <- function(cache_dir) {
  cache_path <- file.path(cache_dir, "toxval_schema.rds")

  fetch_fn <- function() {
    # Static data - should already exist in inst/extdata/
    # If missing, return minimal schema with warning
    warning("toxval_schema.rds not found - returning minimal schema")
    tibble::tibble(
      source_hash = NA_character_,
      dtxsid = NA_character_,
      casrn = NA_character_,
      name = NA_character_,
      toxval_type = NA_character_,
      toxval_numeric = NA_real_,
      toxval_units = NA_character_
    )[0, ]
  }

  load_or_fetch_reference(cache_path, fetch_fn, "ToxVal schema")
}
```

**Also update `load_all_reference_lists()` to include toxval_schema:**

Add to the list return:
```r
list(
  stop_words = load_stop_words(cache_dir),
  block_patterns = load_block_patterns(cache_dir),
  functional_categories = load_functional_categories(cache_dir),
  strip_terms = load_strip_terms(cache_dir),
  isotope_lookup = load_isotope_lookup(cache_dir),
  unit_map = load_unit_map(cache_dir),        # from Plan 01
  toxval_schema = load_toxval_schema(cache_dir)
)
```

**Update the roxygen comment for load_all_reference_lists** to include toxval_schema in the @return and @examples.
</action>
<verify>
<automated>"C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "source('R/cleaning_reference.R'); x <- load_toxval_schema('inst/extdata'); cat('Type:', class(x)[1], '\\nRows:', nrow(x), '\\nCols:', ncol(x))"</automated>
</verify>
<acceptance_criteria>
- R/cleaning_reference.R contains `load_toxval_schema <- function(cache_dir)`
- Function has roxygen documentation with @param cache_dir and @return
- Function has @export tag
- load_all_reference_lists() returns list containing `toxval_schema` key
- Running load_toxval_schema("inst/extdata") returns zero-row tibble with 56 columns
- grep "load_toxval_schema" R/cleaning_reference.R returns matches
</acceptance_criteria>
<done>load_toxval_schema() function added to R/cleaning_reference.R with proper roxygen documentation, @export, and integrated into load_all_reference_lists()</done>
</task>

<task id="3" type="auto">
<name>Task 3: Add unit tests for load_toxval_schema()</name>
<files>tests/testthat/test_cleaning_reference.R</files>
<read_first>
- tests/testthat/test_cleaning_reference.R (existing test patterns)
- R/cleaning_reference.R (load_toxval_schema implementation from Task 2)
</read_first>
<action>
Add test cases for load_toxval_schema() in tests/testthat/test_cleaning_reference.R.

**Add new test_that blocks:**

```r
test_that("load_toxval_schema returns zero-row typed tibble", {
  cache_dir <- "inst/extdata"
  if (!file.exists(file.path(cache_dir, "toxval_schema.rds"))) {
    skip("toxval_schema.rds not found")
  }

  result <- load_toxval_schema(cache_dir)

  # Check type
  expect_s3_class(result, "tbl_df")

  # Check zero rows
  expect_equal(nrow(result), 0)

  # Check 56 columns
  expect_equal(ncol(result), 56)
})

test_that("load_toxval_schema has required columns", {
  cache_dir <- "inst/extdata"
  if (!file.exists(file.path(cache_dir, "toxval_schema.rds"))) {
    skip("toxval_schema.rds not found")
  }

  result <- load_toxval_schema(cache_dir)

  # Core identifier columns
  expect_true("dtxsid" %in% names(result))
  expect_true("casrn" %in% names(result))
  expect_true("name" %in% names(result))

  # Toxicity value columns
  expect_true("toxval_type" %in% names(result))
  expect_true("toxval_numeric" %in% names(result))
  expect_true("toxval_units" %in% names(result))
  expect_true("toxval_numeric_qualifier" %in% names(result))

  # Audit columns
  expect_true("toxval_numeric_original" %in% names(result))
  expect_true("toxval_units_original" %in% names(result))
  expect_true("conversion_factor" %in% names(result))
  expect_true("parse_flag" %in% names(result))
})

test_that("load_toxval_schema uses typed NA values", {
  cache_dir <- "inst/extdata"
  if (!file.exists(file.path(cache_dir, "toxval_schema.rds"))) {
    skip("toxval_schema.rds not found")
  }

  result <- load_toxval_schema(cache_dir)

  # Check that character columns are character type (not logical from bare NA)
  col_types <- sapply(result, typeof)

  # No logical types (indicates bare NA)
  expect_false(any(col_types == "logical"))

  # dtxsid should be character
  expect_equal(typeof(result$dtxsid), "character")

  # toxval_numeric should be double
  expect_equal(typeof(result$toxval_numeric), "double")

  # year should be integer
  expect_equal(typeof(result$year), "integer")
})

test_that("load_all_reference_lists includes toxval_schema", {
  cache_dir <- "inst/extdata/reference_cache"
  result <- load_all_reference_lists(cache_dir)

  expect_true("toxval_schema" %in% names(result))
})
```

**Update the existing "load_all_reference_lists returns correct keys" test** if it exists — it should now expect 7 keys (after both Plan 01 and Plan 02).
</action>
<verify>
<automated>"C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "testthat::test_file('tests/testthat/test_cleaning_reference.R', reporter = 'minimal')"</automated>
</verify>
<acceptance_criteria>
- tests/testthat/test_cleaning_reference.R contains test_that blocks for load_toxval_schema
- Test "load_toxval_schema returns zero-row typed tibble" exists
- Test "load_toxval_schema has required columns" exists
- Test "load_toxval_schema uses typed NA values" exists
- Test "load_all_reference_lists includes toxval_schema" exists
- All new tests pass when run with testthat::test_file()
</acceptance_criteria>
<done>Unit tests added for load_toxval_schema() covering zero-row validation, required columns, typed NA verification, and integration with load_all_reference_lists()</done>
</task>

</tasks>

<verification>
- [ ] inst/extdata/toxval_schema.rds exists with zero rows and 56 columns
- [ ] All columns use typed NA values (no bare NA / logical type)
- [ ] Schema includes audit columns (*_original, conversion_factor, parse_flag)
- [ ] R/cleaning_reference.R contains load_toxval_schema() with @export
- [ ] load_all_reference_lists() includes toxval_schema in returned list
- [ ] All tests in test_cleaning_reference.R pass
</verification>

<success_criteria>
- ToxVal schema manifest (DATA-02) exists as zero-row typed 56-column tibble
- load_toxval_schema() function follows established pattern and is exported
- Tests validate structure, required columns, and typed NA usage
</success_criteria>

<output>
After completion, create `.planning/phases/29-static-data-foundations/29-02-SUMMARY.md`
</output>
