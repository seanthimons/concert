---
phase: 36-wire-toxval-shiny
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/mod_harmonize.R
  - inst/app/app.R
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SCHM-01, UITG-06, SCHM-04]
must_haves:
  truths:
    - "Run Harmonization in Shiny calls map_to_toxval_schema() and writes result to data_store$toxval_output"
    - "Sheet 8 ToxVal Output shows real 56-column data after Shiny harmonization completes"
    - "Harmonize tab only appears after both numeric tags AND curation are complete"
    - "Mapper errors are caught gracefully with showNotification, not a session crash"
  artifacts:
    - path: "R/mod_harmonize.R"
      provides: "map_to_toxval_schema() call in FULL MODE pipeline, toxval_output write"
      contains: "map_to_toxval_schema"
    - path: "inst/app/app.R"
      provides: "Dual-condition tab gating for harmonize_tab"
      contains: "req(data_store$numeric_tags, data_store$resolution_state)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SCHM-01, SCHM-04, UITG-06 marked complete"
      contains: "[x] **SCHM-01**"
  key_links:
    - from: "R/mod_harmonize.R"
      to: "R/toxval_mapper.R"
      via: "map_to_toxval_schema() function call"
      pattern: "map_to_toxval_schema\\("
    - from: "R/mod_harmonize.R"
      to: "R/mod_review_results.R"
      via: "data_store$toxval_output reactive bridge"
      pattern: "data_store\\$toxval_output"
    - from: "inst/app/app.R"
      to: "R/mod_harmonize.R"
      via: "show_tab_with_pulse gated behind curation completion"
      pattern: "req.*resolution_state"
---

<objective>
Wire map_to_toxval_schema() into the Shiny interactive harmonization pipeline so the full E2E flow works: upload -> detect -> clean -> tag -> curate -> harmonize -> export with real ToxVal Output in Sheet 8. Gate the Harmonize tab behind curation completion. Close SCHM-01, UITG-06, and SCHM-04 requirements.

Purpose: Phase 32 built the mapper, Phase 34 built the harmonize module, Phase 35 wired Sheet 8 export -- but the Shiny interactive path never calls the mapper. This plan closes that integration gap so data_store$toxval_output is populated and Sheet 8 shows real data (not the placeholder).

Output: Modified mod_harmonize.R with mapper call, modified app.R with dual-gating, updated REQUIREMENTS.md marking 3 requirements complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-wire-toxval-shiny/36-CONTEXT.md
@.planning/phases/36-wire-toxval-shiny/36-PATTERNS.md
@.planning/phases/35-export-extension-headless/35-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From R/toxval_mapper.R (lines 53):
```r
map_to_toxval_schema <- function(curated_data, harmonized_data, source_name = NULL)
# curated_data: tibble with dtxsid, casrn, name columns (from resolution_state)
# harmonized_data: tibble from harmonize_units() with orig_row_id, orig_unit, harmonized_value, harmonized_unit, conversion_factor, unit_flag
# source_name: character, optional, defaults to "user_upload"
# Returns: tibble with 56 ToxVal columns
```

From R/mod_harmonize.R (lines 345-359) -- current Stage 4 store block:
```r
# Stage 4: Store results
incProgress(0.25, detail = "Finalizing...")
data_store$harmonize_results <- list(
  parsed = parse_tibble,
  harmonized = harmonize_tibble,
  input_data = input_df
)
data_store$harmonize_audit <- dplyr::bind_cols(
  parse_tibble,
  harmonize_tibble[, c(
    "orig_unit", "harmonized_value", "harmonized_unit",
    "conversion_factor", "unit_flag"
  )]
)
```

From inst/app/app.R (lines 360-364) -- current harmonize tab gating:
```r
# Phase 34: Show Harmonize tab when numeric tags are set
shiny::observe({
  shiny::req(data_store$numeric_tags)
  show_tab_with_pulse("harmonize_tab")
})
```

From R/curate_headless.R (lines 275-282) -- reference mapper call:
```r
# Stage 4: Map to ToxVal schema
message("[headless] Stage 4: Mapping to ToxVal schema...")
toxval_tibble <- map_to_toxval_schema(
  curated_data = input_df,
  harmonized_data = harmonize_tibble,
  source_name = tools::file_path_sans_ext(basename(input_path))
)
```

From R/mod_review_results.R (line ~1466) -- already wired, DO NOT MODIFY:
```r
toxval_output = data_store$toxval_output   # already passes to build_export_sheets()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add map_to_toxval_schema() call to mod_harmonize.R FULL MODE pipeline</name>
  <files>R/mod_harmonize.R</files>
  <read_first>
    - R/mod_harmonize.R (full file -- understand pipeline structure, line numbers)
    - R/toxval_mapper.R (lines 1-55 -- function signature and params)
    - R/curate_headless.R (lines 275-283 -- reference implementation of mapper call)
    - .planning/phases/36-wire-toxval-shiny/36-PATTERNS.md (exact insertion point and incProgress budget)
  </read_first>
  <action>
Modify R/mod_harmonize.R FULL MODE withProgress block (starts at line 296) to add the ToxVal schema mapper call as Stage 5. Two changes in the FULL MODE block:

**Change 1 -- Reduce Stage 4 incProgress from 0.25 to 0.15:**
On line 346, change:
```r
incProgress(0.25, detail = "Finalizing...")
```
to:
```r
incProgress(0.15, detail = "Finalizing...")
```
This keeps the total progress at 1.00 (0.15 + 0.30 + 0.30 + 0.15 + 0.10 = 1.00).

**Change 2 -- Insert Stage 5 mapper call after line 359 (after the harmonize_audit write, before the closing `})` of the withProgress block):**

Insert this exact code block between the `data_store$harmonize_audit <- dplyr::bind_cols(...)` assignment (currently ending at line 359) and the closing `})` of the withProgress block (currently line 360):

```r
              # Stage 5: Map to ToxVal schema (SCHM-01, UITG-06)
              incProgress(0.10, detail = "Mapping to ToxVal schema...")
              toxval_tibble <- tryCatch(
                map_to_toxval_schema(
                  curated_data = data_store$resolution_state,
                  harmonized_data = harmonize_tibble,
                  source_name = data_store$file_info$name
                ),
                error = function(e) {
                  showNotification(
                    paste("ToxVal mapping failed:", conditionMessage(e)),
                    type = "warning",
                    duration = 8
                  )
                  NULL
                }
              )
              data_store$toxval_output <- toxval_tibble
```

Key details per CONTEXT.md decisions:
- `curated_data = data_store$resolution_state` (D-04 -- carries dtxsid/casrn/name from curation)
- `harmonized_data = harmonize_tibble` (local variable, NOT data_store$harmonize_results$harmonized -- avoids extra reactive read)
- `source_name = data_store$file_info$name` (D-05 -- uploaded filename)
- Inner tryCatch with type="warning" + duration=8 (D-12 -- non-fatal, Sheet 8 shows placeholder on NULL)
- Outer tryCatch (lines 224-376) already catches fatal errors with type="error"
- Do NOT add mapper call in INCREMENTAL MODE block (lines 226-293) -- mapper always needs full resolved dataset per 36-PATTERNS.md

Indentation: 14 spaces (matching existing withProgress block content).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all('.'); cat('map_to_toxval_schema found:', exists('map_to_toxval_schema'), '\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_harmonize.R contains the string `map_to_toxval_schema(` (grep returns match)
    - R/mod_harmonize.R contains `curated_data = data_store$resolution_state`
    - R/mod_harmonize.R contains `harmonized_data = harmonize_tibble`
    - R/mod_harmonize.R contains `source_name = data_store$file_info$name`
    - R/mod_harmonize.R contains `data_store$toxval_output <- toxval_tibble`
    - R/mod_harmonize.R contains `incProgress(0.10, detail = "Mapping to ToxVal schema...")`
    - R/mod_harmonize.R Stage 4 line reads `incProgress(0.15, detail = "Finalizing...")` (reduced from 0.25)
    - The mapper call is NOT present in the INCREMENTAL MODE block (grep for map_to_toxval_schema in lines 226-293 returns no match)
    - The tryCatch wrapping uses `type = "warning"` and `duration = 8`
    - `devtools::load_all('.')` succeeds without parse errors
  </acceptance_criteria>
  <done>FULL MODE harmonization pipeline in mod_harmonize.R calls map_to_toxval_schema() at Stage 5 and writes result to data_store$toxval_output. Progress budget sums to 1.00. Error handling wraps mapper in inner tryCatch with warning notification.</done>
</task>

<task type="auto">
  <name>Task 2: Gate Harmonize tab behind curation completion in app.R</name>
  <files>inst/app/app.R</files>
  <read_first>
    - inst/app/app.R (lines 355-370 -- current harmonize tab observer and surrounding context)
    - .planning/phases/36-wire-toxval-shiny/36-PATTERNS.md (tab gating section with exact before/after code)
  </read_first>
  <action>
Modify inst/app/app.R at lines 360-364. Change the Harmonize tab show observer from single-condition to dual-condition gating per D-06, D-07, D-08.

**Current code (lines 360-364):**
```r
  # Phase 34: Show Harmonize tab when numeric tags are set
  shiny::observe({
    shiny::req(data_store$numeric_tags)
    show_tab_with_pulse("harmonize_tab")
  })
```

**Replace with:**
```r
  # Phase 36: Show Harmonize tab when numeric tags AND curation are complete
  shiny::observe({
    shiny::req(data_store$numeric_tags, data_store$resolution_state)
    show_tab_with_pulse("harmonize_tab")
  })
```

Changes:
1. Comment updated from "Phase 34" to "Phase 36" and description updated
2. `shiny::req()` now takes two arguments: `data_store$numeric_tags` AND `data_store$resolution_state`
3. Both conditions must be truthy (non-NULL) for the observer to fire
4. No other changes needed -- reset functions already clear resolution_state which hides the tab via the existing bslib::nav_hide calls in reset_chemical_downstream()
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('inst/app/app.R', port=3838, launch.browser=FALSE)" &amp; sleep 12 &amp; kill %1 2>/dev/null; echo "Smoke test done"</automated>
  </verify>
  <acceptance_criteria>
    - inst/app/app.R contains `shiny::req(data_store$numeric_tags, data_store$resolution_state)` (grep returns match)
    - inst/app/app.R does NOT contain `shiny::req(data_store$numeric_tags)` as a standalone single-argument req for the harmonize tab (the old pattern is replaced, not duplicated)
    - inst/app/app.R contains the comment `# Phase 36: Show Harmonize tab when numeric tags AND curation are complete`
    - Shiny cold boot succeeds (app starts without errors on `shiny::runApp()`)
  </acceptance_criteria>
  <done>Harmonize tab only appears after both numeric tags exist AND curation has completed (resolution_state is non-NULL). Enforces full linear workflow: upload -> detect -> clean -> tag -> curate -> harmonize.</done>
</task>

<task type="auto">
  <name>Task 3: Mark SCHM-01, SCHM-04, UITG-06 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file -- see current checkbox and traceability state)
    - .planning/phases/36-wire-toxval-shiny/36-CONTEXT.md (D-09, D-10 -- SCHM-04 resolution wording)
  </read_first>
  <action>
Update .planning/REQUIREMENTS.md with three sets of changes:

**Change 1 -- Checkbox updates (lines 37, 40, 51):**

Line 37, change:
```
- [ ] **SCHM-01**: ToxVal 56-column schema transmutation with `*_original` audit columns for all harmonized fields
```
to:
```
- [x] **SCHM-01**: ToxVal 56-column schema transmutation with `*_original` audit columns for all harmonized fields
```

Line 40, change:
```
- [ ] **SCHM-04**: CSV export fallback when arrow unavailable (arrow in Suggests with `requireNamespace()` guard)
```
to:
```
- [x] **SCHM-04**: CSV export fallback when arrow unavailable -- superseded by D-02 (arrow as hard dep). CSV available as format choice via `format="csv"` in `curate_headless()`, not as a fallback.
```

Line 51, change:
```
- [ ] **UITG-06**: Sheet 8 "ToxVal Output" in existing Excel export (separate sheet, not merged with data)
```
to:
```
- [x] **UITG-06**: Sheet 8 "ToxVal Output" in existing Excel export (separate sheet, not merged with data)
```

**Change 2 -- Traceability table (lines 102, 105, 112):**

Change from `Pending` to `Complete`:
```
| SCHM-01 | Phase 36 | Complete |
```
```
| SCHM-04 | Phase 36 | Complete |
```
```
| UITG-06 | Phase 36 | Complete |
```

**Change 3 -- Coverage summary (lines 117-118):**

Change:
```
- Complete: 24
- Pending (Phase 36 gap closure): 3 (SCHM-01, SCHM-04, UITG-06)
```
to:
```
- Complete: 27
- Pending (Phase 36 gap closure): 0
```
  </action>
  <verify>
    <automated>grep -c "\- \[ \]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains `[x] **SCHM-01**` (grep returns match)
    - REQUIREMENTS.md contains `[x] **SCHM-04**` with "superseded by D-02" note (grep returns match)
    - REQUIREMENTS.md contains `[x] **UITG-06**` (grep returns match)
    - REQUIREMENTS.md contains zero `- [ ]` checkboxes (grep -c returns 0)
    - Traceability table shows `| SCHM-01 | Phase 36 | Complete |`
    - Traceability table shows `| SCHM-04 | Phase 36 | Complete |`
    - Traceability table shows `| UITG-06 | Phase 36 | Complete |`
    - Coverage line reads `Complete: 27`
    - Coverage line reads `Pending (Phase 36 gap closure): 0`
  </acceptance_criteria>
  <done>All 27 v1.9 requirements marked complete. SCHM-04 includes explanatory note about arrow hard dep superseding the CSV fallback requirement. Zero pending items remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data_store reactive state | Internal Shiny reactive state -- no new user input surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Denial of Service | map_to_toxval_schema large dataset | accept | Mapper already operates on in-memory data that passed through existing pipeline stages; no new amplification vector. Large datasets already gated by upload size limit (50MB). |
| T-36-02 | Information Disclosure | error message in showNotification | mitigate | tryCatch uses conditionMessage(e) which exposes R error text to user. Acceptable for single-user desktop Shiny app. No secrets in mapper errors (schema column names only). |
| T-36-03 | Tampering | data_store$resolution_state input to mapper | accept | resolution_state is populated by curation pipeline (Phase 34), not by direct user input. Tab gating (D-06) ensures it exists before mapper runs. No new injection surface. |
</threat_model>

<verification>
1. `grep "map_to_toxval_schema" R/mod_harmonize.R` -- mapper call present in FULL MODE
2. `grep "data_store\\$toxval_output" R/mod_harmonize.R` -- reactive write present
3. `grep "resolution_state" inst/app/app.R` -- dual gating present
4. `grep -c "\- \[ \]" .planning/REQUIREMENTS.md` -- returns 0 (all complete)
5. Shiny cold boot test: app starts without errors
6. Existing test suite passes: `devtools::test()` with 0 new failures
</verification>

<success_criteria>
- map_to_toxval_schema() is called in the Shiny harmonization pipeline (FULL MODE only)
- data_store$toxval_output is populated with real 56-column ToxVal data
- Sheet 8 "ToxVal Output" in Excel export shows real data (not placeholder) after harmonization
- Harmonize tab gated behind both numeric tags AND curation completion
- SCHM-01, SCHM-04, UITG-06 marked complete in REQUIREMENTS.md
- Shiny app cold boots successfully
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/36-wire-toxval-shiny/36-01-SUMMARY.md`
</output>
