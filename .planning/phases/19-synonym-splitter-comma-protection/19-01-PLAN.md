---
phase: 19-synonym-splitter-comma-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/cleaning_pipeline.R
  - tests/test_name_cleaning.R
autonomous: true
requirements:
  - SPLIT-01
  - SPLIT-02

must_haves:
  truths:
    - "A name like '2,4,6-trichlorophenol' passes through split_synonyms as a single name (no split on locant commas)"
    - "A name like '2,4-D' still passes through as a single name (single-locant protection not regressed)"
    - "A name like 'acetone, purified' is still split into two entries"
    - "'butane, 2,4,6-trimethyl' (inverted + multi-locant) stays intact as one name"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "repeat-until-stable loop around digit-comma-digit and letter-comma-letter protection"
      contains: "repeat"
    - path: "tests/test_name_cleaning.R"
      provides: "Multi-locant test cases for split_synonyms"
      contains: "2,4,6-trichlorophenol"
  key_links:
    - from: "R/cleaning_pipeline.R"
      to: "tests/test_name_cleaning.R"
      via: "split_synonyms function tested by test_name_cleaning.R"
      pattern: "split_synonyms"
---

<objective>
Extend the synonym splitter's locant comma protection to handle multi-locant IUPAC names (3+ locants like "2,4,6-trichlorophenol") by wrapping the existing digit-comma-digit and letter-comma-letter placeholder replacements in a repeat-until-stable loop.

Purpose: The current single-pass `str_replace_all` with `(\d+),(\d+)` only protects one comma per adjacent pair in a single pass. "2,4,6" becomes "2@@@4,6" after one pass, leaving the second comma exposed to splitting. A repeat loop applies the replacement until no more matches exist, protecting all locant commas.

Output: Modified `split_synonyms()` in `R/cleaning_pipeline.R` and new test cases in `tests/test_name_cleaning.R`.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-synonym-splitter-comma-protection/19-CONTEXT.md

<interfaces>
<!-- Key code the executor needs. Extracted from R/cleaning_pipeline.R lines 848-868 -->

Current protection logic in split_synonyms() (lines 848-868):
```r
# Step 0: Protect letter-comma-letter IUPAC patterns (N,N- O,O- S,S- etc.)
protected_name <- stringr::str_replace_all(original_name, "([A-Za-z]),([A-Za-z])", "\\1@@@\\2")

# Step 1: Protect digit-comma-digit patterns (IUPAC locants like 2,4- or 1,3-)
protected_name <- stringr::str_replace_all(protected_name, "(\\d+),(\\d+)", "\\1@@@\\2")

# Step 2: Protect IUPAC inverted names (e.g., "butane, 2,2-dimethyl")
protected_name <- stringr::str_replace_all(protected_name, ",\\s+(\\d)", "%%%\\1")

# Step 3: Split on semicolons first, then commas
parts <- protected_name %>%
  stringr::str_split(";") %>%
  unlist() %>%
  stringr::str_split(",") %>%
  unlist() %>%
  stringr::str_trim() %>%
  stringr::str_replace_all("@@@", ",") %>%   # Restore digit-comma-digit
  stringr::str_replace_all("%%%", ", ")      # Restore inverted name comma
```

Existing test pattern (from tests/test_name_cleaning.R):
```r
test_that("split_synonyms protects IUPAC digit-comma-digit patterns", {
  df <- tibble::tibble(
    original_row_id = c(1L, 2L, 3L),
    chemical_name = c("butane, 2,2-dimethyl", "1,4-Dioxane", "2,4-dichlorophenol")
  )
  tag_map <- list(chemical_name = "Name")
  result <- split_synonyms(df, names(tag_map)[tag_map == "Name"], tag_map)
  cleaned <- result$cleaned_data
  expect_equal(nrow(cleaned), 3)  # 3 rows stay 3 rows
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-locant test cases and implement repeat-until-stable loop</name>
  <files>tests/test_name_cleaning.R, R/cleaning_pipeline.R</files>
  <read_first>
    - R/cleaning_pipeline.R (lines 821-928 — full split_synonyms function, current protection logic)
    - tests/test_name_cleaning.R (lines 144-320 — existing split_synonyms tests, test patterns and helper setup)
    - .planning/phases/19-synonym-splitter-comma-protection/19-CONTEXT.md (locked decisions on loop strategy)
  </read_first>
  <behavior>
    - Test: "2,4,6-trichlorophenol" stays as 1 row, name intact (SPLIT-01 core case)
    - Test: "1,2,3,4,5,6-hexachlorocyclohexane" stays as 1 row (6-locant chain)
    - Test: "butane, 2,4,6-trimethyl" stays as 1 row with name "butane, 2,4,6-trimethyl" (inverted + multi-locant coexistence)
    - Test: "acetone, 2,4-dinitrophenylhydrazone" splits into 2 rows — "acetone" and "2,4-dinitrophenylhydrazone" (first comma is synonym separator, 2,4 locant protected)
    - Test: "2,4-D" stays as 1 row (SPLIT-02 regression — already passes but must remain in test suite)
    - Test: "1,4-Dioxane" stays as 1 row (SPLIT-02 regression)
    - Test: "xylene, toluene" splits into 2 rows (plain non-locant comma still splits)
  </behavior>
  <action>
**RED phase — Write failing tests first:**

In `tests/test_name_cleaning.R`, after the existing split_synonyms test block (around line 320), add a new test_that block:

```r
test_that("split_synonyms protects multi-locant IUPAC patterns (3+ locants)", {
  df <- tibble::tibble(
    original_row_id = c(1L, 2L, 3L, 4L, 5L),
    chemical_name = c(
      "2,4,6-trichlorophenol",
      "1,2,3,4,5,6-hexachlorocyclohexane",
      "butane, 2,4,6-trimethyl",
      "acetone, 2,4-dinitrophenylhydrazone",
      "xylene, toluene"
    )
  )
  tag_map <- list(chemical_name = "Name")

  result <- split_synonyms(df, names(tag_map)[tag_map == "Name"], tag_map)
  cleaned <- result$cleaned_data

  # Row 1: multi-locant — must NOT split
  row1 <- cleaned[cleaned$original_row_id == 1L, ]
  expect_equal(nrow(row1), 1)
  expect_equal(row1$chemical_name, "2,4,6-trichlorophenol")

  # Row 2: 6-locant chain — must NOT split
  row2 <- cleaned[cleaned$original_row_id == 2L, ]
  expect_equal(nrow(row2), 1)
  expect_equal(row2$chemical_name, "1,2,3,4,5,6-hexachlorocyclohexane")

  # Row 3: inverted name + multi-locant — must NOT split
  row3 <- cleaned[cleaned$original_row_id == 3L, ]
  expect_equal(nrow(row3), 1)
  expect_equal(row3$chemical_name, "butane, 2,4,6-trimethyl")

  # Row 4: synonym separator + locant — SHOULD split into 2
  row4 <- cleaned[cleaned$original_row_id == 4L, ]
  expect_equal(nrow(row4), 2)
  expect_equal(row4$chemical_name[1], "acetone")
  expect_equal(row4$chemical_name[2], "2,4-dinitrophenylhydrazone")

  # Row 5: plain synonyms — SHOULD split into 2
  row5 <- cleaned[cleaned$original_row_id == 5L, ]
  expect_equal(nrow(row5), 2)
  expect_equal(row5$chemical_name[1], "xylene")
  expect_equal(row5$chemical_name[2], "toluene")
})
```

Run the tests — the multi-locant cases (rows 1, 2, 3) should FAIL because the current single-pass regex only protects one comma pair.

**GREEN phase — Implement the repeat loop:**

In `R/cleaning_pipeline.R`, replace lines 848-853 (the Step 0 and Step 1 blocks) with a repeat-until-stable loop. The new code replaces exactly these 4 lines:

```r
      # Step 0: Protect letter-comma-letter IUPAC patterns (N,N- O,O- S,S- etc.)
      protected_name <- stringr::str_replace_all(original_name, "([A-Za-z]),([A-Za-z])", "\\1@@@\\2")

      # Step 1: Protect digit-comma-digit patterns (IUPAC locants like 2,4- or 1,3-)
      protected_name <- stringr::str_replace_all(protected_name, "(\\d+),(\\d+)", "\\1@@@\\2")
```

Replace with:

```r
      # Step 0+1: Protect IUPAC comma patterns with repeat-until-stable loop
      # Single pass of (\d+),(\d+) only catches one pair per adjacency:
      # "2,4,6" -> "2@@@4,6" (misses second comma). Loop until no changes.
      protected_name <- original_name
      for (iter in seq_len(10)) {
        prev <- protected_name
        # Protect letter-comma-letter (N,N- O,O- S,S- etc.)
        protected_name <- stringr::str_replace_all(protected_name, "([A-Za-z]),([A-Za-z])", "\\1@@@\\2")
        # Protect digit-comma-digit (IUPAC locants like 2,4- or 1,3-)
        protected_name <- stringr::str_replace_all(protected_name, "(\\d+),(\\d+)", "\\1@@@\\2")
        if (identical(prev, protected_name)) break
      }
```

This uses `for` with `seq_len(10)` as the safety cap (max 10 iterations per the CONTEXT.md decision). The loop applies both letter-comma-letter and digit-comma-digit protections each iteration, breaking when no more replacements occur.

Lines 855-868 (Step 2 inverted name protection and Step 3 split/restore) remain UNCHANGED.

Run all tests again — multi-locant cases should now PASS.

Also run the full existing test suite to confirm no regressions in SPLIT-02 (single-locant cases like "2,4-D", "1,4-Dioxane", "butane, 2,2-dimethyl" already tested).
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_file('tests/test_name_cleaning.R')"</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R contains `for (iter in seq_len(10))` inside `split_synonyms`
    - R/cleaning_pipeline.R contains `if (identical(prev, protected_name)) break` inside the loop
    - R/cleaning_pipeline.R still contains the Step 2 inverted name protection line: `str_replace_all(protected_name, ",\\s+(\\d)", "%%%\\1")`
    - tests/test_name_cleaning.R contains test case string "2,4,6-trichlorophenol"
    - tests/test_name_cleaning.R contains test case string "1,2,3,4,5,6-hexachlorocyclohexane"
    - tests/test_name_cleaning.R contains test case string "butane, 2,4,6-trimethyl"
    - tests/test_name_cleaning.R contains test case string "acetone, 2,4-dinitrophenylhydrazone"
    - test_name_cleaning.R test run exits with 0 failures
    - Existing test "split_synonyms protects IUPAC digit-comma-digit patterns" still passes (SPLIT-02 regression check)
  </acceptance_criteria>
  <done>
    All multi-locant IUPAC names (3+ locants) pass through split_synonyms without splitting.
    All existing single-locant and letter-comma-letter protections continue to work.
    Plain non-locant commas still split correctly.
    New test cases cover the multi-locant scenarios documented in CONTEXT.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full cleaning pipeline test suite to confirm no regressions</name>
  <files>tests/test_cleaning_pipeline.R, tests/test_cleaning_pipeline_validation.R</files>
  <read_first>
    - tests/test_cleaning_pipeline.R (full file — verify it runs clean)
    - tests/test_cleaning_pipeline_validation.R (full file — has IUPAC validation tests at lines 124-250)
  </read_first>
  <action>
Run the full cleaning pipeline test suite and the validation test suite to confirm no regressions from the loop change. These tests exercise `run_cleaning_pipeline()` which calls `split_synonyms()` internally, so they serve as integration-level regression checks.

Specifically verify:
1. `test_cleaning_pipeline.R` — all 42+ assertions pass, including the "preserves internal commas" test at line 35
2. `test_cleaning_pipeline_validation.R` — Test Group 3 ("IUPAC letter-comma-letter protected") and Test Group 5 ("butane, 2,2-dimethyl stays intact") both pass

If any test fails, diagnose whether it is caused by the loop change in Task 1 and fix accordingly. The loop should be transparent to all existing patterns — if a regression appears, the likely cause is the loop altering the `protected_name` variable initialization (must start from `original_name`, not some other value).
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_file('tests/test_cleaning_pipeline.R'); testthat::test_file('tests/test_cleaning_pipeline_validation.R')"</automated>
  </verify>
  <acceptance_criteria>
    - test_cleaning_pipeline.R exits with 0 failures
    - test_cleaning_pipeline_validation.R exits with 0 failures
    - No test produces a warning containing "split" or "synonym" (would indicate unexpected splitting behavior)
  </acceptance_criteria>
  <done>
    Full cleaning pipeline regression suite passes with zero failures, confirming the repeat loop does not affect any existing behavior.
  </done>
</task>

</tasks>

<verification>
1. `split_synonyms("2,4,6-trichlorophenol")` returns 1 row with name intact
2. `split_synonyms("butane, 2,4,6-trimethyl")` returns 1 row with name intact (inverted + multi-locant)
3. `split_synonyms("acetone, 2,4-dinitrophenylhydrazone")` returns 2 rows: "acetone" and "2,4-dinitrophenylhydrazone"
4. `split_synonyms("2,4-D")` returns 1 row (regression check)
5. `split_synonyms("xylene, toluene")` returns 2 rows (plain comma still splits)
6. All tests in test_name_cleaning.R, test_cleaning_pipeline.R, and test_cleaning_pipeline_validation.R pass
</verification>

<success_criteria>
- SPLIT-01: Multi-locant IUPAC names with 3+ locants are not split by the synonym splitter
- SPLIT-02: Existing single-locant protection (2,4-D, 1,4-Dioxane, butane 2,2-dimethyl) continues working
- Zero test failures across the cleaning pipeline test suite
- The fix is minimal: only the protection step changed (repeat loop), split/restore logic untouched
</success_criteria>

<output>
After completion, create `.planning/phases/19-synonym-splitter-comma-protection/19-01-SUMMARY.md`
</output>
