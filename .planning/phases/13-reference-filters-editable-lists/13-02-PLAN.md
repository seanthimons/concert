---
phase: 13-reference-filters-editable-lists
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - R/modules/mod_clean_data.R
autonomous: false
requirements: [FILT-02, FILT-05, FILT-06, UIUX-05]

must_haves:
  truths:
    - "User can see blocking flags (red) and warning flags (yellow) in cleaned data table"
    - "User can edit reference lists via rhandsontable editors in accordion panels"
    - "User can upload CSV to bulk-add entries to reference lists"
    - "User can re-run cleaning after editing reference lists with full cascade reset"
    - "User can see flag statistics in value box dashboard (Formulas Blocked, Categories Flagged)"
  artifacts:
    - path: "R/modules/mod_clean_data.R"
      provides: "Reference list editors, flag display, re-run with cascade"
      contains: "rhandsontable|cleaning_flag|reference_editors"
  key_links:
    - from: "R/modules/mod_clean_data.R"
      to: "R/cleaning_pipeline.R"
      via: "detect_bare_formulas and flag_reference_matches calls in pipeline"
      pattern: "detect_bare_formulas|flag_reference_matches"
    - from: "R/modules/mod_clean_data.R"
      to: "R/cleaning_reference.R"
      via: "load_all_reference_lists for initial seeding"
      pattern: "load_all_reference_lists"
    - from: "R/modules/mod_clean_data.R"
      to: "data_store$reference_lists"
      via: "reactiveValues for editable reference list state"
      pattern: "data_store\\$reference_lists"
---

<objective>
Integrate reference list editors, flag display, CSV upload, and re-run cascade into the Clean Data module UI.

Purpose: Users need to see flags on their cleaned data (blocking = red, warning = yellow), edit reference lists in-app (add/remove/suppress entries), upload CSVs to bulk-enrich lists, and re-run cleaning with updated lists. This completes the Phase 13 user-facing workflow.

Output: Extended `mod_clean_data.R` with rhandsontable editors in accordion panels, DT conditional formatting for flags, CSV upload with validation, "Apply & Re-run" button with cascade reset, and extended value box dashboard.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reference-filters-editable-lists/13-CONTEXT.md
@.planning/phases/13-reference-filters-editable-lists/13-RESEARCH.md
@.planning/phases/13-reference-filters-editable-lists/13-01-SUMMARY.md

@R/modules/mod_clean_data.R
@R/cleaning_pipeline.R
@R/cleaning_reference.R

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From R/cleaning_reference.R (after Plan 01):
```r
load_all_reference_lists(cache_dir)
# Returns list with:
#   stop_words: tibble(term, source, active)
#   block_patterns: tibble(term, source, active)
#   functional_categories: tibble(term, source, active)
```

From R/cleaning_pipeline.R (after Plan 01):
```r
detect_bare_formulas(df, name_cols)
# Returns list(cleaned_data, audit_trail)
# Sets cleaning_flag = "BLOCK: bare formula" on matched rows
# Preserves formula in formula_blocked_{col} column

flag_reference_matches(df, name_cols, reference_list, flag_type, flag_label)
# Returns list(cleaned_data, audit_trail)
# flag_type: "warning" or "blocking"
# Sets cleaning_flag = "WARN: {label} [exact|substring]" or "BLOCK: {label} [exact|substring]"
# Skips rows that already have a cleaning_flag (first flag wins)
```

From R/modules/mod_clean_data.R (current UI structure):
```
[Run Cleaning button]
[Value Box Dashboard - 3 rows]
[Cleaned Data Table (DT)]
[Audit Trail Accordion]
[Multi-CAS Flagged Rows Section]
```

Established patterns:
- bslib::accordion() with accordion_panel() for collapsible sections
- bslib::layout_columns(col_widths = c(4, 4, 4)) for value box rows
- incProgress() for step-by-step pipeline progress
- data_store reactiveValues for shared state
- Cascade reset: set data_store$cleaned_data = NULL, data_store$curation_results = NULL, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reference list editors, flag pipeline integration, CSV upload, and re-run cascade to mod_clean_data.R</name>
  <files>R/modules/mod_clean_data.R</files>
  <action>
    Extend `mod_clean_data.R` with five additions:

    **1. Initialize reference lists in data_store on module load**

    At the start of the server function, load reference lists and store in data_store:
    ```r
    # Initialize reference lists if not yet loaded
    observe({
      if (is.null(data_store$reference_lists)) {
        data_store$reference_lists <- load_all_reference_lists("data/reference_cache")
      }
    })
    ```

    **2. Add reference list editor accordion panels to UI**

    Add BELOW the audit trail accordion, ABOVE the multi-CAS section. Use `bslib::accordion()` with `id = ns("reference_editors")`, `open = FALSE`, `multiple = TRUE`.

    Three accordion panels:
    - **Functional Categories**: `rhandsontable::rHandsontableOutput(ns("func_cat_editor"))` + count badge in title
    - **Stop Words**: `rhandsontable::rHandsontableOutput(ns("stop_words_editor"))` + count badge
    - **Block Patterns**: `rhandsontable::rHandsontableOutput(ns("block_patterns_editor"))` + count badge

    Each rhandsontable:
    - `term` column: editable text
    - `source` column: read-only (shows comptoxr/user/app_default)
    - `active` column: checkbox (TRUE/FALSE for soft delete)
    - `hot_context_menu(allowRowEdit = TRUE, allowColEdit = FALSE)` -- users can add/remove rows
    - Height = 300px, rowHeaders = NULL

    Add `observeEvent(input$func_cat_editor, ...)` etc. to capture edits via `hot_to_r()` and update `data_store$reference_lists`.

    When user adds a new row via context menu, the new row should have source = "user" and active = TRUE. Handle this by checking for rows with NA source after hot_to_r() and setting source = "user".

    **3. Add CSV upload button and handler**

    Add a single `fileInput(ns("csv_upload"), "Upload Reference List CSV", accept = ".csv")` above the reference editor accordion.

    On upload:
    - Read CSV with `readr::read_csv()`
    - Validate: must have `type` column with values in c("functional_category", "stop_word", "block_pattern")
    - Validate: must have `term` column
    - Show error modal if validation fails (with clear message about required columns and allowed types)
    - Route entries to correct lists: add rows with `source = "user"`, `active = TRUE`
    - Show success notification with count of entries added
    - Unknown type values: show error modal listing the unknown types and allowed types

    **4. Extend cleaning pipeline with flagging steps**

    In the `observeEvent(input$run_cleaning, ...)` handler, AFTER the synonym splitting and final cleanup (but before the "Finalizing" step), add two new pipeline steps:

    ```r
    # Phase 13: Bare formula detection (after all name cleaning)
    if (length(name_cols) > 0) {
      incProgress(0.05, detail = "Detecting bare formulas...")
      formula_result <- detect_bare_formulas(df, name_cols)
      df <- formula_result$cleaned_data
      all_audits[[length(all_audits) + 1]] <- formula_result$audit_trail
    }

    # Phase 13: Reference list flagging
    if (length(name_cols) > 0 && !is.null(data_store$reference_lists)) {
      incProgress(0.05, detail = "Flagging reference list matches...")

      # Flag functional categories (warning)
      func_cats <- data_store$reference_lists$functional_categories
      if (nrow(func_cats) > 0) {
        func_result <- flag_reference_matches(df, name_cols, func_cats, "warning", "functional category")
        df <- func_result$cleaned_data
        all_audits[[length(all_audits) + 1]] <- func_result$audit_trail
      }

      # Flag stop words (warning)
      stop_words <- data_store$reference_lists$stop_words
      if (nrow(stop_words) > 0) {
        stop_result <- flag_reference_matches(df, name_cols, stop_words, "warning", "stop word")
        df <- stop_result$cleaned_data
        all_audits[[length(all_audits) + 1]] <- stop_result$audit_trail
      }

      # Flag block patterns (blocking)
      block_pats <- data_store$reference_lists$block_patterns
      if (nrow(block_pats) > 0) {
        block_result <- flag_reference_matches(df, name_cols, block_pats, "blocking", "block pattern")
        df <- block_result$cleaned_data
        all_audits[[length(all_audits) + 1]] <- block_result$audit_trail
      }
    }
    ```

    Adjust progress weights to accommodate the two new steps. Redistribute existing weights slightly to make room (reduce some 0.10 steps to 0.08, etc.) so total remains 1.00.

    **5. Add DT conditional formatting for flags**

    In `output$cleaned_table`, add conditional row formatting:
    - If `cleaning_flag` column exists, use `DT::formatStyle("cleaning_flag", target = "row", ...)` to color:
      - Rows with "BLOCK:" prefix -> light red background (#ffcccc)
      - Rows with "WARN:" prefix -> light yellow background (#fff3cd)
    - Add `cleaning_flag` to visible columns (don't hide it -- users need to see/sort/filter by it)

    **6. Extend value box dashboard with flag statistics**

    Add a 4th row of value boxes (always visible, even with zeros -- per research recommendation):
    ```r
    row4 <- bslib::layout_columns(
      col_widths = c(4, 4, 4),
      bslib::value_box(
        title = "Formulas Blocked",
        value = n_formulas_blocked,
        showcase = bsicons::bs_icon("calculator"),
        theme = "danger"
      ),
      bslib::value_box(
        title = "Categories Flagged",
        value = n_categories_flagged,
        showcase = bsicons::bs_icon("tag"),
        theme = "warning"
      ),
      bslib::value_box(
        title = "Stop Words Matched",
        value = n_stop_words_matched,
        showcase = bsicons::bs_icon("hand-thumbs-down"),
        theme = "warning"
      )
    )
    ```

    Count from audit trail: `n_formulas_blocked = sum(audit$step == "detect_bare_formula")`, etc.

    **7. Re-run with cascade reset**

    The existing "Run Cleaning" button already re-runs the full pipeline. Extend it to:
    - Read reference lists from `data_store$reference_lists` (not from cache -- user may have edited them)
    - After re-run, invalidate downstream state:
      ```r
      data_store$curation_results <- NULL
      data_store$resolved_data <- NULL
      ```
    - This matches the established cascade reset pattern from Phase 11

    Per Claude's discretion: repurpose the existing "Run Cleaning" button (simpler UX) rather than adding a separate "Apply & Re-run" button. The pipeline always reads from data_store$reference_lists, so any edits are automatically picked up on next run.

    IMPORTANT: When adjusting progress increment weights for the new steps, ensure they sum to 1.00. The existing steps already sum to 1.00, so reduce some weights slightly to accommodate the two new steps (~0.05 each = 0.10 total to redistribute).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 10 && curl -s http://127.0.0.1:3838 > /dev/null && echo "SMOKE TEST PASSED" || echo "SMOKE TEST FAILED"</automated>
  </verify>
  <done>
    - App starts without error (smoke test passes)
    - Reference list editors render in accordion panels below audit trail
    - rhandsontable editors show term/source/active columns with correct editability
    - CSV upload validates type column and routes entries to correct lists
    - Cleaning pipeline includes bare formula detection and reference list flagging steps
    - Cleaned data table shows red/yellow row highlighting based on cleaning_flag
    - Value box dashboard includes 4th row with flag statistics
    - Re-running cleaning picks up edited reference lists
    - Downstream state (curation_results, resolved_data) reset on re-run
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify reference list editors, flag display, and re-run workflow</name>
  <what-built>Reference list editors with rhandsontable, flag display with conditional formatting, CSV upload, re-run cascade, and extended value box dashboard in Clean Data tab</what-built>
  <how-to-verify>
    1. Start the app and upload a chemical data file (e.g., chemical_validation_test.csv)
    2. Tag columns (Name and/or CASRN)
    3. Navigate to Clean Data tab and click "Run Cleaning"
    4. Verify value box dashboard shows 4 rows (CAS stats, general stats, name cleaning stats, flag stats)
    5. Scroll down to check cleaned data table -- if any flags exist, verify red (BLOCK) and yellow (WARN) row coloring
    6. Check that "Cleaning Audit Trail" accordion still works
    7. Expand reference list editor accordion panels (Functional Categories, Stop Words, Block Patterns)
    8. Verify rhandsontable editors show term/source/active columns
    9. Try adding a new row in Stop Words editor (right-click -> Insert row below) -- verify source defaults to "user"
    10. Try setting an entry's active checkbox to FALSE (soft delete)
    11. Click "Run Cleaning" again to re-run with updated lists
    12. Verify the suppressed entry is no longer flagging rows
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Smoke test: App starts without error, all UI elements render
2. Reference list editors: rhandsontable renders with correct columns and editability
3. Flag display: DT table shows red/yellow row coloring for BLOCK/WARN flags
4. CSV upload: Validates type column, routes entries, shows error on invalid input
5. Re-run: Pipeline reads from data_store$reference_lists, cascade resets downstream state
6. Value boxes: Flag statistics row visible with correct counts
7. Full test suite: `testthat::test_dir("tests")` passes with 0 failures
</verification>

<success_criteria>
- User can see blocking flags (red) and warning flags (yellow) in cleaned data table
- User can edit reference lists via rhandsontable with add/remove/suppress capabilities
- User can upload CSV to bulk-enrich reference lists with type routing
- User can re-run cleaning and see updated flags based on edited reference lists
- Downstream state (curation, resolution) properly invalidated on re-run
- Value box dashboard shows flag statistics (Formulas Blocked, Categories Flagged, Stop Words Matched)
</success_criteria>

<output>
After completion, create `.planning/phases/13-reference-filters-editable-lists/13-02-SUMMARY.md`
</output>
