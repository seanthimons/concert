---
phase: 06-search-pipeline-refinement
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app.R
autonomous: false
requirements:
  - SRCH-01
must_haves:
  truths:
    - "User sees a Match Type column in Review Results showing which search tier resolved each row"
    - "Match Type uses friendly labels: Exact Match, CAS Lookup, Starts-With, No Match"
    - "User sees a transient notification after search with tier breakdown counts"
    - "source_tier_* columns remain hidden in the DT table"
  artifacts:
    - path: "app.R"
      provides: "Match Type column derivation, DT rendering, search notification"
      contains: "match_type"
  key_links:
    - from: "app.R output$curation_table"
      to: "R/curation.R source_tier_* columns"
      via: "Derives match_type from source_tier columns in resolution_state"
      pattern: "source_tier.*match_type"
    - from: "app.R observeEvent(input$run_curation)"
      to: "R/curation.R search_summary"
      via: "Reads pipeline_result$search_summary for notification"
      pattern: "search_summary.*n_exact"
---

<objective>
Add a user-facing Match Type column to the Review Results table and a transient search summary notification after curation completes.

Purpose: Improves transparency — users can see which search method (exact, CAS, starts-with) resolved each chemical, and get an immediate tier breakdown summary.

Output: Modified app.R with Match Type column in curation_table and showNotification after curation.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-search-pipeline-refinement/06-CONTEXT.md
@.planning/phases/06-search-pipeline-refinement/06-01-SUMMARY.md
@app.R
@R/curation.R

<interfaces>
<!-- Key structures the executor needs from Plan 01 output and existing code. -->

From R/curation.R (after Plan 01):
```r
# resolution_state dataframe has per-column source_tier columns:
# source_tier_Name, source_tier_CASRN, source_tier_Other (if Other tagged)
# Values: "exact", "cas", "cas_no_match", "cas_invalid", "starts_with", "miss"

# search_summary from run_curation_pipeline():
# list(n_exact, n_starts_with, n_cas_valid, n_miss)
```

From app.R (existing):
```r
# output$curation_table (line 1370): renderDT using data_store$resolution_state
# Hidden columns (line 1413-1419): dtxsid_*, preferredName_*, searchName_*, rank_*, source_tier_*, .pinned
# observeEvent(input$run_curation) (line 1184): stores pipeline_result, shows notification
# data_store$resolution_state (line 1235): the resolved dataframe
# data_store$consensus_summary (line 1234): consensus counts

# Resolution column (line 1383-1410): sapply building HTML dropdowns for disagree rows
# DT rendering (line 1429-1445): datatable() with hidden_indices, extensions Buttons, escape=FALSE
```

From R/consensus.R:
```r
# consensus_source column: indicates which dtxsid_* column provided consensus_dtxsid
# Values: column names like "Name", "CASRN", "Other", or "consensus" for agree cases
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Match Type column and search notification to app.R</name>
  <files>app.R</files>
  <action>
  Make these modifications to app.R:

  **1. Add Match Type column derivation in output$curation_table (around line 1373)**

  After `df <- data_store$resolution_state` and before the Resolution column construction, add Match Type derivation:

  ```r
  # Derive Match Type from source_tier columns
  # Per user decision: show tier only, not which tagged column produced the match
  tier_label_map <- c(
    "exact" = "Exact Match",
    "cas" = "CAS Lookup",
    "starts_with" = "Starts-With",
    "miss" = "No Match",
    "cas_no_match" = "No Match",
    "cas_invalid" = "No Match"
  )

  df$match_type <- sapply(seq_len(nrow(df)), function(i) {
    # Strategy: find the source_tier of the column that provided consensus_dtxsid
    tier_cols <- grep("^source_tier_", names(df), value = TRUE)

    if (length(tier_cols) == 0) return("Unknown")

    # If consensus_dtxsid exists, find which column provided it
    if (!is.na(df$consensus_dtxsid[i])) {
      # Check each source_tier column for a successful match
      for (tc in tier_cols) {
        tier_val <- df[[tc]][i]
        if (!is.na(tier_val) && tier_val %in% c("exact", "cas", "starts_with")) {
          label <- tier_label_map[tier_val]
          if (!is.na(label)) return(label)
        }
      }
    }

    # No consensus_dtxsid: check if all tiers are miss/error
    all_tiers <- sapply(tier_cols, function(tc) df[[tc]][i])
    all_tiers <- all_tiers[!is.na(all_tiers)]

    if (length(all_tiers) == 0) return("No Match")

    # Pick first non-miss tier if any
    for (tv in all_tiers) {
      if (tv %in% c("exact", "cas", "starts_with")) {
        return(tier_label_map[tv])
      }
    }

    return("No Match")
  })
  ```

  Place the match_type column AFTER consensus_dtxsid and consensus_status columns for logical grouping. Use dplyr::relocate() if needed:
  ```r
  # Position match_type after consensus columns but before Resolution
  df <- dplyr::relocate(df, match_type, .after = consensus_status)
  ```

  **2. Ensure match_type is NOT in hidden_cols (line 1413-1419)**

  The existing hidden_cols list hides source_tier_* columns. match_type should remain VISIBLE. Verify the hidden_cols pattern `grep("^source_tier_", ...)` does NOT match "match_type". It won't (different prefix), but confirm by inspection.

  **3. Add search summary notification in observeEvent(input$run_curation) (around line 1256)**

  Replace the existing generic "Curation completed successfully!" notification (line 1256-1260) with a tier breakdown notification:

  ```r
  # Show tier breakdown notification
  notification_msg <- sprintf(
    "Search complete: %d exact, %d CAS, %d starts-with, %d no match",
    pipeline_result$search_summary$n_exact,
    pipeline_result$search_summary$n_cas_valid,
    pipeline_result$search_summary$n_starts_with,
    pipeline_result$search_summary$n_miss
  )

  showNotification(
    notification_msg,
    type = "message",
    duration = 8
  )
  ```

  This replaces the generic success message with an informative tier breakdown. Duration of 8 seconds gives users enough time to read but doesn't persist permanently.

  **4. Update the curation_report backward-compat block (lines 1243-1252)**

  The existing report already references search_summary fields. No changes needed here — the field names (n_exact, n_starts_with, n_cas_valid, n_miss) are unchanged from Plan 01.

  **ANTI-PATTERN TO AVOID:** Do NOT use `df$source_tier` as if it were a single column — it's per-column as `source_tier_Name`, `source_tier_CASRN`, `source_tier_Other`. The sapply approach above handles this correctly by iterating source_tier_* columns.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); source('R/file_handlers.R'); source('R/data_detection.R'); source('R/consensus.R'); source('R/curation.R'); cat('All sources OK\n')"</automated>
  </verify>
  <done>
  - match_type column derived from source_tier_* columns with friendly labels
  - match_type positioned after consensus_status, visible in DT table
  - source_tier_* columns remain hidden
  - Search summary notification shows tier breakdown (exact, CAS, starts-with, no match) with 8-second duration
  - Generic "Curation completed successfully!" replaced with informative tier counts
  - No DT column index drift (match_type is visible, source_tier_* are hidden)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify search pipeline and Match Type in running app</name>
  <what-built>
  Complete Phase 6 Search Pipeline Refinement:
  - Tier chain reordered to exact → CAS → starts-with (with 3-char minimum on starts-with)
  - Other tagged columns participate in full search pipeline and consensus
  - Match Type column shows friendly tier labels in Review Results table
  - Search summary notification displays tier breakdown after curation
  </what-built>
  <how-to-verify>
  1. Start the app: `shiny::runApp()`
  2. Upload a chemical inventory file with Name and CASRN columns
  3. Tag columns: at least one as Name, one as CASRN, and optionally one as Other
  4. Run curation and verify:
     a. Notification shows tier breakdown (e.g., "Search complete: 5 exact, 2 CAS, 1 starts-with, 0 no match")
     b. Review Results table has a "match_type" column with labels: "Exact Match", "CAS Lookup", "Starts-With", "No Match"
     c. source_tier_* columns are hidden
  5. If Other column was tagged, verify:
     a. Other column values were searched (check match_type for rows where Other value was the identifier)
     b. Consensus includes Other column results (dtxsid_Other participates in consensus_status)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. app.R sources without errors
2. Match Type column visible in DT output with correct labels
3. Search notification shows tier counts after curation
4. source_tier_* columns remain hidden
5. Visual verification of full pipeline with sample data
</verification>

<success_criteria>
- Match Type column present in Review Results with friendly labels (Exact Match, CAS Lookup, Starts-With, No Match)
- Search summary notification appears for 8 seconds after curation with tier breakdown
- source_tier_* columns hidden from user view
- No DT rendering errors or column index drift
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-pipeline-refinement/06-02-SUMMARY.md`
</output>
