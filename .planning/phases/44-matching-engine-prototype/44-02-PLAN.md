---
phase: 44-matching-engine-prototype
plan: "02"
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - scripts/prototype_wqx_matching.R
autonomous: false
requirements:
  - INTG-01

must_haves:
  truths:
    - "Prototype script runs against detections_uat_sample_50.csv and prints match results without starting the Shiny app"
    - "Output shows tier breakdown: N exact, N alias, N fuzzy, N unresolved"
    - "Fuzzy matches are printed with distances for manual review"
    - "Unresolved names are printed with nearest candidate and distance"
  artifacts:
    - path: "scripts/prototype_wqx_matching.R"
      provides: "Standalone WQX matcher validation script"
      min_lines: 50
  key_links:
    - from: "scripts/prototype_wqx_matching.R"
      to: "R/wqx_matching.R"
      via: "source() then match_wqx() call"
      pattern: "match_wqx\\("
    - from: "scripts/prototype_wqx_matching.R"
      to: "R/cleaning_reference.R"
      via: "source() then load_wqx_dictionary() call"
      pattern: "load_wqx_dictionary\\("
    - from: "scripts/prototype_wqx_matching.R"
      to: "detections_uat_sample_50.csv"
      via: "readr::read_csv for training data"
      pattern: "detections_uat_sample_50\\.csv"
---

<objective>
Create and run the standalone prototype script that validates match_wqx() against the 50-row training dataset, printing a tier breakdown and fuzzy match details for manual review.

Purpose: This is the final gate before Phase 45 pipeline integration -- proving the matching engine works against real analyte names from the detections dataset. The human-verify checkpoint confirms match quality is acceptable.

Output: `scripts/prototype_wqx_matching.R` producing console output with accuracy report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-matching-engine-prototype/44-CONTEXT.md
@.planning/phases/44-matching-engine-prototype/44-PATTERNS.md
@.planning/phases/44-matching-engine-prototype/44-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -- match_wqx() signature and return schema -->

From R/wqx_matching.R (created in Plan 01):
```r
#' @param names Character vector of analyte names to match
#' @param dictionary Tibble from load_wqx_dictionary()
#' @param threshold Numeric similarity threshold (default 0.85)
#' @param verbose Logical (default FALSE)
#' @return Tibble: input_name, wqx_name, match_tier, match_distance, alias_type
#' @export
match_wqx <- function(names, dictionary, threshold = 0.85, verbose = FALSE)
```

From R/cleaning_reference.R:
```r
#' @param cache_dir Directory containing reference cache RDS files
#' @return Tibble with columns: name, canonical_name, type, cas_number, group_name, description
#' @export
load_wqx_dictionary <- function(cache_dir)
```

From scripts/build_wqx_dictionary.R (analog pattern):
```r
CHEMREG_ROOT <- here::here()
# stopifnot() guards for required files
# source() R files
# Section separators: # ====...====
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run prototype script</name>
  <files>scripts/prototype_wqx_matching.R</files>

  <read_first>
    - scripts/build_wqx_dictionary.R (analog: file header, CHEMREG_ROOT, stopifnot guard, section separator pattern)
    - scripts/benchmark_pipeline.R lines 1-60 (analog: data loading, section structure, message output)
    - R/wqx_matching.R (just-created: confirm match_wqx() signature and return columns)
    - R/cleaning_reference.R lines 590-603 (load_wqx_dictionary signature)
    - detections_uat_sample_50.csv (confirm analyte column exists, check first few rows)
  </read_first>

  <action>
Create `scripts/prototype_wqx_matching.R` following the structure from 44-PATTERNS.md (scripts section). The script must:

**File header** (per build_wqx_dictionary.R pattern):
```r
# prototype_wqx_matching.R
# Standalone WQX matcher prototype -- validates match_wqx() against training data.
# No Shiny dependency -- runs in any R session.
#
# Prerequisites:
#   1. inst/extdata/reference_cache/wqx_dictionary.rds (built by Phase 43)
#   2. detections_uat_sample_50.csv in repo root
#
# Usage: Rscript scripts/prototype_wqx_matching.R
#
# Output: tier breakdown + fuzzy matches printed to console
```

**Section 1 -- SETUP** (per D-09):
```r
CHEMREG_ROOT <- here::here()
source(file.path(CHEMREG_ROOT, "R", "cleaning_reference.R"))
source(file.path(CHEMREG_ROOT, "R", "wqx_matching.R"))
```

**Section 2 -- PREREQUISITE GUARDS** (per build_wqx_dictionary.R stopifnot pattern):
```r
cache_path <- file.path(CHEMREG_ROOT, "inst", "extdata", "reference_cache", "wqx_dictionary.rds")
stopifnot(
  "wqx_dictionary.rds not found -- run scripts/build_wqx_dictionary.R first" = file.exists(cache_path)
)
train_path <- file.path(CHEMREG_ROOT, "detections_uat_sample_50.csv")
stopifnot(
  "detections_uat_sample_50.csv not found in repo root" = file.exists(train_path)
)
```

**Section 3 -- LOAD DICTIONARY**:
```r
cache_dir <- file.path(CHEMREG_ROOT, "inst", "extdata", "reference_cache")
dict <- load_wqx_dictionary(cache_dir)
message(sprintf("Dictionary loaded: %d rows (%d canonical, %d alias)",
  nrow(dict),
  sum(dict$type == "canonical"),
  sum(dict$type != "canonical")
))
```

**Section 4 -- LOAD TRAINING DATA** (per D-10):
```r
train <- readr::read_csv(train_path, show_col_types = FALSE)
message(sprintf("Training data loaded: %s (%d rows, analyte column: %d unique names)",
  basename(train_path),
  nrow(train),
  length(unique(train$analyte))
))
```

**Section 5 -- RUN MATCHER** (verbose=TRUE per D-10):
```r
results <- match_wqx(train$analyte, dict, threshold = 0.85, verbose = TRUE)
```

**Section 6 -- ACCURACY REPORT** (per D-10, prints tier breakdown + fuzzy for review):
```r
message("\n=== TIER BREAKDOWN ===")
tier_counts <- table(results$match_tier)
print(tier_counts)

message("\n=== FUZZY MATCHES FOR REVIEW ===")
fuzzy_hits <- results[results$match_tier == "fuzzy", ]
if (nrow(fuzzy_hits) > 0) {
  print(fuzzy_hits[, c("input_name", "wqx_name", "match_distance")])
} else {
  message("  (no fuzzy matches)")
}

message("\n=== UNRESOLVED NAMES ===")
unresolved <- results[results$match_tier == "none", ]
if (nrow(unresolved) > 0) {
  print(unresolved[, c("input_name", "match_distance")])
} else {
  message("  (all names resolved)")
}

message(sprintf("\n=== SUMMARY: %d/%d names resolved (%.0f%%) ===",
  sum(results$match_tier != "none"),
  nrow(results),
  100 * sum(results$match_tier != "none") / nrow(results)
))
```

After creating the script:
1. Run `air format scripts/prototype_wqx_matching.R`
2. Execute the prototype: `Rscript scripts/prototype_wqx_matching.R`
3. Capture the full console output for the checkpoint task
  </action>

  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" scripts/prototype_wqx_matching.R 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - scripts/prototype_wqx_matching.R exists with >= 50 lines
    - File contains `CHEMREG_ROOT <- here::here()`
    - File contains `source(file.path(CHEMREG_ROOT, "R", "wqx_matching.R"))`
    - File contains `source(file.path(CHEMREG_ROOT, "R", "cleaning_reference.R"))`
    - File contains `match_wqx(train$analyte, dict, threshold = 0.85, verbose = TRUE)`
    - File contains `detections_uat_sample_50.csv`
    - File contains `TIER BREAKDOWN`
    - File contains `FUZZY MATCHES FOR REVIEW`
    - File contains `UNRESOLVED NAMES`
    - Running the script completes without error (exit code 0)
    - Script output includes tier counts (exact, alias, fuzzy, none)
  </acceptance_criteria>

  <done>Prototype script runs against detections_uat_sample_50.csv, prints tier breakdown showing exact/alias/fuzzy/unresolved counts, lists fuzzy matches with distances, and lists unresolved names. Script exits cleanly without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify prototype match quality</name>

  <what-built>
The standalone WQX matching prototype ran match_wqx() against the 50-row detections training dataset. The output shows:
1. Per-name verbose cli output (which name matched where, at what tier)
2. Tier breakdown (counts of exact, alias, fuzzy, unresolved)
3. Fuzzy matches with JW distances (for manual review of quality)
4. Unresolved names with nearest candidate distances
  </what-built>

  <how-to-verify>
1. Review the console output from `Rscript scripts/prototype_wqx_matching.R` (captured in Task 1)
2. Check tier breakdown -- do the counts seem reasonable for a 50-row environmental chemistry dataset?
3. Review fuzzy matches -- are the fuzzy matches sensible? (e.g., a misspelled name correctly matched to the right canonical name, with a low distance)
4. Review unresolved names -- are these genuinely unresolvable (not in WQX), or did the matcher miss something obvious?
5. Optionally re-run: `Rscript scripts/prototype_wqx_matching.R`
  </how-to-verify>

  <resume-signal>Type "approved" if match quality is acceptable, or describe any issues (e.g., "X name should have matched Y", "threshold too strict/loose")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file -> script | Training CSV is a local file under user control; not untrusted external input |
| Script -> match_wqx() | Script passes analyte column values directly; match_wqx() normalizes internally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Tampering | Training CSV path | mitigate | stopifnot() guard verifies file exists before read; hardcoded filename in script |
| T-44-06 | Information Disclosure | Console output | accept | Script prints only chemical names from public EPA data and a public training dataset. No secrets or PII. |
</threat_model>

<verification>
1. `Rscript scripts/prototype_wqx_matching.R` completes without error
2. Output contains "TIER BREAKDOWN" section with counts
3. Output contains "FUZZY MATCHES FOR REVIEW" section
4. Output contains "UNRESOLVED NAMES" section
5. Human approves match quality at checkpoint
</verification>

<success_criteria>
- Prototype script validates match_wqx() against real training data (INTG-01)
- Tier breakdown printed: N exact, N alias, N fuzzy, N unresolved
- Fuzzy matches shown with distances for manual quality review
- Human confirms match quality is acceptable for Phase 45 integration
</success_criteria>

<output>
After completion, create `.planning/phases/44-matching-engine-prototype/44-02-SUMMARY.md`
</output>
