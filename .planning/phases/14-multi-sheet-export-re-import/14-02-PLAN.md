---
phase: 14-multi-sheet-export-re-import
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - R/modules/mod_review_results.R
  - app.R
autonomous: false
requirements:
  - EXPO-01
  - EXPO-02
  - EXPO-03

must_haves:
  truths:
    - "User can download a 7-sheet Excel export from Review Results tab"
    - "Export is blocked with error notification if curated data exceeds Excel limits"
    - "User can upload a ChemReg export via sidebar config import control"
    - "User sees confirmation modal with checkboxes when valid ChemReg export detected"
    - "User can opt out of restoring reference lists or column tags independently"
    - "Non-ChemReg Excel files show warning notification and do not trigger modal"
    - "Imported reference lists appear in Clean Data tab after import"
  artifacts:
    - path: "R/modules/mod_review_results.R"
      provides: "7-sheet downloadHandler using build_export_sheets()"
      contains: "build_export_sheets"
    - path: "app.R"
      provides: "Config import fileInput in sidebar, modal confirmation, merge logic"
      contains: "config_import"
  key_links:
    - from: "R/modules/mod_review_results.R"
      to: "R/export_helpers.R"
      via: "build_export_sheets() call in downloadHandler"
      pattern: "build_export_sheets"
    - from: "app.R"
      to: "R/config_import.R"
      via: "parse_chemreg_export() and merge_reference_lists() in observeEvent"
      pattern: "parse_chemreg_export|merge_reference_lists"
    - from: "app.R"
      to: "data_store$reference_lists"
      via: "merge_reference_lists updates reactive value, triggering Clean Data tab refresh"
      pattern: "data_store\\$reference_lists"
---

<objective>
Wire the export and import functions into the Shiny UI — replace the existing 3-sheet export with 7-sheet export and add sidebar config import with modal confirmation.

Purpose: Make the backend functions from Plan 01 accessible to users through the existing Review Results download button and a new sidebar config import control.
Output: Updated mod_review_results.R and app.R
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-multi-sheet-export-re-import/14-CONTEXT.md
@.planning/phases/14-multi-sheet-export-re-import/14-01-SUMMARY.md

@R/modules/mod_review_results.R (lines 773-837 — existing downloadHandler to replace)
@app.R (sidebar definition at lines 75-79, server at lines 116-228)
@R/export_helpers.R (from Plan 01 — build_export_sheets, validate_excel_size)
@R/config_import.R (from Plan 01 — parse_chemreg_export, merge_reference_lists)

<interfaces>
<!-- From Plan 01 outputs -->

From R/export_helpers.R:
  build_export_sheets(raw, resolution_state, consensus_summary, cleaning_audit,
                      reference_lists, column_tags, detection, file_info)
    -> named list of 7 data frames ready for writexl::write_xlsx()

  validate_excel_size(df, sheet_name)
    -> invisible(TRUE) or throws error

From R/config_import.R:
  parse_chemreg_export(file_path)
    -> list(reference_lists=df, column_tags=df, config=df) or NULL

  merge_reference_lists(existing_lists, imported_ref_df)
    -> updated list of 3 tibbles (functional_categories, stop_words, block_patterns)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 3-sheet export with 7-sheet export in mod_review_results.R</name>
  <files>R/modules/mod_review_results.R</files>
  <action>
    Replace the downloadHandler content function (lines 783-836) in mod_review_results.R. The filename function stays the same.

    New content function:
    1. req(data_store$resolution_state, data_store$consensus_summary)
    2. Call validate_excel_size(data_store$resolution_state, "Curated Data") wrapped in tryCatch. On error, call showNotification with type="error", duration=NULL, and return() early.
    3. Call build_export_sheets() passing individual data_store components:
       - raw = data_store$raw
       - resolution_state = data_store$resolution_state
       - consensus_summary = data_store$consensus_summary
       - cleaning_audit = data_store$cleaning_audit
       - reference_lists = data_store$reference_lists
       - column_tags = data_store$column_tags
       - detection = data_store$detection
       - file_info = data_store$file_info
    4. Call writexl::write_xlsx(sheets, path = file)

    This replaces ~50 lines of inline sheet building with a clean function call. The filename function (lines 774-781) stays unchanged.

    Keep everything else in mod_review_results.R unchanged.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" &amp; sleep 8 &amp; kill %1 2>/dev/null; echo "Smoke test done"</automated>
  </verify>
  <done>
    - downloadHandler in mod_review_results.R calls build_export_sheets() and writes 7-sheet workbook
    - validate_excel_size blocks export with notification if data too large
    - App starts without error (smoke test)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add config import control to app.R sidebar with modal confirmation</name>
  <files>app.R</files>
  <action>
    Two changes to app.R:

    **UI change (sidebar, after line 78):**
    Add a config import section after mod_file_upload_ui("upload"):
    ```r
    sidebar = sidebar(
      id = "main_sidebar",
      width = 300,
      mod_file_upload_ui("upload"),
      hr(),
      h6("Import Configuration", class = "text-muted"),
      fileInput(
        "config_import",
        label = NULL,
        accept = ".xlsx",
        buttonLabel = "Browse...",
        placeholder = "ChemReg export (.xlsx)"
      ),
      helpText("Optional: restore reference lists from a previous ChemReg export", class = "small")
    )
    ```

    **Server change (after line 130, before gated navigation):**
    Add config import handler with these elements:

    1. `imported_config <- reactiveVal(NULL)` — stores parsed config data between modal show and confirm

    2. `observeEvent(input$config_import, { ... })` — on file upload:
       - req(input$config_import)
       - Call parse_chemreg_export(input$config_import$datapath)
       - If NULL: showNotification("Not a valid ChemReg export file", type="warning"), return()
       - Store result in imported_config()
       - Show modalDialog with:
         - Title: "ChemReg Export Detected"
         - Description paragraph about what was found
         - checkboxInput("restore_ref_lists", "Restore reference lists", value=TRUE)
         - checkboxInput("restore_col_tags", "Restore column tags", value=TRUE)
         - Muted note: "Imported reference lists will merge with existing lists."
         - Footer: actionButton("confirm_config_import", "Import", class="btn-primary") + modalButton("Cancel")

    3. `observeEvent(input$confirm_config_import, { ... })` — on confirm:
       - req(imported_config())
       - Capture checkbox states BEFORE removeModal(): restore_refs <- input$restore_ref_lists, restore_tags <- input$restore_col_tags
       - removeModal()
       - If restore_refs: call merge_reference_lists(data_store$reference_lists, imported_config()$reference_lists), assign result to data_store$reference_lists. Show notification "Reference lists imported and merged".
       - If restore_tags: convert imported_config()$column_tags tibble (Column, Type) to named list via setNames(Type, Column), assign to data_store$column_tags. Show notification "Column tags imported".
       - Clear imported_config(NULL)
       - Wrap each section in tryCatch with error notification

    Place these observers in the server function after `data_store$reference_lists <- reference_lists` (line 130) and before the gated navigation block (line 133).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" &amp; sleep 8 &amp; kill %1 2>/dev/null; echo "Smoke test done"</automated>
  </verify>
  <done>
    - Config import fileInput visible in sidebar below main upload
    - Uploading a non-ChemReg Excel shows warning notification
    - Uploading a ChemReg export shows confirmation modal with checkboxes
    - Confirming import merges reference lists into data_store
    - App starts without error (smoke test)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify export and import workflow end-to-end</name>
  <what-built>
    Complete multi-sheet export and config re-import workflow:
    1. 7-sheet Excel export from Review Results (replacing old 3-sheet export)
    2. Sidebar config import with modal confirmation and reference list merge
  </what-built>
  <how-to-verify>
    1. Upload a chemical data file and run through full pipeline (tag, clean, curate)
    2. In Review Results tab, click the download/export button
    3. Open the exported .xlsx file in Excel — verify 7 sheets exist:
       - Raw Data (original upload)
       - Curated Data (with needs_review column)
       - Summary (curation statistics)
       - Cleaning Audit (per-row audit trail)
       - Reference Lists (with type column)
       - Column Tags (column-to-tag mapping)
       - Pipeline Config (with chemreg_export=true row)
    4. Start a new session (refresh browser), upload a DIFFERENT data file
    5. In the sidebar, use the "Import Configuration" file input to upload the previously exported .xlsx
    6. Verify confirmation modal appears with checkboxes for reference lists and column tags
    7. Click Import — verify notification confirms import
    8. Navigate to Clean Data tab — verify imported reference lists are available
    9. Try uploading a random non-ChemReg .xlsx — verify warning notification appears (no modal)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- App starts without error (smoke test passes)
- Export produces 7-sheet Excel workbook
- Pipeline Config sheet contains chemreg_export=true marker
- Config import detects ChemReg exports and shows modal
- Reference list merge works correctly
- Non-ChemReg files rejected gracefully
- All existing tests still pass
</verification>

<success_criteria>
- Download button in Review Results produces 7-sheet workbook (was 3 sheets)
- Config import control visible in sidebar
- Round-trip works: export from session A, import config into session B
- Non-ChemReg files produce warning, not error
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-multi-sheet-export-re-import/14-02-SUMMARY.md`
</output>
