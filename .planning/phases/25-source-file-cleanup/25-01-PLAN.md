---
phase: 25-source-file-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/cleaning_pipeline.R
  - R/cleaning_reference.R
  - R/consensus.R
  - DESCRIPTION
  - .Rbuildignore
  - NAMESPACE
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04]

must_haves:
  truths:
    - "R/cleaning_pipeline.R has no library() calls and uses pkg::fn() for all external calls"
    - "R/cleaning_reference.R has no library() calls and uses pkg::fn() for all external calls"
    - "R/consensus.R has no library() calls (vestigial library calls removed)"
    - "devtools::check() completes with zero errors"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "Package-compatible cleaning pipeline with :: notation"
      contains: "@importFrom magrittr %>%"
    - path: "R/cleaning_reference.R"
      provides: "Package-compatible reference loaders with :: notation"
    - path: "R/consensus.R"
      provides: "Package-compatible consensus logic (base R only, no external deps)"
    - path: "DESCRIPTION"
      provides: "Updated Imports with magrittr"
      contains: "magrittr"
    - path: "NAMESPACE"
      provides: "Updated NAMESPACE with importFrom(magrittr,%>%)"
      contains: "importFrom(magrittr"
  key_links:
    - from: "R/cleaning_pipeline.R"
      to: "DESCRIPTION"
      via: "magrittr listed in Imports enables %>% usage"
      pattern: "magrittr"
    - from: "NAMESPACE"
      to: "R/cleaning_pipeline.R"
      via: "importFrom(magrittr,%) generated from @importFrom roxygen tag"
      pattern: "importFrom\\(magrittr"
---

<objective>
Remove all bare library() calls from the three target R source files and ensure full package compatibility with :: notation, @importFrom for the pipe operator, and zero-error devtools::check().

Purpose: Make R/cleaning_pipeline.R, R/cleaning_reference.R, and R/consensus.R package-compatible so devtools::check() passes cleanly — prerequisite for Phase 26 (app relocation).
Output: Three clean source files, updated DESCRIPTION/NAMESPACE, zero-error check result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-package-scaffolding/24-01-SUMMARY.md
@.planning/phases/25-source-file-cleanup/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove library() calls, add @importFrom magrittr pipe, update DESCRIPTION</name>
  <files>R/cleaning_pipeline.R, R/cleaning_reference.R, R/consensus.R, DESCRIPTION, .Rbuildignore</files>
  <read_first>R/cleaning_pipeline.R, R/cleaning_reference.R, R/consensus.R, DESCRIPTION, .Rbuildignore</read_first>
  <action>
**R/cleaning_pipeline.R** (per D-01, SRC-01):
1. Delete lines 10-13 (`library(dplyr)`, `library(stringr)`, `library(stringi)`, `library(tibble)`)
2. Add roxygen `@importFrom magrittr %>%` tag to the first exported function's roxygen block (`clean_text_field`). Place it after the existing `@export` tag (per D-02). This makes the pipe operator available to all functions in the file without needing `magrittr::` prefix on every pipe.
3. The file already uses `dplyr::`, `stringr::`, `stringi::`, `tibble::`, `ComptoxR::`, `stats::` notation extensively. Verify no bare external calls remain by scanning for unqualified function names. The `where()` function used in `dplyr::across(where(is.character), ...)` needs to be `tidyselect::where` — check if it's already qualified and fix if not: change `where(is.character)` to `tidyselect::where(is.character)`.
4. The `stats::setNames` call on line ~1925 is already namespaced. Verify `stats` does not need to be added to DESCRIPTION Imports — it is a recommended package that R always loads, but `devtools::check()` flags it. Add `stats` to DESCRIPTION Imports to be safe.

**R/cleaning_reference.R** (per D-01, SRC-02):
1. Delete lines 8-10 (`library(fs)`, `library(tibble)`, `library(dplyr)`)
2. All function calls in this file already use `::` notation: `fs::dir_create`, `tibble::tibble`, `dplyr::bind_rows`, `purrr::map_dfr`, `stringr::str_*`, `ComptoxR::*`. No additional namespacing needed.

**R/consensus.R** (per D-01, SRC-03):
1. Delete lines 6-7 (`library(dplyr)`, `library(tibble)`)
2. This file uses ONLY base R functions (`vapply`, `grep`, `names`, `sub`, `sapply`, `which`, `nrow`, `seq_len`, `paste0`, `isTRUE`, `stop`, `switch`, `unique`, `length`, `is.na`, `as.character`, `as.integer`, `order`, `as.numeric`, `Inf`). No dplyr or tibble functions are called in function bodies. The library calls are vestigial and can simply be removed with no other changes needed.

**DESCRIPTION**:
1. Add `magrittr` to the Imports list (alphabetically between `janitor` and `purrr`)
2. Add `stats` to the Imports list (alphabetically between `stringi` and `stringr`)

**.Rbuildignore** (per Phase 24 summary deferred item):
1. Add `^\.claude$` to exclude `.claude/` worktrees directory from package tarball (causes long tar path warnings)

After all edits, run `devtools::document()` to regenerate NAMESPACE with the new `importFrom(magrittr,"%>%")` directive.
  </action>
  <verify>
    <automated>Write and run an R script that: (1) greps for "^library(" in the three files and asserts zero matches, (2) checks DESCRIPTION contains "magrittr" in Imports, (3) checks NAMESPACE contains "importFrom(magrittr", (4) checks .Rbuildignore contains ".claude"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "^library(" R/cleaning_pipeline.R` returns 0
- `grep -c "^library(" R/cleaning_reference.R` returns 0
- `grep -c "^library(" R/consensus.R` returns 0
- `R/cleaning_pipeline.R` contains the string `@importFrom magrittr %>%`
- `DESCRIPTION` contains `magrittr` in the Imports block
- `DESCRIPTION` contains `stats` in the Imports block
- `NAMESPACE` contains `importFrom(magrittr,"%>%")`
- `.Rbuildignore` contains `^\.claude$`
- `R/cleaning_pipeline.R` contains `tidyselect::where` (not bare `where`)
  </acceptance_criteria>
  <done>All three R source files have zero library() calls, pipe operator is properly imported via roxygen, DESCRIPTION and NAMESPACE updated, .Rbuildignore updated</done>
</task>

<task type="auto">
  <name>Task 2: Run devtools::check() and fix any errors</name>
  <files>R/cleaning_pipeline.R, R/cleaning_reference.R, R/consensus.R, DESCRIPTION, NAMESPACE</files>
  <read_first>DESCRIPTION, NAMESPACE</read_first>
  <action>
Run `devtools::check()` via Rscript (write to temp file per CLAUDE.md R execution rules). The check must complete with zero errors per D-05/SRC-04. NOTEs about non-standard files are acceptable per D-06/D-07.

Expected check output: 0 errors, possibly warnings about documentation or non-standard files.

If errors appear:
- "no visible global function definition for X" → add `pkg::` prefix to the bare call X in the offending file
- "no visible binding for global variable '.data'" → add `@importFrom rlang .data` to the file's first roxygen block (per D-03), then re-run `devtools::document()`
- "Namespace dependency not declared" for package X → add X to DESCRIPTION Imports
- "'library' or 'require' calls in package code" → remove any remaining library() calls missed in Task 1

After fixing any errors, re-run `devtools::check()` to confirm zero errors.
  </action>
  <verify>
    <automated>Write and run an R script: `results <- devtools::check(error_on = "error"); cat("Errors:", length(results$errors), "\n")` — must print "Errors: 0"</automated>
  </verify>
  <acceptance_criteria>
- `devtools::check()` output contains "0 errors" (exact string match in check results)
- No "library() calls in package code" warnings remain
- No "no visible global function definition" errors for any function in the three target files
  </acceptance_criteria>
  <done>devtools::check() completes with zero errors; non-standard file warnings acceptable per D-06</done>
</task>

</tasks>

<verification>
1. `grep -c "^library(" R/cleaning_pipeline.R R/cleaning_reference.R R/consensus.R` — all return 0
2. `devtools::check()` — 0 errors
3. `library(chemreg)` after `devtools::install()` — loads without error
</verification>

<success_criteria>
- SRC-01: R/cleaning_pipeline.R has no library() calls, all external functions use :: notation
- SRC-02: R/cleaning_reference.R has no library() calls, all external functions use :: notation
- SRC-03: R/consensus.R has no library() calls (only uses base R functions)
- SRC-04: devtools::check() completes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-source-file-cleanup/25-01-SUMMARY.md`
</output>
