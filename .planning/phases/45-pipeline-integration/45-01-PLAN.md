---
phase: 45-pipeline-integration
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - R/consensus.R
  - tests/testthat/test-wqx-pipeline-integration.R
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "compute_qc_tier('wqx', 0L, k) returns as.integer(k) -- same tier as 'single'"
    - "classify_consensus assigns consensus_status = 'wqx' for rows where all DTXSIDs are NA but source_tier is wqx_*"
    - "classify_consensus assigns consensus_status = 'error' for rows where all DTXSIDs are NA and source_tier is NOT wqx_*"
  artifacts:
    - path: "R/consensus.R"
      provides: "WQX guard in classify_consensus + wqx case in compute_qc_tier"
      contains: "\"wqx\" = as.integer(n_total)"
    - path: "tests/testthat/test-wqx-pipeline-integration.R"
      provides: "Unit tests for WQX consensus classification"
      min_lines: 60
  key_links:
    - from: "R/consensus.R:classify_consensus"
      to: "R/consensus.R:compute_qc_tier"
      via: "compute_qc_tier('wqx', 0L, k) call inside WQX guard"
      pattern: "compute_qc_tier\\(\"wqx\""
---

<objective>
Add WQX consensus handling to classify_consensus() and compute_qc_tier() using TDD.

Purpose: WQX-resolved rows have NA DTXSIDs (WQX provides canonical names, not DTXSIDs). Without a guard, classify_consensus() assigns "error" status to these rows because all DTXSID columns are NA. The guard must detect WQX source_tier values and assign "wqx" status instead, satisfying INTG-02 (WQX results treated as resolutions, not errors).

Output: Modified R/consensus.R with WQX handling, test file with consensus-specific tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-pipeline-integration/45-CONTEXT.md
@.planning/phases/45-pipeline-integration/45-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/consensus.R -- compute_qc_tier (lines 30-40):
```r
compute_qc_tier <- function(status, n_matched, n_total) {
  tier <- switch(status,
    "agree" = 1L,
    "agree_caveat" = 1L + as.integer(n_total - n_matched),
    "single" = as.integer(n_total),
    "disagree" = as.integer(n_total + 1L),
    "error" = as.integer(n_total + 2L),
    NA_integer_
  )
  tier
}
```

From R/consensus.R -- classify_consensus loop body (lines 67-112):
```r
for (i in seq_len(nrow(df))) {
    values <- vapply(dtxsid_cols, function(col) {
      val <- df[[col]][i]
      if (is.na(val)) NA_character_ else as.character(val)
    }, character(1))

    non_na <- values[!is.na(values)]
    n_present <- length(non_na)
    unique_vals <- unique(non_na)
    n_unique <- length(unique_vals)

    if (n_present == 0) {
      # All NA -> error  <-- THIS IS WHERE WQX GUARD MUST GO (BEFORE this branch)
      consensus_status[i] <- "error"
      ...
    } else if (n_present == 1) { ... }
    ...
}
```

From R/wqx_matching.R -- match_wqx return schema:
```r
tibble::tibble(
  input_name = character(0),
  wqx_name = character(0),
  match_tier = character(0),   # "exact" | "alias" | "fuzzy" | "none"
  match_distance = numeric(0),
  alias_type = character(0)
)
```

Source tier values after WQX wiring (per D-05):
- Existing: "exact", "cas", "cas_no_match", "cas_invalid", "starts_with", "miss", "isotope_match"
- New WQX: "wqx_exact", "wqx_alias", "wqx_fuzzy"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED -- Write failing tests for WQX consensus classification</name>
  <files>tests/testthat/test-wqx-pipeline-integration.R</files>
  <read_first>
    - R/consensus.R (current classify_consensus and compute_qc_tier implementations)
    - tests/testthat/test-consensus.R (existing test patterns for consensus classification)
    - .planning/phases/45-pipeline-integration/45-PATTERNS.md (exact test code patterns in "tests/testthat/test-wqx-pipeline-integration.R" section)
  </read_first>
  <behavior>
    - Test 1: compute_qc_tier("wqx", 0L, 1L) returns 1L (same as "single" when k=1)
    - Test 2: compute_qc_tier("wqx", 0L, 2L) returns 2L (same as "single" when k=2)
    - Test 3: classify_consensus assigns consensus_status = "wqx" for a row with dtxsid_Chemical = NA, source_tier_Chemical = "wqx_exact", preferredName_Chemical = "Arsenic"
    - Test 4: classify_consensus still assigns "error" for a row with dtxsid_Chemical = NA, source_tier_Chemical = "miss" (no wqx_ prefix)
    - Test 5: classify_consensus assigns "wqx" for wqx_alias source tier
    - Test 6: classify_consensus assigns "wqx" for wqx_fuzzy source tier
    - Test 7: Mixed row -- one dtxsid col has NA + wqx_exact tier, another dtxsid col has a real DTXSID -- should NOT be "wqx" (n_present > 0)
  </behavior>
  <action>
Create `tests/testthat/test-wqx-pipeline-integration.R` with the following content:

1. File header comment: `# Test file for WQX pipeline integration (Phase 45)` and `# Tests INTG-02, INTG-03, INTG-04`

2. Group 1: "compute_qc_tier handles wqx status"
   - test_that("compute_qc_tier returns n_total for wqx status"): Call compute_qc_tier("wqx", 0L, 1L) and expect 1L. Call compute_qc_tier("wqx", 0L, 2L) and expect 2L. Call compute_qc_tier("wqx", 0L, 3L) and expect 3L. These confirm WQX gets same tier as "single" (resolved but no multi-source agreement).

3. Group 2: "classify_consensus assigns wqx for WQX-resolved rows"
   - test_that("classify_consensus assigns wqx status for wqx_exact source tier"):
     Build a single-row data.frame:
     ```r
     df <- data.frame(
       Chemical = "Arsenic",
       dtxsid_Chemical = NA_character_,
       preferredName_Chemical = "Arsenic",
       source_tier_Chemical = "wqx_exact",
       stringsAsFactors = FALSE
     )
     dtxsid_cols <- "dtxsid_Chemical"
     result <- classify_consensus(df, dtxsid_cols)
     expect_equal(result$consensus_status[1], "wqx")
     expect_true(is.na(result$consensus_dtxsid[1]))
     ```

   - test_that("classify_consensus assigns wqx status for wqx_alias source tier"):
     Same as above but source_tier_Chemical = "wqx_alias"
     expect_equal(result$consensus_status[1], "wqx")

   - test_that("classify_consensus assigns wqx status for wqx_fuzzy source tier"):
     Same as above but source_tier_Chemical = "wqx_fuzzy"
     expect_equal(result$consensus_status[1], "wqx")

   - test_that("classify_consensus still assigns error for non-wqx NA-DTXSID rows"):
     Build a single-row data.frame with dtxsid_Chemical = NA, source_tier_Chemical = "miss"
     expect_equal(result$consensus_status[1], "error")

   - test_that("classify_consensus assigns single (not wqx) when DTXSID is present alongside wqx"):
     Build a two-column row: dtxsid_Chemical = NA + source_tier_Chemical = "wqx_exact", dtxsid_Other = "DTXSID123" + source_tier_Other = "exact"
     dtxsid_cols <- c("dtxsid_Chemical", "dtxsid_Other")
     result <- classify_consensus(df, dtxsid_cols)
     expect_equal(result$consensus_status[1], "single") -- because n_present == 1 (one real DTXSID)

Run the tests. They MUST fail because compute_qc_tier does not yet have a "wqx" case and classify_consensus has no WQX guard.

Commit message: `test(45-01): add failing tests for WQX consensus classification`
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-wqx-pipeline-integration.R')" 2>&1 | grep -E "FAIL|PASS|Error"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-wqx-pipeline-integration.R exists and contains at least 6 test_that blocks
    - File contains the string compute_qc_tier("wqx"
    - File contains the string consensus_status[1], "wqx"
    - File contains the string consensus_status[1], "error"
    - File contains the string source_tier_Chemical = "wqx_exact"
    - Running the test file produces failures (tests reference unimplemented behavior)
  </acceptance_criteria>
  <done>Test file exists with 6-7 test_that blocks covering compute_qc_tier("wqx") and classify_consensus WQX guard. All tests fail because the production code has not been updated yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN -- Implement WQX consensus handling in R/consensus.R</name>
  <files>R/consensus.R</files>
  <read_first>
    - R/consensus.R (current implementation -- must see exact line numbers and structure)
    - tests/testthat/test-wqx-pipeline-integration.R (the failing tests from Task 1)
    - .planning/phases/45-pipeline-integration/45-PATTERNS.md (exact code patterns for consensus.R modifications)
  </read_first>
  <action>
Two changes to R/consensus.R:

**Change 1: Add "wqx" case to compute_qc_tier() switch statement (around line 35)**

In the switch() call inside compute_qc_tier(), add a "wqx" case AFTER the "single" case and BEFORE the "disagree" case:

```r
"wqx" = as.integer(n_total),
```

The full switch becomes:
```r
tier <- switch(status,
  "agree" = 1L,
  "agree_caveat" = 1L + as.integer(n_total - n_matched),
  "single" = as.integer(n_total),
  "wqx" = as.integer(n_total),
  "disagree" = as.integer(n_total + 1L),
  "error" = as.integer(n_total + 2L),
  NA_integer_
)
```

**Change 2: Add WQX guard inside classify_consensus() loop, BEFORE the `if (n_present == 0)` branch (around line 79)**

After the `n_unique <- length(unique_vals)` line (approximately line 78) and BEFORE the `if (n_present == 0) {` line (line 79), insert the WQX detection and early-assign block:

```r
    # WQX guard: rows with NA DTXSIDs but WQX source_tier get "wqx" status, not "error"
    tier_cols <- sub("^dtxsid_", "source_tier_", dtxsid_cols)
    row_tiers <- vapply(tier_cols, function(col) {
      if (col %in% names(df)) as.character(df[[col]][i]) else NA_character_
    }, character(1))
    is_wqx_row <- any(!is.na(row_tiers) & grepl("^wqx_", row_tiers))

    if (is_wqx_row && n_present == 0) {
      consensus_status[i] <- "wqx"
      consensus_dtxsid[i] <- NA_character_
      # Find which column had the WQX resolution for source attribution
      wqx_col_idx <- which(!is.na(row_tiers) & grepl("^wqx_", row_tiers))[1]
      consensus_source[i] <- sub("^source_tier_", "", tier_cols[wqx_col_idx])
      qc_tier[i] <- compute_qc_tier("wqx", 0L, k)
      next
    }
```

The `next` statement skips the rest of the loop body for WQX rows, which means the existing `if (n_present == 0)` / `else if (n_present == 1)` chain runs only for non-WQX rows. This preserves the existing logic unchanged.

Also update the roxygen @param description for classify_consensus to mention the new "wqx" status value. Change the NOTE comment (around line 48-51) to include "wqx" in the documented values:

```r
#' NOTE: This function returns status values "agree", "agree_caveat", "single",
#' "wqx", "disagree", and "error". Downstream code (manual validation flow, retry merge)
```

After making changes, run `air format R/consensus.R` and `jarl check R/consensus.R`.

Run the tests. They MUST all pass now.

Commit message: `feat(45-01): add WQX consensus classification to classify_consensus and compute_qc_tier`
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-wqx-pipeline-integration.R')" 2>&1 | grep -E "FAIL|PASS|OK"</automated>
  </verify>
  <acceptance_criteria>
    - R/consensus.R contains the string "wqx" = as.integer(n_total)
    - R/consensus.R contains the string is_wqx_row <- any(!is.na(row_tiers) & grepl("^wqx_", row_tiers))
    - R/consensus.R contains the string consensus_status[i] <- "wqx"
    - R/consensus.R contains the string compute_qc_tier("wqx", 0L, k)
    - All test_that blocks in test-wqx-pipeline-integration.R pass (0 failures)
    - Existing tests in test-consensus.R still pass (no regressions)
  </acceptance_criteria>
  <done>compute_qc_tier handles "wqx" status returning same tier as "single". classify_consensus detects WQX rows via source_tier prefix "wqx_" and assigns "wqx" status instead of "error". All new tests pass, existing consensus tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source_tier values | String values from search results flow into consensus classification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Spoofing | consensus_status "wqx" | accept | source_tier values originate from run_curation_pipeline() internal logic, not user input. No external trust boundary crossed. |
| T-45-02 | Tampering | classify_consensus grepl guard | mitigate | Guard uses anchored regex `^wqx_` to prevent partial matches against non-WQX source_tier values (e.g., a hypothetical "not_wqx_exact" would not match) |
</threat_model>

<verification>
```bash
# All new WQX consensus tests pass
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-wqx-pipeline-integration.R')"

# Existing consensus tests still pass (regression check)
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-consensus.R')"
```
</verification>

<success_criteria>
- compute_qc_tier("wqx", 0L, k) returns as.integer(k)
- classify_consensus assigns "wqx" to rows with all-NA DTXSIDs and wqx_* source_tier
- classify_consensus still assigns "error" to rows with all-NA DTXSIDs and non-wqx source_tier
- All new and existing consensus tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/45-pipeline-integration/45-01-SUMMARY.md`
</output>
