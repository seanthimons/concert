---
phase: 32-toxval-schema-mapper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/toxval_mapper.R
  - inst/extdata/reference_cache/toxval_schema.rds
  - tests/testthat/test-toxval-mapper.R
autonomous: true
requirements: [SCHM-01, SCHM-02]

must_haves:
  truths:
    - "map_to_toxval_schema() returns a 56-column tibble matching ToxVal column order"
    - "All unmapped columns contain typed NAs (NA_character_ or NA_real_), never bare NA"
    - "Every harmonized field has a corresponding *_original audit column preserving pre-harmonization values"
    - "source_hash is a SHA256 digest of row content, unique per row"
    - "Identity-mapped fields have *_original == harmonized value"
    - "Zero-row input returns zero-row tibble with correct 56 column types"
  artifacts:
    - path: "R/toxval_mapper.R"
      provides: "map_to_toxval_schema() exported function + internal helpers"
      exports: ["map_to_toxval_schema"]
    - path: "inst/extdata/reference_cache/toxval_schema.rds"
      provides: "56-column zero-row typed tibble template"
    - path: "tests/testthat/test-toxval-mapper.R"
      provides: "Comprehensive test suite for schema mapper"
  key_links:
    - from: "R/toxval_mapper.R"
      to: "inst/extdata/reference_cache/toxval_schema.rds"
      via: "load_toxval_schema() for column template"
      pattern: "load_toxval_schema"
    - from: "R/toxval_mapper.R"
      to: "R/cleaning_reference.R"
      via: "load_toxval_schema() function"
      pattern: "load_toxval_schema"
---

<objective>
Create the ToxVal schema mapper: expand the schema manifest to 56 columns and implement
map_to_toxval_schema() that transforms curated + harmonized data into ToxVal-compatible output
with typed NAs, source_hash, and *_original audit columns.

Purpose: This is the bridge between ChemReg's internal data model and the ToxVal database format.
Downstream phases (34, 35) consume this mapper's output for UI display and parquet export.

Output: R/toxval_mapper.R (new), updated toxval_schema.rds, test-toxval-mapper.R (new)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-toxval-schema-mapper/32-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From R/unit_harmonizer.R — harmonize_units() output:
```r
tibble::tibble(
  orig_row_id = integer(),      # Links back to input position
  orig_unit = character(),       # Original unit string before normalization
  harmonized_value = numeric(),  # Value after conversion (value * multiplier)
  harmonized_unit = character(), # Target unit from table (or original if unmatched)
  conversion_factor = numeric(), # Multiplier applied (1 for pass-through)
  unit_flag = character()        # "", "case_fallback", "unmatched", "needs_mw", "media_inferred"
)
```

From R/numeric_parser.R — parse_numeric_results() output:
```r
tibble::tibble(
  orig_row_id = integer(),      # Links back to input position
  orig_result = character(),     # Pre-normalization string
  numeric_value = numeric(),     # Parsed numeric (NA_real_ for narratives/unparseable)
  qualifier = character(),       # "<", ">", "<=", ">=", "~", "=", ""
  range_bin = character(),       # "as_is", "low", "mid", "high"
  parse_flag = character()       # "", "narrative", "unparseable"
)
```

From R/cleaning_reference.R — load_toxval_schema():
```r
load_toxval_schema <- function(cache_dir)
# Returns zero-row tibble from inst/extdata/reference_cache/toxval_schema.rds
# Currently 7 columns; will be expanded to 56 by this plan
```

ToxVal 56-column schema (from database, authoritative order):
```
 1. dtxsid (VARCHAR)                      2. casrn (VARCHAR)
 3. name (VARCHAR)                        4. source (VARCHAR)
 5. sub_source (VARCHAR)                  6. toxval_type (VARCHAR)
 7. toxval_subtype (VARCHAR)              8. toxval_type_supercategory (VARCHAR)
 9. qualifier (VARCHAR)                  10. toxval_numeric (DOUBLE)
11. toxval_units (VARCHAR)               12. risk_assessment_class (VARCHAR)
13. study_type (VARCHAR)                 14. study_duration_class (VARCHAR)
15. study_duration_value (DOUBLE)        16. study_duration_units (VARCHAR)
17. species_common (VARCHAR)             18. strain (VARCHAR)
19. latin_name (VARCHAR)                 20. species_supercategory (VARCHAR)
21. sex (VARCHAR)                        22. generation (VARCHAR)
23. lifestage (VARCHAR)                  24. exposure_route (VARCHAR)
25. exposure_method (VARCHAR)            26. exposure_form (VARCHAR)
27. media (VARCHAR)                      28. toxicological_effect (VARCHAR)
29. toxicological_effect_category (VARCHAR) 30. experimental_record (VARCHAR)
31. study_group (VARCHAR)                32. year (DOUBLE)
33. qc_category (VARCHAR)               34. qc_status (VARCHAR)
35. source_hash (VARCHAR)                36. source_url (VARCHAR)
37. subsource_url (VARCHAR)              38. toxval_type_original (VARCHAR)
39. toxval_subtype_original (VARCHAR)    40. toxval_numeric_original (DOUBLE)
41. toxval_units_original (VARCHAR)      42. study_type_original (VARCHAR)
43. study_duration_class_original (VARCHAR) 44. study_duration_value_original (DOUBLE)
45. study_duration_units_original (VARCHAR) 46. species_original (VARCHAR)
47. strain_original (VARCHAR)            48. sex_original (VARCHAR)
49. generation_original (VARCHAR)        50. lifestage_original (VARCHAR)
51. exposure_route_original (VARCHAR)    52. exposure_method_original (VARCHAR)
53. exposure_form_original (VARCHAR)     54. media_original (VARCHAR)
55. toxicological_effect_original (VARCHAR) 56. original_year (DOUBLE)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand toxval_schema.rds and create R/toxval_mapper.R with map_to_toxval_schema()</name>
  <files>
    inst/extdata/reference_cache/toxval_schema.rds
    R/toxval_mapper.R
    tests/testthat/test-toxval-mapper.R
  </files>
  <behavior>
    Schema template:
    - Test: load_toxval_schema() returns 0-row tibble with exactly 56 columns
    - Test: Column names match ToxVal database order exactly (dtxsid first, original_year last)
    - Test: Character columns have typeof "character", numeric columns have typeof "double"
    - Test: No column has typeof "logical" (no bare NAs)

    map_to_toxval_schema() core:
    - Test: Returns 56-column tibble for valid input
    - Test: Column order matches schema template exactly
    - Test: dtxsid, casrn, name populated from curated_data
    - Test: toxval_numeric, toxval_units populated from harmonized_data
    - Test: qualifier populated from curated_data (or harmonized_data if present)
    - Test: source defaults to "user_upload" when source_name=NULL
    - Test: source set to custom value when source_name provided
    - Test: qc_category always "user_curated" (per D-22)
    - Test: qc_status = "pass" for clean data (per D-23)

    Typed NA enforcement:
    - Test: All unmapped character columns are NA_character_ (not NA or NA_logical_)
    - Test: All unmapped numeric columns are NA_real_ (not NA or NA_logical_)
    - Test: typeof check passes: no column has typeof "logical" in output
    - Test: Assertion function assert_typed_nas() catches bare NA injection

    *_original audit columns:
    - Test: toxval_numeric_original = orig_result from parsed data (pre-harmonization)
    - Test: toxval_units_original = orig_unit from harmonized_data
    - Test: Identity-mapped fields: species_original == species_common when no transformation
    - Test: *_original columns present for all 18 harmonized fields

    source_hash:
    - Test: source_hash is non-NA character for each row
    - Test: Different rows produce different hashes
    - Test: Same row content produces same hash (deterministic)
    - Test: Hash is SHA256 via digest::digest()

    Edge cases:
    - Test: Zero-row input returns zero-row output with 56 typed columns
    - Test: Single-row input works correctly
    - Test: Missing optional columns in curated_data produce typed NAs (not errors)
    - Test: Rows where harmonized_data has NA values still produce valid typed output
  </behavior>
  <action>
    **Step 1: Regenerate toxval_schema.rds** (per D-17, D-18, D-19)

    Write a temp R script that creates the full 56-column zero-row tibble matching the exact
    ToxVal database column order from the `<interfaces>` block above. Use `NA_character_` for
    VARCHAR columns and `NA_real_` for DOUBLE columns. Create via `tibble::tibble(...)[0, ]`.
    Save to `inst/extdata/reference_cache/toxval_schema.rds`.

    Verify the existing `load_toxval_schema()` in `R/cleaning_reference.R` works with the
    expanded schema — it already reads this file, so no code change needed there.

    **Step 2: Create R/toxval_mapper.R** with these exported/internal functions:

    `map_to_toxval_schema(curated_data, harmonized_data, source_name = NULL)` (exported, per D-01, D-02, D-03):
    - Load schema template via `load_toxval_schema(system.file("extdata/reference_cache", package = "chemreg"))`
    - Build a tibble row-by-row (or vectorized) mapping ChemReg columns to ToxVal columns per D-04:
      - `dtxsid` <- curated_data$dtxsid
      - `casrn` <- curated_data$casrn
      - `name` <- curated_data$name
      - `source` <- source_name %||% "user_upload" (per D-06)
      - `sub_source` <- NA_character_ (per D-07)
      - `toxval_type` <- curated_data$toxval_type if present, else NA_character_
      - `toxval_subtype` <- NA_character_
      - `toxval_type_supercategory` <- NA_character_ (per D-20)
      - `qualifier` <- curated_data$qualifier if present, else NA_character_
      - `toxval_numeric` <- harmonized_data$harmonized_value
      - `toxval_units` <- harmonized_data$harmonized_unit
      - All remaining VARCHAR columns: NA_character_ (per D-05)
      - All remaining DOUBLE columns: NA_real_ (per D-05)
      - `year` <- curated_data$year if present, else NA_real_
      - `qc_category` <- "user_curated" (per D-22)
      - `qc_status` <- "pass" (per D-23; future phases can add review logic)
      - `source_url` <- NA_character_ (per D-09)
      - `subsource_url` <- NA_character_
    - For each column with an `*_original` counterpart, populate the original:
      - `toxval_numeric_original` <- curated_data$orig_result if present (earliest state per D-12), else harmonized_data$harmonized_value
      - `toxval_units_original` <- harmonized_data$orig_unit (per D-10)
      - `toxval_type_original` <- same as toxval_type (identity per D-11)
      - `species_original` <- same as species_common (identity per D-11)
      - `study_duration_value_original` <- same as study_duration_value (identity per D-11)
      - `study_duration_units_original` <- same as study_duration_units (identity per D-11)
      - All other `*_original` columns: identity copy from corresponding harmonized field (per D-11)
      - `original_year` <- same as year (identity per D-11)
    - Generate source_hash via `generate_source_hash()` (per D-08)
    - Run `assert_typed_nas()` before returning (per D-16)

    `generate_source_hash(result_tibble)` (internal, per D-08):
    - For each row, concatenate all non-hash column values with "|" separator
    - Compute SHA256 via `digest::digest(paste_string, algo = "sha256")`
    - Return character vector of hashes

    `assert_typed_nas(tbl)` (internal, per D-16):
    - `stopifnot(all(vapply(tbl, typeof, "") != "logical"))`
    - If any column is logical type, that means it contains bare NA (which R infers as logical)

    Add roxygen2 documentation with @export for map_to_toxval_schema, @keywords internal for helpers.
    Add @importFrom directives: tibble tibble, digest digest.

    **Step 3: Write tests/testthat/test-toxval-mapper.R**

    Create test fixtures:
    ```r
    # Minimal curated_data (like curation pipeline output)
    curated_fixture <- tibble::tibble(
      dtxsid = c("DTXSID7020182", "DTXSID2021731"),
      casrn = c("71-43-2", "7440-02-0"),
      name = c("Benzene", "Nickel"),
      qualifier = c("<", ""),
      orig_result = c("< 0.5", "10.0")
    )

    # Minimal harmonized_data (like harmonize_units() output)
    harmonized_fixture <- tibble::tibble(
      orig_row_id = 1:2,
      orig_unit = c("ug/L", "mg/L"),
      harmonized_value = c(0.0005, 10.0),
      harmonized_unit = c("mg/L", "mg/L"),
      conversion_factor = c(0.001, 1),
      unit_flag = c("", "")
    )
    ```

    Test groups matching the behavior spec above:
    - "schema template" group: column count, names, types, no logical columns
    - "basic mapping" group: correct column mapping, source_name handling
    - "typed NAs" group: no bare NA, character/numeric type enforcement
    - "audit columns" group: *_original values correct, identity mapping
    - "source_hash" group: deterministic, unique, SHA256 format
    - "edge cases" group: zero-row, single-row, missing optional columns, NA values in harmonized
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "testthat::test_file('tests/testthat/test-toxval-mapper.R')"</automated>
  </verify>
  <done>
    - toxval_schema.rds contains 0-row x 56-column tibble with correct column names and types
    - map_to_toxval_schema() returns 56-column tibble with mapped values, typed NAs, audit columns, source_hash
    - assert_typed_nas() enforces no bare NA in output
    - All tests pass covering: schema template, mapping, typed NAs, audit columns, source_hash, edge cases
    - No column in output has typeof "logical"
  </done>
</task>

<task type="auto">
  <name>Task 2: Roxygen and R CMD check validation</name>
  <files>man/map_to_toxval_schema.Rd</files>
  <action>
    Run roxygen2 to generate documentation for the new exported function:
    ```r
    devtools::document()
    ```

    Then run R CMD check (no-manual, no-vignettes) to verify:
    - No undeclared imports
    - No missing documentation
    - NAMESPACE exports are correct
    - No WARNING or ERROR in check output

    Fix any issues found (missing @importFrom, undocumented parameters, etc.).
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/chemreg && "C:/Program Files/R/R-4.5.1/bin/Rscript.exe" -e "devtools::check(args='--no-manual --no-vignettes', error_on='warning')"</automated>
  </verify>
  <done>
    - man/map_to_toxval_schema.Rd exists with complete parameter documentation
    - R CMD check passes with 0 errors and 0 warnings
    - NAMESPACE includes export for map_to_toxval_schema
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated_data input | User-provided tibble from curation pipeline; column names/types not guaranteed |
| harmonized_data input | Output from harmonize_units(); generally trusted but row count may mismatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | source_hash | mitigate | SHA256 digest of all row values; hash computed after all mapping, before return |
| T-32-02 | Information Disclosure | source_hash | accept | Hash is one-way; no PII risk from hash values |
| T-32-03 | Denial of Service | generate_source_hash | accept | Row-by-row digest is O(n); acceptable for typical dataset sizes (<100K rows) |
| T-32-04 | Tampering | input validation | mitigate | Validate curated_data and harmonized_data are tibbles with required columns before mapping; stop() with informative message on mismatch |
</threat_model>

<verification>
1. `testthat::test_file("tests/testthat/test-toxval-mapper.R")` — all tests pass
2. `devtools::check()` — 0 errors, 0 warnings
3. Manual spot-check: `map_to_toxval_schema(curated_fixture, harmonized_fixture)` returns 2x56 tibble
4. `vapply(result, typeof, "")` — no "logical" entries
</verification>

<success_criteria>
- toxval_schema.rds expanded from 7 to 56 columns with correct types
- map_to_toxval_schema() exported and documented
- All 56 columns populated (mapped values or typed NAs)
- 18 *_original audit columns populated correctly
- source_hash generated via SHA256 per row
- No bare NA anywhere in output (assert_typed_nas passes)
- All tests pass, R CMD check clean
</success_criteria>

<output>
After completion, create `.planning/phases/32-toxval-schema-mapper/32-01-SUMMARY.md`
</output>
