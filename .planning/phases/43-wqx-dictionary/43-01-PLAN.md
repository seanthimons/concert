---
phase: 43-wqx-dictionary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-wqx-dictionary.R
  - R/cleaning_reference.R
autonomous: true
requirements:
  - DICT-01
  - DICT-02
  - DICT-03
user_setup: []

must_haves:
  truths:
    - "load_wqx_dictionary() returns cached tibble when RDS exists"
    - "load_wqx_dictionary() triggers .build_wqx_dictionary when RDS is absent"
    - "refresh_wqx_cache() deletes existing RDS and rebuilds from EPA download"
    - ".build_wqx_dictionary() downloads two zips, parses CSVs, returns combined tibble with correct column schema"
  artifacts:
    - path: "tests/testthat/test-wqx-dictionary.R"
      provides: "Unit tests for all DICT requirements"
      min_lines: 80
    - path: "R/cleaning_reference.R"
      provides: "load_wqx_dictionary, .build_wqx_dictionary, refresh_wqx_cache functions"
      contains: "load_wqx_dictionary"
  key_links:
    - from: "R/cleaning_reference.R"
      to: "load_or_fetch_reference"
      via: "load_wqx_dictionary calls load_or_fetch_reference with .build_wqx_dictionary as fetch_fn"
      pattern: "load_or_fetch_reference\\(cache_path.*\\.build_wqx_dictionary"
    - from: "R/cleaning_reference.R"
      to: "inst/extdata/reference_cache/wqx_dictionary.rds"
      via: "refresh_wqx_cache writes to cache_dir/wqx_dictionary.rds"
      pattern: "wqx_dictionary\\.rds"
---

<objective>
Implement the WQX dictionary loader infrastructure: unit tests, the internal build function that downloads and parses EPA CSVs, the lazy loader that integrates with the existing cache-or-fetch pattern, and the exported refresh function.

Purpose: Delivers DICT-01, DICT-02, DICT-03 — the complete code layer for downloading, caching, and refreshing the WQX lookup dictionary. Phase 44 consumes the tibble returned by `load_wqx_dictionary()`.

Output: Working `load_wqx_dictionary()`, `.build_wqx_dictionary()`, and `refresh_wqx_cache()` functions with passing unit tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-wqx-dictionary/43-CONTEXT.md
@.planning/phases/43-wqx-dictionary/43-RESEARCH.md
@.planning/phases/43-wqx-dictionary/43-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From R/cleaning_reference.R (lines 21-38) — the cache-or-fetch engine:
```r
#' @export
load_or_fetch_reference <- function(cache_path, fetch_fn, name) {
  if (file.exists(cache_path)) {
    message(sprintf("Loading %s from cache: %s", name, cache_path))
    return(readRDS(cache_path))
  }
  message(sprintf("Fetching %s (cache not found)...", name))
  result <- fetch_fn()
  fs::dir_create(dirname(cache_path), recurse = TRUE)
  saveRDS(result, cache_path, compress = FALSE)
  message(sprintf("Cached %s to: %s", name, cache_path))
  return(result)
}
```

From R/cleaning_reference.R (lines 55-83) — loader function signature pattern:
```r
load_stop_words <- function(cache_dir) {
  cache_path <- file.path(cache_dir, "stop_words.rds")
  fetch_fn <- function() { ... }
  load_or_fetch_reference(cache_path, fetch_fn, "stop words")
}
```

Expected combined tibble column schema (per D-01 + D-05):
```r
tibble(
  name           = character(),   # trimmed; canonical rows: name == canonical_name
  canonical_name = character(),   # trimmed; canonical rows: canonical_name == name
  type           = character(),   # "canonical" | "synonym" | "standardize" | "retired"
  cas_number     = character(),   # from Characteristic.csv; NA for alias rows
  group_name     = character(),   # from Characteristic.csv; NA for alias rows
  description    = character()    # from both sources; may be NA
)
```

Installed package path resolution (from R/curate_headless.R lines 110-111):
```r
cache_dir <- system.file("extdata", "reference_cache", package = "chemreg")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for WQX dictionary functions</name>
  <files>tests/testthat/test-wqx-dictionary.R</files>
  <read_first>
    - tests/testthat/test-cleaning-reference.R (pattern for cache-hit/miss tests, withr::with_tempdir isolation, two-candidate inst path probe)
    - R/cleaning_reference.R (lines 21-38 for load_or_fetch_reference signature; lines 55-83 for loader pattern)
    - .planning/phases/43-wqx-dictionary/43-PATTERNS.md (test patterns section — exact code to copy)
  </read_first>
  <behavior>
    - Test 1: load_wqx_dictionary() returns cached tibble when wqx_dictionary.rds exists (fetch_fn NOT called)
    - Test 2: load_wqx_dictionary() calls .build_wqx_dictionary when cache is absent; result is saved to disk
    - Test 3: refresh_wqx_cache() deletes existing RDS and writes new one from .build_wqx_dictionary result
    - Test 4: .build_wqx_dictionary() returns tibble with exact columns: name, canonical_name, type, cas_number, group_name, description
    - Test 5: .build_wqx_dictionary() canonical rows have name == canonical_name and type == "canonical"
    - Test 6: .build_wqx_dictionary() alias rows have type in c("synonym", "standardize", "retired") and cas_number == NA
    - Test 7: Pre-built wqx_dictionary.rds (from inst/extdata/reference_cache) has correct structure (skip if not found)
  </behavior>
  <action>
Create `tests/testthat/test-wqx-dictionary.R` with the following tests:

1. **Cache-hit test** — follow exact pattern from `test-cleaning-reference.R` lines 6-31:
   - `withr::with_tempdir({ ... })`
   - Pre-create `wqx_dictionary.rds` with a mock tibble (6 columns: name, canonical_name, type, cas_number, group_name, description)
   - Call `load_wqx_dictionary(cache_dir)` — use the actual function name
   - Assert result matches pre-created data
   - Assert `.build_wqx_dictionary` was NOT called (use a flag variable in a local mock)

2. **Cache-miss test** — follow pattern from `test-cleaning-reference.R` lines 33-62:
   - `withr::with_tempdir({ ... })`
   - No RDS exists
   - Mock `.build_wqx_dictionary` using `testthat::local_mocked_bindings(.build_wqx_dictionary = function() mock_tibble, .package = "chemreg")` — this avoids network calls
   - Call `load_wqx_dictionary(cache_dir)`
   - Assert RDS now exists at `file.path(cache_dir, "wqx_dictionary.rds")`
   - Assert result matches mock_tibble

3. **refresh_wqx_cache overwrite test**:
   - `withr::with_tempdir({ ... })`
   - Pre-create an old `wqx_dictionary.rds` with `tibble(name = "old")`
   - Mock `.build_wqx_dictionary` to return a new mock tibble with correct schema
   - Call `refresh_wqx_cache(cache_dir = ".")`
   - Read back the RDS, assert it matches the new mock tibble (not the old one)

4. **Column schema test** (offline, uses local CSVs):
   - Use `testthat::skip_if_not(file.exists(...))` for both local CSVs
   - Call `.build_wqx_dictionary()` — but this requires network. Instead, test the pre-built RDS:
   - Use the two-candidate probe pattern from `test-cleaning-reference.R` lines 150-158:
     ```r
     candidates <- c(
       file.path(getwd(), "inst", "extdata", "reference_cache"),
       file.path(getwd(), "..", "..", "inst", "extdata", "reference_cache")
     )
     cache_dir <- candidates[sapply(candidates, function(d) file.exists(file.path(d, "wqx_dictionary.rds")))][1]
     if (is.na(cache_dir)) skip("wqx_dictionary.rds not found — run scripts/build_wqx_dictionary.R first")
     ```
   - Load the RDS directly with `readRDS(file.path(cache_dir, "wqx_dictionary.rds"))`
   - Assert: `expect_s3_class(result, "tbl_df")`
   - Assert: `expect_named(result, c("name", "canonical_name", "type", "cas_number", "group_name", "description"))`
   - Assert: `expect_true(all(result$type %in% c("canonical", "synonym", "standardize", "retired")))`
   - Assert: `expect_true(all(!is.na(result$name)))`
   - Assert for canonical rows: `canonicals <- result[result$type == "canonical", ]; expect_true(all(canonicals$name == canonicals$canonical_name))`
   - Assert for alias rows: `aliases <- result[result$type != "canonical", ]; expect_true(all(is.na(aliases$cas_number)))`
   - Assert row count: `expect_gte(nrow(result), 120000)` (expected ~124K)

File header: `# Test file for WQX dictionary loader functions\n# Tests DICT-01, DICT-02, DICT-03 requirements\n\nlibrary(withr)`

Use `library(withr)` at top (same as test-cleaning-reference.R). All test bodies inside `test_that()` blocks.

Mock tibble for tests 1-3 (use this exact structure):
```r
mock_wqx <- tibble::tibble(
  name           = c("Dissolved oxygen", "DO"),
  canonical_name = c("Dissolved oxygen", "Dissolved oxygen"),
  type           = c("canonical", "synonym"),
  cas_number     = c("7782-44-7", NA_character_),
  group_name     = c("Inorganics, Major, Non-metals", NA_character_),
  description    = c("A measure of dissolved O2", "Alias for dissolved oxygen")
)
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-wqx-dictionary.R')"</automated>
    <note>The Rscript -e call above contains no $ or backslash escaping — safe for inline use per CLAUDE.md. If executor prefers, write to a temp .R file instead.</note>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-wqx-dictionary.R exists with >= 80 lines
    - File contains `library(withr)` on line 4
    - File contains `test_that("load_wqx_dictionary returns cached tibble when RDS exists"`
    - File contains `test_that("load_wqx_dictionary calls build when cache is absent"`
    - File contains `test_that("refresh_wqx_cache overwrites existing RDS"`
    - File contains `test_that("pre-built wqx_dictionary.rds has correct structure"`
    - File contains `expect_named(result, c("name", "canonical_name", "type", "cas_number", "group_name", "description"))`
    - Tests that can run without network or pre-built RDS pass (cache-hit, cache-miss with mock, refresh with mock)
  </acceptance_criteria>
  <done>Test file created with 4+ test_that blocks covering cache-hit, cache-miss, refresh overwrite, and pre-built RDS structure validation. Mock-based tests pass; RDS structure test skips gracefully when artifact is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement .build_wqx_dictionary, load_wqx_dictionary, and refresh_wqx_cache</name>
  <files>R/cleaning_reference.R</files>
  <read_first>
    - R/cleaning_reference.R (full file — understand current structure, find insertion point after line 515)
    - .planning/phases/43-wqx-dictionary/43-RESEARCH.md (Pattern 1-3 code examples — copy these exactly)
    - .planning/phases/43-wqx-dictionary/43-PATTERNS.md (exact analog code patterns and pitfall guards)
  </read_first>
  <behavior>
    - load_wqx_dictionary(cache_dir) wraps load_or_fetch_reference with .build_wqx_dictionary
    - .build_wqx_dictionary() downloads two EPA zips, unzips, parses CSVs, returns combined tibble
    - refresh_wqx_cache(cache_dir = NULL) deletes old RDS, calls .build_wqx_dictionary, saves new RDS
    - Combined tibble has columns: name, canonical_name, type, cas_number, group_name, description
    - Only 3 alias types kept: synonym, standardize, retired
  </behavior>
  <action>
Append three functions to `R/cleaning_reference.R` AFTER the `load_all_reference_lists` function (after line 514). Insert them in this order:

**1. `.build_wqx_dictionary` (internal, NOT exported):**

```r
# Internal function — builds WQX dictionary from EPA domain value CSVs
# Downloads Characteristic.csv and Characteristic Alias.csv as zips,
# parses them, and returns a combined tibble per D-01/D-05 schema.
.build_wqx_dictionary <- function() {
  char_url <- "https://cdx.epa.gov/wqx/download/DomainValues/Characteristic_CSV.zip"
  alias_url <- "https://cdx.epa.gov/wqx/download/DomainValues/CharacteristicAlias_CSV.zip"

  tmp_dir <- tempfile()
  dir.create(tmp_dir)
  on.exit(unlink(tmp_dir, recursive = TRUE))

  # Download both zips

  char_zip <- file.path(tmp_dir, "char.zip")
  alias_zip <- file.path(tmp_dir, "alias.zip")
  utils::download.file(char_url, destfile = char_zip, mode = "wb", quiet = TRUE)
  utils::download.file(alias_url, destfile = alias_zip, mode = "wb", quiet = TRUE)

  # Extract
  utils::unzip(char_zip, exdir = tmp_dir)
  utils::unzip(alias_zip, exdir = tmp_dir)

  # Parse Characteristic.csv -> canonical rows
  char_tbl <- readr::read_csv(
    file.path(tmp_dir, "Characteristic.csv"),
    show_col_types = FALSE
  ) |>
    dplyr::select(
      name = Name,
      cas_number = `CAS Number`,
      group_name = `Group Name`,
      description = Description
    ) |>
    dplyr::mutate(
      name = trimws(name),
      canonical_name = name,
      type = "canonical"
    )

  # Parse Characteristic Alias.csv -> alias rows (3 types only)
  # NOTE: CSV filename has a space: "Characteristic Alias.csv" (not "CharacteristicAlias.csv")
  kept_alias_types <- c(
    "WQX SYNONYM REGISTRY (validation)",
    "STANDARDIZE NAME (Normalized)",
    "RETIRED NAME"
  )
  type_map <- c(
    "WQX SYNONYM REGISTRY (validation)" = "synonym",
    "STANDARDIZE NAME (Normalized)" = "standardize",
    "RETIRED NAME" = "retired"
  )
  alias_tbl <- readr::read_csv(
    file.path(tmp_dir, "Characteristic Alias.csv"),
    show_col_types = FALSE
  ) |>
    dplyr::filter(`Alias Type Name` %in% kept_alias_types) |>
    dplyr::select(
      name = `Alias Name`,
      canonical_name = `Characteristic Name`,
      description = Description,
      alias_type = `Alias Type Name`
    ) |>
    dplyr::mutate(
      name = trimws(name),
      canonical_name = trimws(canonical_name),
      type = dplyr::recode(alias_type, !!!type_map),
      cas_number = NA_character_,
      group_name = NA_character_
    ) |>
    dplyr::select(-alias_type)

  dplyr::bind_rows(char_tbl, alias_tbl)
}
```

**2. `load_wqx_dictionary` (exported):**

```r
#' Load WQX dictionary lookup table
#'
#' Returns a combined tibble of canonical WQX Characteristic Names and alias
#' mappings (synonym, standardize, retired). Uses the generic cache-or-fetch
#' infrastructure — builds the dictionary from EPA data on first call if no
#' cached RDS exists.
#'
#' @param cache_dir Directory containing reference cache RDS files
#' @return Tibble with columns: name, canonical_name, type, cas_number, group_name, description
#' @export
load_wqx_dictionary <- function(cache_dir) {
  cache_path <- file.path(cache_dir, "wqx_dictionary.rds")
  load_or_fetch_reference(cache_path, .build_wqx_dictionary, "WQX dictionary")
}
```

**3. `refresh_wqx_cache` (exported):**

```r
#' Refresh WQX dictionary cache
#'
#' Re-downloads Characteristic.csv and Characteristic Alias.csv from EPA,
#' rebuilds the combined lookup tibble, and saves to cache. Overwrites any
#' existing cached RDS silently.
#'
#' @param cache_dir Directory for reference cache. Defaults to installed package path.
#' @return Invisibly returns the rebuilt tibble
#' @export
refresh_wqx_cache <- function(cache_dir = NULL) {
  if (is.null(cache_dir)) {
    cache_dir <- system.file("extdata", "reference_cache", package = "chemreg")
  }
  cache_path <- file.path(cache_dir, "wqx_dictionary.rds")
  if (file.exists(cache_path)) unlink(cache_path)

  result <- .build_wqx_dictionary()
  fs::dir_create(dirname(cache_path), recurse = TRUE)
  saveRDS(result, cache_path, compress = FALSE)
  message(sprintf("WQX dictionary refreshed: %d rows written to %s", nrow(result), cache_path))
  invisible(result)
}
```

**Critical guards (per PATTERNS.md pitfalls):**
- CSV filename is `"Characteristic Alias.csv"` WITH a space (not `"CharacteristicAlias.csv"`)
- Use `readr::read_csv()` (not `utils::read.csv()`) to auto-strip leading tab characters
- Apply `trimws()` to both `name` and `canonical_name` as belt-and-suspenders
- Set `canonical_name = name` for canonical rows (D-01 requirement)
- Filter to only 3 kept alias types (not all 16)
- Use `compress = FALSE` in `saveRDS()` (matching all 10 existing RDS files)
- Do NOT add to `load_all_reference_lists()` (that is a Phase 45 decision)
- Do NOT touch `R/zzz.R` (D-02 forbids .onLoad eager loading)
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-wqx-dictionary.R')"</automated>
    <note>The Rscript -e call above contains no $ or backslash escaping — safe for inline use per CLAUDE.md. If executor prefers, write to a temp .R file instead.</note>
  </verify>
  <acceptance_criteria>
    - R/cleaning_reference.R contains `.build_wqx_dictionary <- function()`
    - R/cleaning_reference.R contains `load_wqx_dictionary <- function(cache_dir)`
    - R/cleaning_reference.R contains `refresh_wqx_cache <- function(cache_dir = NULL)`
    - R/cleaning_reference.R contains `#' @export` before both `load_wqx_dictionary` and `refresh_wqx_cache`
    - R/cleaning_reference.R contains `"Characteristic Alias.csv"` (with space — not CharacteristicAlias.csv)
    - R/cleaning_reference.R contains `compress = FALSE` in the saveRDS call within refresh_wqx_cache
    - R/cleaning_reference.R contains `load_or_fetch_reference(cache_path, .build_wqx_dictionary, "WQX dictionary")`
    - R/cleaning_reference.R contains `system.file("extdata", "reference_cache", package = "chemreg")`
    - R/cleaning_reference.R does NOT contain any modification to `load_all_reference_lists`
    - Mock-based tests in test-wqx-dictionary.R pass (cache-hit, cache-miss, refresh overwrite)
  </acceptance_criteria>
  <done>Three functions appended to R/cleaning_reference.R. load_wqx_dictionary uses load_or_fetch_reference pattern. .build_wqx_dictionary downloads, unzips, parses both CSVs, returns combined tibble. refresh_wqx_cache force-rebuilds the RDS. All mock-based unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EPA CDX endpoint -> R process | External zip files downloaded over HTTPS from government server |
| Zip archive -> filesystem | Zip extraction writes files to temp directory |
| CSV content -> tibble columns | External CSV column values become internal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | .build_wqx_dictionary / download.file | accept | HTTPS transport to cdx.epa.gov (US government endpoint); no additional signature verification. Risk: man-in-middle at network edge. Acceptable for non-security-critical reference data. |
| T-43-02 | Denial of Service | .build_wqx_dictionary / unzip | mitigate | Use `utils::unzip()` which writes to a `tempfile()` directory with `on.exit(unlink(..., recursive = TRUE))`. Zip bombs limited by OS temp space. No path traversal possible — R's unzip extracts flat to exdir. |
| T-43-03 | Information Disclosure | refresh_wqx_cache / message() | accept | Message prints row count and file path to console. No secrets involved — this is public EPA reference data. |
| T-43-04 | Spoofing | .build_wqx_dictionary / URL | accept | URLs hardcoded (D-03/D-04) — no user input influences download target. No injection vector. |
| T-43-05 | Elevation of Privilege | .build_wqx_dictionary / CSV parse | mitigate | Column selection with `dplyr::select()` whitelists only expected columns. `dplyr::filter()` restricts alias types to 3 known values. Unknown/malicious columns are dropped, never executed. |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds without errors
2. `testthat::test_file('tests/testthat/test-wqx-dictionary.R')` — all mock-based tests pass
3. `devtools::test()` — full test suite passes (no regressions)
4. `grep -c "load_wqx_dictionary\|refresh_wqx_cache\|.build_wqx_dictionary" R/cleaning_reference.R` returns 3+ matches
</verification>

<success_criteria>
- load_wqx_dictionary(cache_dir) returns cached tibble when RDS exists (no network call)
- load_wqx_dictionary(cache_dir) triggers .build_wqx_dictionary and caches result when RDS absent
- refresh_wqx_cache() deletes old RDS and writes new one
- .build_wqx_dictionary() produces tibble with 6 columns matching D-01/D-05 schema
- All unit tests pass; no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/43-wqx-dictionary/43-01-SUMMARY.md`
</output>
