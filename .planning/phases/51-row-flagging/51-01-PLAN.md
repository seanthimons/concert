---
phase: 51-row-flagging
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - R/consensus.R
  - R/export_helpers.R
  - tests/testthat/test-consensus.R
  - tests/testthat/test-export-import.R
autonomous: true
requirements:
  - FLAG-01
  - FLAG-03
user_setup: []

must_haves:
  truths:
    - "D-01: row_flag is a human annotation and remains separate from system-generated needs_review"
    - "D-02: row_flag values are BAD, FOLLOW-UP, VERIFIED, or blank"
    - "D-03: setting BAD does not mutate consensus_status and does not force needs_review = TRUE"
    - "D-08: exports include a simple row_flag column"
    - "D-09: exports do not add flag timestamp, method, or note columns"
  artifacts:
    - path: "R/consensus.R"
      provides: "row_flag initialization and validation/mutation helpers"
      contains: "row_flag"
    - path: "R/export_helpers.R"
      provides: "Curated Data export preserves row_flag separately from needs_review"
      contains: "row_flag"
  key_links:
    - from: "R/consensus.R::init_resolution_state"
      to: "R/export_helpers.R::build_export_sheets"
      via: "resolution_state carries row_flag into Curated Data export"
      pattern: "row_flag"
---

<objective>
Add the row-flag state foundation and export persistence for Phase 51.

Purpose: Row flags need a stable session/export data model before the Review Results UI can mutate them. This plan adds `row_flag` to resolution state, provides small helper functions for safe updates, and ensures exported Curated Data always contains the public `row_flag` column while keeping `needs_review` independent.

Output: Updated `R/consensus.R`, `R/export_helpers.R`, and focused tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-row-flagging/51-CONTEXT.md
@.planning/phases/51-row-flagging/51-RESEARCH.md

<interfaces>
Existing state initializer:

```r
init_resolution_state <- function(df) {
  if (!".pinned" %in% names(df)) df$.pinned <- FALSE
  if (!".manual_entry" %in% names(df)) df$.manual_entry <- FALSE
  if (!".resolution_method" %in% names(df)) df$.resolution_method <- NA_character_
  if (!".resolution_reason" %in% names(df)) df$.resolution_reason <- NA_character_
  df
}
```

Existing export pattern:

```r
curated_data_sheet <- resolution_state %>%
  dplyr::mutate(needs_review = (consensus_status %in% c("error", "unresolvable"))) %>%
  dplyr::select(-tidyselect::any_of(c(".pinned", ".manual_entry", ".suggested_column")))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize row_flag in resolution state</name>
  <files>R/consensus.R</files>
  <read_first>
    - R/consensus.R
    - tests/testthat/test-consensus.R
  </read_first>
  <action>
Update `init_resolution_state()` so every resolution state has a public `row_flag` column.

Rules:
- Add `row_flag` if missing.
- Use `NA_character_` for unset flags internally.
- Preserve existing values.
- Normalize `""` to `NA_character_` if convenient, but do not require it if tests treat both blank and `NA` as unset.
- Do not prefix the column with `.` because it is exported user data.
- Update roxygen text to mention `row_flag`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - `init_resolution_state()` adds `row_flag`
    - Existing `row_flag` values are preserved
    - `row_flag` is public, not dot-prefixed
    - `devtools::load_all()` succeeds
  </acceptance_criteria>
  <done>Resolution state always carries `row_flag` without disturbing existing resolution fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add helper functions for flag validation and updates</name>
  <files>R/consensus.R</files>
  <action>
Add small exported or internal helpers near `init_resolution_state()`:

```r
valid_row_flags <- function() c("BAD", "FOLLOW-UP", "VERIFIED")

normalize_row_flag <- function(flag) {
  # returns NA_character_ for NULL/NA/""/"CLEAR"; otherwise uppercase valid flag
}

set_row_flag <- function(df, row_idx, flag) {
  # init_resolution_state(df), validate row_idx, normalize flag, set df$row_flag[row_idx]
}

set_row_flags <- function(df, row_indices, flag) {
  # vectorized/batched version used by Review Results UI
}
```

Implementation details:
- Accept lowercase user input by converting to uppercase.
- Treat `"CLEAR"` and `""` as unset.
- Reject invalid values with a clear error naming valid flags.
- Validate row indices are in bounds.
- Do not modify `consensus_status`, `consensus_dtxsid`, `.pinned`, `.resolution_method`, or `.resolution_reason`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - `R/consensus.R` contains `valid_row_flags`
    - `R/consensus.R` contains `normalize_row_flag`
    - `R/consensus.R` contains `set_row_flag`
    - `R/consensus.R` contains `set_row_flags`
    - Invalid flags throw an error
    - Clearing a flag sets `row_flag` to `NA_character_`
  </acceptance_criteria>
  <done>Flag mutation has one validated helper path that UI code can reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve row_flag in Curated Data export</name>
  <files>R/export_helpers.R</files>
  <read_first>
    - R/export_helpers.R
    - R/curate_headless.R
    - tests/testthat/test-export-import.R
  </read_first>
  <action>
Update `build_export_sheets()`:

1. Call or mirror `init_resolution_state()` behavior before building Curated Data so old resolution states still export a `row_flag` column.
2. Keep `row_flag` out of the internal-column exclusion list.
3. Relocate `row_flag` next to `needs_review` or immediately after `consensus_status`/`consensus_source` so it is easy to find.
4. Leave `needs_review` exactly as a computed system signal:

```r
needs_review = (consensus_status %in% c("error", "unresolvable"))
```

Do not make `BAD` imply `needs_review = TRUE`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - Curated Data export includes `row_flag`
    - Curated Data export still includes `needs_review`
    - `.pinned`, `.manual_entry`, and `.suggested_column` remain excluded
    - No `row_flag_timestamp`, `row_flag_method`, or flag notes column is added
    - Headless export path needs no special-case change beyond passing resolution_state through existing `build_export_sheets()`
  </acceptance_criteria>
  <done>Exported Curated Data contains row_flag while needs_review remains independent.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused unit tests</name>
  <files>tests/testthat/test-consensus.R, tests/testthat/test-export-import.R</files>
  <action>
Add tests covering:

- `init_resolution_state()` adds `row_flag`.
- `init_resolution_state()` preserves existing `row_flag`.
- `set_row_flag()` sets `BAD`, `FOLLOW-UP`, and `VERIFIED`.
- `set_row_flag()` clears to `NA_character_`.
- `set_row_flags()` updates multiple rows.
- Invalid flags error.
- Setting `BAD` does not change `consensus_status`.
- `build_export_sheets()` includes `row_flag`.
- `build_export_sheets()` keeps `needs_review = FALSE` for a resolved row marked `BAD`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-consensus.R'); testthat::test_file('tests/testthat/test-export-import.R')"</automated>
  </verify>
  <acceptance_criteria>
    - Focused consensus tests pass
    - Focused export/import tests pass
    - Tests explicitly prove D-01, D-02, D-03, D-08, and D-09
  </acceptance_criteria>
  <done>State and export behavior are protected by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shiny input -> resolution_state | Flag values from UI events mutate session state |
| resolution_state -> export | Human-entered flags are written to user-downloadable Excel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | row_flag mutation helpers | mitigate | Centralize validation in `normalize_row_flag()` / `set_row_flags()` and reject invalid values. |
| T-51-02 | Information Disclosure | row_flag export | accept | Flags are user-authored annotations on the user's own data and are expected export content. |
| T-51-03 | Repudiation | no flag metadata | accept | D-09 explicitly defers timestamps/method/notes; this phase only stores the current flag value. |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds.
2. `testthat::test_file('tests/testthat/test-consensus.R')` passes.
3. `testthat::test_file('tests/testthat/test-export-import.R')` passes.
4. Manual inspection confirms `needs_review` logic is unchanged.
</verification>

<success_criteria>
- FLAG-01 foundation exists: any row can hold BAD, FOLLOW-UP, VERIFIED, or unset.
- FLAG-03 foundation exists: exported Curated Data includes `row_flag`.
- `row_flag` remains separate from `needs_review` and `consensus_status`.
- No out-of-scope metadata columns are added.
</success_criteria>

<output>
After completion, create `.planning/phases/51-row-flagging/51-01-SUMMARY.md`.
</output>
