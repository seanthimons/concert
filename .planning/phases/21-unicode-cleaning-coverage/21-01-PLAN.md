---
phase: 21-unicode-cleaning-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cleaning_pipeline.R
  - data/chemical_validation_test.csv
  - tests/test_cleaning_pipeline_validation.R
autonomous: true
requirements:
  - UNIC-01
  - UNIC-02
  - UNIC-03

must_haves:
  truths:
    - "Greek alpha (U+03B1) in a chemical name is converted to plain 'alpha' by the pipeline"
    - "Prime symbol (U+2032) in a chemical name is converted to apostrophe by the pipeline"
    - "All unicode cleaning tests pass without dot-notation format errors"
  artifacts:
    - path: "tests/test_cleaning_pipeline.R"
      provides: "Unit and integration tests with correct expected values for unicode cleaning"
      contains: "alpha-tocopherol"
    - path: "data/chemical_validation_test.csv"
      provides: "Validation rows for unicode alpha and prime characters"
      contains: "unicode_cleaning"
    - path: "tests/test_cleaning_pipeline_validation.R"
      provides: "End-to-end validation test for unicode cleaning through full pipeline"
      contains: "unicode_cleaning"
  key_links:
    - from: "tests/test_cleaning_pipeline.R"
      to: "ComptoxR::clean_unicode"
      via: "direct function call assertions"
      pattern: "clean_unicode.*tocopherol"
    - from: "tests/test_cleaning_pipeline_validation.R"
      to: "data/chemical_validation_test.csv"
      via: "read.csv in test setup"
      pattern: "chemical_validation_test"
---

<objective>
Fix unicode cleaning test expectations to match current ComptoxR output format (plain text, not dot-notation) and add validation coverage for alpha and prime characters.

Purpose: The cleaning pipeline already handles alpha and prime correctly via ComptoxR::clean_unicode() at Step 1. The only bug is 3 test assertions expecting the old ".alpha." dot-notation format. This plan fixes the tests and adds end-to-end validation coverage.

Output: All unicode cleaning tests passing; validation CSV with unicode test rows; end-to-end validation test for unicode cleaning.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-unicode-cleaning-coverage/21-CONTEXT.md

<interfaces>
<!-- Current test file structure and the specific lines that need updating -->

From tests/test_cleaning_pipeline.R (lines 10-20, unit tests):
```r
test_that("ComptoxR::clean_unicode handles chemistry-specific unicode", {
  # Line 12: WRONG — expects ".alpha.-tocopherol", actual output is "alpha-tocopherol"
  expect_equal(ComptoxR::clean_unicode("\u03B1-tocopherol"), ".alpha.-tocopherol")
  # Line 15: WRONG — expects ".beta.-carotene", actual output is "beta-carotene"
  expect_equal(ComptoxR::clean_unicode("\u03B2-carotene"), ".beta.-carotene")
})
```

From tests/test_cleaning_pipeline.R (line 62, integration test):
```r
  # Line 62: WRONG — expects ".alpha.-tocopherol", actual output is "alpha-tocopherol"
  expect_equal(cleaned$chemical_name[2], ".alpha.-tocopherol")
```

From data/chemical_validation_test.csv (format):
```csv
chemical_name,casrn,expected_validity,issue_type
```

From tests/test_cleaning_pipeline_validation.R (pattern):
```r
# Tests use tibble::tibble() with tag_map = list(casrn = "CASRN", name = "Name")
# Then run_cleaning_pipeline() and post-pipeline steps
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix unit and integration test expectations for unicode format</name>
  <files>tests/test_cleaning_pipeline.R</files>
  <read_first>
    - tests/test_cleaning_pipeline.R (current test file with wrong expectations)
    - .planning/phases/21-unicode-cleaning-coverage/21-CONTEXT.md (confirmed actual ComptoxR output values)
  </read_first>
  <behavior>
    - Test: ComptoxR::clean_unicode("\u03B1-tocopherol") returns "alpha-tocopherol" (not ".alpha.-tocopherol")
    - Test: ComptoxR::clean_unicode("\u03B2-carotene") returns "beta-carotene" (not ".beta.-carotene")
    - Test: ComptoxR::clean_unicode("\u2032") returns "'" (prime to apostrophe)
    - Test: ComptoxR::clean_unicode("2\u2032-deoxyadenosine") returns "2'-deoxyadenosine"
    - Test: NA passthrough continues to work
    - Test: run_cleaning_pipeline integration — cleaned$chemical_name[2] is "alpha-tocopherol" (not ".alpha.-tocopherol")
  </behavior>
  <action>
    In tests/test_cleaning_pipeline.R, make these exact changes:

    1. Line 11 — Update comment from 'becomes .alpha.' to 'becomes alpha (plain text)':
       OLD: `# Test: Greek alpha becomes '.alpha.' (chemistry dot-notation)`
       NEW: `# Test: Greek alpha becomes 'alpha' (plain text, current ComptoxR format)`

    2. Line 12 — Fix expected value:
       OLD: `expect_equal(ComptoxR::clean_unicode("\u03B1-tocopherol"), ".alpha.-tocopherol")`
       NEW: `expect_equal(ComptoxR::clean_unicode("\u03B1-tocopherol"), "alpha-tocopherol")`

    3. Line 14 — Update comment:
       OLD: `# Test: Greek beta becomes '.beta.'`
       NEW: `# Test: Greek beta becomes 'beta' (plain text, current ComptoxR format)`

    4. Line 15 — Fix expected value:
       OLD: `expect_equal(ComptoxR::clean_unicode("\u03B2-carotene"), ".beta.-carotene")`
       NEW: `expect_equal(ComptoxR::clean_unicode("\u03B2-carotene"), "beta-carotene")`

    5. After line 19 (after the NA passthrough tests, before the closing `})`), add prime symbol tests:
       ```r
       # Test: Prime symbol (U+2032) becomes apostrophe
       expect_equal(ComptoxR::clean_unicode("\u2032"), "'")

       # Test: Prime in chemical context
       expect_equal(ComptoxR::clean_unicode("2\u2032-deoxyadenosine"), "2'-deoxyadenosine")
       ```

    6. Line 62 — Fix integration test expected value and comment:
       OLD: `expect_equal(cleaned$chemical_name[2], ".alpha.-tocopherol")  # Greek alpha -> chemistry dot-notation`
       NEW: `expect_equal(cleaned$chemical_name[2], "alpha-tocopherol")  # Greek alpha -> plain text (current ComptoxR format)`
  </action>
  <verify>
    <automated>Write an R temp script that sources test_cleaning_pipeline.R and runs the tests: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" /tmp/run_unicode_tests.R` — must exit 0 with all tests passing</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cleaning_pipeline.R line 12 contains `"alpha-tocopherol"` (no dots)
    - tests/test_cleaning_pipeline.R line 15 contains `"beta-carotene"` (no dots)
    - tests/test_cleaning_pipeline.R contains `expect_equal(ComptoxR::clean_unicode("\u2032"), "'")`
    - tests/test_cleaning_pipeline.R contains `2\u2032-deoxyadenosine`
    - tests/test_cleaning_pipeline.R line 62 contains `"alpha-tocopherol"` (no dots)
    - tests/test_cleaning_pipeline.R does NOT contain `.alpha.` or `.beta.` anywhere
    - Running `testthat::test_file("tests/test_cleaning_pipeline.R")` exits 0 with 0 failures
  </acceptance_criteria>
  <done>All 3 previously-failing unicode assertions now pass with plain-text format. Prime symbol test added and passing. No dot-notation references remain in test file.</done>
</task>

<task type="auto">
  <name>Task 2: Add unicode validation rows and end-to-end test</name>
  <files>data/chemical_validation_test.csv, tests/test_cleaning_pipeline_validation.R</files>
  <read_first>
    - data/chemical_validation_test.csv (current validation dataset structure and last row)
    - tests/test_cleaning_pipeline_validation.R (existing validation test patterns)
    - R/cleaning_pipeline.R lines 1395-1415 (Step 1 unicode cleaning code to understand pipeline flow)
  </read_first>
  <action>
    1. Add unicode cleaning rows to data/chemical_validation_test.csv at the end of the file.
       Append these rows (after the last existing row, which is line 181 "Manganese(VII) oxide"):

       ```
       α-tocopherol,10191-41-0,valid,unicode_cleaning
       β-carotene,7235-40-7,valid,unicode_cleaning
       2′-deoxyadenosine,958-09-8,valid,unicode_cleaning
       ```

       These use actual unicode characters (U+03B1 alpha, U+03B2 beta, U+2032 prime) in the CSV.

    2. Add a new test group to tests/test_cleaning_pipeline_validation.R after the last test_that block (after line 247).
       Add this test:

       ```r
       # Test Group 6: Unicode cleaning coverage (Phase 21)
       test_that("Validation: Unicode characters cleaned before QC runs", {
         # Create test dataset with unicode characters that must be cleaned
         test_df <- tibble::tibble(
           casrn = c("10191-41-0", "7235-40-7", "958-09-8"),
           name = c(
             "\u03B1-tocopherol",        # Greek alpha (U+03B1)
             "\u03B2-carotene",           # Greek beta (U+03B2)
             "2\u2032-deoxyadenosine"     # Prime symbol (U+2032)
           )
         )

         tag_map <- list(casrn = "CASRN", name = "Name")

         # Run pipeline — unicode cleaning is Step 1
         result <- run_cleaning_pipeline(test_df, tag_map = tag_map, reference_lists = NULL)
         cleaned <- result$cleaned_data

         # UNIC-01: Greek alpha converted to plain text "alpha"
         alpha_row <- cleaned %>% dplyr::filter(stringr::str_detect(name, "tocopherol"))
         expect_equal(nrow(alpha_row), 1)
         expect_equal(alpha_row$name, "alpha-tocopherol")
         expect_false(stringr::str_detect(alpha_row$name, "\u03B1"))

         # UNIC-02: Prime symbol converted to apostrophe
         prime_row <- cleaned %>% dplyr::filter(stringr::str_detect(name, "deoxyadenosine"))
         expect_equal(nrow(prime_row), 1)
         expect_equal(prime_row$name, "2'-deoxyadenosine")
         expect_false(stringr::str_detect(prime_row$name, "\u2032"))

         # Greek beta also converted
         beta_row <- cleaned %>% dplyr::filter(stringr::str_detect(name, "carotene"))
         expect_equal(nrow(beta_row), 1)
         expect_equal(beta_row$name, "beta-carotene")
         expect_false(stringr::str_detect(beta_row$name, "\u03B2"))
       })
       ```
  </action>
  <verify>
    <automated>Write an R temp script that runs test_cleaning_pipeline_validation.R: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" /tmp/run_validation_tests.R` — must exit 0 with all tests passing including the new unicode test group</automated>
  </verify>
  <acceptance_criteria>
    - data/chemical_validation_test.csv contains a row with `α-tocopherol` and `issue_type` value `unicode_cleaning`
    - data/chemical_validation_test.csv contains a row with `2′-deoxyadenosine` and `issue_type` value `unicode_cleaning`
    - data/chemical_validation_test.csv contains a row with `β-carotene` and `issue_type` value `unicode_cleaning`
    - tests/test_cleaning_pipeline_validation.R contains `test_that("Validation: Unicode characters cleaned before QC runs"`
    - tests/test_cleaning_pipeline_validation.R contains `expect_equal(alpha_row$name, "alpha-tocopherol")`
    - tests/test_cleaning_pipeline_validation.R contains `expect_equal(prime_row$name, "2'-deoxyadenosine")`
    - Running `testthat::test_file("tests/test_cleaning_pipeline_validation.R")` exits 0 with 0 failures
  </acceptance_criteria>
  <done>Validation CSV has 3 unicode cleaning rows. End-to-end test proves alpha, beta, and prime are cleaned by the pipeline before QC. All validation tests pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run full test suite: `testthat::test_dir("tests")` — all tests pass with 0 failures
2. Confirm no `.alpha.` or `.beta.` dot-notation remains in any test file
3. Confirm data/chemical_validation_test.csv has unicode_cleaning rows
</verification>

<success_criteria>
- UNIC-01: test_cleaning_pipeline.R asserts alpha-tocopherol (not .alpha.-tocopherol) and validation test proves alpha cleaned through full pipeline
- UNIC-02: test_cleaning_pipeline.R has prime symbol test; validation test proves prime cleaned through full pipeline
- UNIC-03: Zero dot-notation format references remain in test files; all tests pass
- Full test suite (`testthat::test_dir("tests")`) exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/21-unicode-cleaning-coverage/21-01-SUMMARY.md`
</output>
