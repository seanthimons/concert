---
phase: 50-auto-resolve-suggest
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - R/consensus.R
  - tests/testthat/test-consensus.R
autonomous: true
requirements:
  - SCORE-03
  - SCORE-04
user_setup: []

must_haves:
  truths:
    - "classify_auto_resolve() sets consensus_status to 'auto_resolved' when score >= 0.95 AND gap >= 0.15"
    - "classify_auto_resolve() sets consensus_status to 'suggested' when score >= 0.70 but below auto-resolve threshold or gap < 0.15"
    - "classify_auto_resolve() leaves 'disagree' rows with best score < 0.70 unchanged"
    - "init_resolution_state() adds .resolution_method and .resolution_reason columns"
    - "accept_all_suggestions() resolves all non-pinned suggested rows to their best-scoring candidate"
    - "resolve_row() accepts 'auto_resolved' and 'suggested' statuses for override flow"
    - "get_resolution_options() returns candidates for 'auto_resolved' and 'suggested' rows"
    - "recalc_consensus_summary() counts n_auto_resolved and n_suggested"
  artifacts:
    - path: "R/consensus.R"
      provides: "classify_auto_resolve, accept_all_suggestions, extended init_resolution_state/resolve_row/get_resolution_options/recalc_consensus_summary"
      exports: ["classify_auto_resolve", "accept_all_suggestions"]
    - path: "tests/testthat/test-consensus.R"
      provides: "Tests for all new and extended functions"
  key_links:
    - from: "R/consensus.R::classify_auto_resolve"
      to: "R/consensus.R::compute_similarity_scores"
      via: "runs after scoring, reads similarity_score and per-candidate scores"
      pattern: "classify_auto_resolve"
    - from: "R/consensus.R::accept_all_suggestions"
      to: "R/consensus.R::resolve_row"
      via: "calls resolve_row for each suggested row"
      pattern: "accept_all_suggestions"
---

<objective>
Build the backend classification and resolution functions for auto-resolve and suggest.

Purpose: Phase 49 added similarity scores to disagree rows. This plan adds the logic that consumes those scores to classify rows as auto_resolved (clear winner) or suggested (likely best match), plus the bulk-accept function for suggested rows. All downstream UI wiring depends on these functions.

Output: Extended `R/consensus.R` with 2 new exported functions and 4 extended functions, plus comprehensive tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-auto-resolve-suggest/50-CONTEXT.md
@.planning/phases/50-auto-resolve-suggest/50-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/consensus.R — functions to extend:

```r
# Line 165-173: init_resolution_state() — add .resolution_method, .resolution_reason
init_resolution_state <- function(df) {
  if (!".pinned" %in% names(df)) df$.pinned <- FALSE
  if (!".manual_entry" %in% names(df)) df$.manual_entry <- FALSE
  df
}

# Line 190-191: get_resolution_options() — currently rejects non-"disagree"
get_resolution_options <- function(df, row_idx, dtxsid_cols, enrichment_cache = NULL) {
  if (df$consensus_status[row_idx] != "disagree") return(list())
  ...
}

# Line 395-421: resolve_row() — currently rejects non-"disagree"
resolve_row <- function(df, row_idx, chosen_column, dtxsid_cols) {
  if (df$consensus_status[row_idx] != "disagree") {
    stop("Row ", row_idx, " is not a disagree row ...")
  }
  ...
  df$consensus_dtxsid[row_idx] <- val
  df$consensus_source[row_idx] <- sub("^dtxsid_", "", chosen_column)
  df$.pinned[row_idx] <- TRUE
  df
}

# Line 434-458: apply_priority_chain() — pattern for accept_all_suggestions
apply_priority_chain <- function(df, priority_order, dtxsid_cols) {
  df <- init_resolution_state(df)
  for (i in seq_len(nrow(df))) {
    if (df$consensus_status[i] != "disagree") next
    if (isTRUE(df$.pinned[i])) next
    ...
  }
  df
}

# Line 313-381: compute_similarity_scores() — provides similarity_score column
# classify_auto_resolve() runs AFTER this, reading per-candidate scores

# From R/mod_review_results.R line 5-16:
recalc_consensus_summary <- function(df) {
  list(
    n_agree = sum(df$consensus_status == "agree", na.rm = TRUE),
    n_disagree = sum(df$consensus_status == "disagree" & !isTRUE(df$.pinned), na.rm = TRUE),
    n_agree_caveat = ..., n_single = ..., n_error = ...,
    n_manual = ..., n_unresolvable = ..., n_wqx = ...
  )
}
```

From R/consensus.R — score_one_candidate() signature (unchanged, reused):
```r
score_one_candidate <- function(input_name, preferred_name, synonyms_str, rank)
# Returns numeric in [0, 1] or NA_real_
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend existing functions and add classify_auto_resolve()</name>
  <files>R/consensus.R</files>
  <read_first>
    - R/consensus.R (full file — current function signatures, patterns, line positions)
    - .planning/phases/50-auto-resolve-suggest/50-CONTEXT.md (D-01 through D-15 decisions)
    - .planning/phases/50-auto-resolve-suggest/50-PATTERNS.md (exact code patterns to follow)
  </read_first>
  <action>
**1. Extend `init_resolution_state()` (after existing `.manual_entry` guard):**

Add two new column guards following the identical `if (!".col" %in% names(df))` pattern:

```r
if (!".resolution_method" %in% names(df)) {
  df$.resolution_method <- NA_character_
}
if (!".resolution_reason" %in% names(df)) {
  df$.resolution_reason <- NA_character_
}
```

Valid `.resolution_method` values: `"auto"`, `"suggested-accept"`, `"bulk-accept"`, `"manual"`, `NA` (per D-11).

**2. Extend `resolve_row()` status validation (line ~398):**

Change the status guard from:
```r
if (df$consensus_status[row_idx] != "disagree") {
  stop("Row ", row_idx, " is not a disagree row ...")
}
```
To:
```r
allowed_statuses <- c("disagree", "auto_resolved", "suggested")
if (!df$consensus_status[row_idx] %in% allowed_statuses) {
  stop("Row ", row_idx, " cannot be resolved (status: ", df$consensus_status[row_idx], ")")
}
```

This enables D-14 (override auto-resolution) and D-08 (manual pick from suggested row).

After the existing `.pinned <- TRUE` assignment (line ~418), add:
```r
df$.resolution_method[row_idx] <- "manual"
```

This ensures any manual pick through resolve_row sets the method to "manual" (per D-14).

**3. Extend `get_resolution_options()` status guard (line ~191):**

Change from:
```r
if (df$consensus_status[row_idx] != "disagree") {
  return(list())
}
```
To:
```r
allowed_statuses <- c("disagree", "auto_resolved", "suggested")
if (!df$consensus_status[row_idx] %in% allowed_statuses) {
  return(list())
}
```

**4. Create `classify_auto_resolve()` — new exported function:**

Place after `compute_similarity_scores()` (after line ~381). Add section header comment matching existing style. The function signature:

```r
#' Classify disagree rows as auto-resolved, suggested, or leave as disagree
#'
#' Runs after compute_similarity_scores(). For each disagree row, collects
#' per-candidate scores and applies threshold logic (D-01/D-02/D-03):
#'   - Auto-resolve: best score >= 0.95 AND gap >= 0.15
#'   - Suggest: best score >= 0.70 (but not auto-resolve eligible)
#'   - Leave as disagree: best score < 0.70
#'
#' @param resolution_state Data frame with consensus_status, similarity_score, dtxsid_*, preferredName_*, rank_* columns
#' @param enrichment_cache Tibble with dtxsid and (optionally) synonyms columns
#' @param dtxsid_cols Character vector of dtxsid column names
#' @param column_tags Named list (col_name -> "Name"|"CASRN"|"Other")
#' @param auto_threshold Numeric, minimum score for auto-resolve (default 0.95, per D-01)
#' @param gap_threshold Numeric, minimum gap between best and second-best (default 0.15, per D-03)
#' @param suggest_threshold Numeric, minimum score for suggestion (default 0.70, per D-02)
#' @return resolution_state with updated consensus_status, .pinned, .resolution_method, .resolution_reason, and .suggested_column columns
#' @export
classify_auto_resolve <- function(
  resolution_state,
  enrichment_cache,
  dtxsid_cols,
  column_tags,
  auto_threshold = 0.95,
  gap_threshold = 0.15,
  suggest_threshold = 0.70
) {
```

Implementation logic:

a. Call `init_resolution_state(resolution_state)` at the top.
b. Add `.suggested_column <- NA_character_` column if not present (stores which dtxsid column is the best candidate, needed by accept_all_suggestions).
c. Find Name-tagged column from `column_tags` (same pattern as `compute_similarity_scores()`).
d. Get `disagree_idx <- which(resolution_state$consensus_status == "disagree" & !isTRUE(resolution_state$.pinned))`.
e. If no disagree rows or no Name column, return early.
f. Build synonym lookup (same `stats::setNames` pattern as compute_similarity_scores).
g. Loop over `disagree_idx`. For each row `i`:
   - Get `input_name` from the Name column.
   - Collect per-candidate scores: loop over `dtxsid_cols`, call `score_one_candidate()` for each non-NA DTXSID. Store as named numeric vector `candidate_scores` (names = column names).
   - If no valid scores, `next`.
   - Sort scores descending. `best_score <- candidate_scores[1]`, `best_col <- names(candidate_scores)[1]`.
   - `second_best <- if (length(candidate_scores) >= 2) candidate_scores[2] else 0.0`.
   - `gap <- best_score - second_best`.
   - Classification:
     - If `best_score >= auto_threshold && gap >= gap_threshold`: set `consensus_status[i] <- "auto_resolved"`, `.pinned[i] <- TRUE`, `.resolution_method[i] <- "auto"`, `.resolution_reason[i] <- sprintf("score=%.2f, gap=%.2f, threshold=%.2f", best_score, gap, auto_threshold)`. Also set `consensus_dtxsid[i]` and `consensus_source[i]` from best_col (same pattern as resolve_row). Set `.suggested_column[i] <- best_col`.
     - Else if `best_score >= suggest_threshold`: set `consensus_status[i] <- "suggested"`, `.resolution_method[i] <- NA_character_` (not yet resolved), `.resolution_reason[i] <- sprintf("score=%.2f, gap=%.2f, suggest_threshold=%.2f", best_score, gap, suggest_threshold)`. Set `.suggested_column[i] <- best_col`. Do NOT set .pinned (user must act).
     - Else: leave as "disagree" (no change).
h. Return `resolution_state`.

**5. Create `accept_all_suggestions()` — new exported function:**

Place after `classify_auto_resolve()`. Following the `apply_priority_chain()` pattern exactly (per PATTERNS.md):

```r
#' Accept all suggested resolutions in bulk
#'
#' For each row with consensus_status == "suggested" that is not pinned,
#' resolves to the best-scoring candidate (stored in .suggested_column).
#' Sets .resolution_method to "bulk-accept" and .pinned to TRUE.
#'
#' @param df Data frame with consensus_status, .suggested_column, dtxsid_* columns
#' @param dtxsid_cols Character vector of DTXSID column names
#' @return Modified df with suggested rows resolved
#' @export
accept_all_suggestions <- function(df, dtxsid_cols) {
  df <- init_resolution_state(df)
  
  for (i in seq_len(nrow(df))) {
    if (df$consensus_status[i] != "suggested") next
    if (isTRUE(df$.pinned[i])) next
    
    best_col <- df$.suggested_column[i]
    if (is.na(best_col) || !best_col %in% dtxsid_cols) next
    
    val <- df[[best_col]][i]
    if (is.na(val)) next
    
    df$consensus_dtxsid[i] <- val
    df$consensus_source[i] <- sub("^dtxsid_", "", best_col)
    df$.pinned[i] <- TRUE
    df$.resolution_method[i] <- "bulk-accept"
    # Preserve existing .resolution_reason from classification
  }
  
  df
}
```

Note: `accept_all_suggestions` does NOT change `consensus_status` from "suggested" to "agree" — it stays "suggested" but gets pinned with a consensus_dtxsid. The UI will show it as resolved via the .pinned + consensus_dtxsid state. (This matches how "disagree" rows resolved via priority chain still show "disagree" status but with pinned DTXSID.)

**6. Move and extend `recalc_consensus_summary()` from `mod_review_results.R` to `consensus.R`:**

Actually, do NOT move it — it's a module-internal helper in mod_review_results.R. Instead, just note that it needs extending in Plan 02. Skip this sub-task.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n'); stopifnot(is.function(classify_auto_resolve)); stopifnot(is.function(accept_all_suggestions)); cat('exports OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/consensus.R contains `classify_auto_resolve <- function(`
    - R/consensus.R contains `accept_all_suggestions <- function(`
    - R/consensus.R `init_resolution_state` contains `".resolution_method"` and `".resolution_reason"` guards
    - R/consensus.R `resolve_row` contains `allowed_statuses <- c("disagree", "auto_resolved", "suggested")`
    - R/consensus.R `resolve_row` contains `df$.resolution_method[row_idx] <- "manual"`
    - R/consensus.R `get_resolution_options` contains `c("disagree", "auto_resolved", "suggested")`
    - R/consensus.R `classify_auto_resolve` contains `auto_threshold = 0.95`
    - R/consensus.R `classify_auto_resolve` contains `gap_threshold = 0.15`
    - R/consensus.R `classify_auto_resolve` contains `suggest_threshold = 0.70`
    - R/consensus.R `classify_auto_resolve` contains `.suggested_column`
    - R/consensus.R `accept_all_suggestions` contains `"bulk-accept"`
    - `devtools::load_all()` succeeds without errors
  </acceptance_criteria>
  <done>All 6 functions exist with correct signatures, load_all succeeds, exports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for classify_auto_resolve() and accept_all_suggestions()</name>
  <files>tests/testthat/test-consensus.R</files>
  <read_first>
    - tests/testthat/test-consensus.R (current test patterns, test data fixtures)
    - R/consensus.R (after Task 1 modifications — function signatures)
  </read_first>
  <action>
Append test blocks to the end of `tests/testthat/test-consensus.R`. Use the existing Silica/Estradiol disagree fixture pattern from the `compute_similarity_scores` tests (line ~870-912).

**Test fixture helper** (place before tests):

```r
# Helper: build a 3-row disagree fixture for auto-resolve/suggest tests
build_auto_resolve_fixture <- function() {
  # Row 1: Clear mismatch — "Silica" input, Silica candidate (high score) vs Estradiol candidate (low score)
  # Row 2: Ambiguous — "Acetone" input, Acetone candidate (0.85ish) vs Aceton candidate (0.80ish) — gap < 0.15
  # Row 3: Low score — "XYZ Unknown" input, unrelated candidates
  df <- data.frame(
    Chemical = c("Silica", "Acetone", "XYZ Unknown Compound"),
    dtxsid_Chemical = c("DTXSID5000001", "DTXSID1020001", "DTXSID0000099"),
    dtxsid_CAS = c("DTXSID9000001", "DTXSID7021360", "DTXSID0000088"),
    preferredName_Chemical = c("Silica", "Acetone", "Benzene"),
    preferredName_CAS = c("Estradiol", "Toluene", "Xylene"),
    rank_Chemical = c(1L, 1L, 5L),
    rank_CAS = c(2L, 2L, 5L),
    stringsAsFactors = FALSE
  )
  dtxsid_cols <- c("dtxsid_Chemical", "dtxsid_CAS")
  df <- classify_consensus(df, dtxsid_cols)
  enrichment_cache <- tibble::tibble(
    dtxsid = c("DTXSID5000001", "DTXSID9000001", "DTXSID1020001", "DTXSID7021360", "DTXSID0000099", "DTXSID0000088"),
    synonyms = c("Silica gel|Silica|Quartz", NA_character_, "Propan-2-one|Acetone", "Methylbenzene|Toluene", NA_character_, NA_character_),
    casrn = rep(NA_character_, 6),
    molecular_formula = rep(NA_character_, 6),
    molecular_weight = rep(NA_real_, 6)
  )
  column_tags <- list(Chemical = "Name")
  df <- compute_similarity_scores(df, enrichment_cache, dtxsid_cols, column_tags)
  list(df = df, enrichment_cache = enrichment_cache, dtxsid_cols = dtxsid_cols, column_tags = column_tags)
}
```

**Tests to write (each as its own `test_that` block):**

1. `"classify_auto_resolve: high-score + high-gap row becomes auto_resolved"` — Row 1 (Silica vs Estradiol): expect `consensus_status == "auto_resolved"`, `.pinned == TRUE`, `.resolution_method == "auto"`, `.resolution_reason` contains `"score="`, `consensus_dtxsid == "DTXSID5000001"`.

2. `"classify_auto_resolve: high-score + low-gap row becomes suggested"` — Row 2 (Acetone with similar candidates): expect `consensus_status == "suggested"`, `.pinned == FALSE`, `.resolution_method == NA_character_`, `.suggested_column` is not NA.

3. `"classify_auto_resolve: low-score row stays disagree"` — Row 3 (XYZ Unknown): expect `consensus_status == "disagree"`.

4. `"classify_auto_resolve: score >= 0.95 but gap < 0.15 becomes suggested not auto_resolved"` — Construct a fixture where both candidates score >= 0.95 (e.g., input "Silica" with candidates "Silica" and "Silica gel"). Verify status is "suggested", not "auto_resolved" (per D-03).

5. `"classify_auto_resolve: pinned disagree rows are skipped"` — Set `.pinned[1] <- TRUE` before calling. Expect row 1 status unchanged ("disagree").

6. `"classify_auto_resolve: non-disagree rows are skipped"` — Verify agree/single/error rows are untouched.

7. `"classify_auto_resolve: init_resolution_state adds .resolution_method and .resolution_reason"` — Call init_resolution_state on a plain df. Verify both columns exist and are NA_character_.

8. `"accept_all_suggestions: resolves all suggested rows"` — Use fixture, run classify_auto_resolve, then accept_all_suggestions. Verify all previously-suggested rows now have `.pinned == TRUE`, `.resolution_method == "bulk-accept"`, non-NA `consensus_dtxsid`.

9. `"accept_all_suggestions: skips pinned rows"` — Pin a suggested row before calling. Verify it's unchanged.

10. `"resolve_row: accepts auto_resolved status for override"` — Create an auto_resolved row, call resolve_row with a different column. Verify `.resolution_method == "manual"`.

11. `"resolve_row: accepts suggested status for manual pick"` — Create a suggested row, call resolve_row. Verify `.resolution_method == "manual"`.

12. `"get_resolution_options: returns candidates for auto_resolved rows"` — Verify non-empty list returned.

13. `"get_resolution_options: returns candidates for suggested rows"` — Verify non-empty list returned.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); testthat::test_file('tests/testthat/test-consensus.R')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-consensus.R contains `"classify_auto_resolve: high-score + high-gap row becomes auto_resolved"`
    - tests/testthat/test-consensus.R contains `"classify_auto_resolve: high-score + low-gap row becomes suggested"`
    - tests/testthat/test-consensus.R contains `"classify_auto_resolve: low-score row stays disagree"`
    - tests/testthat/test-consensus.R contains `"accept_all_suggestions: resolves all suggested rows"`
    - tests/testthat/test-consensus.R contains `"resolve_row: accepts auto_resolved status for override"`
    - tests/testthat/test-consensus.R contains `"get_resolution_options: returns candidates for auto_resolved rows"`
    - All new tests pass (exit code 0 from test_file)
    - No regressions in existing tests (pre-existing failure count unchanged)
  </acceptance_criteria>
  <done>13 new test blocks pass. No regressions in existing consensus tests. All threshold logic (D-01, D-02, D-03) and status transitions verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User data -> classification | Similarity scores derived from user-uploaded chemical names compared against CompTox data |
| Threshold parameters -> classification | Auto-resolve and suggest thresholds control how rows are classified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | classify_auto_resolve thresholds | mitigate | Thresholds are function parameters with safe defaults (0.95/0.15/0.70); not user-controllable from UI. Only code changes can alter them. |
| T-50-02 | Information Disclosure | .resolution_reason column | accept | Contains only numeric scores and threshold values (no user PII or chemical data beyond what's already in the row). Exported to Excel which is user's own data. |
| T-50-03 | Denial of Service | classify_auto_resolve loop | accept | Loop bounded by nrow(disagree_idx); same O(n*k) as existing compute_similarity_scores(). No amplification risk. |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds
2. `testthat::test_file('tests/testthat/test-consensus.R')` passes all new tests
3. `classify_auto_resolve` and `accept_all_suggestions` are exported and callable
4. `init_resolution_state()` adds `.resolution_method` and `.resolution_reason`
</verification>

<success_criteria>
- classify_auto_resolve() correctly classifies disagree rows into auto_resolved/suggested/disagree per D-01/D-02/D-03 thresholds
- accept_all_suggestions() bulk-resolves non-pinned suggested rows per D-09/D-10
- resolve_row() and get_resolution_options() accept auto_resolved/suggested statuses for D-14/D-08 override flow
- All 13 new tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/50-auto-resolve-suggest/50-01-SUMMARY.md`
</output>
