---
phase: 50-auto-resolve-suggest
plan: "03"
type: execute
wave: 3
depends_on: ["50-02"]
files_modified:
  - R/export_helpers.R
autonomous: false
requirements:
  - SCORE-03
  - SCORE-04
user_setup: []

must_haves:
  truths:
    - ".resolution_method and .resolution_reason columns appear in exported Excel Curated Data sheet"
    - "Export Summary sheet includes Auto-Resolved and Suggested counts"
    - "Shiny app starts without errors after all Phase 50 changes"
  artifacts:
    - path: "R/export_helpers.R"
      provides: ".resolution_method/.resolution_reason column positioning and summary counts"
      contains: "Auto-Resolved"
  key_links:
    - from: "R/export_helpers.R"
      to: "R/consensus.R::init_resolution_state"
      via: ".resolution_method and .resolution_reason flow through export"
      pattern: "resolution_method"
---

<objective>
Extend export to include resolution audit columns and new summary counts, then verify the app starts cleanly.

Purpose: Users need .resolution_method and .resolution_reason in their exported Excel to audit how each row was resolved (per D-12). The smoke test catches any startup regressions from all Phase 50 changes.

Output: Modified `R/export_helpers.R` with column positioning and summary counts, plus verified clean app startup.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/50-auto-resolve-suggest/50-CONTEXT.md
@.planning/phases/50-auto-resolve-suggest/50-PATTERNS.md
@.planning/phases/50-auto-resolve-suggest/50-01-SUMMARY.md
@.planning/phases/50-auto-resolve-suggest/50-02-SUMMARY.md

<interfaces>
<!-- From R/export_helpers.R — current export pattern -->

```r
# Sheet 2: Curated Data — .pinned and .manual_entry excluded, similarity_score flows through
curated_data_sheet <- resolution_state %>%
  dplyr::mutate(needs_review = (consensus_status %in% c("error", "unresolvable"))) %>%
  dplyr::select(-tidyselect::any_of(c(".pinned", ".manual_entry")))

# Sheet 3: Summary — Metric/Value tibble with consensus counts
summary_sheet <- tibble::tibble(
  Metric = c("Total Rows", "Consensus - Agree", "Consensus - Disagree", ...),
  Value = c(nrow(resolution_state), consensus_summary$n_agree, ...)
)
```

<!-- New columns from Phase 50 (Plan 01) -->
# .resolution_method: "auto"|"suggested-accept"|"bulk-accept"|"manual"|NA
# .resolution_reason: "score=0.98, gap=0.45, threshold=0.95" or NA
# .suggested_column: internal use only — should be EXCLUDED from export
# .pinned and .manual_entry already excluded
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend export with resolution audit columns and summary counts</name>
  <files>R/export_helpers.R</files>
  <read_first>
    - R/export_helpers.R (full file — current Sheet 2 and Sheet 3 logic)
    - .planning/phases/50-auto-resolve-suggest/50-CONTEXT.md (D-11, D-12, D-13)
    - .planning/phases/50-auto-resolve-suggest/50-PATTERNS.md (export pattern section)
  </read_first>
  <action>
**Sheet 2 (Curated Data) changes:**

1. Add `.suggested_column` to the exclusion list (internal column, not for export). Change:
```r
dplyr::select(-tidyselect::any_of(c(".pinned", ".manual_entry")))
```
To:
```r
dplyr::select(-tidyselect::any_of(c(".pinned", ".manual_entry", ".suggested_column")))
```

2. After the exclusion line, add column repositioning so `.resolution_method` and `.resolution_reason` appear after `consensus_source`:
```r
curated_data_sheet <- curated_data_sheet %>%
  dplyr::relocate(
    tidyselect::any_of(c(".resolution_method", ".resolution_reason")),
    .after = tidyselect::any_of("consensus_source")
  )
```

3. Update the comment above the exclusion line to mention the new columns:
```r
# Note: similarity_score, .resolution_method, .resolution_reason flow through automatically.
# .pinned, .manual_entry, .suggested_column are internal state -- excluded from export.
```

**Sheet 3 (Summary) changes:**

Add two new rows to the Metric/Value tibble after "Consensus - Unresolvable":
```r
"Consensus - Auto-Resolved",
"Consensus - Suggested",
```
With values:
```r
sum(resolution_state$consensus_status == "auto_resolved", na.rm = TRUE),
sum(resolution_state$consensus_status == "suggested", na.rm = TRUE),
```

Insert these BEFORE the "Match Rate (%)" row so the match rate stays last.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/export_helpers.R contains `".suggested_column"` in the select exclusion list
    - R/export_helpers.R contains `dplyr::relocate(` with `.resolution_method`
    - R/export_helpers.R contains `"Consensus - Auto-Resolved"`
    - R/export_helpers.R contains `"Consensus - Suggested"`
    - R/export_helpers.R does NOT contain `.resolution_method` in the exclusion list (it should flow through)
    - `devtools::load_all()` succeeds
  </acceptance_criteria>
  <done>Export excludes .suggested_column, positions .resolution_method/.resolution_reason after consensus_source, and summary sheet includes Auto-Resolved and Suggested counts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test and end-to-end visual verification</name>
  <files>R/consensus.R, R/mod_run_curation.R, R/mod_review_results.R, R/export_helpers.R</files>
  <action>
Run the Shiny cold boot test to verify all Phase 50 changes work together:

1. Start the app: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); concert::run_app(port=3838, launch.browser=FALSE)"`
2. Verify "Listening on http://127.0.0.1:3838" appears with no errors
3. Present verification steps to user for manual UAT

Phase 50 changes across 4 files:
- R/consensus.R: classify_auto_resolve(), accept_all_suggestions(), extended init_resolution_state/resolve_row/get_resolution_options
- R/mod_run_curation.R: Pipeline wiring for classify_auto_resolve
- R/mod_review_results.R: Status chips, modal suggestions, bulk accept, value boxes
- R/export_helpers.R: Resolution audit columns in export, summary counts
  </action>
  <verify>
User verifies:
1. App starts — "Listening on http://127.0.0.1:3838" with no errors
2. Upload a test CSV with chemical names that produce disagree rows
3. Run curation — auto-resolved rows show blue "auto_resolved" chip, suggested rows show cyan "suggested" chip
4. Value boxes show Auto-Resolved and Suggested counts
5. Click a suggested row's "Review Suggestion" button — modal highlights suggested candidate with blue border and "Suggested" badge
6. "Accept Suggestion" button appears in modal footer for suggested rows
7. "Accept All Suggestions" button appears in action bar when suggested rows exist
8. Download Excel — .resolution_method and .resolution_reason columns appear in Curated Data sheet
  </verify>
  <done>App starts cleanly. User confirms auto-resolve chips, suggestion modal, bulk accept, and export columns all work correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resolution_state -> Excel export | .resolution_method and .resolution_reason written to user-downloadable file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Information Disclosure | .resolution_reason in export | accept | Contains only numeric scores and threshold values from the user's own data. No secrets or PII. User downloads their own curated data. |
| T-50-08 | Tampering | .suggested_column exclusion | mitigate | Internal column excluded from export via dplyr::select(-any_of(...)) — users cannot see or modify the suggestion column reference |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds
2. Shiny app starts: "Listening on http://127.0.0.1:3838"
3. Export includes .resolution_method and .resolution_reason columns
4. Export summary includes Auto-Resolved and Suggested counts
5. All unit tests pass (no regressions)
</verification>

<success_criteria>
- .resolution_method and .resolution_reason appear in exported Curated Data sheet positioned after consensus_source (per D-12)
- .suggested_column is NOT in the exported file
- Summary sheet includes "Consensus - Auto-Resolved" and "Consensus - Suggested" rows
- App starts cleanly with no errors
- User can visually verify the full auto-resolve and suggest flow end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/50-auto-resolve-suggest/50-03-SUMMARY.md`
</output>
