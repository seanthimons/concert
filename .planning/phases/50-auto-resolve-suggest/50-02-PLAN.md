---
phase: 50-auto-resolve-suggest
plan: "02"
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - R/mod_run_curation.R
  - R/mod_review_results.R
autonomous: true
requirements:
  - SCORE-03
  - SCORE-04
user_setup: []

must_haves:
  truths:
    - "classify_auto_resolve() runs in the curation pipeline after compute_similarity_scores()"
    - "AUTO-RESOLVED rows show a blue #0D6EFD chip in the status column"
    - "SUGGESTED rows show a cyan #0DCAF0 chip in the status column"
    - "Auto-resolved rows show checkmark + DTXSID + auto badge + Compare button in Resolution column"
    - "Suggested rows show a 'Review Suggestion' button in Resolution column"
    - "Comparison modal highlights the suggested candidate card with blue border and 'Suggested' badge"
    - "Comparison modal for suggested rows has an 'Accept Suggestion' button"
    - "Comparison modal for auto_resolved rows highlights the auto-selected card with amber border and 'Auto-Selected' badge"
    - "Accept All Suggestions bulk button appears when suggested rows exist"
    - "Value boxes show Auto-Resolved and Suggested counts"
    - "recalc_consensus_summary includes n_auto_resolved and n_suggested"
  artifacts:
    - path: "R/mod_run_curation.R"
      provides: "Pipeline wiring for classify_auto_resolve after scoring"
      contains: "classify_auto_resolve("
    - path: "R/mod_review_results.R"
      provides: "Status chips, modal changes, bulk accept, value boxes"
      contains: "auto_resolved"
  key_links:
    - from: "R/mod_run_curation.R"
      to: "R/consensus.R::classify_auto_resolve"
      via: "function call after compute_similarity_scores"
      pattern: "classify_auto_resolve"
    - from: "R/mod_review_results.R"
      to: "R/consensus.R::accept_all_suggestions"
      via: "observeEvent handler for bulk accept button"
      pattern: "accept_all_suggestions"
---

<objective>
Wire auto-resolve classification into the curation pipeline and implement all UI changes in the Review Results module.

Purpose: Users see auto-resolved and suggested rows visually distinguished, can accept suggestions individually via modal or in bulk, and can override auto-resolutions. This is the user-facing delivery of SCORE-03 and SCORE-04.

Output: Modified `R/mod_run_curation.R` (pipeline wiring) and `R/mod_review_results.R` (full UI integration).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-auto-resolve-suggest/50-CONTEXT.md
@.planning/phases/50-auto-resolve-suggest/50-PATTERNS.md
@.planning/phases/50-auto-resolve-suggest/50-UI-SPEC.md
@.planning/phases/50-auto-resolve-suggest/50-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — new functions available in R/consensus.R -->

```r
classify_auto_resolve <- function(
  resolution_state, enrichment_cache, dtxsid_cols, column_tags,
  auto_threshold = 0.95, gap_threshold = 0.15, suggest_threshold = 0.70
)
# Returns resolution_state with consensus_status updated to "auto_resolved"/"suggested",
# .pinned, .resolution_method, .resolution_reason, .suggested_column set

accept_all_suggestions <- function(df, dtxsid_cols)
# Resolves all non-pinned "suggested" rows to their .suggested_column candidate
# Sets .resolution_method = "bulk-accept", .pinned = TRUE
```

<!-- From R/mod_run_curation.R — compute_similarity_scores call site (lines ~266-276) -->
```r
# Inside enrichment tryCatch, after enrich_synonyms:
if (!is.null(data_store$resolution_state) &&
    !is.null(data_store$dtxsid_cols) &&
    !is.null(data_store$column_tags)) {
  data_store$resolution_state <- compute_similarity_scores(
    resolution_state = data_store$resolution_state,
    enrichment_cache = data_store$enrichment_cache,
    dtxsid_cols = data_store$dtxsid_cols,
    column_tags = data_store$column_tags
  )
}
```

<!-- From R/mod_review_results.R — key patterns (line references) -->
# recalc_consensus_summary: lines 5-16
# derive_resolution_html: lines 49-170
# mod_review_results_ui: line 205+
# compare_js: lines 275-307
# action buttons area: lines 386-430
# curation_stats value boxes: lines 492-536
# status_levels + status_colors: lines 868-903
# row_bg_colors: lines 965-974
# compare modal builder: lines 1184-1343
# modal_confirm handler: lines 1350-1387
# apply_priority handler: lines 1528-1575
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire classify_auto_resolve into curation pipeline</name>
  <files>R/mod_run_curation.R</files>
  <read_first>
    - R/mod_run_curation.R (full file — find compute_similarity_scores call site)
    - R/consensus.R (classify_auto_resolve signature from Plan 01)
  </read_first>
  <action>
In `R/mod_run_curation.R`, immediately after the `compute_similarity_scores()` call block (after the line `data_store$resolution_state <- compute_similarity_scores(...)`), add a new block inside the same guard and tryCatch:

```r
# Phase 50: Classify auto-resolved and suggested rows
data_store$resolution_state <- classify_auto_resolve(
  resolution_state = data_store$resolution_state,
  enrichment_cache = data_store$enrichment_cache,
  dtxsid_cols = data_store$dtxsid_cols,
  column_tags = data_store$column_tags
)
```

This runs inside the existing enrichment tryCatch, so failures are non-fatal (same pattern as scoring). Add a message log:
```r
n_auto <- sum(data_store$resolution_state$consensus_status == "auto_resolved", na.rm = TRUE)
n_sugg <- sum(data_store$resolution_state$consensus_status == "suggested", na.rm = TRUE)
message(sprintf("[auto-resolve] %d auto-resolved, %d suggested", n_auto, n_sugg))
```

After the tryCatch block, update the consensus summary to reflect new statuses:
```r
data_store$consensus_summary <- recalc_consensus_summary(data_store$resolution_state)
```

(Check if this line already exists after the tryCatch — if so, it will now pick up the new counts automatically once recalc_consensus_summary is extended in Task 2.)
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_run_curation.R contains `classify_auto_resolve(`
    - R/mod_run_curation.R contains `"[auto-resolve]"`
    - The classify_auto_resolve call appears AFTER the compute_similarity_scores call
    - `devtools::load_all()` succeeds
  </acceptance_criteria>
  <done>classify_auto_resolve wired into pipeline immediately after scoring, inside existing tryCatch error handler.</done>
</task>

<task type="auto">
  <name>Task 2: Update mod_review_results.R — summary, chips, resolution HTML, modal, bulk accept</name>
  <files>R/mod_review_results.R</files>
  <read_first>
    - R/mod_review_results.R (full file — all sections referenced below)
    - .planning/phases/50-auto-resolve-suggest/50-UI-SPEC.md (visual specs, colors, copy)
    - .planning/phases/50-auto-resolve-suggest/50-PATTERNS.md (exact code patterns)
    - .planning/phases/50-auto-resolve-suggest/50-CONTEXT.md (D-04 through D-15)
    - R/consensus.R (accept_all_suggestions signature)
  </read_first>
  <action>
This task modifies `R/mod_review_results.R` in 8 specific locations. Read the full file first, then apply each change.

**Change 1: Extend `recalc_consensus_summary()` (lines 5-16)**

Add two new entries to the returned list:
```r
n_auto_resolved = sum(df$consensus_status == "auto_resolved", na.rm = TRUE),
n_suggested = sum(df$consensus_status == "suggested", na.rm = TRUE)
```

**Change 2: Extend `derive_resolution_html()` (lines 49-170)**

Add two new status blocks BEFORE the `# disagree + not pinned` block (line ~132). Order matters — auto_resolved and suggested must be matched before the generic disagree fallthrough.

After the `# disagree + pinned` block (line ~130), add:

```r
# auto_resolved
auto_mask <- status == "auto_resolved" & !is.na(dtxsid)
auto_badge <- '<span class="badge ms-1" style="background:#0D6EFD;color:#fff;font-size:0.7em;">auto</span>'
auto_pref <- auto_mask & !is.na(pref_name)
# Build tooltip from .resolution_reason if available
auto_tooltip <- rep("", n)
if (".resolution_reason" %in% names(df)) {
  has_reason <- auto_mask & !is.na(df$.resolution_reason)
  auto_tooltip[has_reason] <- paste0(' title="', htmltools::htmlEscape(df$.resolution_reason[has_reason]), '"')
}
result[auto_pref] <- paste0(
  "\u2705 ",
  htmltools::htmlEscape(dtxsid[auto_pref]),
  " \u2014 ",
  htmltools::htmlEscape(pref_name[auto_pref]),
  " <span", auto_tooltip[auto_pref], ">", sub("^<span", "<span", auto_badge), "</span>",
  ' <button class="compare-btn btn btn-sm btn-outline-primary" data-row="',
  row_indices[auto_pref],
  '">', as.character(shiny::icon("search")), ' Compare</button>'
)
result[auto_mask & is.na(dtxsid)] <- paste0(
  "\u2705 (Auto-resolved)",
  " <span", auto_tooltip[auto_mask & is.na(dtxsid)], ">", auto_badge, "</span>"
)

# suggested (not yet resolved)
suggested_mask <- status == "suggested" & !pinned
result[suggested_mask] <- paste0(
  '<button class="compare-btn btn btn-sm btn-outline-info" data-row="',
  row_indices[suggested_mask],
  '">', as.character(shiny::icon("search")), ' Review Suggestion</button>'
)

# suggested + pinned (accepted suggestion)
suggested_pinned <- status == "suggested" & pinned & !is.na(dtxsid)
sp_pref <- suggested_pinned & !is.na(pref_name)
accepted_badge <- '<span class="badge ms-1" style="background:#0DCAF0;color:#fff;font-size:0.7em;">accepted</span>'
result[sp_pref] <- paste0(
  "\u2705 ",
  htmltools::htmlEscape(dtxsid[sp_pref]),
  " \u2014 ",
  htmltools::htmlEscape(pref_name[sp_pref]),
  " ", accepted_badge,
  ' <button class="compare-btn btn btn-sm btn-outline-primary" data-row="',
  row_indices[sp_pref],
  '">', as.character(shiny::icon("search")), ' Compare</button>'
)
```

Simplify the auto_badge tooltip approach — use a cleaner pattern:

Actually, simplify: for the auto_resolved block, render the tooltip on the badge span directly:
```r
auto_mask <- status == "auto_resolved" & !is.na(dtxsid)
auto_pref <- auto_mask & !is.na(pref_name)
auto_reasons <- if (".resolution_reason" %in% names(df)) df$.resolution_reason else rep(NA_character_, n)
auto_title <- ifelse(!is.na(auto_reasons), paste0(' title="', htmltools::htmlEscape(auto_reasons), '"'), "")
auto_badge_vec <- paste0('<span class="badge ms-1" style="background:#0D6EFD;color:#fff;font-size:0.7em;"', auto_title, '>auto</span>')
compare_btn_vec <- paste0(
  ' <button class="compare-btn btn btn-sm btn-outline-primary" data-row="',
  row_indices, '">', search_icon, ' Compare</button>'
)
result[auto_pref] <- paste0(
  "\u2705 ", htmltools::htmlEscape(dtxsid[auto_pref]),
  " \u2014 ", htmltools::htmlEscape(pref_name[auto_pref]),
  " ", auto_badge_vec[auto_pref], compare_btn_vec[auto_pref]
)
result[auto_mask & !auto_pref & !is.na(dtxsid)] <- paste0(
  "\u2705 ", htmltools::htmlEscape(dtxsid[auto_mask & !auto_pref & !is.na(dtxsid)]),
  " ", auto_badge_vec[auto_mask & !auto_pref & !is.na(dtxsid)], compare_btn_vec[auto_mask & !auto_pref & !is.na(dtxsid)]
)
```

Use the `search_icon` variable already defined at line ~134: `search_icon <- as.character(shiny::icon("search"))`. Make sure this variable is defined BEFORE the new blocks (it currently is at line 134, which is inside the disagree+not-pinned block). Move the `search_icon` definition earlier — right after `pref_name` initialization (~line 67), add:
```r
search_icon <- as.character(shiny::icon("search"))
```

Then remove the duplicate at the original location (line ~134).

**Change 3: Extend `status_levels` and `status_colors` (lines 868-881)**

In `status_levels` vector, add `"auto_resolved"` and `"suggested"` to the `intersect()` input vector:
```r
status_levels <- intersect(
  c("agree", "agree_caveat", "single", "wqx", "disagree", "error", "manual", "unresolvable", "auto_resolved", "suggested"),
  unique(as.character(df_display$consensus_status))
)
```

In `status_colors`, add:
```r
"auto_resolved" = "#0D6EFD",
"suggested"     = "#0DCAF0"
```

**Change 4: Extend `row_bg_colors` (lines 965-974)**

Add:
```r
"auto_resolved" = "rgba(13, 110, 253, 0.08)",
"suggested"     = "rgba(13, 202, 240, 0.08)"
```

**Change 5: Extend `curation_stats` value boxes (lines 509-536)**

Add `n_auto_resolved` and `n_suggested` to the `resolved` count:
```r
resolved <- summary$n_agree +
  (summary$n_agree_caveat %||% 0) +
  (summary$n_single %||% 0) +
  (summary$n_manual %||% 0) +
  (summary$n_wqx %||% 0) +
  (summary$n_auto_resolved %||% 0)
```

Change `layout_columns` from `col_widths = c(3, 3, 3, 3)` to `col_widths = c(2, 2, 2, 2, 2, 2)` and add two new value_box calls:

```r
value_box(
  title = "Auto-Resolved",
  value = summary$n_auto_resolved %||% 0,
  showcase = bsicons::bs_icon("magic"),
  theme = "primary"
),
value_box(
  title = "Suggested",
  value = summary$n_suggested %||% 0,
  showcase = bsicons::bs_icon("lightbulb"),
  theme = "info"
)
```

Place these after the "Disagree" value box.

**Change 6: Add "Accept All Suggestions" button to UI (lines 386-430)**

In `mod_review_results_ui`, inside the `div(class = "d-flex gap-2", ...)` action button row, add after the "Show Errors" button:

```r
shinyjs::hidden(
  actionButton(
    ns("accept_all_suggestions"),
    "Accept All Suggestions",
    icon = icon("wand-magic-sparkles"),
    class = "btn-sm btn-primary"
  )
),
```

**Change 7: Add observer to show/hide bulk accept button + handler (in server function)**

After the `observe` that toggles `validate_all` visibility (~line 1129-1136), add:

```r
# Show/hide Accept All Suggestions based on suggested row count
observe({
  req(data_store$resolution_state)
  has_suggested <- any(
    data_store$resolution_state$consensus_status == "suggested" &
      !isTRUE(data_store$resolution_state$.pinned),
    na.rm = TRUE
  )
  if (has_suggested) {
    shinyjs::show("accept_all_suggestions")
  } else {
    shinyjs::hide("accept_all_suggestions")
  }
})
```

After the `apply_priority` handler (~line 1575), add the bulk accept handler following the identical pattern:

```r
# Handle Accept All Suggestions
observeEvent(input$accept_all_suggestions, {
  req(data_store$resolution_state, data_store$dtxsid_cols)

  tryCatch(
    {
      before_count <- sum(
        data_store$resolution_state$consensus_status == "suggested" &
          !isTRUE(data_store$resolution_state$.pinned),
        na.rm = TRUE
      )

      updated_df <- accept_all_suggestions(
        data_store$resolution_state,
        data_store$dtxsid_cols
      )

      data_store$resolution_state <- updated_df
      data_store$consensus_summary <- recalc_consensus_summary(updated_df)

      showNotification(
        sprintf("%d suggestion(s) accepted", before_count),
        type = "message"
      )
    },
    error = function(e) {
      showNotification(
        "Failed to accept suggestions. Please try again.",
        type = "error"
      )
    }
  )
})
```

**Change 8: Modify comparison modal for suggested/auto_resolved rows (lines 1184-1343)**

In `observeEvent(input$compare_row_click, ...)`:

a. After `row_idx <- input$compare_row_click$row` (line ~1188), get the row status:
```r
row_status <- as.character(data_store$resolution_state$consensus_status[row_idx])
```

b. The existing `get_resolution_options()` call will now work for auto_resolved/suggested rows (Plan 01 extended it).

c. Inside the `lapply(names(options), ...)` card builder, determine if this candidate is the suggested/auto-resolved one. Before the `lapply`, get the suggested column:
```r
suggested_col <- if (".suggested_column" %in% names(data_store$resolution_state)) {
  data_store$resolution_state$.suggested_column[row_idx]
} else {
  NA_character_
}
```

d. Inside `lapply`, at the `div(class = "candidate-card card mb-2", style = "border: 2px solid #dee2e6; ...")` line, change the style conditionally:
```r
is_suggested_candidate <- !is.na(suggested_col) && col == suggested_col
card_border <- if (is_suggested_candidate && row_status == "suggested") {
  "border: 2px solid #0D6EFD; background-color: #f0f7ff;"
} else if (is_suggested_candidate && row_status == "auto_resolved") {
  "border: 2px solid #FFC107; background-color: #fffdf0;"
} else {
  "border: 2px solid #dee2e6;"
}
```

e. Inside the card heading area (after `tags$h6(class = "mb-1 fw-bold", opt$dtxsid)`), add a candidate badge:
```r
candidate_badge <- if (is_suggested_candidate && row_status == "suggested") {
  tags$span(class = "badge bg-primary ms-2", "Suggested")
} else if (is_suggested_candidate && row_status == "auto_resolved") {
  tags$span(class = "badge bg-warning text-dark ms-2", "Auto-Selected")
} else {
  NULL
}
```

Then include `candidate_badge` in the heading div:
```r
div(
  tags$h6(class = "mb-1 fw-bold d-inline", opt$dtxsid),
  candidate_badge,
  ...
)
```

f. Modify the modal footer to include "Accept Suggestion" button for suggested rows:
```r
accept_suggestion_btn <- if (row_status == "suggested") {
  tags$button(
    class = "btn btn-primary",
    onclick = sprintf(
      "Shiny.setInputValue('%s', {t: Math.random()}, {priority: 'event'});",
      session$ns("accept_suggestion")
    ),
    "Accept Suggestion"
  )
} else {
  NULL
}

footer <- tagList(
  accept_suggestion_btn,
  div(
    id = session$ns("confirm_container"),
    style = "display:none;",
    actionButton(session$ns("modal_confirm"), "Confirm & Close", class = "btn-primary")
  ),
  tags$button(
    class = "btn btn-outline-secondary",
    onclick = sprintf(
      "Shiny.setInputValue('%s', {t: Math.random()}, {priority: 'event'});",
      session$ns("modal_skip")
    ),
    "Skip this row"
  ),
  modalButton("Cancel")
)
```

g. Add the "Accept Suggestion" handler (after modal_confirm handler):

```r
# Handle Accept Suggestion from modal
observeEvent(input$accept_suggestion, {
  row_idx <- data_store$modal_row_idx
  if (is.null(row_idx)) return()

  suggested_col <- data_store$resolution_state$.suggested_column[row_idx]
  if (is.na(suggested_col)) {
    showNotification("No suggestion available for this row", type = "warning")
    return()
  }

  tryCatch(
    {
      group_rows <- get_group_rows(row_idx, isolate(data_store$dedup_group_map))
      updated_df <- data_store$resolution_state
      for (r in group_rows) {
        updated_df <- resolve_row(updated_df, r, suggested_col, data_store$dtxsid_cols)
        updated_df$.resolution_method[r] <- "suggested-accept"
      }
      data_store$resolution_state <- updated_df
      data_store$consensus_summary <- recalc_consensus_summary(updated_df)

      opt_dtxsid <- updated_df$consensus_dtxsid[row_idx]
      pref_col <- sub("^dtxsid_", "preferredName_", suggested_col)
      opt_pref <- if (pref_col %in% names(updated_df)) updated_df[[pref_col]][row_idx] else NA_character_
      notification_msg <- if (!is.na(opt_pref)) {
        sprintf("Accepted suggestion for %d row(s): %s - %s", length(group_rows), opt_dtxsid, opt_pref)
      } else {
        sprintf("Accepted suggestion for %d row(s): %s", length(group_rows), opt_dtxsid)
      }

      removeModal()
      showNotification(notification_msg, type = "message")

      data_store$modal_row_idx <- NULL
      data_store$modal_selected_column <- NULL
    },
    error = function(e) {
      showNotification(paste0("Error accepting suggestion: ", e$message), type = "error")
    }
  )
})
```

h. In the existing `modal_confirm` handler (~line 1350-1387), after `resolve_row()` is called, set `.resolution_method`:
After the `for (r in group_rows)` loop that calls `resolve_row()`, add:
```r
for (r in group_rows) {
  updated_df$.resolution_method[r] <- "manual"
}
```

(resolve_row from Plan 01 already sets this, but this is a safety reinforcement.)
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); cat('load_all OK\n')"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_run_curation.R contains `classify_auto_resolve(`
    - R/mod_review_results.R `recalc_consensus_summary` contains `n_auto_resolved`
    - R/mod_review_results.R `recalc_consensus_summary` contains `n_suggested`
    - R/mod_review_results.R contains `"auto_resolved" = "#0D6EFD"`
    - R/mod_review_results.R contains `"suggested"     = "#0DCAF0"`
    - R/mod_review_results.R contains `"auto_resolved" = "rgba(13, 110, 253, 0.08)"`
    - R/mod_review_results.R contains `accept_all_suggestions`
    - R/mod_review_results.R contains `"Accept All Suggestions"`
    - R/mod_review_results.R contains `icon("wand-magic-sparkles")`
    - R/mod_review_results.R contains `"Accept Suggestion"`
    - R/mod_review_results.R contains `accept_suggestion`
    - R/mod_review_results.R contains `"suggested-accept"`
    - R/mod_review_results.R contains `"Auto-Selected"`
    - R/mod_review_results.R contains `bs_icon("magic")`
    - R/mod_review_results.R contains `bs_icon("lightbulb")`
    - R/mod_review_results.R contains `"Review Suggestion"`
    - `devtools::load_all()` succeeds
  </acceptance_criteria>
  <done>Pipeline wires classify_auto_resolve after scoring. Review Results shows AUTO-RESOLVED (#0D6EFD) and SUGGESTED (#0DCAF0) chips. Modal highlights suggested/auto-resolved candidates. Accept Suggestion and Accept All Suggestions buttons functional. Value boxes show counts. recalc_consensus_summary includes n_auto_resolved and n_suggested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client JS -> Shiny server | Modal button clicks (accept_suggestion, accept_all_suggestions) cross client-server boundary |
| resolution_state -> UI render | .resolution_reason rendered as HTML title attribute in Resolution column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | Tampering | accept_suggestion input | mitigate | Handler validates row_idx against data_store$resolution_state bounds and .suggested_column existence before resolving |
| T-50-05 | Tampering | .resolution_reason tooltip | mitigate | Value passed through htmltools::htmlEscape() before rendering in title attribute — prevents XSS |
| T-50-06 | Spoofing | accept_all_suggestions button | accept | Button is server-side hidden when no suggested rows exist; handler validates row status before resolving. No elevated privilege — same user session. |
</threat_model>

<verification>
1. `devtools::load_all()` succeeds
2. R/mod_run_curation.R calls classify_auto_resolve after compute_similarity_scores
3. R/mod_review_results.R renders auto_resolved and suggested chips with correct colors
4. Accept Suggestion and Accept All Suggestions handlers present with tryCatch error handling
5. Value boxes include Auto-Resolved and Suggested counts
</verification>

<success_criteria>
- Pipeline runs classify_auto_resolve() after scoring without error
- Status chips render in correct colors per UI-SPEC
- Modal shows suggestion highlight and Accept Suggestion button for suggested rows
- Modal shows auto-selected highlight for auto_resolved rows
- Bulk accept button appears when suggested rows exist and resolves all non-pinned suggestions
- Value boxes display Auto-Resolved and Suggested counts
</success_criteria>

<output>
After completion, create `.planning/phases/50-auto-resolve-suggest/50-02-SUMMARY.md`
</output>
