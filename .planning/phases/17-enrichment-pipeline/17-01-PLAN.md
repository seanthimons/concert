---
phase: "17-enrichment-pipeline"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["R/curation.R", "R/consensus.R", "R/export_helpers.R", "R/modules/mod_run_curation.R", "R/modules/mod_review_results.R", "tests/test_enrichment.R"]
autonomous: false
requirements: ["ENRCH-01", "ENRCH-02", "ENRCH-03", "COMPAT-03"]
must_haves:
  truths: ["enrich_candidates() returns structured cache tibble with dtxsid, casrn, molecular_formula, molecular_weight", "Enrichment fires automatically after curation completes", "Enrichment cache persists per-session and only fetches new DTXSIDs on re-curation", "get_resolution_options() returns source_column, source_tier, and enrichment metadata per candidate", "Export Curated Data sheet includes consensus_casrn, consensus_formula, consensus_mw columns", "API failures degrade gracefully — pipeline continues, missing fields show NA"]
  artifacts: ["R/curation.R:enrich_candidates", "R/consensus.R:get_resolution_options", "R/export_helpers.R:consensus_casrn", "R/modules/mod_run_curation.R:enrichment_cache", "tests/test_enrichment.R"]
  key_links: ["mod_run_curation.R -> curation.R::enrich_candidates (call after pipeline)", "mod_review_results.R -> consensus.R::get_resolution_options (pass enrichment_cache)", "export_helpers.R -> enrichment_cache (consensus columns)"]
---

<objective>
Enrich disagreement candidate DTXSIDs with CASRN, molecular formula, and molecular weight via CompTox API. Extend resolution options with source attribution and enrichment metadata. Add enrichment columns to export.

Purpose: Provide the data foundation that Phase 18's comparison modal will consume. Users get enrichment metadata in exports immediately.
Output: Refactored enrich_candidates(), extended get_resolution_options(), enrichment columns in export, auto-trigger in curation module.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-enrichment-pipeline/17-CONTEXT.md

@R/curation.R
@R/consensus.R
@R/export_helpers.R
@R/modules/mod_run_curation.R
@R/modules/mod_review_results.R

<interfaces>
<!-- Existing interfaces the executor needs -->

From R/curation.R (current enrich_candidates, line 736):
```r
enrich_candidates <- function(resolution_state, dtxsid_cols, projection = "standard")
# Returns: list(raw = tibble, dtxsids_queried = character) or NULL
# Uses: ComptoxR::ct_details(unique_dtxsids, projection = projection)
```

From R/consensus.R (current get_resolution_options, line 156):
```r
get_resolution_options <- function(df, row_idx, dtxsid_cols)
# Returns: named list of column_name = list(dtxsid, preferredName, rank)
```

From R/export_helpers.R (current build_export_sheets, line 22):
```r
build_export_sheets <- function(raw, resolution_state, consensus_summary,
                                 cleaning_audit, reference_lists, column_tags,
                                 detection, file_info)
# Returns: named list of 7 data frames
```

From R/modules/mod_run_curation.R (curation trigger, line 152):
```r
# After pipeline_result, stores into data_store$consensus_data, data_store$resolution_state, etc.
# data_store$dtxsid_cols <- find_dtxsid_cols(pipeline_result$results)
```

Source tier column pattern in resolution_state:
- `source_tier_{column_name}` columns with values: "exact", "cas", "starts_with", "miss"
- DTXSID column pattern: `dtxsid_{column_name}`
- So source_column is derived: `sub("^dtxsid_", "", col)` gives the tagged column name
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor enrich_candidates and extend get_resolution_options</name>
  <files>R/curation.R, R/consensus.R, tests/test_enrichment.R</files>
  <behavior>
    - enrich_candidates() with disagree rows returns tibble(dtxsid, casrn, molecular_formula, molecular_weight) — one row per unique DTXSID
    - enrich_candidates() with existing_cache skips already-cached DTXSIDs (incremental enrichment)
    - enrich_candidates() with no disagree rows but with all_dtxsids param enriches those too (for export of agree/single rows)
    - enrich_candidates() handles API failure gracefully — returns partial cache + failed_dtxsids list
    - enrich_candidates() handles partial API response (some DTXSIDs missing from result) — fills NA for missing
    - get_resolution_options() with enrichment_cache returns source_column, source_tier (human-readable), casrn, molecular_formula, molecular_weight per candidate
    - get_resolution_options() without enrichment_cache still works (backward compatible) — enrichment fields are NA
    - source_tier labels: "exact" -> "Exact match", "cas" -> "CAS lookup", "starts_with" -> "Starts-with", "miss" -> "No match"
  </behavior>
  <action>
  **R/curation.R — Refactor enrich_candidates():**
  Change signature to: `enrich_candidates(dtxsids, existing_cache = NULL, projection = "standard")`
  - Accept a character vector of DTXSIDs directly (caller extracts them)
  - If `existing_cache` is a tibble, filter out DTXSIDs already present — only fetch new ones
  - Call `ComptoxR::ct_details` wrapped in `purrr::safely()` as before
  - From the raw API response, extract columns: `dtxsid` (the ID field), `casrn`, `molFormula` (rename to molecular_formula), `monoisotopicMass` or `molecularWeight` (rename to molecular_weight). Check actual ct_details return column names — they may be camelCase.
  - For DTXSIDs that were queried but missing from the API response, create rows with NA values
  - Return: `list(cache = tibble(dtxsid, casrn, molecular_formula, molecular_weight), failed_dtxsids = character())`
  - On total API failure: return `list(cache = existing_cache %||% tibble(...empty...), failed_dtxsids = dtxsids_to_fetch)`
  - Bind new results with existing_cache before returning

  **R/consensus.R — Extend get_resolution_options():**
  Add optional `enrichment_cache = NULL` parameter (backward compatible).
  For each candidate option, add:
  - `source_column`: derived from column name via `sub("^dtxsid_", "", col)`
  - `source_tier`: read from `source_tier_{source_column}` column in df, then map to human-readable: "exact" -> "Exact match", "cas" -> "CAS lookup", "starts_with" -> "Starts-with", "miss" -> "No match", NA -> "Unknown"
  - If enrichment_cache is provided and has a row for this DTXSID, add `casrn`, `molecular_formula`, `molecular_weight` from the cache
  - If enrichment_cache is NULL or DTXSID not found, set those fields to NA

  **tests/test_enrichment.R:**
  Create test file covering the behavior cases listed above. Mock ComptoxR::ct_details using a local function override or test with synthetic data. Test both enrich_candidates() and get_resolution_options() extension.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_file('tests/test_enrichment.R')"</automated>
  </verify>
  <done>
  - enrich_candidates() returns structured cache tibble, handles incremental caching, handles API failures
  - get_resolution_options() returns source_column, source_tier (human-readable), and enrichment metadata per candidate
  - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire enrichment into curation module and extend export</name>
  <files>R/modules/mod_run_curation.R, R/export_helpers.R, R/modules/mod_review_results.R</files>
  <action>
  **R/modules/mod_run_curation.R — Auto-trigger enrichment after curation:**
  After the existing `data_store$curation_status <- "completed"` line (around line 181), add enrichment logic:
  1. Check if disagree rows exist: `n_disagree <- pipeline_result$consensus_summary$n_disagree`
  2. Collect ALL unique DTXSIDs for enrichment (not just disagree — per user decision, agree/single consensus DTXSIDs also get enriched for export):
     - From disagree rows: all candidate DTXSIDs across dtxsid_* columns
     - From agree/single rows: consensus_dtxsid values
     - Combine and deduplicate
  3. Call `enrich_candidates(all_unique_dtxsids, existing_cache = data_store$enrichment_cache)`
  4. Store result: `data_store$enrichment_cache <- enrich_result$cache`
  5. Store failed list: `data_store$enrichment_failed <- enrich_result$failed_dtxsids`
  6. Show progress notification: "Enriching N candidates..." before the call
  7. Show completion notification: "Enrichment complete: N of M DTXSIDs enriched" or warning if failures occurred
  8. On total failure: show warning notification, continue — curation results still work

  Also ensure `data_store$enrichment_cache` is initialized as NULL in the app's reactiveValues (check app.R or wherever data_store is created — add it if missing).

  **R/modules/mod_review_results.R — Pass enrichment_cache to get_resolution_options:**
  Find all calls to `get_resolution_options()` and add `enrichment_cache = data_store$enrichment_cache` parameter. This is the only wiring needed — the function signature change in Task 1 handles the rest.

  **R/export_helpers.R — Add consensus enrichment columns:**
  Add `enrichment_cache = NULL` parameter to `build_export_sheets()`.
  In the Curated Data sheet construction (around line 30), after existing mutations:
  1. If enrichment_cache is not NULL and has rows:
     - For each row in resolution_state, look up `consensus_dtxsid` in enrichment_cache
     - Add columns: `consensus_casrn`, `consensus_formula`, `consensus_mw`
     - Use left_join on dtxsid == consensus_dtxsid, then select the 3 new columns
  2. If enrichment_cache is NULL: add the 3 columns as all NA (columns still exist for consistency)
  3. Place new columns after `consensus_dtxsid` or at the end before `needs_review`

  Update the call to `build_export_sheets()` in mod_review_results.R (around line 929) to pass `enrichment_cache = data_store$enrichment_cache`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); testthat::test_file('tests/test_enrichment.R'); testthat::test_file('tests/test_export_import.R')"</automated>
  </verify>
  <done>
  - Enrichment auto-fires after curation and stores cache in data_store$enrichment_cache
  - get_resolution_options calls pass enrichment_cache throughout mod_review_results
  - Export Curated Data sheet has consensus_casrn, consensus_formula, consensus_mw columns
  - Existing tests still pass, no regressions
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify enrichment pipeline end-to-end</name>
  <files>N/A</files>
  <action>
  Human verification of the complete enrichment pipeline. The executor should run the Shiny smoke test first (`"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)"` in background, wait for "Listening on" or error, then kill), then present the checkpoint to the user.
  </action>
  <what-built>Complete enrichment pipeline: refactored enrich_candidates(), extended get_resolution_options() with source attribution + enrichment metadata, auto-trigger after curation, and export columns</what-built>
  <how-to-verify>
    1. Start the app: Rscript -e "shiny::runApp('app.R', port=3838)"
    2. Upload a test chemical file and tag columns
    3. Run curation — watch for enrichment notification after curation completes
    4. Check Review Results tab — disagree rows should still render correctly
    5. Export to Excel — open the Curated Data sheet and verify consensus_casrn, consensus_formula, consensus_mw columns exist
    6. For rows with resolved DTXSIDs, check that enrichment values are populated (not all NA)
  </how-to-verify>
  <verify>Shiny smoke test passes (app starts without crash), then user confirms</verify>
  <done>User approves enrichment pipeline behavior and export columns</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. All unit tests pass: `Rscript -e "source('load_packages.R'); testthat::test_dir('tests')"`
2. Shiny smoke test: app starts without crash
3. Export file contains consensus_casrn, consensus_formula, consensus_mw columns
4. Enrichment handles API being unavailable gracefully (no crash)
</verification>

<success_criteria>
- enrich_candidates() returns structured tibble with dtxsid/casrn/molecular_formula/molecular_weight
- Incremental caching works: re-curation only fetches new DTXSIDs
- get_resolution_options() includes source_column, human-readable source_tier, and enrichment metadata
- Export has consensus_casrn, consensus_formula, consensus_mw in Curated Data sheet
- API failures produce warnings, not crashes
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-enrichment-pipeline/17-01-SUMMARY.md`
</output>
