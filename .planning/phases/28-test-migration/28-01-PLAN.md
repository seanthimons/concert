---
phase: 28-test-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat.R
  - tests/testthat/test-data-detection.R
  - tests/testthat/test-prototype-pipeline.R
  - tests/testthat/test-consensus.R
  - tests/testthat/test-cas-pipeline.R
  - tests/testthat/test-reference-provenance.R
  - tests/testthat/test-cleaning-reference.R
  - tests/testthat/test-export-import.R
  - tests/testthat/test-unicode-qc.R
  - tests/testthat/test-bare-formula-detection.R
  - tests/testthat/test-flag-matching.R
  - tests/testthat/test-cleaning-pipeline.R
  - tests/testthat/test-name-cleaning.R
  - tests/testthat/test-unicode-comptoxr.R
  - tests/testthat/test-cleaning-pipeline-validation.R
  - tests/testthat/test-enrichment.R
  - tests/testthat/test-isotope-chiral-multianalyte.R
  - tests/testthat/test-modules-render.R
  - DESCRIPTION
autonomous: true
requirements:
  - TST-01
  - TST-02
  - TST-03
  - TST-04

must_haves:
  truths:
    - "devtools::test() discovers and runs all 17 test files"
    - "All test files live under tests/testthat/ with dash naming convention"
    - "The load_all_reference_lists test expects 5 keys (not 3)"
    - "devtools::test() completes with zero failures and zero errors"
  artifacts:
    - path: "tests/testthat.R"
      provides: "testthat runner file"
      contains: "test_check"
    - path: "tests/testthat/test-cleaning-reference.R"
      provides: "Fixed reference list test"
      contains: "isotope_lookup"
    - path: "DESCRIPTION"
      provides: "Package metadata with withr in Suggests"
      contains: "withr"
  key_links:
    - from: "tests/testthat.R"
      to: "tests/testthat/test-*.R"
      via: "test_check discovers test files by convention"
      pattern: "test_check.*chemreg"
    - from: "tests/testthat/test-*.R"
      to: "chemreg package namespace"
      via: "library(chemreg) in runner loads all exported functions"
      pattern: "No source\\(here::here calls remain"
---

<objective>
Migrate all 17 test files from the legacy `tests/test_*.R` layout into the standard `tests/testthat/test-*.R` structure, clean up library/source calls so tests run against the installed package namespace, fix the pre-existing key-count failure, and verify `devtools::test()` passes green.

Purpose: Final phase of v1.8 R Package Migration -- makes the test suite compatible with standard R package tooling.
Output: Standard testthat structure with all tests passing under devtools::test().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-test-migration/28-CONTEXT.md
@DESCRIPTION
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create testthat structure and migrate all test files</name>
  <files>
    tests/testthat.R
    tests/testthat/test-data-detection.R
    tests/testthat/test-prototype-pipeline.R
    tests/testthat/test-consensus.R
    tests/testthat/test-cas-pipeline.R
    tests/testthat/test-reference-provenance.R
    tests/testthat/test-cleaning-reference.R
    tests/testthat/test-export-import.R
    tests/testthat/test-unicode-qc.R
    tests/testthat/test-bare-formula-detection.R
    tests/testthat/test-flag-matching.R
    tests/testthat/test-cleaning-pipeline.R
    tests/testthat/test-name-cleaning.R
    tests/testthat/test-unicode-comptoxr.R
    tests/testthat/test-cleaning-pipeline-validation.R
    tests/testthat/test-enrichment.R
    tests/testthat/test-isotope-chiral-multianalyte.R
    tests/testthat/test-modules-render.R
    tests/testthat/data/reference_cache/
  </files>
  <read_first>
    tests/test_cleaning_reference.R
    tests/test_modules_render.R
    tests/test_consensus.R
    tests/test_unicode_qc.R
    tests/test_unicode_comptoxr.R
    DESCRIPTION
  </read_first>
  <action>
Per D-01 and D-02 (TST-01): Create `tests/testthat.R` with exactly this content:

```r
library(testthat)
library(chemreg)

test_check("chemreg")
```

Per D-04: Move `tests/data/` to `tests/testthat/data/` (contains `reference_cache/` subdirectory used by cleaning_reference tests). Use `git mv tests/data tests/testthat/data`.

Per D-05: Delete `tests/air.toml` — an identical `air.toml` already exists at the project root. Use `git rm tests/air.toml`.

Per D-03: The `tests/_snaps/` directory exists but is empty (verified: contains no snapshot files). D-03 specified relocation to `tests/testthat/_snaps/`, but since there are no files to relocate, delete the empty directory instead: `rmdir tests/_snaps` (git doesn't track empty directories, so no `git rm` needed).

Per D-06 and D-07 (TST-02): Move and rename all 17 test files from `tests/test_*.R` to `tests/testthat/test-*.R` using `git mv`. The mapping (underscore to dash):

- `tests/test_bare_formula_detection.R` -> `tests/testthat/test-bare-formula-detection.R`
- `tests/test_cas_pipeline.R` -> `tests/testthat/test-cas-pipeline.R`
- `tests/test_cleaning_pipeline.R` -> `tests/testthat/test-cleaning-pipeline.R`
- `tests/test_cleaning_pipeline_validation.R` -> `tests/testthat/test-cleaning-pipeline-validation.R`
- `tests/test_cleaning_reference.R` -> `tests/testthat/test-cleaning-reference.R`
- `tests/test_consensus.R` -> `tests/testthat/test-consensus.R`
- `tests/test_data_detection.R` -> `tests/testthat/test-data-detection.R`
- `tests/test_enrichment.R` -> `tests/testthat/test-enrichment.R`
- `tests/test_export_import.R` -> `tests/testthat/test-export-import.R`
- `tests/test_flag_matching.R` -> `tests/testthat/test-flag-matching.R`
- `tests/test_isotope_chiral_multianalyte.R` -> `tests/testthat/test-isotope-chiral-multianalyte.R`
- `tests/test_modules_render.R` -> `tests/testthat/test-modules-render.R`
- `tests/test_name_cleaning.R` -> `tests/testthat/test-name-cleaning.R`
- `tests/test_prototype_pipeline.R` -> `tests/testthat/test-prototype-pipeline.R`
- `tests/test_reference_provenance.R` -> `tests/testthat/test-reference-provenance.R`
- `tests/test_unicode_comptoxr.R` -> `tests/testthat/test-unicode-comptoxr.R`
- `tests/test_unicode_qc.R` -> `tests/testthat/test-unicode-qc.R`

Commit this structural move before making content edits (separate commit for clean git history).
  </action>
  <verify>
    <automated>ls tests/testthat/test-*.R | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `tests/testthat.R` exists and contains `test_check("chemreg")`
    - `tests/testthat/` directory contains exactly 17 `test-*.R` files (dash, not underscore)
    - `tests/testthat/data/reference_cache/` directory exists
    - No `test_*.R` files remain in `tests/` root (only `testthat.R` and `testthat/` dir)
    - `tests/air.toml` is deleted
    - `tests/_snaps/` is deleted
  </acceptance_criteria>
  <done>All 17 test files relocated to tests/testthat/ with dash naming, runner file created, data directory moved, stale files removed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean test file headers, fix key-count failure, add withr to Suggests, verify green</name>
  <files>
    tests/testthat/test-data-detection.R
    tests/testthat/test-prototype-pipeline.R
    tests/testthat/test-consensus.R
    tests/testthat/test-cas-pipeline.R
    tests/testthat/test-reference-provenance.R
    tests/testthat/test-cleaning-reference.R
    tests/testthat/test-export-import.R
    tests/testthat/test-unicode-qc.R
    tests/testthat/test-bare-formula-detection.R
    tests/testthat/test-flag-matching.R
    tests/testthat/test-cleaning-pipeline.R
    tests/testthat/test-name-cleaning.R
    tests/testthat/test-unicode-comptoxr.R
    tests/testthat/test-cleaning-pipeline-validation.R
    tests/testthat/test-enrichment.R
    tests/testthat/test-isotope-chiral-multianalyte.R
    tests/testthat/test-modules-render.R
    DESCRIPTION
  </files>
  <read_first>
    tests/testthat/test-cleaning-reference.R
    tests/testthat/test-modules-render.R
    tests/testthat/test-unicode-qc.R
    tests/testthat/test-unicode-comptoxr.R
    tests/testthat/test-consensus.R
    tests/testthat/test-enrichment.R
    tests/testthat/test-prototype-pipeline.R
    tests/testthat/test-export-import.R
    DESCRIPTION
  </read_first>
  <action>
**Per D-08 (remove library(testthat)):** Every test file has `library(testthat)` — remove it from all 17 files. The runner file (`tests/testthat.R`) loads testthat.

**Per D-09 (remove library(here)):** Remove `library(here)` from all files that have it. Files with this call: test-bare-formula-detection, test-cas-pipeline, test-cleaning-pipeline, test-cleaning-pipeline-validation, test-cleaning-reference, test-data-detection, test-export-import, test-flag-matching, test-isotope-chiral-multianalyte, test-name-cleaning, test-reference-provenance, test-unicode-comptoxr, test-unicode-qc.

**Per D-10 (remove source() calls):** Remove ALL `source(here::here(...))` and `source(file.path(here::here(), ...))` lines from every test file. After `library(chemreg)` in the runner, all exported functions are available via the package namespace. Specific files and their source() calls to remove:

- test-bare-formula-detection.R line 10: `source(here::here("R", "cleaning_pipeline.R"))`
- test-cas-pipeline.R line 9: `source(here::here("R", "cleaning_pipeline.R"))`
- test-cleaning-pipeline.R line 8: `source(here::here("R", "cleaning_pipeline.R"))`
- test-cleaning-pipeline-validation.R line 17: `source(here::here("R", "cleaning_pipeline.R"))`
- test-cleaning-reference.R line 9: `source(here::here("R", "cleaning_reference.R"))`
- test-consensus.R line 4: `source(file.path(here::here(), "R", "consensus.R"))` and line 528: `source(file.path(here::here(), "R", "curation.R"))`
- test-data-detection.R lines 10-11: source of data_detection.R and file_handlers.R
- test-enrichment.R lines 4-5: source of consensus.R and curation.R
- test-export-import.R lines 12-13: source of export_helpers.R and config_import.R
- test-flag-matching.R line 10: `source(here::here("R", "cleaning_pipeline.R"))`
- test-isotope-chiral-multianalyte.R line 10: `source(here::here("R", "cleaning_pipeline.R"))`
- test-name-cleaning.R line 11: `source(here::here("R", "cleaning_pipeline.R"))`
- test-prototype-pipeline.R lines 4-5: source of consensus.R and curation.R
- test-reference-provenance.R line 10: `source(here::here("R", "cleaning_reference.R"))`
- test-unicode-comptoxr.R line 8: `source(here::here("load_packages.R"))` (also remove any inline `source(here::here("R", ...))` if present — check line 24)
- test-unicode-qc.R lines 8-9: source of load_packages.R and cleaning_pipeline.R

**Remove other redundant library() calls per D-08/D-09:** Remove `library(tibble)`, `library(dplyr)`, `library(stringr)`, `library(readxl)`, `library(writexl)` — these are all in chemreg's Imports and available after `library(chemreg)`. Files affected:
- test-bare-formula-detection.R: `library(tibble)`, `library(dplyr)`
- test-cas-pipeline.R: `library(tibble)`
- test-cleaning-pipeline-validation.R: `library(tibble)`, `library(dplyr)`, `library(stringr)`
- test-data-detection.R: `library(tibble)`, `library(dplyr)`
- test-export-import.R: `library(dplyr)`, `library(tibble)`, `library(readxl)`, `library(writexl)`
- test-flag-matching.R: `library(tibble)`, `library(dplyr)`
- test-isotope-chiral-multianalyte.R: `library(tibble)`, `library(dplyr)`
- test-name-cleaning.R: `library(tibble)`, `library(dplyr)`
- test-reference-provenance.R: `library(tibble)`

**Per D-11 (keep library(withr)):** Keep `library(withr)` in test-cleaning-reference.R and test-reference-provenance.R. Withr is NOT an Import of chemreg so it must be explicitly loaded.

**For test-modules-render.R:** Remove `library(shiny)`, `library(reactable)`, `library(reactable.extras)`, `library(bslib)`, `library(htmltools)` — these are all in chemreg Imports. Keep only the `test_that()` blocks.

**Per D-13 (TST-03 — fix pre-existing failure):** In `tests/testthat/test-cleaning-reference.R`, find the line (was line 99, may shift after header cleanup):
```r
expect_named(result, c("stop_words", "block_patterns", "functional_categories"))
```
Replace with:
```r
expect_named(result, c("stop_words", "block_patterns", "functional_categories", "strip_terms", "isotope_lookup"))
```

Also per D-14, update the comment on line 101 (or nearby) from referencing "Phase 13 change" to note that the function now returns 5 keys (stop_words, block_patterns, functional_categories added Phase 13; strip_terms added Phase 21; isotope_lookup added Phase 23). Add `expect_true(tibble::is_tibble(result$strip_terms))` and `expect_true(tibble::is_tibble(result$isotope_lookup))` assertions for the new keys.

**Per D-12 (TST-01):** Add `withr` to Suggests in DESCRIPTION. Current Suggests section is:
```
Suggests:
    testthat (>= 3.0.0)
```
Change to:
```
Suggests:
    testthat (>= 3.0.0),
    withr
```

**After all edits:** Clean up resulting blank lines at the top of each file. Each file should start with a comment line (the existing description comment) followed immediately by the `test_that()` blocks, or `library(withr)` where applicable. Remove any leftover blank lines from stripped headers.

**Final verification:** Run `devtools::test()` via Rscript. Write a temp R script:
```r
devtools::load_all()
devtools::test()
```
Run with `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" /tmp/run_tests.R` and confirm zero failures and zero errors.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" /tmp/run_tests.R</automated>
    <note>The temp file /tmp/run_tests.R is created in the action step above with: devtools::load_all(); devtools::test(). Expect "0 failures" and "0 errors" in output.</note>
  </verify>
  <acceptance_criteria>
    - No test file in `tests/testthat/` contains `library(testthat)` (grep returns 0 matches)
    - No test file in `tests/testthat/` contains `library(here)` (grep returns 0 matches)
    - No test file in `tests/testthat/` contains `source(here::here` or `source(file.path(here::here` (grep returns 0 matches)
    - `tests/testthat/test-cleaning-reference.R` contains the string `"strip_terms", "isotope_lookup"`
    - `tests/testthat/test-cleaning-reference.R` contains `is_tibble(result$strip_terms)`
    - `tests/testthat/test-cleaning-reference.R` contains `is_tibble(result$isotope_lookup)`
    - DESCRIPTION Suggests section contains `withr`
    - `devtools::test()` output shows 0 failures and 0 errors
    - `library(withr)` is present in test-cleaning-reference.R and test-reference-provenance.R
  </acceptance_criteria>
  <done>All test files cleaned of legacy library/source calls, pre-existing key-count failure fixed to expect 5 keys, withr added to Suggests, devtools::test() passes with zero failures.</done>
</task>

</tasks>

<verification>
1. `ls tests/testthat/test-*.R | wc -l` returns 17
2. `grep -r "library(testthat)" tests/testthat/test-*.R` returns no matches
3. `grep -r "source(here" tests/testthat/test-*.R` returns no matches
4. `grep -r "source(file.path(here" tests/testthat/test-*.R` returns no matches
5. `grep "strip_terms" tests/testthat/test-cleaning-reference.R` returns a match
6. `grep "withr" DESCRIPTION` returns a match
7. `devtools::test()` shows all tests passing with 0 failures, 0 errors
</verification>

<success_criteria>
- tests/testthat.R runner exists with standard content (TST-01)
- All 17 test files use dash naming under tests/testthat/ (TST-02)
- test-cleaning-reference.R expects 5 keys including strip_terms and isotope_lookup (TST-03)
- devtools::test() completes with zero failures and zero errors (TST-04)
</success_criteria>

<output>
After completion, create `.planning/phases/28-test-migration/28-01-SUMMARY.md`
</output>
