---
phase: 34-harmonize-tab-module
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - R/mod_harmonize.R
autonomous: true
requirements:
  - DATA-04
  - PARS-06
  - UNIT-06

must_haves:
  truths:
    - "Unit table editor displays chips with 'from_unit -> to_unit (xN)' format, clicking opens 6-field edit modal"
    - "Corrections editor displays chips with 'pattern -> replacement' format, clicking opens 2-field edit modal"
    - "Unmatched units panel lists units with row counts after pipeline run"
    - "Add All as Pass-through adds identity mappings for every unmatched unit"
    - "Individual Add Mapping button opens unit modal pre-filled with from_unit"
    - "User-added chips show x-remove button; app-default chips do not"
    - "Accordion panels start collapsed and show item counts in titles"
  artifacts:
    - path: "R/mod_harmonize.R"
      provides: "Complete harmonize module with editors"
      contains: "render_unit_chip_editor"
      min_lines: 400
  key_links:
    - from: "R/mod_harmonize.R chip_click observer"
      to: "showModal(modalDialog(...))"
      via: "observeEvent(input$chip_click) opens edit modal"
      pattern: "chip_click.*showModal"
    - from: "R/mod_harmonize.R save_unit_mapping observer"
      to: "data_store$unit_map_working"
      via: "bind_rows to append or update unit map row"
      pattern: "save_unit_mapping.*unit_map_working"
    - from: "R/mod_harmonize.R add_all_passthrough observer"
      to: "data_store$unit_map_working"
      via: "add_passthrough_mapping for each unmatched unit"
      pattern: "add_all_passthrough.*passthrough"

user_setup: []
---

<objective>
Add the three editor UIs to mod_harmonize.R: unit table chip editor (DATA-04), corrections chip editor (PARS-06 UI), and unmatched units batch panel (UNIT-06), with accordion layout, modal forms, and all chip interaction handlers.

Purpose: Users need to review and edit the unit mapping table, add one-off corrections for malformed values, and resolve unmatched units before re-running harmonization. These editors are the primary user interaction surface of the harmonize tab beyond the pipeline button.

Output: Fully functional editor UIs within the existing mod_harmonize.R module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-harmonize-tab-module/34-CONTEXT.md
@.planning/phases/34-harmonize-tab-module/34-RESEARCH.md
@.planning/phases/34-harmonize-tab-module/34-PATTERNS.md
@.planning/phases/34-harmonize-tab-module/34-UI-SPEC.md
@.planning/phases/34-harmonize-tab-module/34-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -- mod_harmonize.R already exists with these elements: -->
<!-- UI: chip CSS/JS, conditionalPanel with button + qc_dashboard + editors_panel placeholder -->
<!-- Server: has_numeric_tags output, button enable/disable, working copy init, apply_corrections, -->
<!--         add_passthrough_mapping, pipeline observeEvent, qc_dashboard renderUI, cascade resets -->
<!-- The editors_panel renderUI is a NULL placeholder that this plan replaces. -->

From R/mod_clean_data.R chip editor pattern (lines 529-582):
```r
# render_chip_editor() generates chip HTML with data-ns, data-type, data-term attributes
# Each chip: span.ref-chip > span.ref-chip-body + span.ref-chip-remove
# Badge classes: bg-success (app_default), bg-primary (user), bg-secondary (passthrough)
```

Unit map tibble schema (from cleaning_reference.R):
```r
# from_unit (character), to_unit (character), multiplier (numeric),
# category (character), confidence (character), source (character)
```

Corrections tibble schema:
```r
# pattern (character), replacement (character)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit table chip editor with modal add/edit/remove</name>
  <files>R/mod_harmonize.R</files>

  <read_first>
    - R/mod_harmonize.R (full file -- current state from Plan 01, find editors_panel placeholder and chip_click/chip_remove JS handlers)
    - R/mod_clean_data.R lines 529-582 (render_chip_editor helper -- adapt for unit chips)
    - R/mod_clean_data.R lines 663-687 (chip_toggle and chip_remove handlers)
    - R/mod_clean_data.R lines 957-968 (modal pattern)
    - .planning/phases/34-harmonize-tab-module/34-PATTERNS.md (render_unit_chip_editor, chip click -> modal, chip remove, save_unit_mapping patterns)
    - .planning/phases/34-harmonize-tab-module/34-UI-SPEC.md (chip label format, modal fields, interaction contracts)
  </read_first>

  <action>
Modify `R/mod_harmonize.R` to add unit table editor. All changes go inside the existing `moduleServer()` body.

**1. Replace editors_panel renderUI placeholder** with the full accordion structure. Find the `output$editors_panel <- renderUI({ NULL })` line and replace with:

```r
output$editors_panel <- renderUI({
  # Only show after unit_map_working is initialized
  req(unit_map_ready())

  bslib::accordion(
    id = session$ns("editors"),
    open = FALSE,
    multiple = TRUE,
    bslib::accordion_panel(
      title = uiOutput(session$ns("unit_editor_title")),
      icon = bsicons::bs_icon("rulers"),
      uiOutput(session$ns("unit_chip_editor")),
      actionButton(session$ns("add_unit_mapping"), "Add Unit Mapping",
                   class = "btn-outline-primary btn-sm mt-2",
                   icon = icon("plus"))
    ),
    bslib::accordion_panel(
      title = uiOutput(session$ns("corrections_editor_title")),
      icon = bsicons::bs_icon("pencil-square"),
      uiOutput(session$ns("corrections_chip_editor")),
      actionButton(session$ns("add_correction"), "Add Correction",
                   class = "btn-outline-primary btn-sm mt-2",
                   icon = icon("plus"))
    ),
    bslib::accordion_panel(
      title = uiOutput(session$ns("unmatched_title")),
      icon = bsicons::bs_icon("question-circle"),
      uiOutput(session$ns("unmatched_panel"))
    )
  )
})
```

**2. Add unit editor title with count:**
```r
output$unit_editor_title <- renderUI({
  n <- if (!is.null(data_store$unit_map_working)) nrow(data_store$unit_map_working) else 0
  sprintf("Unit Table Editor (%d mappings)", n)
})
```

**3. Add render_unit_chip_editor helper** (internal function inside server body):
```r
render_unit_chip_editor <- function(unit_map_tbl) {
  ns <- session$ns
  chips <- lapply(seq_len(nrow(unit_map_tbl)), function(i) {
    row <- unit_map_tbl[i, ]

    badge_class <- switch(as.character(row$source),
      ECOTOX = "badge bg-success ref-chip",
      SSWQS  = "badge bg-info ref-chip",
      user   = "badge bg-primary ref-chip",
      user_passthrough = "badge bg-secondary ref-chip",
      "badge bg-light text-dark ref-chip"
    )

    # Chip label: "from_unit -> to_unit (xmultiplier)" per D-07
    chip_label <- sprintf("%s \u2192 %s", row$from_unit, row$to_unit)
    if (!is.na(row$multiplier) && row$multiplier != 1) {
      chip_label <- sprintf("%s (\u00d7%s)", chip_label, row$multiplier)
    }

    # X button for user-added rows only (app defaults not removable)
    remove_btn <- if (row$source %in% c("user", "user_passthrough")) {
      tags$span(
        class = "ref-chip-remove",
        `data-ns` = ns(""),
        `data-type` = "unit_map",
        `data-term` = row$from_unit,
        HTML("&times;")
      )
    }

    tags$span(
      class = badge_class,
      tags$span(
        class = "ref-chip-body",
        `data-ns` = ns(""),
        `data-type` = "unit_map",
        `data-term` = row$from_unit,
        chip_label
      ),
      remove_btn
    )
  })

  div(class = "ref-chip-container", chips)
}
```

**4. Add unit chip editor renderUI:**
```r
output$unit_chip_editor <- renderUI({
  req(data_store$unit_map_working)
  render_unit_chip_editor(data_store$unit_map_working)
})
```

**5. Add chip_click observer (unit table) -> opens edit modal (D-08):**
```r
observeEvent(input$chip_click, {
  msg <- input$chip_click
  req(msg$type, msg$term)

  if (msg$type == "unit_map") {
    tbl <- data_store$unit_map_working
    row <- tbl[tbl$from_unit == msg$term, ][1, ]

    showModal(modalDialog(
      title = "Edit Unit Mapping",
      textInput(session$ns("modal_from_unit"), "From Unit", value = row$from_unit),
      textInput(session$ns("modal_to_unit"), "To Unit", value = row$to_unit),
      numericInput(session$ns("modal_multiplier"), "Multiplier", value = row$multiplier),
      selectInput(session$ns("modal_category"), "Category",
        choices = c("mass_concentration", "mass_per_mass", "volume_concentration",
                    "molar", "radioactivity", "biological", "dimensionless", "other"),
        selected = row$category),
      selectInput(session$ns("modal_confidence"), "Confidence",
        choices = c("HIGH", "MEDIUM", "LOW"), selected = row$confidence),
      textInput(session$ns("modal_source"), "Source", value = row$source),
      # Store original from_unit for row lookup on save
      tags$input(type = "hidden", id = session$ns("modal_orig_from"), value = row$from_unit),
      footer = tagList(
        modalButton("Discard"),
        actionButton(session$ns("save_unit_mapping"), "Save Mapping", class = "btn-primary")
      ),
      easyClose = FALSE
    ))
  } else if (msg$type == "corrections") {
    tbl <- data_store$corrections_working
    row <- tbl[tbl$pattern == msg$term, ][1, ]

    showModal(modalDialog(
      title = "Edit Correction",
      textInput(session$ns("modal_corr_pattern"), "Pattern (regex)", value = row$pattern),
      textInput(session$ns("modal_corr_replacement"), "Replacement", value = row$replacement),
      tags$input(type = "hidden", id = session$ns("modal_corr_orig_pattern"), value = row$pattern),
      footer = tagList(
        modalButton("Discard"),
        actionButton(session$ns("save_correction"), "Save Correction", class = "btn-primary")
      ),
      easyClose = FALSE
    ))
  }
})
```

**6. Add chip_remove observer:**
```r
observeEvent(input$chip_remove, {
  msg <- input$chip_remove
  req(msg$type, msg$term)

  if (msg$type == "unit_map") {
    tbl <- data_store$unit_map_working
    idx <- which(tbl$from_unit == msg$term & tbl$source %in% c("user", "user_passthrough"))
    if (length(idx) > 0) {
      data_store$unit_map_working <- tbl[-idx[1], ]
    }
  } else if (msg$type == "corrections") {
    tbl <- data_store$corrections_working
    idx <- which(tbl$pattern == msg$term)
    if (length(idx) > 0) {
      data_store$corrections_working <- tbl[-idx[1], ]
    }
  }
})
```

**7. Add "Add Unit Mapping" button observer -> opens blank modal (D-09):**
```r
observeEvent(input$add_unit_mapping, {
  showModal(modalDialog(
    title = "Add Unit Mapping",
    textInput(session$ns("modal_from_unit"), "From Unit", placeholder = "e.g., ug/L"),
    textInput(session$ns("modal_to_unit"), "To Unit", placeholder = "e.g., mg/L"),
    numericInput(session$ns("modal_multiplier"), "Multiplier", value = NA),
    selectInput(session$ns("modal_category"), "Category",
      choices = c("mass_concentration", "mass_per_mass", "volume_concentration",
                  "molar", "radioactivity", "biological", "dimensionless", "other")),
    selectInput(session$ns("modal_confidence"), "Confidence",
      choices = c("HIGH", "MEDIUM", "LOW")),
    textInput(session$ns("modal_source"), "Source", value = "user"),
    tags$input(type = "hidden", id = session$ns("modal_orig_from"), value = ""),
    footer = tagList(
      modalButton("Discard"),
      actionButton(session$ns("save_unit_mapping"), "Save Mapping", class = "btn-primary")
    ),
    easyClose = FALSE
  ))
})
```

**8. Add save_unit_mapping observer:**
```r
observeEvent(input$save_unit_mapping, {
  req(input$modal_from_unit, input$modal_to_unit)

  from_val <- trimws(input$modal_from_unit)
  to_val   <- trimws(input$modal_to_unit)

  if (from_val == "" || to_val == "") {
    showNotification("From Unit and To Unit are required.", type = "warning", duration = 3)
    return()
  }

  new_row <- tibble::tibble(
    from_unit   = from_val,
    to_unit     = to_val,
    multiplier  = if (is.na(input$modal_multiplier)) 1 else input$modal_multiplier,
    category    = input$modal_category,
    confidence  = input$modal_confidence,
    source      = input$modal_source
  )

  tbl <- data_store$unit_map_working
  orig_from <- input$modal_orig_from

  if (!is.null(orig_from) && orig_from != "") {
    # Edit mode: replace existing row
    idx <- which(tbl$from_unit == orig_from)
    if (length(idx) > 0) {
      tbl <- tbl[-idx[1], ]
    }
  }

  data_store$unit_map_working <- dplyr::bind_rows(tbl, new_row)
  removeModal()
})
```
  </action>

  <verify>
    <automated>grep -q "render_unit_chip_editor" R/mod_harmonize.R && grep -q "save_unit_mapping" R/mod_harmonize.R && grep -q "chip_click" R/mod_harmonize.R && grep -q "chip_remove" R/mod_harmonize.R && grep -q "add_unit_mapping" R/mod_harmonize.R && grep -q "accordion" R/mod_harmonize.R && echo "Unit editor verified"</automated>
  </verify>

  <acceptance_criteria>
    - `R/mod_harmonize.R` contains `render_unit_chip_editor <- function(unit_map_tbl)` helper
    - `R/mod_harmonize.R` contains chip label format with unicode arrow `\u2192` and optional multiplier suffix `\u00d7`
    - `R/mod_harmonize.R` contains `observeEvent(input$chip_click` that opens `showModal(modalDialog(title = "Edit Unit Mapping"` for `type == "unit_map"`
    - `R/mod_harmonize.R` contains `observeEvent(input$chip_remove` that removes user-added rows from `data_store$unit_map_working`
    - `R/mod_harmonize.R` contains `observeEvent(input$add_unit_mapping` that opens blank add modal
    - `R/mod_harmonize.R` contains `observeEvent(input$save_unit_mapping` that validates from/to non-empty, handles edit vs add mode via `modal_orig_from`
    - `R/mod_harmonize.R` contains `bslib::accordion(` with `open = FALSE, multiple = TRUE` and 3 `bslib::accordion_panel` children
    - `R/mod_harmonize.R` unit chip badge class uses `bg-success` for ECOTOX, `bg-info` for SSWQS, `bg-primary` for user, `bg-secondary` for user_passthrough
    - `R/mod_harmonize.R` only user-added chips (`source %in% c("user", "user_passthrough")`) have the `ref-chip-remove` span
  </acceptance_criteria>

  <done>Unit table chip editor fully functional with modal add/edit, x-remove for user chips, accordion panel with count badge, and save handler that appends/updates unit_map_working</done>
</task>

<task type="auto">
  <name>Task 2: Corrections chip editor and unmatched units batch panel</name>
  <files>R/mod_harmonize.R</files>

  <read_first>
    - R/mod_harmonize.R (current state after Task 1 -- has accordion, unit editor, chip_click handler with corrections branch)
    - .planning/phases/34-harmonize-tab-module/34-PATTERNS.md (corrections chip, unmatched panel pattern)
    - .planning/phases/34-harmonize-tab-module/34-UI-SPEC.md (copywriting contract, interaction contracts for corrections and unmatched)
    - .planning/phases/34-harmonize-tab-module/34-CONTEXT.md (D-10 through D-16 decisions)
  </read_first>

  <action>
Add corrections chip editor and unmatched units panel to the existing `R/mod_harmonize.R`. All additions go inside the `moduleServer()` body.

**1. Add corrections editor title with count:**
```r
output$corrections_editor_title <- renderUI({
  n <- if (!is.null(data_store$corrections_working)) nrow(data_store$corrections_working) else 0
  sprintf("Corrections Editor (%d corrections)", n)
})
```

**2. Add render_corrections_chip_editor helper:**
```r
render_corrections_chip_editor <- function(corrections_tbl) {
  ns <- session$ns

  if (nrow(corrections_tbl) == 0) {
    return(p(class = "text-muted small", "No corrections defined. Add corrections for source-specific malformed values."))
  }

  chips <- lapply(seq_len(nrow(corrections_tbl)), function(i) {
    row <- corrections_tbl[i, ]
    chip_label <- sprintf("%s \u2192 %s", row$pattern, row$replacement)

    tags$span(
      class = "badge bg-primary ref-chip",
      tags$span(
        class = "ref-chip-body",
        `data-ns` = ns(""),
        `data-type` = "corrections",
        `data-term` = row$pattern,
        chip_label
      ),
      tags$span(
        class = "ref-chip-remove",
        `data-ns` = ns(""),
        `data-type` = "corrections",
        `data-term` = row$pattern,
        HTML("&times;")
      )
    )
  })

  div(class = "ref-chip-container", chips)
}
```

**3. Add corrections chip editor renderUI:**
```r
output$corrections_chip_editor <- renderUI({
  req(corrections_ready())
  render_corrections_chip_editor(data_store$corrections_working)
})
```

**4. Add "Add Correction" button observer (D-10):**
```r
observeEvent(input$add_correction, {
  showModal(modalDialog(
    title = "Add Correction",
    textInput(session$ns("modal_corr_pattern"), "Pattern (regex)",
              placeholder = "e.g., 1\\.5E 3"),
    textInput(session$ns("modal_corr_replacement"), "Replacement",
              placeholder = "e.g., 1.5E3"),
    tags$input(type = "hidden", id = session$ns("modal_corr_orig_pattern"), value = ""),
    footer = tagList(
      modalButton("Discard"),
      actionButton(session$ns("save_correction"), "Save Correction", class = "btn-primary")
    ),
    easyClose = FALSE
  ))
})
```

**5. Add save_correction observer:**
```r
observeEvent(input$save_correction, {
  req(input$modal_corr_pattern)

  pattern_val <- trimws(input$modal_corr_pattern)
  if (pattern_val == "") {
    showNotification("Pattern is required.", type = "warning", duration = 3)
    return()
  }

  replacement_val <- if (is.null(input$modal_corr_replacement)) "" else input$modal_corr_replacement

  new_row <- tibble::tibble(
    pattern     = pattern_val,
    replacement = replacement_val
  )

  tbl <- data_store$corrections_working
  orig_pattern <- input$modal_corr_orig_pattern

  if (!is.null(orig_pattern) && orig_pattern != "") {
    # Edit mode: remove old row
    idx <- which(tbl$pattern == orig_pattern)
    if (length(idx) > 0) {
      tbl <- tbl[-idx[1], ]
    }
  }

  data_store$corrections_working <- dplyr::bind_rows(tbl, new_row)
  removeModal()
})
```

**6. Add unmatched title with count:**
```r
output$unmatched_title <- renderUI({
  if (is.null(data_store$harmonize_results)) {
    return("Unmatched Units")
  }
  harmonized <- data_store$harmonize_results$harmonized
  n_unmatched <- sum(harmonized$unit_flag == "unmatched", na.rm = TRUE)
  n_unique <- length(unique(harmonized$orig_unit[harmonized$unit_flag == "unmatched"]))
  sprintf("Unmatched Units (%d)", n_unique)
})
```

**7. Add unmatched_panel renderUI (D-12 through D-16):**
```r
output$unmatched_panel <- renderUI({
  ns <- session$ns

  if (is.null(data_store$harmonize_results)) {
    return(p(class = "text-muted small",
             "Run harmonization to see unmatched units."))
  }

  harmonized <- data_store$harmonize_results$harmonized
  unmatched  <- harmonized[harmonized$unit_flag == "unmatched", ]

  if (nrow(unmatched) == 0) {
    return(div(class = "alert alert-success",
               bsicons::bs_icon("check-circle"),
               " All units matched successfully."))
  }

  # Summarize by unique unit string with row counts (D-13)
  unmatched_summary <- unmatched |>
    dplyr::count(orig_unit, name = "n") |>
    dplyr::arrange(dplyr::desc(n))

  div(
    class = "mb-3",
    # Batch action: Add All as Pass-through (D-14)
    actionButton(ns("add_all_passthrough"), "Add All as Pass-through",
                 class = "btn-outline-secondary btn-sm"),
    hr(),
    # Per-unit rows with counts and individual Add Mapping buttons (D-13, D-15)
    lapply(seq_len(nrow(unmatched_summary)), function(i) {
      u <- unmatched_summary[i, ]
      safe_id <- make.names(u$orig_unit)
      div(
        class = "d-flex justify-content-between align-items-center mb-2",
        span(sprintf("%s (%d rows)", u$orig_unit, u$n)),
        actionButton(
          ns(paste0("add_map_", safe_id)),
          "Add Mapping",
          class = "btn-primary btn-sm",
          onclick = sprintf(
            "Shiny.setInputValue('%s', {unit: '%s', ts: Date.now()});",
            ns("add_unmatched_mapping"),
            gsub("'", "\\\\'", u$orig_unit)
          )
        )
      )
    })
  )
})
```

**8. Add "Add All as Pass-through" observer (D-14):**
```r
observeEvent(input$add_all_passthrough, {
  req(data_store$harmonize_results)

  harmonized <- data_store$harmonize_results$harmonized
  unmatched_units <- unique(harmonized$orig_unit[harmonized$unit_flag == "unmatched"])

  if (length(unmatched_units) == 0) return()

  tbl <- data_store$unit_map_working
  for (u in unmatched_units) {
    tbl <- add_passthrough_mapping(u, tbl)
  }
  data_store$unit_map_working <- tbl

  showNotification(
    sprintf("Added %d pass-through mappings. Re-run harmonization to apply.", length(unmatched_units)),
    type = "message", duration = 5
  )
})
```

**9. Add individual "Add Mapping" observer (D-15) -- opens modal pre-filled with from_unit:**
```r
observeEvent(input$add_unmatched_mapping, {
  msg <- input$add_unmatched_mapping
  req(msg$unit)

  showModal(modalDialog(
    title = "Add Unit Mapping",
    textInput(session$ns("modal_from_unit"), "From Unit", value = msg$unit),
    textInput(session$ns("modal_to_unit"), "To Unit", placeholder = "e.g., mg/L"),
    numericInput(session$ns("modal_multiplier"), "Multiplier", value = NA),
    selectInput(session$ns("modal_category"), "Category",
      choices = c("mass_concentration", "mass_per_mass", "volume_concentration",
                  "molar", "radioactivity", "biological", "dimensionless", "other")),
    selectInput(session$ns("modal_confidence"), "Confidence",
      choices = c("HIGH", "MEDIUM", "LOW")),
    textInput(session$ns("modal_source"), "Source", value = "user"),
    tags$input(type = "hidden", id = session$ns("modal_orig_from"), value = ""),
    footer = tagList(
      modalButton("Discard"),
      actionButton(session$ns("save_unit_mapping"), "Save Mapping", class = "btn-primary")
    ),
    easyClose = FALSE
  ))
})
```

**10. Run `Rscript -e "devtools::document()"` to ensure NAMESPACE stays current.**

**11. Format with air:** Run `air format R/mod_harmonize.R` (if `air` is available on PATH; otherwise ensure code follows air.toml conventions: 120-char line width, 2-space indent).
  </action>

  <verify>
    <automated>grep -q "render_corrections_chip_editor" R/mod_harmonize.R && grep -q "save_correction" R/mod_harmonize.R && grep -q "add_correction" R/mod_harmonize.R && grep -q "unmatched_panel" R/mod_harmonize.R && grep -q "add_all_passthrough" R/mod_harmonize.R && grep -q "add_unmatched_mapping" R/mod_harmonize.R && grep -q "All units matched" R/mod_harmonize.R && echo "Corrections + unmatched editors verified"</automated>
  </verify>

  <acceptance_criteria>
    - `R/mod_harmonize.R` contains `render_corrections_chip_editor <- function(corrections_tbl)` helper
    - `R/mod_harmonize.R` corrections chips use `data-type` = `"corrections"` and `data-term` = `row$pattern`
    - `R/mod_harmonize.R` all corrections chips have `ref-chip-remove` span (all user-added, all removable)
    - `R/mod_harmonize.R` contains `observeEvent(input$add_correction` opening modal with title "Add Correction"
    - `R/mod_harmonize.R` contains `observeEvent(input$save_correction` that validates pattern non-empty, handles edit/add via `modal_corr_orig_pattern`
    - `R/mod_harmonize.R` `output$unmatched_panel` shows "Run harmonization to see unmatched units." when `harmonize_results` is NULL
    - `R/mod_harmonize.R` `output$unmatched_panel` shows "All units matched successfully." when no unmatched units
    - `R/mod_harmonize.R` `output$unmatched_panel` renders `dplyr::count(orig_unit, name = "n")` summary with per-unit "Add Mapping" buttons
    - `R/mod_harmonize.R` contains `observeEvent(input$add_all_passthrough` that calls `add_passthrough_mapping` for each unique unmatched unit
    - `R/mod_harmonize.R` contains `observeEvent(input$add_unmatched_mapping` opening unit modal pre-filled with `msg$unit` as from_unit
    - `R/mod_harmonize.R` contains `output$unmatched_title` showing "Unmatched Units (N)" with unique unmatched count
    - `R/mod_harmonize.R` contains `output$corrections_editor_title` showing "Corrections Editor (N corrections)"
  </acceptance_criteria>

  <done>Corrections chip editor with add/edit/remove modal handlers, unmatched units batch panel with "Add All as Pass-through" and per-unit "Add Mapping" buttons, all wired into accordion panels with count badges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> corrections regex | User-entered pattern strings become gsub() regex arguments |
| User input -> unit mapping modal | User-entered from_unit/to_unit become tibble values used in harmonization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Tampering | save_unit_mapping modal inputs | accept | Session-scoped; user is the data curator and has authority to add/edit mappings. No persistence to disk. |
| T-34-05 | Denial of Service | corrections regex via gsub() | mitigate | Already mitigated in Plan 01 -- apply_corrections() wraps each gsub() in tryCatch, skips failed patterns with warning |
| T-34-06 | Spoofing | onclick JS injection in unmatched unit names | mitigate | Unit names are escaped via gsub("'", "\\\\\\\\'", u$orig_unit) before embedding in onclick JS string to prevent injection |
</threat_model>

<verification>
1. Unit chip editor renders chips with arrow format and optional multiplier
2. Click unit chip -> edit modal opens pre-filled with all 6 fields
3. Click x on user chip -> chip removed from unit_map_working
4. "Add Unit Mapping" button -> blank modal with all 6 fields
5. Save modal -> row appended/updated in unit_map_working
6. Corrections chip editor renders pattern -> replacement chips
7. "Add Correction" -> modal with pattern + replacement fields
8. Unmatched panel shows unit list with counts after pipeline run
9. "Add All as Pass-through" adds identity mappings for all unmatched
10. Per-unit "Add Mapping" opens modal pre-filled with from_unit
</verification>

<success_criteria>
- All three accordion panels render with correct titles and item counts
- Unit table chips display from_unit -> to_unit format per D-07
- Corrections chips display pattern -> replacement format per D-10
- Modal forms validate required fields before saving
- Unmatched units panel transitions between pre-run, all-matched, and unmatched-list states
- Cascade reset fires correctly when editors change (already wired in Plan 01)
</success_criteria>

<output>
After completion, create `.planning/phases/34-harmonize-tab-module/34-02-SUMMARY.md`
</output>
