---
phase: 31.5-units-package-assimilation
plan: 03
type: execute
wave: 2
depends_on: [31.5-01, 31.5-02]
files_modified:
  - R/unit_harmonizer.R
  - R/cleaning_reference.R
  - tests/testthat/test-unit-harmonizer.R
autonomous: true
requirements: [UNIT-01, UNIT-02, UNIT-03, UNIT-04, UNIT-05]

must_haves:
  truths:
    - "harmonize_units() accepts optional media, dtxsid, and molecular_weight parameters"
    - "Molarity units trigger MW lookup when DTXSID provided"
    - "ppb/ppm route to correct target based on media context"
    - "Extended unit_flag values track conversion pathway"
    - "Existing calls without new params continue to work (backward compatible)"
    - "Synonyms are loaded internally via system.file() - no external parameter needed"
  artifacts:
    - path: "R/unit_harmonizer.R"
      provides: "Upgraded harmonize_units() with context-aware conversions"
      exports: ["harmonize_units", "normalize_unit_string"]
      min_lines: 200
    - path: "R/cleaning_reference.R"
      provides: "load_unit_synonyms() function added"
      exports: ["load_unit_synonyms"]
    - path: "tests/testthat/test-unit-harmonizer.R"
      provides: "Expanded test coverage for new features"
      min_tests: 90
  key_links:
    - from: "R/unit_harmonizer.R"
      to: "ComptoxR::ct_chemical_detail_search_bulk"
      via: "MW lookup for molarity conversion"
      pattern: "ct_chemical_detail_search_bulk"
    - from: "R/unit_harmonizer.R"
      to: "inst/extdata/unit_synonyms.rds"
      via: "system.file() internal load"
      pattern: "system\\.file.*unit_synonyms"
---

<objective>
Upgrade harmonize_units() to support context-aware conversions: molarity-to-mg/L with MW lookup, and ppb/ppm routing based on media context.

Purpose: The current harmonize_units() does simple table lookup. Molarity units (M, mM, uM) require molecular weight to convert to mg/L, and ppb/ppm are ambiguous (aqueous=mg/L, solid=mg/kg, air=mg/m3). This plan adds optional context parameters per D-11/D-12 from CONTEXT.md.

Output:
- Upgraded R/unit_harmonizer.R with extended signature
- load_unit_synonyms() in R/cleaning_reference.R
- Expanded test coverage (90+ tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.5-units-package-assimilation/31.5-CONTEXT.md
@.planning/phases/31-unit-harmonization-engine/31-01-SUMMARY.md
@R/unit_harmonizer.R
@R/cleaning_reference.R

<interfaces>
<!-- Current harmonize_units() signature (to be extended) -->
From R/unit_harmonizer.R (Phase 31):
```r
#' @export
harmonize_units <- function(values, units, unit_map) { ... }
```

<!-- Target signature per D-12 -->
New signature:
```r
harmonize_units <- function(
  values,
  units,
  unit_map,
  media = NULL,           # "aqueous" | "air" | "solid" | NULL (default aqueous)
  dtxsid = NULL,          # for MW lookup when molarity detected
  molecular_weight = NULL # optional override, skips API call
) { ... }
```

<!-- MW lookup pattern from Phase 17 -->
From R/consensus.R (enrich_candidates pattern):
```r
# ComptoxR bulk lookup
result <- ComptoxR::ct_chemical_detail_search_bulk(dtxsid_vector)
# result has columns: dtxsid, mol_weight, ...
```

<!-- Conversion formulas per D-06 -->
mg_L = molarity_M * molecular_weight_g_mol * 1000
mg_L = molarity_mM * molecular_weight_g_mol
mg_L = molarity_uM * molecular_weight_g_mol * 0.001
mg_L = molarity_nM * molecular_weight_g_mol * 1e-6
mg_L = molarity_pM * molecular_weight_g_mol * 1e-9
```
</interfaces>

<decisions>
Per D-05: Live CompTox API lookup for MW when molarity detected AND DTXSID available.
Per D-06: Conversion formula: mg/L = molarity x MW x scale_factor.
Per D-07: If DTXSID unavailable or API fails, flag as "needs_mw" and pass through unconverted.
Per D-08: Media context from column tags when available.
Per D-09: Default to aqueous when media context unknown. Add "media_inferred" to audit trail.
Per D-10: ppb/ppm routing uses inferred media.
Per D-13: Backward compatible - existing calls without new params continue to work.
Per D-17: Extended unit_flag values: "", "case_fallback", "unmatched", "needs_mw", "media_inferred".
</decisions>

<synonym_loading_design>
## Synonym Loading Strategy

**Decision:** harmonize_units() loads synonyms INTERNALLY via system.file().
Users do NOT pass synonyms as a parameter - this maintains backward compatibility
and simplifies the API.

Implementation:
```r
# Inside harmonize_units(), load synonyms once per call via closure/memoization:
get_synonyms <- function() {
  path <- system.file("extdata/unit_synonyms.rds", package = "chemreg")
  if (file.exists(path)) readRDS(path) else NULL
}
```

Rationale:
1. Backward compatible - existing calls work unchanged
2. Single source of truth - synonyms always come from package data
3. No user burden to load/pass synonyms
4. load_unit_synonyms() in cleaning_reference.R exists for inspection/debugging only
</synonym_loading_design>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add load_unit_synonyms() to cleaning_reference.R</name>
  <files>R/cleaning_reference.R</files>
  <action>
Add load_unit_synonyms() function to R/cleaning_reference.R, following the pattern of load_unit_map().
This function is for inspection/debugging - harmonize_units() loads synonyms internally.

```r
#' Load unit synonym normalization table
#'
#' Returns a tibble for normalizing variant unit spellings to canonical forms
#' before lookup in the main unit conversion table.
#'
#' Note: harmonize_units() loads this table internally via system.file().
#' This exported function is for inspection and debugging purposes.
#'
#' Structure:
#' - input_pattern: String or regex pattern to match
#' - normalized_unit: Canonical form to use for lookup
#' - is_regex: TRUE if pattern is regex, FALSE if exact match
#' - notes: Documentation for the mapping
#'
#' @param cache_dir Directory for cache files (e.g., "inst/extdata")
#' @return Tibble with columns: input_pattern, normalized_unit, is_regex, notes
#' @export
load_unit_synonyms <- function(cache_dir) {
  cache_path <- file.path(cache_dir, "unit_synonyms.rds")

  fetch_fn <- function() {
    warning("unit_synonyms.rds not found - returning minimal default")
    tibble::tibble(
      input_pattern = c("mg/kg bw/day", "ug/kg bw/day"),
      normalized_unit = c("mg/kg/d", "ug/kg/d"),
      is_regex = c(FALSE, FALSE),
      notes = c("body weight qualifier", "body weight qualifier")
    )
  }

  load_or_fetch_reference(cache_path, fetch_fn, "unit synonyms")
}
```

Also update load_all_reference_lists() to include unit_synonyms in the returned list.
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); load_unit_synonyms" | grep -q "function"</automated>
  </verify>
  <done>load_unit_synonyms() exported, load_all_reference_lists() updated</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade harmonize_units() with context-aware conversions</name>
  <files>R/unit_harmonizer.R</files>
  <action>
Upgrade R/unit_harmonizer.R to support context-aware conversions while maintaining backward compatibility.

Key changes:

1. **Extended signature** (per D-12):
```r
harmonize_units <- function(
  values,
  units,
  unit_map,
  media = NULL,
  dtxsid = NULL,
  molecular_weight = NULL
) { ... }
```

2. **Internal synonym loading** (NOT a parameter - loaded via system.file):
```r
# Load synonyms internally at start of function
get_unit_synonyms <- function() {
  path <- system.file("extdata/unit_synonyms.rds", package = "chemreg")
  if (file.exists(path)) readRDS(path) else NULL
}

# Apply synonym normalization (after normalize_unit_string)
apply_synonyms <- function(unit_strings, synonyms) {
  if (is.null(synonyms) || nrow(synonyms) == 0) return(unit_strings)

  result <- unit_strings
  for (i in seq_len(nrow(synonyms))) {
    pattern <- synonyms$input_pattern[i]
    replacement <- synonyms$normalized_unit[i]
    if (synonyms$is_regex[i]) {
      result <- gsub(pattern, replacement, result, ignore.case = TRUE)
    } else {
      result <- ifelse(tolower(result) == tolower(pattern), replacement, result)
    }
  }
  result
}
```

3. **Molarity detection and MW lookup** (per D-05, D-06):
```r
# Helper to detect molarity units
is_molarity_unit <- function(unit) {
  tolower(unit) %in% c("m", "mm", "um", "nm", "pm", "mol/l", "mmol/l", "umol/l", "nmol/l", "pmol/l")
}

# Molarity scale factors (to mg/L given MW in g/mol)
get_molarity_scale <- function(unit) {
  scales <- c(
    "m" = 1000, "mol/l" = 1000,      # M * MW * 1000 = mg/L
    "mm" = 1, "mmol/l" = 1,          # mM * MW = mg/L
    "um" = 0.001, "umol/l" = 0.001,  # uM * MW * 0.001 = mg/L
    "nm" = 1e-6, "nmol/l" = 1e-6,
    "pm" = 1e-9, "pmol/l" = 1e-9
  )
  scales[tolower(unit)]
}

# MW lookup via ComptoxR (with caching)
fetch_molecular_weight <- function(dtxsid_unique) {
  if (!requireNamespace("ComptoxR", quietly = TRUE)) {
    return(rep(NA_real_, length(dtxsid_unique)))
  }
  tryCatch({
    result <- ComptoxR::ct_chemical_detail_search_bulk(dtxsid_unique)
    # Return MW vector aligned with input dtxsids
    mw <- result$mol_weight[match(dtxsid_unique, result$dtxsid)]
    mw
  }, error = function(e) {
    rep(NA_real_, length(dtxsid_unique))
  })
}
```

4. **Media-based ppb/ppm routing** (per D-08, D-09, D-10):
```r
# Get target unit for ambiguous units based on media
get_media_target <- function(unit, media) {
  if (is.null(media)) media <- "aqueous"  # D-09: default aqueous

  unit_lower <- tolower(unit)
  if (unit_lower %in% c("ppb", "ppm")) {
    switch(media,
      "aqueous" = "mg/L",
      "air" = "mg/m3",
      "solid" = "mg/kg",
      "mg/L"  # default
    )
  } else {
    NULL  # not a media-dependent unit
  }
}
```

5. **Extended unit_flag values** (per D-17):
- "" = exact match, successful conversion
- "case_fallback" = case-insensitive match
- "unmatched" = no match, passed through
- "needs_mw" = molarity unit but no MW available
- "media_inferred" = ppb/ppm converted using default aqueous assumption

6. **Main conversion logic**:
- Load synonyms internally via system.file() (NOT from parameter)
- Apply synonym normalization to unit strings
- Then existing normalize_unit_string()
- Detect molarity: if molarity AND (dtxsid OR molecular_weight provided), convert
- Detect ppb/ppm: route based on media
- Fall back to table lookup
- Preserve backward compatibility: calls without new params work exactly as before

Update roxygen documentation for new parameters.
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); args(harmonize_units)" | grep -q "media"</automated>
  </verify>
  <done>harmonize_units() has extended signature, loads synonyms internally via system.file(), supports molarity MW conversion, ppb/ppm media routing, backward compatible</done>
</task>

<task type="auto">
  <name>Task 3: Expand test coverage for context-aware conversions</name>
  <files>tests/testthat/test-unit-harmonizer.R</files>
  <action>
Expand tests/testthat/test-unit-harmonizer.R to cover new functionality. Add ~50 new tests to existing 40 tests for 90+ total.

New test sections:

**Backward compatibility (5 tests)**:
- Existing 3-param calls still work
- No behavior change when new params are NULL
- Old test cases still pass

**Molarity detection (8 tests)**:
- is_molarity_unit() detects M, mM, uM, nM, pM
- is_molarity_unit() detects mol/L, mmol/L variants
- is_molarity_unit() returns FALSE for mg/L, ug/L, ppb

**Molarity conversion with MW (10 tests)**:
- M with MW=100 -> mg/L = value * 100 * 1000
- mM with MW=100 -> mg/L = value * 100
- uM with MW=100 -> mg/L = value * 100 * 0.001
- nM with MW=100 -> mg/L = value * 100 * 1e-6
- Molarity without MW -> unit_flag = "needs_mw", value unchanged
- molecular_weight param overrides API lookup
- dtxsid param triggers API lookup (mock ComptoxR)

**ppb/ppm media routing (12 tests)**:
- ppb + media="aqueous" -> mg/L
- ppb + media="solid" -> mg/kg
- ppb + media="air" -> mg/m3
- ppm + media="aqueous" -> mg/L
- ppm + media=NULL -> mg/L (default aqueous), flag="media_inferred"
- Conversion factors: ppb->mg/L = x0.001, ppm->mg/L = x1

**Synonym normalization (8 tests)**:
- "mg/kg bw/day" -> normalized to "mg/kg/d"
- "% (w/v)" -> normalized to "%w/v"
- Synonym applied before table lookup
- Non-matching strings pass through unchanged
- Synonyms loaded internally (not passed as param)

**Extended unit_flag values (7 tests)**:
- "" for exact match
- "case_fallback" for case-insensitive
- "unmatched" for no match
- "needs_mw" for molarity without MW
- "media_inferred" for ppb/ppm with default media

**Integration tests (5 tests)**:
- Vector input with mixed molarity/ppb/mg/L
- Molarity + media context combined
- Real-ish SSWQS-style input

Use withr::local_mocked_bindings() to mock ComptoxR::ct_chemical_detail_search_bulk for MW lookup tests.
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); testthat::test_file('tests/testthat/test-unit-harmonizer.R')" | grep -q "PASS"</automated>
  </verify>
  <done>test-unit-harmonizer.R has 90+ tests covering all new functionality, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R -> CompTox API | MW lookup sends DTXSIDs to external API |
| User input -> conversion | Unit strings and values from untrusted sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-05 | I (Information Disclosure) | MW lookup | accept | DTXSIDs are public identifiers; API call is read-only |
| T-31.5-06 | D (Denial of Service) | API failure | mitigate | tryCatch with graceful fallback; "needs_mw" flag instead of error |
| T-31.5-07 | T (Tampering) | unit input | mitigate | Input validation via normalize_unit_string(); no code execution from unit strings |
</threat_model>

<verification>
1. `devtools::check()` passes without errors
2. Existing harmonize_units() calls work unchanged (backward compatible)
3. Molarity with MW converts correctly
4. Molarity without MW returns "needs_mw" flag
5. ppb/ppm route correctly based on media
6. Synonyms loaded internally via system.file() (no param needed)
7. All 90+ tests pass
</verification>

<success_criteria>
- [ ] harmonize_units() accepts media, dtxsid, molecular_weight params
- [ ] Existing 3-param calls work identically (backward compatible)
- [ ] Synonyms loaded internally via system.file() (not passed as param)
- [ ] M/mM/uM + MW converts to mg/L using correct formula
- [ ] Molarity without MW sets unit_flag="needs_mw"
- [ ] ppb/ppm + media routes to correct target (mg/L, mg/kg, mg/m3)
- [ ] ppb/ppm without media defaults to aqueous with "media_inferred" flag
- [ ] load_unit_synonyms() exported (for inspection/debugging only)
- [ ] 90+ tests pass including all new coverage
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-units-package-assimilation/31.5-03-SUMMARY.md`
</output>
