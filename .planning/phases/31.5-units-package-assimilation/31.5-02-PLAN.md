---
phase: 31.5-units-package-assimilation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - R/zzz.R
  - tests/testthat/test-unit-registrations.R
autonomous: true
requirements: [UNIT-01, UNIT-02]

must_haves:
  truths:
    - "`units` package is declared as hard Imports dependency"
    - "Domain-specific units (molarity, turbidity, microbial) are registered on package load"
    - "Registration tests verify units are parseable after chemreg loads"
  artifacts:
    - path: "DESCRIPTION"
      provides: "Package dependency declaration"
      contains: "units"
    - path: "R/zzz.R"
      provides: "Package initialization with unit registrations"
      exports: []
    - path: "tests/testthat/test-unit-registrations.R"
      provides: "Tests for unit registration"
      min_tests: 15
  key_links:
    - from: "R/zzz.R"
      to: "units::install_unit"
      via: ".onLoad hook"
      pattern: "units::install_unit"
---

<objective>
Add the `units` package as a hard dependency and register domain-specific units (molarity, turbidity, microbial) on package load.

Purpose: The R `units` package provides automatic conversion via the udunits2 engine, but lacks chemistry-specific units like molarity (M, mM, uM) and environmental units (NTU, CFU). Registering these on package load enables `units::set_units()` to work with regulatory data.

Output:
- DESCRIPTION with units in Imports
- R/zzz.R with .onLoad() registrations
- Test file verifying registrations work
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31.5-units-package-assimilation/31.5-CONTEXT.md
@.planning/phases/31.5-units-package-assimilation/31.5-RESEARCH.md

<interfaces>
<!-- From RESEARCH.md - proven registration patterns -->
```r
# Molarity (based on mol/L)
units::install_unit("M", "mol/L", "molar")
units::install_unit("mM", "mmol/L", "millimolar")
units::install_unit("uM", "umol/L", "micromolar")
units::install_unit("nM", "nmol/L", "nanomolar")
units::install_unit("pM", "pmol/L", "picomolar")

# Turbidity (dimensionless)
units::install_unit("NTU", name = "nephelometric turbidity unit")

# Test (proven working per RESEARCH.md):
x <- units::set_units(1, "M")
y <- units::set_units(x, "mM")
# Result: 1000 mM
```
</interfaces>

<decisions>
Per D-03 from CONTEXT.md: `units` package as hard dependency (Imports, not Suggests).
Per D-04 from CONTEXT.md: Register on package load via .onLoad() in R/zzz.R.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add units package to DESCRIPTION Imports</name>
  <files>DESCRIPTION</files>
  <action>
Edit DESCRIPTION to add `units` to the Imports section.

1. Read current DESCRIPTION
2. Add `units (>= 0.8-0)` to Imports (alphabetically sorted)
3. Write updated DESCRIPTION

The version constraint >= 0.8-0 ensures install_unit() is available (added in 0.8-0).
  </action>
  <verify>
    <automated>grep -q "units" DESCRIPTION</automated>
  </verify>
  <done>DESCRIPTION contains "units" in Imports section with version constraint</done>
</task>

<task type="auto">
  <name>Task 2: Create R/zzz.R with unit registrations</name>
  <files>R/zzz.R</files>
  <action>
Create R/zzz.R with .onLoad() hook that registers domain-specific units.

```r
# zzz.R
# Package initialization: register domain-specific units on load
#
# The `units` package (udunits2 engine) lacks chemistry-specific units.
# We register them here so they're available immediately after library(chemreg).

.onLoad <- function(libname, pkgname) {
  register_chemreg_units()
}

#' Register chemistry and environmental domain units
#'
#' Called automatically on package load. Registers units not in udunits2:
#' - Molarity: M, mM, uM, nM, pM (based on mol/L)
#' - Turbidity: NTU, FTU, JTU (dimensionless, not interconvertible)
#' - Microbial: CFU, MPN (dimensionless counts)
#' - Hardness: (reserved for future if needed)
#'
#' @return NULL (called for side effects)
#' @keywords internal
register_chemreg_units <- function() {
  if (!requireNamespace("units", quietly = TRUE)) {
    return(invisible(NULL))
  }

  # Molarity (based on mol/L - udunits2 has mol and L)
  # M = 1 mol/L, mM = 0.001 mol/L, etc.
  tryCatch({
    units::install_unit("M", "mol/L", "molar")
    units::install_unit("mM", "mmol/L", "millimolar")
    units::install_unit("uM", "umol/L", "micromolar")
    units::install_unit("nM", "nmol/L", "nanomolar")
    units::install_unit("pM", "pmol/L", "picomolar")
  }, error = function(e) {
    # Units may already be registered (e.g., package reloaded)
    # This is not an error condition
  })

  # Turbidity (dimensionless - not interconvertible with each other)
  tryCatch({
    units::install_unit("NTU", name = "nephelometric turbidity unit")
    units::install_unit("FTU", name = "formazin turbidity unit")
    units::install_unit("JTU", name = "jackson turbidity unit")
  }, error = function(e) {
    # Already registered or not critical
  })

  # Microbial counts (dimensionless)
  tryCatch({
    units::install_unit("CFU", name = "colony forming unit")
    units::install_unit("MPN", name = "most probable number")
  }, error = function(e) {
    # Already registered or not critical
  })

  invisible(NULL)
}
```

Key points:
- Use requireNamespace() guard for units package
- Wrap each registration block in tryCatch() - units may already be registered if package is reloaded
- Molarity uses mol/L as base (udunits2 has mol and L)
- Turbidity/microbial are dimensionless - just named, no conversion relation
- Do NOT export register_chemreg_units() - it's internal
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); x <- units::set_units(1, 'M'); print(x)"</automated>
  </verify>
  <done>R/zzz.R exists with .onLoad() that registers molarity, turbidity, and microbial units</done>
</task>

<task type="auto">
  <name>Task 3: Create unit registration tests</name>
  <files>tests/testthat/test-unit-registrations.R</files>
  <action>
Create tests/testthat/test-unit-registrations.R to verify registrations work.

```r
# test-unit-registrations.R
# Tests for domain-specific unit registrations
# These units are registered by R/zzz.R on package load

# Skip all tests if units package not available
skip_if_not_installed("units")

# ---- Molarity conversions ----

test_that("M (molar) is recognized", {
  x <- units::set_units(1, "M")
  expect_true(inherits(x, "units"))
})

test_that("mM (millimolar) is recognized", {
  x <- units::set_units(1, "mM")
  expect_true(inherits(x, "units"))
})

test_that("uM (micromolar) is recognized", {
  x <- units::set_units(1, "uM")
  expect_true(inherits(x, "units"))
})

test_that("nM (nanomolar) is recognized", {
  x <- units::set_units(1, "nM")
  expect_true(inherits(x, "units"))
})

test_that("pM (picomolar) is recognized", {
  x <- units::set_units(1, "pM")
  expect_true(inherits(x, "units"))
})

test_that("M -> mM conversion is correct (x1000)", {
  x <- units::set_units(1, "M")
  y <- units::set_units(x, "mM")
  expect_equal(as.numeric(y), 1000)
})

test_that("mM -> uM conversion is correct (x1000)", {
  x <- units::set_units(1, "mM")
  y <- units::set_units(x, "uM")
  expect_equal(as.numeric(y), 1000)
})

test_that("uM -> nM conversion is correct (x1000)", {
  x <- units::set_units(1, "uM")
  y <- units::set_units(x, "nM")
  expect_equal(as.numeric(y), 1000)
})

test_that("M -> mol/L conversion is correct (x1)", {
  x <- units::set_units(1, "M")
  y <- units::set_units(x, "mol/L")
  expect_equal(as.numeric(y), 1)
})

# ---- Turbidity (dimensionless) ----

test_that("NTU is recognized", {
  x <- units::set_units(5, "NTU")
  expect_true(inherits(x, "units"))
  expect_equal(as.numeric(x), 5)
})

test_that("FTU is recognized", {
  x <- units::set_units(10, "FTU")
  expect_true(inherits(x, "units"))
})

test_that("JTU is recognized", {
  x <- units::set_units(3, "JTU")
  expect_true(inherits(x, "units"))
})

# ---- Microbial (dimensionless) ----

test_that("CFU is recognized", {
  x <- units::set_units(100, "CFU")
  expect_true(inherits(x, "units"))
})

test_that("MPN is recognized", {
  x <- units::set_units(50, "MPN")
  expect_true(inherits(x, "units"))
})

# ---- udunits2 built-in units still work ----

test_that("mg/L (udunits2 built-in) still works", {
  x <- units::set_units(1, "mg/L")
  expect_true(inherits(x, "units"))
})

test_that("ug/L -> mg/L conversion works", {
  x <- units::set_units(1000, "ug/L")
  y <- units::set_units(x, "mg/L")
  expect_equal(as.numeric(y), 1)
})
```

Ensure:
- Tests skip gracefully if units package not installed
- Molarity conversion chain verified (M -> mM -> uM -> nM)
- Dimensionless units (NTU, CFU, MPN) recognized
- Built-in udunits2 units (mg/L, ug/L) still work
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); testthat::test_file('tests/testthat/test-unit-registrations.R')"</automated>
  </verify>
  <done>test-unit-registrations.R has 17+ tests, all pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R session -> udunits2 | Unit registration calls into udunits2 C library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-03 | D (Denial of Service) | unit registration | mitigate | tryCatch around each registration block; already-registered units don't crash |
| T-31.5-04 | I (Information Disclosure) | package load | accept | Unit names are not secrets; registration is logged to console in verbose mode |
</threat_model>

<verification>
1. `devtools::check()` passes without errors related to units
2. After `library(chemreg)`, `units::set_units(1, "M")` works
3. All 17+ registration tests pass
4. Package reload doesn't crash (units already registered)
</verification>

<success_criteria>
- [ ] DESCRIPTION has `units (>= 0.8-0)` in Imports
- [ ] R/zzz.R exists with .onLoad() calling register_chemreg_units()
- [ ] Molarity units (M, mM, uM, nM, pM) are registered
- [ ] Turbidity units (NTU, FTU, JTU) are registered
- [ ] Microbial units (CFU, MPN) are registered
- [ ] All registration tests pass
- [ ] Package reloads without error (tryCatch guards work)
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-units-package-assimilation/31.5-02-SUMMARY.md`
</output>
