---
phase: 31.5-units-package-assimilation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/unit_extraction.R
  - inst/extdata/reference_cache/unit_conversion.rds
  - inst/extdata/unit_synonyms.rds
autonomous: true
requirements: [UNIT-01, UNIT-02]

must_haves:
  truths:
    - "Unit extraction script pulls distinct units from ECOTOX, ToxVal, and SSWQS sources"
    - "Expanded unit_conversion.rds contains 600+ conversion entries from ECOTOX foundation"
    - "Synonym normalization table maps variant spellings to canonical forms"
    - "Conversion targets use UCUM conventions (mg/L capital L)"
  artifacts:
    - path: "R/unit_extraction.R"
      provides: "One-time extraction functions for ECOTOX/ToxVal/SSWQS units"
      exports: ["extract_ecotox_units", "extract_toxval_units", "extract_sswqs_units", "build_unit_conversion_table"]
    - path: "inst/extdata/reference_cache/unit_conversion.rds"
      provides: "Comprehensive unit conversion table (600+ rows)"
      min_rows: 600
    - path: "inst/extdata/unit_synonyms.rds"
      provides: "Synonym normalization table (~40 rows)"
      min_rows: 30
  key_links:
    - from: "R/unit_extraction.R"
      to: "ECOTOX ecotox.duckdb"
      via: "DBI/duckdb query"
      pattern: "DBI::dbConnect.*duckdb"
    - from: "R/unit_extraction.R"
      to: "inst/extdata/reference_cache/unit_conversion.rds"
      via: "saveRDS"
      pattern: "saveRDS.*unit_conversion"
---

<objective>
Extract distinct units from ECOTOX/ToxVal/SSWQS data sources and build comprehensive unit conversion and synonym normalization tables.

Purpose: Replace the minimal 4-row unit_conversion.rds with a comprehensive table derived from real regulatory data sources. The ECOTOX database contains 706 well-curated conversion entries that serve as the foundation; ToxVal and SSWQS provide additional coverage for units seen in wild datasets.

Output:
- R/unit_extraction.R with extraction functions
- Expanded unit_conversion.rds (600+ rows)
- New unit_synonyms.rds (~40 rows for variant spelling normalization)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31.5-units-package-assimilation/31.5-CONTEXT.md
@.planning/phases/31.5-units-package-assimilation/31.5-RESEARCH.md
@.planning/phases/31-unit-harmonization-engine/31-01-SUMMARY.md

<interfaces>
<!-- Existing interfaces from Phase 31 -->
From R/unit_harmonizer.R:
```r
#' @export
harmonize_units <- function(values, units, unit_map) { ... }
normalize_unit_string <- function(x) { ... }  # internal
```

From R/cleaning_reference.R:
```r
#' @export
load_unit_map <- function(cache_dir) { ... }
```

Expected unit_map structure (6 columns):
- from_unit: character - source unit string
- to_unit: character - target canonical unit
- multiplier: numeric - conversion factor
- category: character - unit category
- confidence: character - HIGH/LOW/NEEDS_MW
- source: character - provenance (ECOTOX/ToxVal/SSWQS)
</interfaces>

<data_sources>
Per D-02a/D-02b/D-02c from CONTEXT.md:

ECOTOX: C:/Users/sxthi/AppData/Roaming/R/data/R/ComptoxR/ecotox.duckdb
  - unit_conversion table: 706 rows, 15 domains
  - Columns: orig, cur_unit_result, multiplier
  - Also extract exposure_type and media_type for context

ToxVal: C:/Users/sxthi/AppData/Roaming/R/data/R/ComptoxR/toxval.duckdb
  - toxval table: 253 distinct units in toxval_units column

SSWQS: C:/Users/sxthi/Documents/curation/final/sswqs.parquet
  - 81 distinct units in unit column
</data_sources>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit extraction module</name>
  <files>R/unit_extraction.R</files>
  <action>
Create R/unit_extraction.R with four exported functions:

1. `extract_ecotox_units(db_path)` - Query ECOTOX ecotox.duckdb:
   - Connect via DBI::dbConnect(duckdb::duckdb(), db_path, read_only=TRUE)
   - Query unit_conversion table: SELECT orig, cur_unit_result, multiplier
   - Transform SI base targets to UCUM: g/l -> mg/L (multiply by 1000), mol/l -> M (keep), etc.
   - Return tibble with: from_unit, to_unit, multiplier, category, confidence, source
   - Add category inference from cur_unit_result patterns (concentration, mass, dose, etc.)
   - Mark molarity entries (mol/l, M, mM, etc.) with confidence="NEEDS_MW"

2. `extract_toxval_units(db_path)` - Query ToxVal toxval.duckdb:
   - SELECT DISTINCT toxval_units FROM toxval WHERE toxval_units IS NOT NULL
   - Return tibble with distinct unit strings for gap analysis
   - Do NOT create conversions - just inventory for coverage checking

3. `extract_sswqs_units(parquet_path)` - Read SSWQS parquet:
   - Use arrow::read_parquet() to read file
   - Extract distinct(unit) column values
   - Return tibble with distinct unit strings for gap analysis

4. `build_unit_conversion_table(ecotox_path, toxval_path = NULL, sswqs_path = NULL, output_path)`:
   - Call extract_ecotox_units() as foundation
   - If toxval_path provided, call extract_toxval_units() for gap analysis
   - If sswqs_path provided, call extract_sswqs_units() for gap analysis
   - Log coverage stats: N units covered, M units missing
   - Transform ECOTOX SI targets to UCUM conventions per D-02d:
     * g/l -> mg/L (multiply original multiplier by 1000)
     * mol/l -> M (keep multiplier, mark NEEDS_MW)
     * g/kg -> mg/kg (multiply by 1000)
   - Deduplicate by from_unit (keep first occurrence)
   - Save to output_path via saveRDS()
   - Return the tibble

Add roxygen2 documentation with @export tags for all four functions.
Add @importFrom statements: DBI dbConnect dbDisconnect dbGetQuery, duckdb duckdb, arrow read_parquet, tibble tibble, dplyr distinct mutate case_when filter bind_rows.
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); extract_ecotox_units" | grep -q "function"</automated>
  </verify>
  <done>R/unit_extraction.R exists with 4 exported functions, roxygen headers complete, no syntax errors</done>
</task>

<task type="auto">
  <name>Task 2: Build expanded unit_conversion.rds from ECOTOX</name>
  <files>inst/extdata/reference_cache/unit_conversion.rds</files>
  <action>
Execute build_unit_conversion_table() to create the expanded conversion table:

1. Write a temp R script that:
   - Loads chemreg via devtools::load_all()
   - Calls build_unit_conversion_table() with:
     * ecotox_path = "C:/Users/sxthi/AppData/Roaming/R/data/R/ComptoxR/ecotox.duckdb"
     * toxval_path = "C:/Users/sxthi/AppData/Roaming/R/data/R/ComptoxR/toxval.duckdb"
     * sswqs_path = "C:/Users/sxthi/Documents/curation/final/sswqs.parquet"
     * output_path = "inst/extdata/reference_cache/unit_conversion.rds"
   - Prints summary stats: nrow, unique categories, coverage against ToxVal/SSWQS

2. Verify the output table:
   - Has >= 600 rows (ECOTOX has 706, some deduplication expected)
   - Has all 6 required columns: from_unit, to_unit, multiplier, category, confidence, source
   - Categories include at least: concentration, mass_fraction, dose, molarity
   - Targets use UCUM conventions: mg/L (capital L), mg/kg, mg/m3

3. Log coverage analysis:
   - What % of ToxVal units are covered?
   - What % of SSWQS units are covered?
   - List top 10 uncovered units for manual review
  </action>
  <verify>
    <automated>Rscript -e "x <- readRDS('inst/extdata/reference_cache/unit_conversion.rds'); stopifnot(nrow(x) >= 600, all(c('from_unit','to_unit','multiplier','category','confidence','source') %in% names(x)))"</automated>
  </verify>
  <done>unit_conversion.rds contains 600+ rows with proper schema, UCUM target conventions, coverage logged</done>
</task>

<task type="auto">
  <name>Task 3: Create unit_synonyms.rds normalization table</name>
  <files>inst/extdata/unit_synonyms.rds</files>
  <action>
Create a synonym normalization table for variant unit spellings. This table maps non-canonical spellings to canonical forms BEFORE lookup in unit_conversion.rds.

Structure:
```r
tibble::tibble(
  input_pattern = character(),  # Regex or exact string
  normalized_unit = character(), # Canonical form for lookup
  is_regex = logical(),         # TRUE if pattern is regex, FALSE if exact
  notes = character()           # Documentation
)
```

Include mappings (per RESEARCH.md findings):

Micro symbol variants:
- "ug/L", "ug/l" (already ASCII) -> no mapping needed (handled by normalize_unit_string)
- micro sign variants handled by existing normalize_unit_string()

Case normalization (for units not in table):
- Liter variants: l -> L (when lowercase l appears as unit)
- This is tricky - L/l distinction matters in some contexts

Body weight qualifiers (D-02d):
- "mg/kg bw/day" -> "mg/kg/d"
- "mg/kg-bw/day" -> "mg/kg/d"
- "ug/kg bw/day" -> "ug/kg/d"
- "g/kg bw/day" -> "g/kg/d"
- "ng/kg bw/day" -> "ng/kg/d"

Percent variants:
- "% (w/v)" -> "%w/v"
- "% (v/v)" -> "%v/v"
- "% (w/w)" -> "%w/w"
- "% w/v" -> "%w/v"
- "% v/v" -> "%v/v"
- "% w/w" -> "%w/w"

Dimensionless standardization:
- "ratio" -> "1" (or keep as-is, document)
- "fraction" -> "1"
- "unitless" -> "1"
- "dimensionless" -> "1"
- "none" -> "1"
- "-" -> "1"

Save to inst/extdata/unit_synonyms.rds.
  </action>
  <verify>
    <automated>Rscript -e "x <- readRDS('inst/extdata/unit_synonyms.rds'); stopifnot(nrow(x) >= 30, all(c('input_pattern','normalized_unit','is_regex','notes') %in% names(x)))"</automated>
  </verify>
  <done>unit_synonyms.rds exists with 30+ mappings, proper schema, covers body weight qualifiers and percent variants</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file system -> R | Reading from user's local DuckDB and Parquet files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31.5-01 | I (Information Disclosure) | DuckDB paths | accept | Paths are local user data, not secrets; logged for reproducibility |
| T-31.5-02 | T (Tampering) | unit_conversion.rds | mitigate | Output file is in package inst/extdata, version controlled |
</threat_model>

<verification>
1. R/unit_extraction.R passes devtools::check() without errors
2. unit_conversion.rds has >= 600 rows
3. unit_synonyms.rds has >= 30 rows
4. Coverage analysis logged showing ToxVal/SSWQS coverage percentages
5. ECOTOX SI base units transformed to UCUM (mg/L not g/l)
</verification>

<success_criteria>
- [ ] extract_ecotox_units() returns 600+ rows from ECOTOX
- [ ] build_unit_conversion_table() creates expanded unit_conversion.rds
- [ ] Molarity units marked with confidence="NEEDS_MW"
- [ ] Targets use UCUM conventions (capital L)
- [ ] unit_synonyms.rds contains body weight qualifier and percent variant mappings
- [ ] Coverage analysis documents gap between extracted and available units
</success_criteria>

<output>
After completion, create `.planning/phases/31.5-units-package-assimilation/31.5-01-SUMMARY.md`
</output>
