---
phase: 26-app-relocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/app/app.R
  - inst/extdata/reference_cache/block_patterns.rds
  - inst/extdata/reference_cache/functional_categories.rds
  - inst/extdata/reference_cache/isotope_lookup.rds
  - inst/extdata/reference_cache/stop_words.rds
  - inst/extdata/reference_cache/strip_terms.rds
  - R/run_app.R
  - DESCRIPTION
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - APP-05

must_haves:
  truths:
    - "chemreg::run_app() launches the Shiny app after devtools::install()"
    - "inst/app/app.R exists and project root app.R is removed"
    - "Reference cache files are accessible via system.file() after install"
    - "No source() loop exists in inst/app/app.R"
  artifacts:
    - path: "inst/app/app.R"
      provides: "Relocated Shiny app entry point"
      min_lines: 100
    - path: "R/run_app.R"
      provides: "Exported run_app() launcher function"
      exports: ["run_app"]
    - path: "inst/extdata/reference_cache/stop_words.rds"
      provides: "Relocated reference cache"
  key_links:
    - from: "R/run_app.R"
      to: "inst/app/app.R"
      via: "system.file with app and chemreg"
      pattern: "system\\.file.*app.*chemreg"
    - from: "inst/app/app.R"
      to: "inst/extdata/reference_cache"
      via: "system.file with extdata and reference_cache"
      pattern: "system\\.file.*extdata.*reference_cache"
---

<objective>
Relocate the Shiny app to inst/app/ and create run_app() launcher so the app is launchable via chemreg::run_app() after package install.

Purpose: Complete the R package structure by placing the Shiny app in the standard inst/ location and providing a proper launcher function.

Output:
- inst/app/app.R (modified app without source loop)
- inst/extdata/reference_cache/*.rds (relocated cache files)
- R/run_app.R (exported launcher function)
- Root app.R removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-app-relocation/26-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create inst/ directories and relocate reference cache</name>
  <files>inst/extdata/reference_cache/*, inst/app/</files>
  <read_first>
    - data/reference_cache/ (verify files exist: block_patterns.rds, functional_categories.rds, isotope_lookup.rds, stop_words.rds, strip_terms.rds)
  </read_first>
  <action>
1. Create directory structure:
   - inst/app/
   - inst/extdata/reference_cache/

2. Move reference cache files from data/reference_cache/ to inst/extdata/reference_cache/:
   - block_patterns.rds
   - functional_categories.rds
   - isotope_lookup.rds
   - stop_words.rds
   - strip_terms.rds

3. Verify all 5 RDS files exist in new location.

4. Do NOT delete data/reference_cache/ yet - leave until app.R is updated.
  </action>
  <verify>
    <automated>ls inst/extdata/reference_cache/*.rds | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - inst/extdata/reference_cache/ contains exactly 5 .rds files
    - ls inst/extdata/reference_cache/ shows: block_patterns.rds, functional_categories.rds, isotope_lookup.rds, stop_words.rds, strip_terms.rds
  </acceptance_criteria>
  <done>Reference cache relocated to inst/extdata/reference_cache/ with all 5 files present</done>
</task>

<task type="auto">
  <name>Task 2: Create R/run_app.R with exported launcher</name>
  <files>R/run_app.R, DESCRIPTION</files>
  <read_first>
    - DESCRIPTION (current Suggests list, verify shiny is present)
  </read_first>
  <action>
1. Create R/run_app.R with roxygen documentation:
   - Function name: run_app
   - Parameters: ... (passed to shiny::runApp)
   - Returns: invisible NULL
   - Tag: @export
   - Body:
     - Get app_dir via system.file("app", package = "chemreg")
     - If app_dir is empty string, stop with error "Could not find app directory. Is chemreg installed?"
     - Call shiny::runApp(app_dir, ...)

2. Run devtools::document() to update NAMESPACE with the export.

3. Verify NAMESPACE now contains export(run_app).
  </action>
  <verify>
    <automated>grep "export(run_app)" NAMESPACE</automated>
  </verify>
  <acceptance_criteria>
    - R/run_app.R exists and contains run_app function definition
    - R/run_app.R contains @export roxygen tag
    - R/run_app.R contains system.file("app", package = "chemreg")
    - NAMESPACE contains line: export(run_app)
  </acceptance_criteria>
  <done>run_app() function created, documented, and exported in NAMESPACE</done>
</task>

<task type="auto">
  <name>Task 3: Relocate and modify app.R to inst/app/app.R</name>
  <files>inst/app/app.R, app.R</files>
  <read_first>
    - app.R (current content - lines 1-50 especially: library calls, source loop, reference_lists loading)
  </read_first>
  <action>
1. Copy app.R to inst/app/app.R

2. Modify inst/app/app.R with these changes:

   a) KEEP these library() calls (Shiny UI dependencies - per CONTEXT.md Decision 3):
      - library(shiny)
      - library(bslib)
      - library(bsicons)
      - library(reactable)
      - library(reactable.extras)
      - library(shinyjs)

   b) REMOVE these library() calls (now from chemreg namespace):
      - library(here)
      - library(janitor)
      - library(rio)
      - library(readxl)
      - library(writexl)
      - library(readr)
      - library(dplyr)
      - library(tidyr)
      - library(tibble)
      - library(purrr)
      - library(stringr)
      - library(tidyselect)
      - library(rlang)
      - library(ComptoxR)

   c) REMOVE the custom operators block (lines 28-32 in original):
      The %ni% and %||% operators are already defined in R/utils.R or should be added there.

   d) REMOVE the source() loop (lines 34-37 in original):
      The for loop that sources all R files must be removed.

   e) CHANGE the reference_lists loading (line 40 in original):
      FROM: reference_lists <- load_all_reference_lists(here::here("data", "reference_cache"))
      TO:   reference_lists <- load_all_reference_lists(system.file("extdata", "reference_cache", package = "chemreg"))

3. Delete the root app.R file.

4. If %ni% is not already exported from the package:
   - Check if R/utils.R exists and contains %ni%
   - If not, create/update R/utils.R with exported %ni% operator
   - Run devtools::document() again

5. Verify inst/app/app.R has no source() loop and no here::here() calls.
  </action>
  <verify>
    <automated>grep -c "for (f in list.files" inst/app/app.R; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - inst/app/app.R exists with 100+ lines
    - inst/app/app.R contains library(shiny) (Shiny UI deps kept)
    - inst/app/app.R does NOT contain library(dplyr) (removed)
    - inst/app/app.R does NOT contain for (f in list.files (source loop removed)
    - inst/app/app.R does NOT contain here::here (replaced with system.file)
    - inst/app/app.R contains system.file("extdata", "reference_cache", package = "chemreg")
    - Root app.R does NOT exist
  </acceptance_criteria>
  <done>App relocated to inst/app/app.R with source loop removed, library calls cleaned, and system.file() for reference cache</done>
</task>

<task type="auto">
  <name>Task 4: Verify installation and run_app() launch</name>
  <files>None (verification only)</files>
  <read_first>
    - inst/app/app.R (verify modifications are correct)
    - R/run_app.R (verify function exists)
  </read_first>
  <action>
1. Run devtools::install() to install the package fresh.

2. Create and run a verification script (write to temp file, not inline -e):

   Verification checks:
   - library(chemreg) loads without error
   - run_app function exists in package:chemreg namespace
   - system.file("app", package = "chemreg") returns existing directory
   - system.file("app", package = "chemreg") contains app.R file
   - system.file("extdata", "reference_cache", package = "chemreg") returns existing directory
   - Reference cache contains stop_words.rds file

3. Perform Shiny smoke test:
   - Launch app via chemreg::run_app(port = 3939, launch.browser = FALSE)
   - Wait for "Listening on http://..." message
   - Kill process after confirming startup
   - If crash occurs, diagnose and fix before marking complete

4. Clean up old data/reference_cache/ directory (now safe to remove).
  </action>
  <verify>
    <automated>Write temp R script that: loads chemreg, checks run_app exists, checks system.file paths work, prints OK</automated>
  </verify>
  <acceptance_criteria>
    - devtools::install() completes without error
    - library(chemreg) loads without error
    - exists("run_app", where = "package:chemreg") returns TRUE
    - system.file("app", package = "chemreg") returns non-empty path
    - system.file("extdata", "reference_cache", package = "chemreg") returns non-empty path
    - Shiny smoke test shows "Listening on" message without crash
  </acceptance_criteria>
  <done>Package installs cleanly and chemreg::run_app() launches the Shiny app successfully</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. Root app.R no longer exists
2. inst/app/app.R exists and has no source() loop
3. inst/extdata/reference_cache/ contains all 5 RDS files
4. R/run_app.R exports run_app() function
5. devtools::install() succeeds
6. chemreg::run_app() launches app without errors
</verification>

<success_criteria>
- APP-01: Shiny app lives at inst/app/app.R (verified by file existence)
- APP-02: run_app() exported and uses system.file("app", package = "chemreg")
- APP-03: Reference cache uses system.file("extdata", "reference_cache", package = "chemreg")
- APP-04: inst/app/app.R has no source() loop
- APP-05: chemreg::run_app() launches successfully after package install
</success_criteria>

<output>
After completion, create `.planning/phases/26-app-relocation/26-01-SUMMARY.md`
</output>
