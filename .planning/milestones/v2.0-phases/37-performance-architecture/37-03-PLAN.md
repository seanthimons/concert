---
phase: 37-performance-architecture
plan: 03
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
files_modified:
  - R/cleaning_pipeline.R
  - tests/testthat/test-cleaning-pipeline.R
autonomous: true
requirements:
  - PERF-04
must_haves:
  truths:
    - "run_cleaning_pipeline() uses dedup_step() for all dedup-eligible steps per D-01"
    - "run_cleaning_pipeline() calls pre-check predicates before each step and skips when should_run=FALSE per D-12"
    - "Name cleaning chain uses two dedup passes split at the synonym boundary per D-10"
    - "953+ test suite is green after the migration"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "Migrated run_cleaning_pipeline() with dedup_step and pre-check wiring"
      contains: "dedup_step("
    - path: "tests/testthat/test-cleaning-pipeline.R"
      provides: "Extended tests validating dedup integration in pipeline"
      contains: "dedup"
  key_links:
    - from: "R/cleaning_pipeline.R run_cleaning_pipeline()"
      to: "dedup_step()"
      via: "Wraps each dedup-eligible step call"
      pattern: "dedup_step\\("
    - from: "R/cleaning_pipeline.R run_cleaning_pipeline()"
      to: "precheck_ functions"
      via: "Called before each step, skip path uses build_skip_result"
      pattern: "precheck_"
---

<objective>
Wire dedup_step() and pre-check predicates into `run_cleaning_pipeline()`, migrating one step at a time with the full test suite green after each (PERF-04). The name cleaning chain uses two dedup passes split at the synonym boundary (D-10).

Purpose: This is the core migration that delivers the actual performance gains for the cleaning pipeline. Steps 1-2 (unicode, whitespace) get individual dedup wrapping. Steps 3 (CAS) gets dedup wrapping. Steps 6-pre through 6d3 share one dedup pass (Pass 1). Steps 7-9 share a second dedup pass (Pass 2). Synonym split (Step 6e) runs without dedup.

Output: Modified `run_cleaning_pipeline()` in `R/cleaning_pipeline.R` and extended integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-performance-architecture/37-CONTEXT.md
@.planning/phases/37-performance-architecture/37-PATTERNS.md
@.planning/phases/37-performance-architecture/37-01-SUMMARY.md
@.planning/phases/37-performance-architecture/37-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- dedup_step signature -->
```r
dedup_step(step_fn, df, ..., dedup_cols, uniqueness_threshold = 0.5)
# Returns list(cleaned_data, audit_trail) -- same as direct step call
# Preserves new_tags if step returns them
```

<!-- From Plan 02 -- pre-check signatures -->
```r
precheck_unicode_to_ascii(df)        # -> list(should_run, est_changes)
precheck_trim_whitespace(df)         # -> list(should_run, est_changes)
precheck_normalize_cas(df, tag_map)  # -> list(should_run, est_changes)
precheck_name_cleaning(df, name_cols)# -> list(should_run, est_changes)
precheck_isotope_shortcodes(df, name_cols, isotope_lookup) # -> list(should_run, est_changes)
precheck_multi_analyte(df, name_cols)# -> list(should_run, est_changes)
precheck_chiral_restore(df, name_cols)# -> list(should_run, est_changes)
build_skip_result(df, step_name)     # -> list(cleaned_data, audit_trail) with message()
```

<!-- Current orchestrator structure (lines 1546-1704) -->
Step 1: unicode_to_ascii -- mutate across all char cols
Step 2: trim_whitespace -- mutate across all char cols
Step 3: normalize_cas_fields(df, tag_map)
Step 4: rescue_cas_from_text -- NOT dedup-eligible (adds columns)
Step 5: detect_multi_cas -- NOT dedup-eligible (adds flags)
Steps 6-pre through 6d3: Name chain Pass 1 (dedup-eligible as group)
Step 6e: synonym split -- NOT dedup-eligible (changes row count)
Step 6f: final cleanup -- inline mutate
Steps 7-9: Post-synonym chain Pass 2 (dedup-eligible as group)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire dedup and pre-checks into run_cleaning_pipeline() Steps 1-3</name>
  <files>R/cleaning_pipeline.R</files>
  <read_first>
    - R/cleaning_pipeline.R (lines 1546-1595 for Steps 1-3 current implementation; also the dedup_step and precheck functions added in Plans 01-02)
    - .planning/phases/37-performance-architecture/37-CONTEXT.md (D-08, D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/37-performance-architecture/37-PATTERNS.md (orchestrator integration section)
  </read_first>
  <action>
Modify `run_cleaning_pipeline()` in `R/cleaning_pipeline.R` (starting at line 1546). The changes are ONLY to the orchestrator call sites -- no step function internals change.

**Step 1: Unicode to ASCII (lines 1550-1559)**

Currently Steps 1-2 use inline `dplyr::mutate(across(...))` + `build_audit_trail()`. These are NOT step functions -- they're inline transformations. To use `dedup_step()`, wrap them in local step-function closures.

Replace the Step 1 block with:

```r
# Step 1: Unicode to ASCII
unicode_check <- precheck_unicode_to_ascii(df_after_lineage)
if (!unicode_check$should_run) {
  df_after_unicode <- df_after_lineage
  audit_unicode <- build_skip_result(df_after_lineage, "unicode_to_ascii")$audit_trail
} else {
  # Wrap inline transform as step function for dedup_step compatibility
  unicode_step_fn <- function(df_in, ...) {
    df_out <- df_in %>%
      dplyr::mutate(dplyr::across(tidyselect::where(is.character), ComptoxR::clean_unicode))
    audit <- build_audit_trail(df_in, df_out, "unicode_to_ascii",
      function(field) paste0("Convert unicode characters to ASCII equivalents in ", field))
    list(cleaned_data = df_out, audit_trail = audit)
  }
  char_cols <- names(df_after_lineage)[vapply(df_after_lineage, is.character, logical(1))]
  unicode_result <- dedup_step(unicode_step_fn, df_after_lineage, dedup_cols = char_cols)
  df_after_unicode <- unicode_result$cleaned_data
  audit_unicode <- unicode_result$audit_trail
}
```

**Step 2: Whitespace trimming (lines 1562-1570)**

Replace with analogous pattern:

```r
# Step 2: Whitespace and punctuation artifact stripping
whitespace_check <- precheck_trim_whitespace(df_after_unicode)
if (!whitespace_check$should_run) {
  df_after_trim <- df_after_unicode
  audit_trim <- build_skip_result(df_after_unicode, "trim_whitespace_punctuation")$audit_trail
} else {
  trim_step_fn <- function(df_in, ...) {
    df_out <- df_in %>%
      dplyr::mutate(dplyr::across(tidyselect::where(is.character), clean_text_field))
    audit <- build_audit_trail(df_in, df_out, "trim_whitespace_punctuation",
      function(field) paste0("Strip leading/trailing whitespace and punctuation artifacts from ", field))
    list(cleaned_data = df_out, audit_trail = audit)
  }
  char_cols_trim <- names(df_after_unicode)[vapply(df_after_unicode, is.character, logical(1))]
  trim_result <- dedup_step(trim_step_fn, df_after_unicode, dedup_cols = char_cols_trim)
  df_after_trim <- trim_result$cleaned_data
  audit_trim <- trim_result$audit_trail
}
```

**Step 3: CAS normalization (lines 1580-1583)**

Replace:

```r
# Step 3: Normalize CAS fields
cas_check <- precheck_normalize_cas(df_after_trim, tag_map)
if (!cas_check$should_run) {
  df_after_cas <- df_after_trim
  audit_combined <- dplyr::bind_rows(audit_combined, build_skip_result(df_after_trim, "normalize_cas")$audit_trail)
} else {
  cas_cols <- names(tag_map)[tag_map == "CASRN"]
  cas_result <- dedup_step(normalize_cas_fields, df_after_trim, tag_map, dedup_cols = cas_cols)
  df_after_cas <- cas_result$cleaned_data
  audit_combined <- dplyr::bind_rows(audit_combined, cas_result$audit_trail)
}
```

Leave Steps 4 (rescue_cas_from_text) and 5 (detect_multi_cas) UNCHANGED -- they are not dedup-eligible per D-01.

After making these changes, run the full test suite: `devtools::test()`. All 953+ tests must pass before proceeding to Task 2.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); devtools::test()" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `precheck_unicode_to_ascii(df_after_lineage)`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `precheck_trim_whitespace(df_after_unicode)`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `precheck_normalize_cas(df_after_trim, tag_map)`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `dedup_step(unicode_step_fn,`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `dedup_step(trim_step_fn,`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `dedup_step(normalize_cas_fields,`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `build_skip_result(` in at least 3 locations
    - Steps 4 and 5 are unchanged (rescue_cas_from_text, detect_multi_cas still called directly)
    - devtools::test() exits 0 with 0 failures
  </acceptance_criteria>
  <done>Steps 1-3 migrated to dedup+precheck, full test suite green</done>
</task>

<task type="auto">
  <name>Task 2: Wire dedup Pass 1 (name chain 6-pre through 6d3) and Pass 2 (Steps 7-9)</name>
  <files>R/cleaning_pipeline.R, tests/testthat/test-cleaning-pipeline.R</files>
  <read_first>
    - R/cleaning_pipeline.R (lines 1596-1704 for the name cleaning chain and post-synonym steps; also the now-modified Steps 1-3 from Task 1)
    - .planning/phases/37-performance-architecture/37-CONTEXT.md (D-10 two-pass dedup architecture)
    - .planning/phases/37-performance-architecture/37-PATTERNS.md (two-pass dedup boundary section)
  </read_first>
  <action>
**Part A: Name chain pre-check gate (before entering name block)**

After `name_cols <- names(tag_map)[tag_map == "Name"]` and `if (length(name_cols) > 0) {`, add:

```r
name_check <- precheck_name_cleaning(df_after_multi_cas, name_cols)
```

This pre-check gates entry to the entire name block. If `should_run == FALSE` (all name values are NA/empty), skip the entire block.

**Part B: Pass 1 dedup wrapper (Steps 6-pre through 6d3)**

Per D-10, the pre-synonym steps share one dedup pass. Create a composite step function that runs the entire chain:

```r
if (name_check$should_run) {
  # Pass 1: Pre-synonym name chain (6-pre through 6d3) as single dedup group
  name_chain_pass1 <- function(df_in, name_cols_inner, tag_map_inner, reference_lists_inner) {
    audit_parts <- list()

    # 6-pre: Protect chiral
    chiral_result <- protect_chiral_designations(df_in, name_cols_inner)
    df_work <- chiral_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- chiral_result$audit_trail

    # 6a: Strip terminal enclosures
    enclosure_result <- strip_terminal_enclosures(df_work, name_cols_inner)
    df_work <- enclosure_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- enclosure_result$audit_trail

    # 6b: Strip quality adjectives
    quality_result <- strip_quality_adjectives(df_work, name_cols_inner)
    df_work <- quality_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- quality_result$audit_trail

    # 6c: Strip salt references
    salt_result <- strip_salt_references(df_work, name_cols_inner)
    df_work <- salt_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- salt_result$audit_trail

    # 6d: Strip terminal unspecified
    unspec_result <- strip_terminal_unspecified(df_work, name_cols_inner)
    df_work <- unspec_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- unspec_result$audit_trail

    # 6d1: Strip reference terms (if provided)
    if (!is.null(reference_lists_inner$strip_terms)) {
      strip_ref_result <- strip_reference_terms(df_work, name_cols_inner, reference_lists_inner$strip_terms)
      df_work <- strip_ref_result$cleaned_data
      audit_parts[[length(audit_parts) + 1]] <- strip_ref_result$audit_trail
    }

    # 6d2: Final cleanup before second stripping
    df_work <- df_work %>%
      dplyr::mutate(dplyr::across(dplyr::all_of(name_cols_inner), ~ {
        .x %>%
          stringr::str_squish() %>%
          stringr::str_remove("\\(\\s*\\)\\s*$") %>%
          stringr::str_trim() %>%
          stringr::str_remove("[,;-]+$") %>%
          stringr::str_trim()
      }))

    # 6d3: Strip terminal enclosures again
    enclosure_result2 <- strip_terminal_enclosures(df_work, name_cols_inner)
    df_work <- enclosure_result2$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- enclosure_result2$audit_trail

    list(
      cleaned_data = df_work,
      audit_trail = dplyr::bind_rows(audit_parts),
      new_tags = enclosure_result$new_tags
    )
  }

  pass1_result <- dedup_step(
    name_chain_pass1, df_after_multi_cas,
    name_cols, tag_map_updated, reference_lists,
    dedup_cols = name_cols
  )
  df_after_enclosures2 <- pass1_result$cleaned_data
  audit_combined <- dplyr::bind_rows(audit_combined, pass1_result$audit_trail)
  new_tags <- c(new_tags, pass1_result$new_tags %||% list())
  tag_map_updated <- c(tag_map_updated, pass1_result$new_tags %||% list())
```

**Part C: Synonym split unchanged (Step 6e)**

Keep synonym split exactly as-is (not dedup-eligible, changes row count):
```r
  # Step 6e: Split synonyms (no dedup -- changes row count)
  synonym_result <- split_synonyms(df_after_enclosures2, name_cols, tag_map_updated)
  # ... rest of synonym handling unchanged including 6f cleanup and empty-row removal
```

**Part D: Pass 2 dedup wrapper (Steps 7-9)**

```r
  # Pass 2: Post-synonym chain (Steps 7-9) as single dedup group
  name_chain_pass2 <- function(df_in, name_cols_inner, isotope_lookup_inner) {
    audit_parts <- list()

    # Step 7: Expand isotope shortcodes
    isotope_result <- expand_isotope_shortcodes(df_in, name_cols_inner, isotope_lookup_inner)
    df_work <- isotope_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- isotope_result$audit_trail

    # Step 8: Flag multi-analyte
    multi_result <- flag_multi_analyte(df_work, name_cols_inner)
    df_work <- multi_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- multi_result$audit_trail

    # Step 9: Restore chiral
    chiral_restore_result <- restore_chiral_designations(df_work, name_cols_inner)
    df_work <- chiral_restore_result$cleaned_data
    audit_parts[[length(audit_parts) + 1]] <- chiral_restore_result$audit_trail

    list(cleaned_data = df_work, audit_trail = dplyr::bind_rows(audit_parts))
  }

  # Pre-checks for pass 2 steps (any of the 3 needing work triggers the pass)
  isotope_check <- precheck_isotope_shortcodes(df_final, name_cols, reference_lists$isotope_lookup)
  multi_check <- precheck_multi_analyte(df_final, name_cols)
  chiral_check <- precheck_chiral_restore(df_final, name_cols)

  if (isotope_check$should_run || multi_check$should_run || chiral_check$should_run) {
    pass2_result <- dedup_step(
      name_chain_pass2, df_final,
      name_cols, reference_lists$isotope_lookup,
      dedup_cols = name_cols
    )
    df_final <- pass2_result$cleaned_data
    audit_combined <- dplyr::bind_rows(audit_combined, pass2_result$audit_trail)
  } else {
    audit_combined <- dplyr::bind_rows(
      audit_combined,
      build_skip_result(df_final, "isotope_shortcodes")$audit_trail,
      build_skip_result(df_final, "multi_analyte")$audit_trail,
      build_skip_result(df_final, "chiral_restore")$audit_trail
    )
  }
```

Remove the old individual Step 7, 8, 9 calls that are now inside `name_chain_pass2`.

**Part E: Add integration test to test-cleaning-pipeline.R**

Add one test_that block at the end of `tests/testthat/test-cleaning-pipeline.R`:

```r
test_that("run_cleaning_pipeline with dedup produces identical results to pre-dedup baseline", {
  # Create data with many duplicates (dedup should fire)
  base_names <- c("  acetone  ", "\u03B1-tocopherol", "__ethanol__", "benzene", "toluene")
  test_df <- tibble::tibble(
    chemical_name = rep(base_names, 20),
    cas_number = rep(c("67641", "  58-08-2  ", "*108-95-2*", "71-43-2", "108-88-3"), 20)
  )
  tag_map <- list(chemical_name = "Name", cas_number = "CASRN")

  result <- run_cleaning_pipeline(test_df, tag_map)

  # Audit trail row_ids must be valid (PERF-02)
  expect_true(all(result$audit_trail$row_id <= nrow(result$cleaned_data)))
  # Must have cleaning results
  expect_gt(nrow(result$audit_trail), 0)
  # Structure preserved
  expect_named(result, c("cleaned_data", "audit_trail", "new_tags"))
})
```

Run `devtools::test()` after all changes. All tests must pass.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::load_all(); devtools::test()" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `name_chain_pass1 <- function(`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `name_chain_pass2 <- function(`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `dedup_step(name_chain_pass1,`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `dedup_step(name_chain_pass2,`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `precheck_name_cleaning(`
    - R/cleaning_pipeline.R run_cleaning_pipeline contains `precheck_isotope_shortcodes(`
    - Synonym split (split_synonyms call) is NOT wrapped in dedup_step
    - tests/testthat/test-cleaning-pipeline.R contains "dedup" in a test_that description
    - devtools::test() exits 0 with 0 failures
  </acceptance_criteria>
  <done>Full cleaning pipeline migrated to dedup+precheck with two-pass name chain architecture, all tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dedup_step -> composite name chain function | The composite function runs 8 substeps internally; a bug in any substep could be masked by dedup |
| Pass 1 -> synonym split -> Pass 2 | Row count changes at synonym split; Pass 2 dedup keys are on the new (expanded) row set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-08 | Tampering | Composite name chain loses audit entries | mitigate | dplyr::bind_rows(audit_parts) inside composite function; existing step tests verify each sub-step's audit output individually |
| T-37-09 | Tampering | Pass 2 dedup keys stale after synonym split | mitigate | Pass 2 dedup operates on df_final (post-synonym, post-empty-removal), not on pre-synonym data; test confirms row_ids valid |
| T-37-10 | Denial of Service | Dedup on wide name columns creates huge composite keys | accept | Name columns are typically 1-2 columns; paste0 key is bounded by column count; uniqueness bypass (D-03) handles high-cardinality cases |
</threat_model>

<verification>
1. `devtools::test()` -- full suite green after each task (PERF-04: one step at a time)
2. Integration test with 100-row duplicate dataset produces valid audit trail (row_ids <= nrow)
3. Synonym split is NOT wrapped in dedup_step (verified by grep)
4. Pre-checks produce skip messages when data is clean (verified by test-precheck-infrastructure.R from Plan 02)
</verification>

<success_criteria>
- run_cleaning_pipeline() uses dedup_step() for Steps 1, 2, 3 individually and Steps 6-chain, 7-9-chain as two grouped passes
- Pre-checks gate each dedup-eligible step/group
- Synonym split runs without dedup (D-01 exclusion)
- All 953+ existing tests pass after migration (PERF-04)
- New integration test with duplicate data validates PERF-02 audit trail integrity
</success_criteria>

<output>
After completion, create `.planning/phases/37-performance-architecture/37-03-SUMMARY.md`
</output>
