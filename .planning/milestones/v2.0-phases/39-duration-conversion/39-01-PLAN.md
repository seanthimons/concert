---
phase: 39-duration-conversion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - inst/extdata/unit_conversion.rds
  - inst/extdata/unit_synonyms.rds
  - R/unit_harmonizer.R
  - tests/testthat/test-unit-harmonizer.R
autonomous: true
requirements:
  - DUR-01
  - DUR-02
  - DUR-05
user_setup: []

must_haves:
  truths:
    - "harmonize_units() with category='duration' converts day values to hours (multiplier 24)"
    - "harmonize_units() with category=NULL still works identically to before (backward compat)"
    - "The synonym 'm' normalizes to 'min' and produces an 'ambiguous_unit' flag in the output"
    - "Unrecognized duration units like 'dph' pass through unchanged with 'unmatched' flag"
    - "unit_conversion.rds contains ~23 rows with category='duration' and to_unit='hr'"
    - "unit_synonyms.rds contains ~34 new duration synonym rows including the ambiguous 'm' entry"
  artifacts:
    - path: "inst/extdata/unit_conversion.rds"
      provides: "Duration conversion rows with hr as base unit"
      contains: "category='duration' rows with to_unit='hr'"
    - path: "inst/extdata/unit_synonyms.rds"
      provides: "Duration synonym normalization entries"
      contains: "m -> min AMBIGUOUS entry"
    - path: "R/unit_harmonizer.R"
      provides: "category parameter and ambiguous_unit flag logic"
    - path: "tests/testthat/test-unit-harmonizer.R"
      provides: "Duration-specific test sections 16-19"
  key_links:
    - from: "R/unit_harmonizer.R"
      to: "inst/extdata/unit_conversion.rds"
      via: "unit_map parameter filtered by category"
      pattern: "unit_map\\[unit_map\\$category == category"
    - from: "R/unit_harmonizer.R"
      to: "inst/extdata/unit_synonyms.rds"
      via: "apply_synonyms() normalizes duration abbreviations"
      pattern: "apply_synonyms"
    - from: "tests/testthat/test-unit-harmonizer.R"
      to: "R/unit_harmonizer.R"
      via: "harmonize_units() calls with category='duration'"
      pattern: "category = \"duration\""
---

<objective>
Add duration conversion data and the `category` parameter to `harmonize_units()`, with comprehensive tests covering conversion arithmetic, synonym normalization, the "m" ambiguity flag, and backward compatibility.

Purpose: This is the foundation layer for Phase 39. Duration conversion rows and the category filter must exist before the pipeline wiring (Plan 02) can route DurationUnit-tagged columns through `harmonize_units()`.

Output: Updated RDS data files with duration rows, `harmonize_units()` with `category` parameter and ambiguous_unit flag, test coverage for all duration behaviors.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-duration-conversion/39-CONTEXT.md
@.planning/phases/39-duration-conversion/39-RESEARCH.md
@.planning/phases/39-duration-conversion/39-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/unit_harmonizer.R (current signature, line 273):
```r
harmonize_units <- function(
  values,
  units,
  unit_map,
  media = NULL,
  dtxsid = NULL,
  molecular_weight = NULL,
  use_dedup = TRUE
) {
```

Return tibble schema (line 592-599):
```r
tibble::tibble(
  orig_row_id = as.integer(orig_row_id),
  orig_unit = orig_unit,
  harmonized_value = harmonized_value,
  harmonized_unit = harmonized_unit,
  conversion_factor = conversion_factor,
  unit_flag = unit_flag
)
```

Existing unit_flag values: "", "case_fallback", "unmatched", "needs_mw", "media_inferred"

From inst/extdata/unit_conversion.rds schema (6 columns):
```
from_unit (character), to_unit (character), multiplier (numeric),
category (character), confidence (character), source (character)
```
Currently 151 rows, 15 categories. "time" category has 19 rows with to_unit="day".

From inst/extdata/unit_synonyms.rds schema (4 columns):
```
input_pattern (character), normalized_unit (character),
is_regex (logical), notes (character)
```
Currently 80 rows. All exact-match entries use is_regex=FALSE.

From tests/testthat/test-unit-harmonizer.R test helper (line 8):
```r
make_test_unit_map <- function() {
  tibble::tibble(
    from_unit  = c("mg/L", "ug/L", ...),
    to_unit    = c("mg/L", "mg/L", ...),
    multiplier = c(1, 0.001, ...),
    category   = c("concentration", ...),
    confidence = rep("HIGH", 6),
    source     = rep("test", 6)
  )
}
```
Test file has 930 lines, 15 sections, last section is "Edge cases". New sections append after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duration rows to RDS data files and extend harmonize_units() with category parameter + ambiguous_unit flag</name>
  <files>inst/extdata/unit_conversion.rds, inst/extdata/unit_synonyms.rds, R/unit_harmonizer.R</files>
  <read_first>
    R/unit_harmonizer.R
    inst/extdata/unit_conversion.rds (via readRDS in temp R script)
    inst/extdata/unit_synonyms.rds (via readRDS in temp R script)
    .planning/phases/39-duration-conversion/39-PATTERNS.md
  </read_first>
  <action>
Three sub-steps. Use temp R scripts for all RDS operations (per project CLAUDE.md).

**Sub-step 1a: Append 23 duration rows to unit_conversion.rds (D-07, DUR-02)**

Write a temp R script that:
1. Reads `inst/extdata/unit_conversion.rds`
2. Creates a tibble with these 23 rows (all have `to_unit="hr"`, `category="duration"`, `confidence="HIGH"`, `source="ECOTOX"`):

| from_unit | multiplier |
|-----------|-----------|
| hr | 1 |
| h | 1 |
| hour | 1 |
| hours | 1 |
| day | 24 |
| d | 24 |
| days | 24 |
| wk | 168 |
| week | 168 |
| weeks | 168 |
| mo | 730.5 |
| month | 730.5 |
| months | 730.5 |
| yr | 8766 |
| year | 8766 |
| years | 8766 |
| min | 1/60 |
| minute | 1/60 |
| minutes | 1/60 |
| s | 1/3600 |
| sec | 1/3600 |
| second | 1/3600 |
| seconds | 1/3600 |

Use exact fractions (`1/60`, `1/3600`) not rounded decimals. `bind_rows()` to existing and `saveRDS()`.

**Sub-step 1b: Append 34 duration synonym rows to unit_synonyms.rds (D-08, DUR-05)**

Write a temp R script that:
1. Reads `inst/extdata/unit_synonyms.rds`
2. Creates a tibble with these 34 rows (all `is_regex=FALSE`):

```
hrs -> hr, Hr -> hr, HR -> hr, Hrs -> hr, HRS -> hr,
Day -> day, DAY -> day, Days -> day, DAYS -> day,
Wk -> wk, WK -> wk, Week -> week, WEEK -> week, Weeks -> week, WEEKS -> week,
Mo -> mo, MO -> mo, Month -> month, MONTH -> month, Months -> month, MONTHS -> month,
Yr -> yr, YR -> yr, Year -> year, YEAR -> year, Years -> year, YEARS -> year,
Min -> min, MIN -> min, Minute -> minute,
Sec -> sec, SEC -> sec, Second -> second,
m -> min (notes: "AMBIGUOUS -- maps to minutes per D-01; ambiguous_unit flag set by harmonize_units()")
```

`bind_rows()` to existing and `saveRDS()`.

**Sub-step 1c: Add `category` parameter and `ambiguous_unit` flag to harmonize_units() (D-12, D-01)**

Modify `R/unit_harmonizer.R`:

1. Add `category = NULL` parameter to the function signature, after `use_dedup = TRUE`:
```r
harmonize_units <- function(
  values,
  units,
  unit_map,
  media = NULL,
  dtxsid = NULL,
  molecular_weight = NULL,
  use_dedup = TRUE,
  category = NULL
) {
```

2. Insert the category filter immediately after the opening `{` of the function body, BEFORE "Step 0: Handle empty input" (currently line 282):
```r
  # Category filter (D-12): isolate conversion table to a single category
  if (!is.null(category)) {
    unit_map <- unit_map[unit_map$category == category, , drop = FALSE]
  }
```

3. Insert the ambiguous_unit flag logic AFTER the dedup if/else block ends (after line 589 `} # end dedup if/else`) and BEFORE the "Build output tibble" comment (line 591):
```r
  # Flag ambiguous original units (D-01): "m" could be minutes or months
  ambiguous_originals <- c("m")
  ambiguous_mask <- trimws(tolower(orig_unit)) %in% ambiguous_originals
  if (any(ambiguous_mask)) {
    unit_flag[ambiguous_mask] <- "ambiguous_unit"
  }
```

4. Update roxygen2 docs to document the new `category` parameter:
```r
#' @param category Character or NULL. When non-NULL, filters unit_map to rows
#'   matching this category before conversion. Use "duration" for duration
#'   harmonization. Default NULL uses all rows (backward compatible).
```

Run `air format R/unit_harmonizer.R` and `jarl check R/unit_harmonizer.R` after edits.
  </action>
  <verify>
    <automated>
Write a temp R script that:
1. Loads unit_conversion.rds via readRDS, checks nrow >= 174 (151 + 23), checks sum(uc$category == "duration") == 23, checks all duration rows have to_unit == "hr"
2. Loads unit_synonyms.rds via readRDS, checks nrow >= 114 (80 + 34), checks "m" is present in input_pattern
3. Sources R/unit_harmonizer.R and checks that "category" appears in formals(harmonize_units)
4. Runs `air format R/unit_harmonizer.R` and `jarl check R/unit_harmonizer.R`
    </automated>
  </verify>
  <acceptance_criteria>
    - `readRDS("inst/extdata/unit_conversion.rds")` has >= 174 rows
    - `sum(uc$category == "duration")` returns 23
    - All 23 duration rows have `to_unit == "hr"`
    - Duration multipliers use exact fractions: `uc[uc$from_unit == "min" & uc$category == "duration", "multiplier"]` equals `1/60` (not 0.016667)
    - `readRDS("inst/extdata/unit_synonyms.rds")` has >= 114 rows
    - Synonym row with `input_pattern == "m"` has `normalized_unit == "min"`
    - `"category" %in% names(formals(harmonize_units))` returns TRUE
    - `formals(harmonize_units)$category` is NULL (default value)
    - R/unit_harmonizer.R contains the string `ambiguous_originals <- c("m")`
    - R/unit_harmonizer.R contains `unit_flag[ambiguous_mask] <- "ambiguous_unit"`
    - `air format` and `jarl check` pass cleanly on R/unit_harmonizer.R
  </acceptance_criteria>
  <done>unit_conversion.rds has 23 duration rows (hr base, exact fractions), unit_synonyms.rds has 34 duration synonyms including ambiguous "m", harmonize_units() accepts category parameter and flags ambiguous units</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add duration test sections to test-unit-harmonizer.R</name>
  <files>tests/testthat/test-unit-harmonizer.R</files>
  <read_first>
    tests/testthat/test-unit-harmonizer.R
    R/unit_harmonizer.R
    .planning/phases/39-duration-conversion/39-PATTERNS.md (test section from "### tests/testthat/test-unit-harmonizer.R")
  </read_first>
  <behavior>
    - Test: category=NULL uses all rows including duration (backward compat) -- 24 day units -> 576 hr
    - Test: category='duration' filters to hr-base rows when both "time" and "duration" categories exist for "day"
    - Test: unrecognized duration unit "dph" passes through with value=5, unit="dph", flag="unmatched" (D-06)
    - Test: hr -> hr identity (96 hr -> 96 hr)
    - Test: day -> hr (14 day -> 336 hr)
    - Test: min -> hr (120 min -> 2 hr, tolerance 1e-10)
    - Test: wk -> hr (2 wk -> 336 hr)
    - Test: yr -> hr (1 yr -> 8766 hr)
    - Test: decimal fraction 1.5 day -> 36 hr (D-03)
    - Test: synonym "hrs" -> normalized to "hr", 48 hrs -> 48 hr
    - Test: synonym "Days" -> normalized to "day", 7 Days -> 168 hr
    - Test: "m" maps to minutes AND sets ambiguous_unit flag (60 m -> 1 hr with flag "ambiguous_unit") (D-01, DUR-05)
    - Test: "min" does NOT get ambiguous_unit flag (60 min -> 1 hr with flag "")
  </behavior>
  <action>
Append four new test sections (16-19) after the existing Section 15 "Edge cases" at the end of `tests/testthat/test-unit-harmonizer.R`.

First, add a `make_duration_unit_map()` helper function near the top of the file (after the existing `make_test_unit_map()` helper around line 17). This helper returns a tibble with 15 representative duration rows:

```r
make_duration_unit_map <- function() {
  tibble::tibble(
    from_unit = c(
      "hr", "h", "day", "d", "wk", "week", "mo", "month",
      "yr", "year", "min", "minute", "s", "sec", "second"
    ),
    to_unit = rep("hr", 15),
    multiplier = c(
      1, 1, 24, 24, 168, 168, 730.5, 730.5,
      8766, 8766, 1 / 60, 1 / 60, 1 / 3600, 1 / 3600, 1 / 3600
    ),
    category = rep("duration", 15),
    confidence = rep("HIGH", 15),
    source = rep("test", 15)
  )
}
```

Then append these test sections after the last line of the file:

**Section 16: Duration category filter (D-12)** -- 3 tests:
- `category=NULL uses all rows (backward compat)` -- use `make_duration_unit_map()`, pass `category = NULL`, 24 day -> expect 576 hr
- `category='duration' filters to hr-base rows` -- build a combined map with "time" row (`from_unit="day"`, `to_unit="day"`, `multiplier=1`, `category="time"`) and "duration" row (`from_unit="day"`, `to_unit="hr"`, `multiplier=24`, `category="duration"`), pass `category="duration"`, 1 day -> expect 24 hr, expect `harmonized_unit == "hr"`
- `unrecognized unit passes through with 'unmatched' flag (D-06)` -- 5 "dph" with duration map, category="duration" -> expect value=5, unit="dph", flag="unmatched"

**Section 17: Duration conversion arithmetic (DUR-02)** -- 6 tests:
- hr identity: 96 hr -> 96 hr
- day to hr: 14 day -> 336 hr
- min to hr: 120 min -> 2 hr (tolerance 1e-10)
- wk to hr: 2 wk -> 336 hr
- yr to hr: 1 yr -> 8766 hr
- decimal fraction: 1.5 day -> 36 hr (D-03)

**Section 18: Duration synonym normalization (DUR-05)** -- 2 tests:
- "hrs" normalizes to "hr": 48 hrs -> 48 hr
- "Days" normalizes to "day": 7 Days -> 168 hr

**Section 19: "m" ambiguity flag (D-01, DUR-05)** -- 2 tests:
- "m" maps to minutes with ambiguous_unit flag: 60 "m" with duration map, category="duration" -> expect value ~1 hr, flag "ambiguous_unit"
- "min" does NOT get ambiguous flag: 60 "min" with duration map, category="duration" -> expect value ~1 hr, flag ""

Run `air format tests/testthat/test-unit-harmonizer.R` after edits.

Run the full test suite with `devtools::test()` to confirm all existing + new tests pass.
  </action>
  <verify>
    <automated>
Run: `"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "devtools::test(filter='unit-harmonizer')"` and verify all test_that blocks pass including the new Sections 16-19.
    </automated>
  </verify>
  <acceptance_criteria>
    - tests/testthat/test-unit-harmonizer.R contains `make_duration_unit_map <- function()`
    - tests/testthat/test-unit-harmonizer.R contains `SECTION 16: Duration category filter`
    - tests/testthat/test-unit-harmonizer.R contains `SECTION 17: Duration conversion arithmetic`
    - tests/testthat/test-unit-harmonizer.R contains `SECTION 18: Duration synonym normalization`
    - tests/testthat/test-unit-harmonizer.R contains `SECTION 19:` and `ambiguity flag`
    - tests/testthat/test-unit-harmonizer.R contains `expect_equal(result$unit_flag, "ambiguous_unit")`
    - `devtools::test(filter='unit-harmonizer')` exits with 0 failures
    - All 13 new test_that blocks pass (3 + 6 + 2 + 2)
  </acceptance_criteria>
  <done>13 new duration tests in 4 sections pass, covering category filter, conversion arithmetic, synonym normalization, and "m" ambiguity flag. Full test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> harmonize_units() | Duration values and unit strings from user-tagged columns enter the conversion pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | unit_conversion.rds | accept | RDS is package-internal, not user-editable at runtime; loaded via system.file() |
| T-39-02 | Information Disclosure | ambiguous_unit flag | accept | Flag is informational -- exposes no PII or secrets |
| T-39-03 | Denial of Service | category filter on large unit_map | accept | Filter is O(n) row subset on ~170-row table; negligible cost |
</threat_model>

<verification>
1. `readRDS("inst/extdata/unit_conversion.rds")` has >= 174 rows with 23 duration category rows, all to_unit="hr"
2. `readRDS("inst/extdata/unit_synonyms.rds")` has >= 114 rows with "m" -> "min" entry
3. `harmonize_units(c(14), c("day"), unit_map, category="duration")` returns harmonized_value=336, harmonized_unit="hr"
4. `harmonize_units(c(60), c("m"), unit_map, category="duration")` returns unit_flag="ambiguous_unit"
5. `harmonize_units(c(1), c("mg/L"), unit_map)` still works identically (backward compat, category=NULL)
6. `devtools::test(filter='unit-harmonizer')` -- 0 failures
</verification>

<success_criteria>
Duration conversion data exists in RDS files with hours as base unit, harmonize_units() filters by category when specified, "m" ambiguity is flagged, all 13+ new tests pass, existing tests unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/39-duration-conversion/39-01-SUMMARY.md`
</output>
