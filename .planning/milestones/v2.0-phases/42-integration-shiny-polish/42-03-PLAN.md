---
phase: 42-integration-shiny-polish
plan: 03
type: execute
wave: 3
depends_on: ["42-01", "42-02"]
files_modified:
  - R/mod_clean_data.R
  - R/mod_harmonize.R
autonomous: false
requirements:
  - RECO-01
  - RECO-02

must_haves:
  truths:
    - "A single Run Pipeline button replaces both Run Cleaning and Run Harmonization buttons"
    - "Clicking Run Pipeline collects all pre-check results and shows a modal with fire/skip checklist"
    - "The pre-flight modal has two sections: Cleaning Steps and Harmonization Steps"
    - "Each checklist row shows step name, checkbox (pre-checked if firing), and badge with estimated change count or skip"
    - "User can run all steps or only checked steps from the modal"
    - "When all pre-checks show 0 changes, modal is skipped and a notification offers to open it anyway"
    - "Shiny app starts cleanly from cold boot after all button changes"
  artifacts:
    - path: "R/mod_clean_data.R"
      provides: "Run Pipeline button, pre-flight modal, pipeline execution with step mask"
      contains: "run_pipeline"
    - path: "R/mod_harmonize.R"
      provides: "Run Harmonization button removed from UI"
  key_links:
    - from: "R/mod_clean_data.R"
      to: "R/cleaning_pipeline.R"
      via: "Calls all 11 precheck_* functions before showing modal"
      pattern: "precheck_harmonize_"
    - from: "R/mod_clean_data.R"
      to: "R/mod_harmonize.R"
      via: "run_harmonization observer still exists internally for shinyjs::click re-run path"
      pattern: "run_harmonization"
---

<objective>
Replace the separate Run Cleaning and Run Harmonization buttons with a single Run Pipeline button that opens a unified pre-flight modal showing which steps will fire vs skip, per D-01 through D-05. The user confirms which steps to run before any processing begins.

Purpose: Users currently must run cleaning and harmonization separately with no visibility into what each will do. The pre-flight modal gives them a single entry point with full transparency and control over step selection. This closes RECO-01 and RECO-02.

Output: Modified R/mod_clean_data.R (new button + modal + execution), modified R/mod_harmonize.R (button removal). Shiny cold boot verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-integration-shiny-polish/42-CONTEXT.md
@.planning/phases/42-integration-shiny-polish/42-RESEARCH.md
@.planning/phases/42-integration-shiny-polish/42-PATTERNS.md
@.planning/phases/42-integration-shiny-polish/42-UI-SPEC.md
@.planning/phases/42-integration-shiny-polish/42-01-SUMMARY.md
@.planning/phases/42-integration-shiny-polish/42-02-SUMMARY.md

<interfaces>
<!-- Existing pre-check signatures from cleaning_pipeline.R (7 cleaning + 4 harmonization from Plan 01) -->

Cleaning pre-checks (existing):
```r
precheck_unicode_to_ascii(df)                             # -> list(should_run, est_changes)
precheck_trim_whitespace(df)                              # -> list(should_run, est_changes)
precheck_normalize_cas(df, tag_map)                       # -> list(should_run, est_changes)
precheck_name_cleaning(df, name_cols)                     # -> list(should_run, est_changes)
precheck_isotope_shortcodes(df, name_cols, isotope_lookup)# -> list(should_run, est_changes)
precheck_multi_analyte(df, name_cols)                     # -> list(should_run, est_changes)
precheck_chiral_restore(df, name_cols)                    # -> list(should_run, est_changes)
```

Harmonization pre-checks (from Plan 01):
```r
precheck_harmonize_units(df, unit_cols, unit_map)         # -> list(should_run, est_changes)
precheck_harmonize_duration(df, dur_cols, dur_unit_cols, unit_map) # -> list(should_run, est_changes)
precheck_harmonize_dates(df, date_cols)                   # -> list(should_run, est_changes)
precheck_harmonize_media(df, media_cols, media_map)       # -> list(should_run, est_changes)
```

From R/mod_clean_data.R (current button, lines 63-68):
```r
actionButton(ns("run_cleaning"), "Run Cleaning", class = "btn-success btn-lg", icon = icon("magic"))
```

From R/mod_clean_data.R (pipeline observer, line 130):
```r
observeEvent(input$run_cleaning, { req(data_store$clean) ... })
```

From R/mod_harmonize.R (current button, lines 74-80):
```r
shinyjs::disabled(actionButton(ns("run_harmonization"), "Run Harmonization",
  class = "btn-success btn-lg mt-3 mb-3", icon = icon("play")))
```

From R/mod_harmonize.R (pipeline observer, line 204):
```r
observeEvent(input$run_harmonization, { req(data_store$clean) ... })
```

From R/mod_harmonize.R (re-run wiring, lines 761-764):
```r
observeEvent(input$rerun_now, { shinyjs::click("run_harmonization") })
observeEvent(input$media_rerun_now, { shinyjs::click("run_harmonization") })
```

Tag extraction patterns:
```r
tag_map <- data_store$column_tags
name_cols <- names(tag_map)[tag_map == "Name"]
unit_cols <- names(tag_map)[tag_map == "Unit"]
result_cols <- names(tag_map)[tag_map == "Result"]
date_cols <- names(tag_map)[tag_map == "StudyDate"]     # from study_type_tags
dur_cols <- names(tag_map)[tag_map == "Duration"]        # from study_type_tags
dur_unit_cols <- names(tag_map)[tag_map == "DurationUnit"] # from study_type_tags
media_cols <- names(tag_map)[tag_map == "Media"]         # from study_type_tags
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace buttons and build pre-flight modal with step-mask execution</name>
  <files>R/mod_clean_data.R, R/mod_harmonize.R</files>
  <read_first>
    - R/mod_clean_data.R (full file -- button at lines 63-68, observer at lines 130-340, has_data output)
    - R/mod_harmonize.R (full file -- button at lines 74-80, observer at lines 204-622, re-run wiring at lines 761-764, media_rerun_now observer from Plan 02)
    - R/cleaning_pipeline.R (lines 248-458 for pre-check signatures; lines 1948-2210 for orchestration pattern)
    - .planning/phases/42-integration-shiny-polish/42-UI-SPEC.md (pre-flight modal structure, copywriting, zero-changes state)
    - .planning/phases/42-integration-shiny-polish/42-CONTEXT.md (D-01 through D-05)
    - .planning/phases/42-integration-shiny-polish/42-PATTERNS.md (pre-flight modal code template)
  </read_first>
  <action>
**Part A: R/mod_clean_data.R -- Replace button and add modal**

1. Replace `actionButton(ns("run_cleaning"), "Run Cleaning", class = "btn-success btn-lg", icon = icon("magic"))` (lines 63-68) with:
```r
shinyjs::disabled(
  actionButton(
    ns("run_pipeline"),
    "Run Pipeline",
    class = "btn-success btn-lg mt-3 mb-3",
    icon = icon("play")
  )
)
```

2. Add an enable/disable observer for the new button (in the server function, near the has_data output):
```r
observe({
  has_tags <- !is.null(data_store$column_tags) && has_required_chemical_tags(data_store$column_tags)
  if (has_tags) shinyjs::enable("run_pipeline") else shinyjs::disable("run_pipeline")
})
```

3. Replace the existing `observeEvent(input$run_cleaning, {...})` block (lines 130-340) with a new `observeEvent(input$run_pipeline, {...})` that:
   - Calls `req(data_store$clean)`.
   - Extracts tag_map, name_cols, unit_cols, result_cols from `data_store$column_tags`.
   - Extracts study_type tags: date_cols, dur_cols, dur_unit_cols, media_cols from `data_store$study_type_tags` (with NULL-safe extraction: `if (!is.null(data_store$study_type_tags)) { stv <- unlist(data_store$study_type_tags); ... } else { character(0) }`).
   - Runs all 11 pre-check functions synchronously, building a named list `checks`:
     ```r
     checks <- list(
       unicode    = precheck_unicode_to_ascii(df),
       whitespace = precheck_trim_whitespace(df),
       cas        = precheck_normalize_cas(df, tag_map),
       names      = precheck_name_cleaning(df, name_cols),
       isotopes   = precheck_isotope_shortcodes(df, name_cols, data_store$reference_lists$isotope_lookup),
       multi      = precheck_multi_analyte(df, name_cols),
       chiral     = precheck_chiral_restore(df, name_cols),
       units      = precheck_harmonize_units(df, unit_cols, data_store$unit_map_working %||% data_store$reference_lists$unit_map),
       duration   = precheck_harmonize_duration(df, dur_cols, dur_unit_cols, data_store$unit_map_working %||% data_store$reference_lists$unit_map),
       dates      = precheck_harmonize_dates(df, date_cols),
       media      = precheck_harmonize_media(df, media_cols, data_store$media_map_working)
     )
     ```
   - Stores `checks` in a module-level `reactiveVal` called `preflight_checks` so the checklist renderer can access it.
   - Calculates `total_changes <- sum(vapply(checks, function(x) x$est_changes, integer(1)))`.
   - If `total_changes == 0L` (D-04): show notification instead of modal:
     ```r
     showNotification(
       tagList(
         "Pre-flight: no steps have changes to apply.",
         actionLink(session$ns("open_preflight_anyway"), "Open pre-flight modal", class = "alert-link ms-2")
       ),
       type = "message",
       duration = 0
     )
     return()
     ```
   - Otherwise: show the pre-flight modal:
     ```r
     showModal(modalDialog(
       title = "Pre-flight Check",
       size = "m",
       easyClose = FALSE,
       uiOutput(session$ns("preflight_checklist")),
       footer = tagList(
         modalButton("Cancel"),
         actionButton(session$ns("run_all"), "Run All Steps", class = "btn-outline-secondary"),
         actionButton(session$ns("run_checked"), "Run Checked Steps", class = "btn-primary")
       )
     ))
     ```

4. Add the `open_preflight_anyway` observer that forces the modal open:
```r
observeEvent(input$open_preflight_anyway, {
  checks <- preflight_checks()
  req(checks)
  showModal(modalDialog(
    title = "Pre-flight Check",
    size = "m",
    easyClose = FALSE,
    uiOutput(session$ns("preflight_checklist")),
    footer = tagList(
      modalButton("Cancel"),
      actionButton(session$ns("run_all"), "Run All Steps", class = "btn-outline-secondary"),
      actionButton(session$ns("run_checked"), "Run Checked Steps", class = "btn-primary")
    )
  ))
})
```

5. Add the checklist renderUI with accordion sections. Step display names and their check keys:

Cleaning steps: `unicode` = "Unicode to ASCII", `whitespace` = "Trim Whitespace", `cas` = "Normalize CAS", `names` = "Name Cleaning", `isotopes` = "Isotope Shortcodes", `multi` = "Multi-Analyte Detection", `chiral` = "Chiral Restoration".

Harmonization steps: `units` = "Unit Harmonization", `duration` = "Duration Conversion", `dates` = "Date Parsing", `media` = "Media Classification".

```r
output$preflight_checklist <- renderUI({
  checks <- preflight_checks()
  req(checks)

  make_row <- function(key, label, check) {
    badge <- if (check$should_run && check$est_changes > 0) {
      tags$span(class = "badge bg-secondary ms-2", sprintf("~%d changes", check$est_changes))
    } else {
      tags$span(class = "badge bg-light text-muted ms-2", "skip")
    }
    div(
      class = "d-flex align-items-center py-2",
      checkboxInput(
        session$ns(paste0("step_", key)),
        label = NULL,
        value = check$should_run && check$est_changes > 0,
        width = "auto"
      ),
      tags$span(class = "ms-2 flex-grow-1", label),
      badge
    )
  }

  cleaning_rows <- tagList(
    make_row("unicode",    "Unicode to ASCII",       checks$unicode),
    make_row("whitespace", "Trim Whitespace",        checks$whitespace),
    make_row("cas",        "Normalize CAS",          checks$cas),
    make_row("names",      "Name Cleaning",          checks$names),
    make_row("isotopes",   "Isotope Shortcodes",     checks$isotopes),
    make_row("multi",      "Multi-Analyte Detection", checks$multi),
    make_row("chiral",     "Chiral Restoration",     checks$chiral)
  )

  harmonize_rows <- tagList(
    make_row("units",    "Unit Harmonization",    checks$units),
    make_row("duration", "Duration Conversion",   checks$duration),
    make_row("dates",    "Date Parsing",          checks$dates),
    make_row("media",    "Media Classification",  checks$media)
  )

  bslib::accordion(
    id = session$ns("preflight_accordion"),
    open = TRUE,
    multiple = TRUE,
    bslib::accordion_panel(title = "Cleaning Steps", value = "cleaning", cleaning_rows),
    bslib::accordion_panel(title = "Harmonization Steps", value = "harmonization", harmonize_rows)
  )
})
```

6. Add `run_all` and `run_checked` observers. Both must:
   - Call `removeModal()`.
   - Disable the `run_pipeline` button.
   - Build a step mask (named logical list).
   - For `run_all`: all steps TRUE.
   - For `run_checked`: read each `input$step_{key}` checkbox value.
   - Execute the existing cleaning pipeline logic (from the old `observeEvent(input$run_cleaning, ...)`) with step-mask guards: wrap each cleaning step in `if (mask$unicode) { ... } else { skip }`.
   - Then execute harmonization by triggering `shinyjs::click("run_harmonization")` (the existing observer in mod_harmonize.R still handles harmonization execution). Store the harmonization mask in `data_store$harmonize_step_mask` so mod_harmonize.R can read it.
   - Re-enable `run_pipeline` in `finally {}`.

For the cleaning pipeline execution within run_checked/run_all, copy the FULL existing pipeline logic from the old `observeEvent(input$run_cleaning, {...})` but wrap each step in a mask guard:
```r
# Example for unicode step:
if (mask$unicode) {
  incProgress(0.08, detail = "Converting unicode to ASCII...")
  df_before <- df
  df <- dplyr::mutate(df, dplyr::across(where(is.character), ComptoxR::clean_unicode))
  all_audits[[length(all_audits) + 1]] <- build_audit_trail(...)
} else {
  incProgress(0.08)
}
```

After all cleaning steps complete, store results in data_store as the existing code does. Then if any harmonization steps are checked, store the mask and trigger harmonization:
```r
data_store$harmonize_step_mask <- list(
  units = mask$units, duration = mask$duration,
  dates = mask$dates, media = mask$media
)
shinyjs::click("run_harmonization")
```

7. In mod_harmonize.R, at the TOP of `observeEvent(input$run_harmonization, {...})` (line 204), read the step mask:
```r
h_mask <- data_store$harmonize_step_mask
if (is.null(h_mask)) h_mask <- list(units = TRUE, duration = TRUE, dates = TRUE, media = TRUE)
```
Then wrap each harmonization stage in a mask guard. For the media pre-stage:
```r
if (h_mask$media && length(media_cols_pre) > 0) { ... }
```
For units (Stage 3):
```r
if (h_mask$units && length(unit_cols) > 0) { ... }
```
And similarly for duration and dates stages.

**Part B: R/mod_harmonize.R -- Remove Run Harmonization button from UI**

Remove the `shinyjs::disabled(actionButton(ns("run_harmonization"), ...))` block (lines 74-80) from `mod_harmonize_ui`. The `observeEvent(input$run_harmonization, ...)` stays in the server because it is triggered programmatically by `shinyjs::click("run_harmonization")` from:
- The re-run link (`input$rerun_now`)
- The media editor re-run link (`input$media_rerun_now`)
- The new `run_checked`/`run_all` observers in mod_clean_data.R

Also remove the button enable/disable observer block (lines 163-173) since the button no longer exists in UI.

Run `air format R/mod_clean_data.R R/mod_harmonize.R` after all changes. Run `jarl check` on both files.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('inst/app/app.R', port=3838, launch.browser=FALSE)" & sleep 12 && curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3838 && kill %1 2>/dev/null; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_clean_data.R contains `actionButton(` with `ns("run_pipeline")` and `"Run Pipeline"`
    - R/mod_clean_data.R does NOT contain `ns("run_cleaning")` in any actionButton call
    - R/mod_clean_data.R contains `observeEvent(input$run_pipeline,`
    - R/mod_clean_data.R contains `preflight_checks` reactiveVal
    - R/mod_clean_data.R contains `output$preflight_checklist <- renderUI(`
    - R/mod_clean_data.R contains `observeEvent(input$run_checked,`
    - R/mod_clean_data.R contains `observeEvent(input$run_all,`
    - R/mod_clean_data.R contains `"Pre-flight Check"` as modal title
    - R/mod_clean_data.R contains `"Pre-flight: no steps have changes to apply."` notification text
    - R/mod_clean_data.R contains `"Cleaning Steps"` and `"Harmonization Steps"` accordion panel titles
    - R/mod_clean_data.R contains `"Run All Steps"` and `"Run Checked Steps"` button labels
    - R/mod_clean_data.R contains `precheck_harmonize_units(` call
    - R/mod_clean_data.R contains `data_store$harmonize_step_mask`
    - R/mod_harmonize.R does NOT contain `actionButton(` with `ns("run_harmonization")` in the UI function
    - R/mod_harmonize.R still contains `observeEvent(input$run_harmonization,` in the server function
    - R/mod_harmonize.R contains `h_mask <- data_store$harmonize_step_mask`
    - Shiny cold boot produces "Listening on" without errors
  </acceptance_criteria>
  <done>Single Run Pipeline button replaces both Run Cleaning and Run Harmonization. Pre-flight modal shows fire/skip checklist with estimated changes. Users can run all or checked steps. Zero-change case shows notification with link to open modal. Shiny app starts cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of pre-flight modal and media editor</name>
  <files>R/mod_clean_data.R, R/mod_harmonize.R</files>
  <action>
    No code changes. Human verifies the full Phase 42 feature set end-to-end:
    - Pre-flight modal with fire/skip checklist
    - Step-mask pipeline execution
    - Media editor with UNMATCHED/MAPPED sections, badges, modals, persistence
  </action>
  <verify>
    Human performs the following verification steps:
    1. Start the app: `concert::run_app()` or `shiny::runApp('inst/app/app.R')`
    2. Upload a test file and tag columns (at minimum: Name, CASRN, Result, Unit, Media)
    3. Verify the Clean Data tab shows "Run Pipeline" button (NOT "Run Cleaning")
    4. Verify the Harmonize tab does NOT show "Run Harmonization" button
    5. Click "Run Pipeline" -- verify the pre-flight modal opens with two accordion sections (Cleaning Steps, Harmonization Steps)
    6. Verify each step row has a checkbox, step name, and badge ("~N changes" or "skip")
    7. Uncheck a step and click "Run Checked Steps" -- verify unchecked step is skipped
    8. In the Harmonize tab, expand the "Media Classification" accordion panel
    9. Verify the DT table shows term/canonical/source/active columns
    10. Verify unmatched rows appear at top with yellow highlighting
    11. Click an unmatched row -- verify edit modal opens with pre-filled term
    12. Fill in a canonical value and save -- verify re-run notification appears
    13. Click an AMOS row -- verify "AMOS entries are read-only" notification appears
  </verify>
  <done>Human confirms all Phase 42 features work correctly end-to-end. Type "approved" or describe issues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pre-check input data | Pre-checks run on user-uploaded data before pipeline execution |
| Step mask from UI checkboxes | User-controlled boolean mask determines which steps execute |
| Cross-module data_store communication | harmonize_step_mask passes from mod_clean_data to mod_harmonize via shared reactive store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-09 | T (Tampering) | harmonize_step_mask | accept | Mask values are booleans from Shiny checkboxInput; worst case: user skips a step they chose to skip (intended behavior) |
| T-42-10 | D (DoS) | 11 pre-checks running synchronously | accept | All pre-checks are vectorized O(n) operations on in-memory dataframes; same cost as existing 7 cleaning pre-checks that already run inline |
| T-42-11 | E (Elevation) | shinyjs::click("run_harmonization") cross-module trigger | accept | Standard Shiny pattern; runs within same session; observer validates req(data_store$clean) before proceeding |
</threat_model>

<verification>
1. Run Pipeline button visible and enabled when columns are tagged
2. Pre-flight modal opens with correct step listing and badges
3. Zero-change notification appears when all pre-checks return 0
4. Run Checked Steps respects unchecked steps
5. Run All Steps runs everything regardless of checkboxes
6. Harmonization steps respect the step mask from the modal
7. Shiny cold boot passes cleanly
</verification>

<success_criteria>
- Single Run Pipeline button replaces both previous buttons
- Pre-flight modal shows 11 steps across 2 sections with fire/skip badges
- Step mask execution works for both cleaning and harmonization
- Zero-change shortcut notification works with "Open pre-flight modal" link
- Shiny cold boot passes
- Human verification confirms end-to-end workflow
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-shiny-polish/42-03-SUMMARY.md`
</output>
