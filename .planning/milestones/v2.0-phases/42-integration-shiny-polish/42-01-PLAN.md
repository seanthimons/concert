---
phase: 42-integration-shiny-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/cleaning_pipeline.R
  - R/cleaning_reference.R
  - R/media_harmonizer.R
  - tests/testthat/test-harmonize-prechecks.R
  - tests/testthat/test-media-persistence.R
autonomous: true
requirements:
  - RECO-01
  - MEDIT-03

must_haves:
  truths:
    - "Four harmonization pre-check functions exist and return list(should_run, est_changes) matching the cleaning pre-check contract"
    - "load_media_map() merges user RDS with AMOS table, user entries taking precedence for duplicate terms"
    - "harmonize_media() accepts an optional media_map parameter and falls back to get_media_table() when NULL"
    - "load_all_reference_lists() returns a media_map key in its output list"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "precheck_harmonize_units, precheck_harmonize_duration, precheck_harmonize_dates, precheck_harmonize_media"
      contains: "precheck_harmonize_units"
    - path: "R/cleaning_reference.R"
      provides: "load_media_map function and media_map key in load_all_reference_lists"
      contains: "load_media_map"
    - path: "R/media_harmonizer.R"
      provides: "harmonize_media with optional media_map parameter"
      contains: "media_map = NULL"
    - path: "tests/testthat/test-harmonize-prechecks.R"
      provides: "Unit tests for 4 harmonization pre-check functions"
      contains: "precheck_harmonize_units"
    - path: "tests/testthat/test-media-persistence.R"
      provides: "Round-trip tests for load_media_map and user RDS persistence"
      contains: "load_media_map"
  key_links:
    - from: "R/cleaning_reference.R"
      to: "R/media_harmonizer.R"
      via: "load_media_map() calls get_media_table()"
      pattern: "get_media_table\\(\\)"
    - from: "R/cleaning_pipeline.R"
      to: "R/mod_clean_data.R"
      via: "pre-check functions called before modal display (wired in Plan 03)"
      pattern: "precheck_harmonize_"
---

<objective>
Build backend infrastructure for the pre-flight modal and media editor features: four harmonization pre-check functions, media map persistence layer with user/AMOS merge, and harmonize_media() API extension to accept a passed-in media map.

Purpose: Plan 02 (media editor) needs load_media_map() and harmonize_media(media_map=) to wire user edits into the pipeline. Plan 03 (pre-flight modal) needs the four harmonization pre-check functions to populate the checklist. This plan creates all backend services with tests before any UI work begins.

Output: Modified R/cleaning_pipeline.R, R/cleaning_reference.R, R/media_harmonizer.R; new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-integration-shiny-polish/42-CONTEXT.md
@.planning/phases/42-integration-shiny-polish/42-RESEARCH.md
@.planning/phases/42-integration-shiny-polish/42-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From R/cleaning_pipeline.R (pre-check contract, lines 248-301):
```r
# Every pre-check function returns this exact structure:
# list(should_run = logical(1), est_changes = integer(1))

# Example: precheck_unicode_to_ascii (reference template)
precheck_unicode_to_ascii <- function(df) {
  char_cols <- names(df)[vapply(df, is.character, logical(1))]
  if (length(char_cols) == 0) {
    return(list(should_run = FALSE, est_changes = 0L))
  }
  # ... vectorized computation ...
  list(should_run = should_run, est_changes = est_changes)
}
```

From R/cleaning_reference.R (load_corrections template, lines 213-224):
```r
load_corrections <- function(cache_dir) {
  cache_path <- file.path(cache_dir, "corrections.rds")
  fetch_fn <- function() {
    tibble::tibble(pattern = character(), replacement = character())
  }
  load_or_fetch_reference(cache_path, fetch_fn, "one-off corrections")
}
```

From R/cleaning_reference.R (load_all_reference_lists, lines 414-426):
```r
load_all_reference_lists <- function(cache_dir) {
  list(
    stop_words = load_stop_words(cache_dir),
    block_patterns = load_block_patterns(cache_dir),
    functional_categories = load_functional_categories(cache_dir),
    strip_terms = load_strip_terms(cache_dir),
    corrections = load_corrections(cache_dir),
    isotope_lookup = load_isotope_lookup(cache_dir),
    unit_map = load_unit_map(cache_dir),
    unit_synonyms = load_unit_synonyms(cache_dir),
    toxval_schema = load_toxval_schema(cache_dir)
  )
}
```

From R/media_harmonizer.R (current harmonize_media signature, line 124):
```r
harmonize_media <- function(raw_media, orig_row_id = seq_along(raw_media)) {
```

From R/media_harmonizer.R (get_media_table usage, line 139):
```r
media_tbl <- get_media_table()
```

From R/media_harmonizer.R (internal lookup uses media_tbl$term and media_tbl$canonical_term):
```r
lookup_hash <- stats::setNames(seq_len(nrow(media_tbl)), media_tbl$term)
canonical_out[exact_mask] <- media_tbl$canonical_term[idx_vec]
envo_out[exact_mask] <- media_tbl$envo_id[idx_vec]
category_out[exact_mask] <- media_tbl$media_category[idx_vec]
```

amos_media.rds schema (7 columns):
  term, canonical_term, envo_id, parent, media_category, source, fetch_timestamp

user_media_map.rds schema (4 columns per D-08):
  term, canonical, source, active
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add four harmonization pre-check functions to cleaning_pipeline.R</name>
  <files>R/cleaning_pipeline.R, tests/testthat/test-harmonize-prechecks.R</files>
  <read_first>
    - R/cleaning_pipeline.R (lines 248-458 for existing pre-check pattern; full file for insertion point)
    - R/media_harmonizer.R (harmonize_media output schema — media_flag values)
    - R/tag_helpers.R (classify_tags — how tags route to numeric_tags vs study_type_tags)
    - .planning/phases/42-integration-shiny-polish/42-PATTERNS.md (pre-check analog patterns)
    - .planning/phases/42-integration-shiny-polish/42-RESEARCH.md (pre-check gap table)
  </read_first>
  <action>
Add four new pre-check functions to R/cleaning_pipeline.R immediately after the existing `precheck_chiral_restore` function (after line 458). Each function MUST return `list(should_run = logical(1), est_changes = integer(1))` matching the existing contract. Use `@keywords internal` and `@export` roxygen tags matching existing pre-checks.

**1. precheck_harmonize_units(df, unit_cols, unit_map)**
- If `length(unit_cols) == 0`, return `list(should_run = FALSE, est_changes = 0L)`.
- Collect all non-NA, non-empty values from `unit_cols` columns: `all_unit_vals <- unlist(lapply(unit_cols, function(col) df[[col]]))`, filter with `!is.na(x) & nzchar(x)`.
- Count values NOT found in `unit_map$from_unit`: `unmapped <- sum(!tolower(trimws(all_unit_vals)) %in% tolower(trimws(unit_map$from_unit)))`.
- `should_run = length(all_unit_vals) > 0L`, `est_changes = as.integer(length(all_unit_vals))` (all values get processed; unmapped is supplementary info).

**2. precheck_harmonize_duration(df, dur_cols, dur_unit_cols, unit_map)**
- If `length(dur_cols) == 0 && length(dur_unit_cols) == 0`, return `list(should_run = FALSE, est_changes = 0L)`.
- Count non-NA values in duration-tagged columns: `est_changes <- as.integer(sum(vapply(c(dur_cols, dur_unit_cols), function(col) sum(!is.na(df[[col]]) & nzchar(as.character(df[[col]])), na.rm = TRUE), integer(1))))`.
- `should_run = est_changes > 0L`.

**3. precheck_harmonize_dates(df, date_cols)**
- If `length(date_cols) == 0`, return `list(should_run = FALSE, est_changes = 0L)`.
- Count non-NA, non-empty values in date-tagged columns: `est_changes <- as.integer(sum(vapply(date_cols, function(col) sum(!is.na(df[[col]]) & nzchar(as.character(df[[col]])), na.rm = TRUE), integer(1))))`.
- `should_run = est_changes > 0L`.

**4. precheck_harmonize_media(df, media_cols, media_map)**
- If `length(media_cols) == 0`, return `list(should_run = FALSE, est_changes = 0L)`.
- Count non-NA, non-empty values: `total_vals <- sum(vapply(media_cols, function(col) sum(!is.na(df[[col]]) & nzchar(as.character(df[[col]])), na.rm = TRUE), integer(1)))`.
- If `media_map` is provided (not NULL, nrow > 0), count values not in `media_map$term` as supplementary metric but `est_changes = as.integer(total_vals)`.
- `should_run = total_vals > 0L`.

Then create `tests/testthat/test-harmonize-prechecks.R` with these test cases:

```r
# Test data: 5-row tibble with chemical, unit, duration, date, media columns
test_df <- tibble::tibble(
  chemical_name = c("Acetone", "Ethanol", "Benzene", "Toluene", "Xylene"),
  cas = c("67-64-1", "64-17-5", "71-43-2", "108-88-3", "1330-20-7"),
  result = c("1.5", "2.0", "0.5", "3.1", "4.2"),
  unit = c("mg/L", "ug/L", "ppb", "WEIRD_UNIT", "mg/L"),
  duration_val = c("96", "14", NA, "2", "6"),
  duration_unit = c("hr", "days", NA, "wk", "mo"),
  study_date = c("2020-01-15", "15JAN2020", NA, "01/15/2020", "2020"),
  media = c("freshwater", "soil", NA, "UNKNOWN_MEDIA", "air")
)
unit_map <- tibble::tibble(from_unit = c("mg/L", "ug/L", "ppb", "ppm"), to_unit = rep("mg/L", 4), multiplier = c(1, 0.001, 0.001, 1), category = rep("concentration", 4), confidence = rep("HIGH", 4), source = rep("test", 4))
media_map <- tibble::tibble(term = c("freshwater", "soil", "air"), canonical_term = c("freshwater", "soil", "air"), source = rep("amos", 3))
```

Test cases:
- `precheck_harmonize_units`: should_run=TRUE, est_changes=5 for 5 non-NA unit values. With empty unit_cols → should_run=FALSE, est_changes=0.
- `precheck_harmonize_duration`: should_run=TRUE, est_changes > 0 for non-NA duration values. With no dur columns → should_run=FALSE.
- `precheck_harmonize_dates`: should_run=TRUE, est_changes=4 for 4 non-NA dates. With no date_cols → should_run=FALSE.
- `precheck_harmonize_media`: should_run=TRUE, est_changes=4 for 4 non-NA media values. With no media_cols → should_run=FALSE.
- All-NA column test: should_run=FALSE for each function when all values in tagged columns are NA.

Run `air format` on both files after writing. Run `jarl check` after formatting.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-harmonize-prechecks.R')"</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R contains `precheck_harmonize_units <- function(df, unit_cols, unit_map)`
    - R/cleaning_pipeline.R contains `precheck_harmonize_duration <- function(df, dur_cols, dur_unit_cols, unit_map)`
    - R/cleaning_pipeline.R contains `precheck_harmonize_dates <- function(df, date_cols)`
    - R/cleaning_pipeline.R contains `precheck_harmonize_media <- function(df, media_cols, media_map)`
    - All four functions contain `list(should_run =` in their return statements
    - All four functions contain the early-return guard `return(list(should_run = FALSE, est_changes = 0L))`
    - tests/testthat/test-harmonize-prechecks.R exits 0 with all tests passing
    - No lint errors from `jarl check R/cleaning_pipeline.R`
  </acceptance_criteria>
  <done>Four harmonization pre-check functions exist in cleaning_pipeline.R following the exact contract of the seven existing pre-checks. All unit tests pass. Files formatted and lint-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add load_media_map() to cleaning_reference.R and extend harmonize_media() API</name>
  <files>R/cleaning_reference.R, R/media_harmonizer.R, tests/testthat/test-media-persistence.R</files>
  <read_first>
    - R/cleaning_reference.R (full file — load_corrections template at lines 204-224, load_all_reference_lists at lines 414-426)
    - R/media_harmonizer.R (full file — harmonize_media signature at line 124, get_media_table usage at line 139, internal lookup uses media_tbl$canonical_term)
    - .planning/phases/42-integration-shiny-polish/42-PATTERNS.md (load_media_map code template and harmonize_media API change)
    - .planning/phases/42-integration-shiny-polish/42-RESEARCH.md (Risk 6: column name mismatch between amos_media.rds and user_media_map.rds)
  </read_first>
  <action>
**Part A: Add load_media_map() to R/cleaning_reference.R**

Insert the `load_media_map` function BEFORE `load_all_reference_lists` (before the current line 414). The function:

```r
#' Load merged media harmonization map (user edits + AMOS fallback)
#'
#' User rows (user_media_map.rds) take precedence for the same term.
#' Falls back to amos_media.rds via get_media_table() for all other terms.
#' Returns a display-schema tibble with 4 columns for the media editor DT,
#' plus the additional columns harmonize_media() needs internally (canonical_term,
#' envo_id, media_category) when passed as media_map parameter.
#'
#' @param cache_dir Directory for cache files
#' @return Tibble with columns: term, canonical, canonical_term, envo_id,
#'   media_category, source, active
#' @export
load_media_map <- function(cache_dir) {
  user_path <- file.path(cache_dir, "user_media_map.rds")
  user_map <- if (file.exists(user_path)) readRDS(user_path) else NULL

  amos_raw <- get_media_table()

  amos_map <- if (!is.null(amos_raw) && nrow(amos_raw) > 0) {
    tibble::tibble(
      term           = amos_raw$term,
      canonical      = amos_raw$canonical_term,
      canonical_term = amos_raw$canonical_term,
      envo_id        = amos_raw$envo_id,
      media_category = amos_raw$media_category,
      source         = "amos",
      active         = TRUE
    )
  } else {
    tibble::tibble(
      term = character(), canonical = character(),
      canonical_term = character(), envo_id = character(),
      media_category = character(), source = character(),
      active = logical()
    )
  }

  if (!is.null(user_map) && nrow(user_map) > 0) {
    # Ensure user_map has all columns (backfill if saved with 4-col schema)
    if (!"canonical_term" %in% names(user_map)) {
      user_map$canonical_term <- user_map$canonical
    }
    if (!"envo_id" %in% names(user_map)) {
      user_map$envo_id <- NA_character_
    }
    if (!"media_category" %in% names(user_map)) {
      user_map$media_category <- NA_character_
    }
    amos_fallback <- amos_map[!amos_map$term %in% user_map$term, ]
    dplyr::bind_rows(user_map, amos_fallback)
  } else {
    amos_map
  }
}
```

Then add `media_map = load_media_map(cache_dir)` to the list inside `load_all_reference_lists()`, after the `toxval_schema` entry. Also update the roxygen `@return` documentation to include `media_map` in the list of keys.

**Part B: Extend harmonize_media() in R/media_harmonizer.R**

Change the function signature from:
```r
harmonize_media <- function(raw_media, orig_row_id = seq_along(raw_media))
```
to:
```r
harmonize_media <- function(raw_media, orig_row_id = seq_along(raw_media), media_map = NULL)
```

Replace line 139 (`media_tbl <- get_media_table()`) with:
```r
# Use passed-in map or fall back to bundled AMOS table (D-14 priority order)
media_tbl <- if (!is.null(media_map) && nrow(media_map) > 0) {
  # Ensure the map has the columns harmonize_media needs internally
  if ("canonical_term" %in% names(media_map)) {
    media_map
  } else if ("canonical" %in% names(media_map)) {
    # Translate display schema to internal schema
    mm <- media_map
    mm$canonical_term <- mm$canonical
    if (!"envo_id" %in% names(mm)) mm$envo_id <- NA_character_
    if (!"media_category" %in% names(mm)) mm$media_category <- NA_character_
    mm
  } else {
    get_media_table()
  }
} else {
  get_media_table()
}
```

Also update the roxygen docs to document the new `media_map` parameter:
```r
#' @param media_map Optional tibble with columns term, canonical_term, envo_id,
#'   media_category. When NULL (default), falls back to the bundled AMOS table
#'   via get_media_table(). Pass a merged user+AMOS map from load_media_map()
#'   to enable user-defined mappings (MEDIT-03, D-14).
```

**Part C: Create tests/testthat/test-media-persistence.R**

Test cases:
1. `load_media_map` returns tibble with expected columns (term, canonical, canonical_term, envo_id, media_category, source, active) when no user RDS exists (AMOS-only).
2. `load_media_map` returns user rows prepended before AMOS rows when user RDS exists.
3. User entry for term "freshwater" overrides AMOS entry for same term (user row present, AMOS row for "freshwater" absent in output).
4. `harmonize_media()` with `media_map = NULL` still works (backward compatibility — calls get_media_table internally).
5. `harmonize_media()` with custom `media_map` tibble uses the passed-in map instead of get_media_table().
6. `harmonize_media()` with media_map containing `canonical` column (not `canonical_term`) still works via internal translation.

For tests that need a user RDS, use `withr::with_tempdir()` to create a temp directory, write a user_media_map.rds there, and call `load_media_map(tempdir_path)`. Mock `get_media_table()` return with a small tibble.

Run `air format` on all modified files. Run `jarl check` after formatting.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/testthat/test-media-persistence.R')"</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_reference.R contains `load_media_map <- function(cache_dir)`
    - R/cleaning_reference.R contains `media_map = load_media_map(cache_dir)` inside load_all_reference_lists
    - R/media_harmonizer.R contains `harmonize_media <- function(raw_media, orig_row_id = seq_along(raw_media), media_map = NULL)`
    - R/media_harmonizer.R contains `if (!is.null(media_map) && nrow(media_map) > 0)`
    - R/media_harmonizer.R no longer has a bare `media_tbl <- get_media_table()` without the conditional
    - tests/testthat/test-media-persistence.R exits 0 with all tests passing
    - Existing media harmonizer tests still pass: `testthat::test_file('tests/testthat/test-media-harmonizer.R')` exits 0
    - No lint errors from `jarl check R/cleaning_reference.R` and `jarl check R/media_harmonizer.R`
  </acceptance_criteria>
  <done>load_media_map() exists in cleaning_reference.R with user/AMOS merge logic. load_all_reference_lists() includes media_map key. harmonize_media() accepts optional media_map parameter with backward-compatible NULL default. All new and existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_media_map.rds file I/O | User-controlled RDS file read into R session via readRDS() |
| media_map parameter injection | External caller can pass arbitrary tibble as media_map to harmonize_media() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | T (Tampering) | load_media_map / readRDS | mitigate | readRDS only from known cache_dir path (inst/extdata/reference_cache); no user-supplied arbitrary paths; file.exists guard before read |
| T-42-02 | T (Tampering) | harmonize_media media_map parameter | mitigate | Validate media_map has required columns (term, canonical_term or canonical) before use; fall back to get_media_table() if schema invalid |
| T-42-03 | D (DoS) | precheck functions with large dataframes | accept | Pre-checks use vectorized operations (vapply, sum) — same O(n) cost as existing cleaning pre-checks; no unbounded loops |
| T-42-04 | I (Information Disclosure) | user_media_map.rds | accept | File contains term/canonical mappings only, no PII or secrets; stored in package cache directory with OS-level permissions |
</threat_model>

<verification>
1. All 4 harmonization pre-check functions return `list(should_run, est_changes)` matching existing contract
2. `load_media_map()` returns a tibble with the 7-column merged schema
3. `harmonize_media(raw_media, media_map = custom_map)` uses the custom map
4. `harmonize_media(raw_media)` (no media_map) still works identically to pre-change behavior
5. Existing test suite passes without regressions
</verification>

<success_criteria>
- 4 new pre-check functions in cleaning_pipeline.R pass unit tests
- load_media_map() in cleaning_reference.R passes round-trip persistence tests
- harmonize_media() API extension is backward-compatible (existing tests pass unchanged)
- All new test files exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-shiny-polish/42-01-SUMMARY.md`
</output>
