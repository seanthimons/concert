---
phase: 42-integration-shiny-polish
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - R/mod_harmonize.R
autonomous: true
requirements:
  - MEDIT-01
  - MEDIT-02
  - MEDIT-03

must_haves:
  truths:
    - "User can see a media classification table in the Harmonize tab with term, canonical, source, and active columns"
    - "Unmatched media terms appear at the top of the table with yellow highlighting"
    - "User can click an unmatched or user-source row to open an edit modal and assign a canonical value"
    - "User edits are persisted to user_media_map.rds and survive session restart"
    - "AMOS-source rows display a gray badge and user-source rows display a blue badge"
    - "After saving a media edit, a notification prompts user to re-run harmonization with a Re-run now link"
    - "When user maps a term that already has an AMOS entry, a confirmation modal asks before overriding"
  artifacts:
    - path: "R/mod_harmonize.R"
      provides: "Media editor accordion panel with DT table, edit modal, save logic, AMOS override confirmation, re-run notification"
      contains: "media_map_working"
  key_links:
    - from: "R/mod_harmonize.R"
      to: "R/cleaning_reference.R"
      via: "data_store$reference_lists$media_map loaded by load_all_reference_lists()"
      pattern: "media_map_working"
    - from: "R/mod_harmonize.R"
      to: "R/media_harmonizer.R"
      via: "harmonize_media(media_map = data_store$media_map_working) call with user map"
      pattern: "media_map = data_store\\$media_map_working"
---

<objective>
Build the media classification editor UI in the Harmonize tab: a DT table displaying merged user+AMOS media mappings with UNMATCHED/MAPPED sections, row-click edit modals with AMOS override confirmation, RDS persistence for user edits, and a re-run notification link.

Purpose: Users need to see which media terms are unmatched after harmonization and add their own canonical mappings. User mappings take priority over AMOS entries and persist across sessions. This closes MEDIT-01, MEDIT-02, and MEDIT-03.

Output: Modified R/mod_harmonize.R with media editor panel, working copy initialization, edit/save/persist logic, and harmonize_media() call updated to pass media_map_working.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-integration-shiny-polish/42-CONTEXT.md
@.planning/phases/42-integration-shiny-polish/42-RESEARCH.md
@.planning/phases/42-integration-shiny-polish/42-PATTERNS.md
@.planning/phases/42-integration-shiny-polish/42-UI-SPEC.md
@.planning/phases/42-integration-shiny-polish/42-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and existing codebase -->

From R/cleaning_reference.R (Plan 01 addition):
```r
# load_media_map returns 7-column tibble:
# term, canonical, canonical_term, envo_id, media_category, source, active
load_media_map <- function(cache_dir) { ... }

# load_all_reference_lists now includes media_map key:
load_all_reference_lists <- function(cache_dir) {
  list(
    ...,
    media_map = load_media_map(cache_dir)
  )
}
```

From R/media_harmonizer.R (Plan 01 API extension):
```r
harmonize_media <- function(raw_media, orig_row_id = seq_along(raw_media), media_map = NULL)
# When media_map is non-NULL, uses it instead of get_media_table()
```

From R/mod_harmonize.R (existing working copy pattern, lines 180-200):
```r
unit_map_ready <- reactiveVal(FALSE)
observe({
  if (is.null(data_store$unit_map_working) &&
      !is.null(data_store$reference_lists$unit_map)) {
    data_store$unit_map_working <- data_store$reference_lists$unit_map
    if (!unit_map_ready()) unit_map_ready(TRUE)
  }
})
```

From R/mod_harmonize.R (edit modal pattern, lines 906-951):
```r
observeEvent(input$open_edit_modal, {
  tbl <- data_store$unit_map_working
  row <- tbl[tbl$from_unit == msg$term, ][1, ]
  showModal(modalDialog(
    title = "Edit Unit Mapping",
    textInput(session$ns("modal_from_unit"), "From Unit", value = row$from_unit),
    tags$input(type = "hidden", id = session$ns("modal_orig_from"), value = row$from_unit),
    footer = tagList(
      modalButton("Discard"),
      actionButton(session$ns("save_unit_mapping"), "Save Mapping", class = "btn-primary")
    ),
    easyClose = FALSE
  ))
})
```

From R/mod_harmonize.R (re-run link pattern, lines 755-764):
```r
actionLink(session$ns("rerun_now"), "Re-run now", class = "alert-link ms-2")
observeEvent(input$rerun_now, { shinyjs::click("run_harmonization") })
```

From R/mod_harmonize.R (editors_panel accordion, lines 770-809):
```r
output$editors_panel <- renderUI({
  req(unit_map_ready())
  bslib::accordion(
    id = session$ns("editors"),
    open = FALSE,
    multiple = TRUE,
    bslib::accordion_panel(title = ..., value = "unit_editor", ...),
    bslib::accordion_panel(title = ..., value = "corrections_editor", ...),
    bslib::accordion_panel(title = ..., value = "unmatched_units", ...)
  )
})
```

From R/mod_harmonize.R (harmonize_media call site, lines 348-351):
```r
harmonize_media(
  raw_media = as.character(input_df[[media_cols_pre[1]]]),
  orig_row_id = seq_len(nrow(input_df))
)
```

data_store reactive values (existing):
  data_store$unit_map_working     # session-local unit map
  data_store$corrections_working  # session-local corrections
  data_store$reference_lists      # loaded at startup (now includes media_map)
  data_store$media_results        # output of harmonize_media() after pipeline run

user_media_map.rds save path:
  system.file("extdata/reference_cache", package = "concert")
  OR "data/reference_cache" (non-package fallback)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add media_map_working initialization and media editor accordion panel</name>
  <files>R/mod_harmonize.R</files>
  <read_first>
    - R/mod_harmonize.R (full file — working copy init at lines 175-200, editors_panel at lines 770-809, DT callback pattern for row clicks)
    - .planning/phases/42-integration-shiny-polish/42-UI-SPEC.md (media table columns, row grouping, badge rendering, interaction states)
    - .planning/phases/42-integration-shiny-polish/42-PATTERNS.md (DT table pattern, working copy init pattern, badge rendering)
    - .planning/phases/42-integration-shiny-polish/42-CONTEXT.md (D-06 through D-14)
    - .planning/phases/42-integration-shiny-polish/42-01-SUMMARY.md (load_media_map outputs)
  </read_first>
  <action>
Make these changes to R/mod_harmonize.R:

**1. Add media_map_working one-shot initialization (after the corrections_ready block, ~line 200):**

```r
media_map_ready <- reactiveVal(FALSE)
observe({
  if (
    is.null(data_store$media_map_working) &&
      !is.null(data_store$reference_lists$media_map)
  ) {
    data_store$media_map_working <- data_store$reference_lists$media_map
    if (!media_map_ready()) media_map_ready(TRUE)
  }
})
```

**2. Add a fourth accordion panel to the editors_panel renderUI (inside the bslib::accordion call, after the "unmatched_units" panel):**

```r
bslib::accordion_panel(
  title = uiOutput(session$ns("media_editor_title")),
  value = "media_editor",
  icon = bsicons::bs_icon("globe"),
  DT::DTOutput(session$ns("media_table")),
  div(
    class = "mt-2",
    actionButton(
      session$ns("add_media_mapping"),
      "Add Media Mapping",
      class = "btn-outline-primary btn-sm",
      icon = icon("plus")
    )
  )
)
```

Also update the `req()` in `output$editors_panel` from `req(unit_map_ready())` to `req(unit_map_ready())` (unchanged — media_map_ready is not gating the panel since media map may not exist if AMOS cache is missing).

**3. Add media editor title with count:**

```r
output$media_editor_title <- renderUI({
  n <- if (!is.null(data_store$media_map_working)) {
    nrow(data_store$media_map_working)
  } else {
    0
  }
  n_unmatched <- 0
  if (!is.null(data_store$media_results)) {
    n_unmatched <- sum(data_store$media_results$media_flag == "media_unmatched", na.rm = TRUE)
  }
  if (n_unmatched > 0) {
    sprintf("Media Classification (%d mappings, %d unmatched)", n, n_unmatched)
  } else {
    sprintf("Media Classification (%d mappings)", n)
  }
})
```

**4. Render the media DT table with UNMATCHED/MAPPED sections and badge rendering (D-07, D-08, D-12):**

```r
output$media_table <- DT::renderDT({
  req(media_map_ready())
  tbl <- data_store$media_map_working

  if (is.null(tbl) || nrow(tbl) == 0) {
    return(DT::datatable(
      tibble::tibble(term = character(), canonical = character(),
                     source = character(), active = character()),
      escape = FALSE, selection = "none", rownames = FALSE,
      options = list(language = list(emptyTable = "No media data. Run harmonization with a Media-tagged column to populate the classification table."))
    ))
  }

  # Determine unmatched status: rows where canonical is NA or empty
  is_unmatched <- is.na(tbl$canonical) | !nzchar(tbl$canonical)

  # Build display tibble (4 columns per D-08)
  display_tbl <- tibble::tibble(
    term = tbl$term,
    canonical = dplyr::if_else(is_unmatched, "(unmatched)", tbl$canonical),
    source = dplyr::case_when(
      tbl$source == "user" ~ '<span class="badge bg-primary">user</span>',
      TRUE                 ~ '<span class="badge bg-secondary">amos</span>'
    ),
    active = dplyr::if_else(tbl$active, "Yes", "No")
  )

  # Sort: unmatched first (D-07), then alphabetical within each group
  display_tbl <- display_tbl[order(!is_unmatched, display_tbl$term), ]

  # Track row classes for highlighting
  row_classes <- ifelse(is_unmatched[order(!is_unmatched, tbl$term)], "table-warning", "")

  DT::datatable(
    display_tbl,
    escape = FALSE,
    selection = "single",
    rownames = FALSE,
    options = list(
      pageLength = 25,
      dom = "ftp",
      rowCallback = DT::JS(sprintf(
        "function(row, data, displayNum, index) {
           var classes = %s;
           if (classes[index]) $(row).addClass(classes[index]);
         }",
        jsonlite::toJSON(row_classes)
      ))
    ),
    callback = DT::JS(sprintf(
      "table.on('click', 'tr', function() {
         var d = table.row(this).data();
         if (d) Shiny.setInputValue('%s', {term: d[0], ts: Date.now()}, {priority: 'event'});
       });",
      session$ns("open_media_edit_modal")
    ))
  )
}, server = FALSE)
```

**5. Add the row-click edit modal observer (D-06):**

```r
observeEvent(input$open_media_edit_modal, {
  msg <- input$open_media_edit_modal
  req(msg$term)

  tbl <- data_store$media_map_working

  # Clean the "(unmatched)" display value back to the raw term
  search_term <- trimws(tolower(msg$term))
  row <- tbl[tbl$term == search_term, ]

  # AMOS entries are read-only (D-12)
  if (nrow(row) > 0 && row$source[1] == "amos") {
    showNotification(
      "AMOS entries are read-only. Add a user mapping for this term to override.",
      type = "message",
      duration = 5
    )
    return()
  }

  # Determine modal title: "Add" for unmatched, "Edit" for existing user row
  is_new <- nrow(row) == 0 || is.na(row$canonical[1]) || !nzchar(row$canonical[1])
  modal_title <- if (is_new) "Add Media Mapping" else "Edit Media Mapping"

  canonical_val <- if (!is_new) row$canonical[1] else ""
  active_val <- if (!is_new) isTRUE(row$active[1]) else TRUE
  orig_term <- if (!is_new) row$term[1] else search_term

  showModal(modalDialog(
    title = modal_title,
    easyClose = FALSE,
    textInput(session$ns("modal_media_term"), "Term", value = orig_term),
    textInput(session$ns("modal_media_canonical"), "Canonical",
              value = canonical_val, placeholder = "e.g., freshwater"),
    checkboxInput(session$ns("modal_media_active"), "Active", value = active_val),
    tags$input(
      type = "hidden",
      id = session$ns("modal_media_orig_term"),
      value = orig_term
    ),
    footer = tagList(
      modalButton("Discard"),
      actionButton(session$ns("save_media_mapping"), "Save Mapping", class = "btn-primary")
    )
  ))
})
```

**6. Add the "Add Media Mapping" button observer (blank modal):**

```r
observeEvent(input$add_media_mapping, {
  showModal(modalDialog(
    title = "Add Media Mapping",
    easyClose = FALSE,
    textInput(session$ns("modal_media_term"), "Term", value = ""),
    textInput(session$ns("modal_media_canonical"), "Canonical",
              value = "", placeholder = "e.g., freshwater"),
    checkboxInput(session$ns("modal_media_active"), "Active", value = TRUE),
    tags$input(
      type = "hidden",
      id = session$ns("modal_media_orig_term"),
      value = ""
    ),
    footer = tagList(
      modalButton("Discard"),
      actionButton(session$ns("save_media_mapping"), "Save Mapping", class = "btn-primary")
    )
  ))
})
```

**7. Add the save observer with AMOS override confirmation (D-13), RDS persistence (D-09), and re-run notification (D-10):**

```r
# Reactive value to stage the pending save when AMOS override is needed
media_pending_save <- reactiveVal(NULL)

observeEvent(input$save_media_mapping, {
  req(input$modal_media_term, input$modal_media_canonical)

  new_row <- tibble::tibble(
    term           = trimws(tolower(input$modal_media_term)),
    canonical      = trimws(input$modal_media_canonical),
    canonical_term = trimws(input$modal_media_canonical),
    envo_id        = NA_character_,
    media_category = NA_character_,
    source         = "user",
    active         = isTRUE(input$modal_media_active)
  )

  tbl <- data_store$media_map_working

  # Check for AMOS conflict (D-13)
  amos_conflict <- tbl$term == new_row$term & tbl$source == "amos"
  orig_term <- input$modal_media_orig_term
  is_new_term <- is.null(orig_term) || orig_term == "" || orig_term != new_row$term

  if (is_new_term && any(amos_conflict)) {
    existing_canonical <- tbl$canonical[which(amos_conflict)[1]]
    media_pending_save(new_row)
    showModal(modalDialog(
      title = "Override AMOS Mapping?",
      easyClose = FALSE,
      p(sprintf(
        'This term already has an AMOS mapping (canonical: "%s").',
        existing_canonical
      )),
      p("Your mapping will take priority at runtime. The AMOS entry will remain as fallback."),
      footer = tagList(
        modalButton("Cancel"),
        actionButton(session$ns("confirm_amos_override"), "Override", class = "btn-primary")
      )
    ))
    return()
  }

  # No conflict — proceed with save
  do_save_media_mapping(new_row, orig_term)
})

observeEvent(input$confirm_amos_override, {
  pending <- media_pending_save()
  req(pending)
  media_pending_save(NULL)
  do_save_media_mapping(pending, pending$term)
})

do_save_media_mapping <- function(new_row, orig_term) {
  tbl <- data_store$media_map_working

  # Remove old user entry if updating (upsert logic)
  if (!is.null(orig_term) && orig_term != "") {
    idx <- which(tbl$term == orig_term & tbl$source == "user")
    if (length(idx) > 0) tbl <- tbl[-idx[1], ]
  }

  data_store$media_map_working <- dplyr::bind_rows(new_row, tbl)

  # Persist user rows only to RDS (D-09)
  user_rows <- data_store$media_map_working[data_store$media_map_working$source == "user", ]
  cache_path <- system.file("extdata/reference_cache", package = "concert")
  if (nzchar(cache_path)) {
    saveRDS(user_rows, file.path(cache_path, "user_media_map.rds"), compress = FALSE)
  }

  removeModal()

  # Re-run notification (D-10)
  showNotification(
    tagList(
      "Media mappings updated. Re-run harmonization to apply changes?",
      actionLink(session$ns("media_rerun_now"), "Re-run now", class = "alert-link ms-2")
    ),
    type = "message",
    duration = 8
  )
}

observeEvent(input$media_rerun_now, {
  shinyjs::click("run_harmonization")
})
```

**8. Update the harmonize_media() call site (currently ~line 349) to pass media_map_working (D-14, MEDIT-03):**

Change:
```r
harmonize_media(
  raw_media = as.character(input_df[[media_cols_pre[1]]]),
  orig_row_id = seq_len(nrow(input_df))
)
```
to:
```r
harmonize_media(
  raw_media = as.character(input_df[[media_cols_pre[1]]]),
  orig_row_id = seq_len(nrow(input_df)),
  media_map = data_store$media_map_working
)
```

Run `air format R/mod_harmonize.R` after all changes. Run `jarl check R/mod_harmonize.R`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests/testthat')" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - R/mod_harmonize.R contains `media_map_ready <- reactiveVal(FALSE)`
    - R/mod_harmonize.R contains `data_store$media_map_working <- data_store$reference_lists$media_map`
    - R/mod_harmonize.R contains `DT::DTOutput(session$ns("media_table"))`
    - R/mod_harmonize.R contains `output$media_table <- DT::renderDT(`
    - R/mod_harmonize.R contains `badge bg-primary` and `badge bg-secondary`
    - R/mod_harmonize.R contains `observeEvent(input$open_media_edit_modal,`
    - R/mod_harmonize.R contains `observeEvent(input$save_media_mapping,`
    - R/mod_harmonize.R contains `observeEvent(input$confirm_amos_override,`
    - R/mod_harmonize.R contains `do_save_media_mapping <- function(`
    - R/mod_harmonize.R contains `saveRDS(user_rows, file.path(cache_path, "user_media_map.rds")`
    - R/mod_harmonize.R contains `observeEvent(input$media_rerun_now,`
    - R/mod_harmonize.R contains `media_map = data_store$media_map_working` in the harmonize_media() call
    - R/mod_harmonize.R contains `"Media Classification"` or `Media Classification` in the accordion panel title render
    - R/mod_harmonize.R contains `"AMOS entries are read-only"` notification text
    - R/mod_harmonize.R contains `table-warning` class for unmatched row highlighting
    - R/mod_harmonize.R contains `Override AMOS Mapping?` modal title
    - Existing test suite passes without regressions (no new failures beyond pre-existing 3)
  </acceptance_criteria>
  <done>Media classification editor is fully wired in mod_harmonize.R: DT table with UNMATCHED/MAPPED sections, source badges, row-click edit modals, AMOS override confirmation, RDS persistence for user edits, and re-run notification link. harmonize_media() receives the merged media_map_working at runtime per D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input from modal fields | User-supplied term and canonical values flow into media_map_working and RDS persistence |
| DT row click JavaScript | Client-side JS sends Shiny.setInputValue with row data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | T (Tampering) | modal_media_term / modal_media_canonical | mitigate | Input sanitized with trimws() and tolower() before storage; no SQL or file path injection possible (RDS serialization only) |
| T-42-06 | S (Spoofing) | DT JS callback Shiny.setInputValue | accept | Standard Shiny DT pattern; session namespace prevents cross-module spoofing; priority: 'event' is standard usage |
| T-42-07 | T (Tampering) | saveRDS to package directory | mitigate | Only user_rows (source == "user") written; path from system.file() not user-controllable; compress = FALSE per convention |
| T-42-08 | D (DoS) | Large media_map_working DT render | accept | DT pagination (pageLength = 25) limits render cost; server = FALSE is appropriate for typical map sizes (<1000 rows) |
</threat_model>

<verification>
1. Media editor accordion panel renders in Harmonize tab with term/canonical/source/active columns
2. Unmatched rows (canonical is NA) appear at top with yellow table-warning highlighting
3. Clicking a user-source row opens edit modal; clicking AMOS row shows read-only notification
4. Saving a mapping updates media_map_working, persists user rows to RDS, shows re-run notification
5. AMOS override confirmation appears when adding a mapping for a term that already has an AMOS entry
6. Re-run now link triggers harmonization pipeline
</verification>

<success_criteria>
- Media classification accordion panel visible in Harmonize tab editors area
- DT table renders with badge styling, unmatched section, pagination
- Edit/add/save workflow persists to RDS and updates working copy
- AMOS override confirmation modal works correctly
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/42-integration-shiny-polish/42-02-SUMMARY.md`
</output>
