---
phase: "01-multi-tab-structure"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "app.R"
autonomous: true
requirements:
  - "TAB-01"
  - "TAG-01"
  - "TAG-02"
  - "TAG-03"
  - "CURE-01"
  - "CURE-02"
  - "CURE-03"
  - "REV-01"
  - "REV-02"
  - "REV-03"
  - "UX-03"
must_haves:
  truths:
    - "User sees Tag Columns, Run Curation, and Review Results as separate tabs in main navigation"
    - "User can assign column types via dropdowns in Tag Columns tab"
    - "User can see tagged column summary and click Start Curation in Run Curation tab"
    - "User can browse results and download Excel in Review Results tab"
    - "All tabs use full available width without nested card containers"
    - "Each tab has an icon and label"
  artifacts:
    - path: "app.R"
      provides: "Multi-tab UI structure with 6 tabs total"
      contains: "navset_underline"
  key_links:
    - from: "Tag Columns nav_panel"
      to: "output$column_tagging_ui"
      via: "uiOutput rendering"
      pattern: "column_tagging_ui"
    - from: "Run Curation nav_panel"
      to: "output$curation_summary, input$run_curation"
      via: "uiOutput and actionButton"
      pattern: "run_curation"
    - from: "Review Results nav_panel"
      to: "output$curation_stats, output$curation_table, output$download_curated"
      via: "uiOutput, DTOutput, downloadButton"
      pattern: "curation_table"
---

<objective>
Restructure app.R UI to split the single Curation tab into 3 separate top-level tabs (Tag Columns, Run Curation, Review Results) and switch to a full-width tab layout.

Purpose: Users see a clear 3-step workflow as separate tabs instead of scrolling through stacked cards in one tab. All tabs use full available width.
Output: Updated app.R with 6 tabs in a navset_underline layout.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-tab-structure/01-CONTEXT.md
@.planning/phases/01-multi-tab-structure/01-RESEARCH.md

<interfaces>
<!-- Current app.R structure (key UI elements to migrate) -->

Current tab container (line 155): navset_card_tab(id = "main_tabs", ...)
  - nav_panel("Data Preview", ...) with summary_cards + data_table in card()
  - nav_panel("Detection Info", ...) with detection_details
  - nav_panel("Raw Data", ...) with raw_table in card()
  - nav_panel("Curation", ...) with 3 stacked card() elements:
    - Card 1 (Tag Columns): column_tagging_ui (uiOutput) + apply_tags (actionButton)
    - Card 2 (Run Curation): curation_summary (uiOutput) + run_curation (actionButton) + curation_progress (uiOutput), wrapped in conditionalPanel("output.tags_applied")
    - Card 3 (Review Results): curation_stats (uiOutput) + curation_table (DTOutput) + download_curated (downloadButton), wrapped in conditionalPanel("output.curation_completed")

Server outputs (unchanged by this plan):
  - output$column_tagging_ui: renderUI with selectInput dropdowns per column
  - output$tags_applied: reactive boolean
  - output$curation_summary: renderUI with tagged column counts
  - output$curation_progress: renderUI (currently empty/unused)
  - output$curation_completed: reactive boolean
  - output$curation_stats: renderUI with value_box layout
  - output$curation_table: renderDT
  - output$download_curated: downloadHandler
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace navset_card_tab with navset_underline and split Curation into 3 tabs</name>
  <files>app.R</files>
  <action>
In the UI definition section of app.R:

1. Replace `navset_card_tab(id = "main_tabs", ...)` with `navset_underline(id = "main_tabs", ...)`. This removes the card wrapper so tabs use full available width (UX-03).

2. Remove the single "Curation" nav_panel (lines ~198-273) and replace it with 3 new nav_panel entries:

**Tag Columns tab:**
```r
nav_panel(
  title = "Tag Columns",
  value = "tag_columns",
  icon = bsicons::bs_icon("tags"),

  div(
    class = "d-flex justify-content-between align-items-center mb-3",
    h4("Tag Columns"),
    actionButton(
      "apply_tags",
      "Apply Tags",
      class = "btn-primary",
      icon = icon("tag")
    )
  ),
  p("Categorize selected columns for chemical curation."),
  uiOutput("column_tagging_ui")
)
```

Key changes from current:
- Apply Tags button positioned top-right in header area (per CONTEXT.md)
- No card() wrapper (full width)
- Column tagging UI is the main content

**Run Curation tab:**
```r
nav_panel(
  title = "Run Curation",
  value = "run_curation_tab",
  icon = bsicons::bs_icon("play-circle"),

  conditionalPanel(
    condition = "output.tags_applied",

    div(
      class = "alert alert-info",
      uiOutput("curation_summary")
    ),

    actionButton(
      "run_curation",
      "Start Curation",
      class = "btn-success btn-lg mt-3",
      icon = icon("play")
    ),

    uiOutput("curation_progress")
  ),

  conditionalPanel(
    condition = "!output.tags_applied",
    div(
      class = "text-center text-muted py-5",
      bsicons::bs_icon("tags", size = "3em"),
      h4("No columns tagged yet"),
      p("Go to the Tag Columns tab and assign column types first.")
    )
  )
)
```

Key changes: Adds empty state when tags not applied; removes card wrapper.

**Review Results tab:**
```r
nav_panel(
  title = "Review Results",
  value = "review_results",
  icon = bsicons::bs_icon("clipboard-check"),

  conditionalPanel(
    condition = "output.curation_completed",

    uiOutput("curation_stats"),

    div(
      class = "d-flex justify-content-between align-items-center mb-3 mt-3",
      h4("Curated Results"),
      downloadButton(
        "download_curated",
        "Download Excel",
        class = "btn-primary"
      )
    ),

    DTOutput("curation_table")
  ),

  conditionalPanel(
    condition = "!output.curation_completed",
    div(
      class = "text-center text-muted py-5",
      bsicons::bs_icon("hourglass-split", size = "3em"),
      h4("No results yet"),
      p("Run curation first to see results here.")
    )
  )
)
```

Key changes: Download button positioned top-right; value_box stats at top without card wrapper; empty state shown when curation not complete.

3. Add `value` parameter to existing tabs too for consistent programmatic access:
- Data Preview: `value = "data_preview"`
- Detection Info: `value = "detection_info"`
- Raw Data: `value = "raw_data"`

4. Also add `value = "tag_columns"` to Tag Columns empty state (the empty state for when no data is uploaded):
In the Tag Columns tab, add a second conditionalPanel for when no data exists:
```r
conditionalPanel(
  condition = "!output.has_data",
  div(
    class = "text-center text-muted py-5",
    bsicons::bs_icon("upload", size = "3em"),
    h4("Upload a file to start tagging columns"),
    p("Upload a CSV or XLSX file using the sidebar.")
  )
)
```

5. Remove the card() wrappers from existing Data Preview and Raw Data tabs too, so all tabs have consistent full-width layouts. Keep the content (DTOutput, uiOutput) but remove the enclosing card(card_header(), card_body()).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('app.R', local=TRUE); cat('UI parsed OK\n')" 2>&1 | head -5</automated>
  </verify>
  <done>
- app.R UI uses navset_underline instead of navset_card_tab
- 6 nav_panels exist: Data Preview, Detection Info, Raw Data, Tag Columns, Run Curation, Review Results
- Each nav_panel has value, title, and icon parameters
- No card() wrappers around tab content (full-width layout)
- Tag Columns has Apply Tags button top-right and column_tagging_ui
- Run Curation has curation_summary, Start Curation button, and empty state
- Review Results has curation_stats, Download Excel button top-right, curation_table, and empty state
- All existing input/output IDs preserved (no server changes needed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sidebar toggle and auto-navigation behavior</name>
  <files>app.R</files>
  <action>
In the server section of app.R, add two new observers:

1. **Sidebar toggle observer** — Hide sidebar on curation tabs, show on upload/detection tabs.

Per CONTEXT.md: "Sidebar hidden on curation tabs (Tag Columns, Run Curation, Review Results) for maximum space; sidebar visible on upload/detection tabs."

Add an observer on input$main_tabs that uses shinyjs to toggle the sidebar:

```r
# Sidebar visibility based on active tab
observeEvent(input$main_tabs, {
  curation_tabs <- c("tag_columns", "run_curation_tab", "review_results")
  if (input$main_tabs %in% curation_tabs) {
    # Hide sidebar on curation tabs
    shinyjs::runjs("$('.bslib-page-sidebar .sidebar').addClass('collapse').removeClass('show');")
    shinyjs::runjs("$('.bslib-page-sidebar .sidebar-toggle').attr('aria-expanded', 'false');")
  } else {
    # Show sidebar on upload/detection tabs
    shinyjs::runjs("$('.bslib-page-sidebar .sidebar').removeClass('collapse').addClass('show');")
    shinyjs::runjs("$('.bslib-page-sidebar .sidebar-toggle').attr('aria-expanded', 'true');")
  }
})
```

Note: bslib page_sidebar uses Bootstrap 5 collapse classes for sidebar. The JS approach is most reliable cross-version. Test that the sidebar CSS selectors match the actual rendered HTML — if bslib uses different class names, adjust accordingly. Check the rendered page HTML in browser dev tools.

2. **Auto-navigate to Review Results after curation completes.**

Per CONTEXT.md: "After curation completes, auto-navigate to Review Results tab."

In the existing `observeEvent(input$run_curation, ...)` handler, after the line `data_store$curation_status <- "completed"`, add:

```r
# Auto-navigate to Review Results
nav_select("main_tabs", "review_results")
```

This uses bslib's `nav_select()` to programmatically switch tabs. The `"review_results"` value matches the `value` parameter set in Task 1.

3. **Disable Start Curation button until prerequisites met.**

Per CONTEXT.md: "Start Curation button disabled until prerequisites are met (grayed out with tooltip)."

Add an observer that enables/disables the button based on tags AND API key:

```r
# Disable Start Curation until tags applied
observe({
  has_tags <- !is.null(data_store$column_tags) && length(data_store$column_tags) > 0
  has_api_key <- Sys.getenv("ctx_api_key") != ""

  if (has_tags && has_api_key) {
    shinyjs::enable("run_curation")
  } else {
    shinyjs::disable("run_curation")
  }
})
```

Also add `shinyjs::disabled()` wrapper around the Start Curation button in the UI definition (Task 1's Run Curation tab) so it starts disabled:
Replace the actionButton line with:
```r
shinyjs::disabled(
  actionButton(
    "run_curation",
    "Start Curation",
    class = "btn-success btn-lg mt-3",
    icon = icon("play")
  )
)
```

4. **Table-based tagging layout.**

Per CONTEXT.md: "Table-based interface: one row per uploaded column, with a dropdown to assign type."

Update `output$column_tagging_ui` renderUI to use a table layout instead of vertical selectInputs:

```r
output$column_tagging_ui <- renderUI({
  req(data_store$selected_columns)

  selected_cols <- data_store$selected_columns

  if (length(selected_cols) == 0) {
    return(div(class = "alert alert-warning", "No columns selected. Please select columns in the sidebar."))
  }

  # Table-based layout: one row per column
  tags$table(
    class = "table table-striped table-hover",
    tags$thead(
      tags$tr(
        tags$th("Column Name"),
        tags$th("Type", style = "width: 200px;")
      )
    ),
    tags$tbody(
      lapply(selected_cols, function(col) {
        tags$tr(
          tags$td(tags$strong(col)),
          tags$td(
            selectInput(
              inputId = paste0("tag_", make.names(col)),
              label = NULL,
              choices = c(
                "Select type..." = "",
                "Chemical Name" = "Name",
                "CASRN" = "CASRN",
                "Other" = "Other"
              ),
              selected = "",
              selectize = FALSE,
              width = "100%"
            )
          )
        )
      })
    )
  )
})
```

This replaces the current vertical list of labeled selectInputs with a compact table where column name and dropdown are side-by-side.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('app.R', local=TRUE); cat('Server parsed OK\n')" 2>&1 | head -5</automated>
  </verify>
  <done>
- Sidebar hides when user clicks Tag Columns, Run Curation, or Review Results tabs
- Sidebar shows when user clicks Data Preview, Detection Info, or Raw Data tabs
- After curation completes, app auto-navigates to Review Results tab
- Start Curation button starts disabled and enables only when tags are applied and API key is set
- Column tagging uses table layout (Column Name | Type dropdown) instead of vertical selectInputs
- All existing functionality preserved (upload, detection, tagging, curation, export)
  </done>
</task>

</tasks>

<verification>
1. Upload a CSV/XLSX file — Data Preview, Detection Info, Raw Data tabs work as before
2. Click Tag Columns tab — sidebar hides, table-based tagging interface appears with full width
3. Tag columns and click Apply Tags — tags saved successfully
4. Click Run Curation tab — sidebar hides, summary shows, Start Curation button is enabled
5. Click Start Curation — progress shown, then auto-navigates to Review Results tab
6. Review Results tab — value boxes at top, results table with Download Excel button top-right
7. Click Data Preview — sidebar reappears
8. All 6 tabs have icons and labels
9. No card wrappers constraining content width
</verification>

<success_criteria>
- 6 separate tabs visible in main navigation: Data Preview, Detection Info, Raw Data, Tag Columns, Run Curation, Review Results
- navset_underline used instead of navset_card_tab
- Tag Columns: table-based interface with Apply Tags top-right, empty state when no data
- Run Curation: summary + disabled-until-ready Start Curation button, empty state when no tags
- Review Results: value boxes + table + Download Excel top-right, empty state when no curation
- Sidebar toggles based on active tab (hidden on curation tabs, visible on upload tabs)
- Auto-navigation to Review Results after curation completes
- All existing input/output IDs and server logic preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tab-structure/01-01-SUMMARY.md`
</output>
