---
phase: 02-gated-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.R]
autonomous: true
requirements: [TAB-02, TAB-03, TAB-04, UX-01, UX-02]

must_haves:
  truths:
    - "On app startup, only Data Preview tab is visible — no other tabs appear"
    - "After uploading and detecting data, Detection Info, Raw Data, and Tag Columns tabs silently appear"
    - "After applying at least one column tag, Run Curation tab silently appears"
    - "After curation completes, Review Results tab appears and auto-switches to it"
    - "Re-uploading a file shows confirmation modal warning progress will be lost"
    - "Cancelling re-upload modal preserves existing data and file input resets"
    - "Confirming re-upload clears all downstream state and hides all downstream tabs"
    - "Changing tags silently hides Run Curation and Review Results tabs"
    - "Completed tabs remain accessible — user can navigate back to Tag Columns after moving to Run Curation"
    - "Newly unlocked tabs get a brief pulse animation (~1 second)"
  artifacts:
    - path: "app.R"
      provides: "Complete gated navigation with conditional tab visibility"
      contains: "nav_panel_hidden"
  key_links:
    - from: "app.R (upload observer)"
      to: "nav_show for detection/raw/tag tabs"
      via: "observeEvent on data_store$clean"
      pattern: "nav_show.*main_tabs.*tag_columns"
    - from: "app.R (apply_tags observer)"
      to: "nav_show for run_curation_tab"
      via: "observeEvent on input$apply_tags"
      pattern: "nav_show.*main_tabs.*run_curation_tab"
    - from: "app.R (curation complete)"
      to: "nav_show + nav_select for review_results"
      via: "curation_status reactive"
      pattern: "nav_show.*main_tabs.*review_results"
    - from: "app.R (re-upload)"
      to: "confirmation modal + full reset"
      via: "showModal/modalDialog"
      pattern: "showModal.*modalDialog.*Replace"
---

<objective>
Implement gated tab navigation so tabs appear only when workflow prerequisites are met.

Purpose: Users follow the correct workflow sequence (Upload -> Tag -> Curate -> Review) because unavailable steps are hidden. Back-navigation to completed steps works. No flash of hidden tabs on startup.

Output: Updated app.R with nav_panel_hidden tabs, reactive show/hide observers, confirmation modal for re-upload, cascade reset on state changes, and CSS pulse animation for newly unlocked tabs.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-gated-navigation/02-RESEARCH.md
@.planning/phases/02-gated-navigation/02-CONTEXT.md
@.planning/phases/01-multi-tab-structure/01-01-SUMMARY.md
@app.R

<interfaces>
<!-- Key patterns already in app.R that executor needs to work with -->

From app.R — Current tab structure (lines 156-311):
- navset_underline(id = "main_tabs") with 6 nav_panel() tabs
- Tab values: "data_preview", "detection_info", "raw_data", "tag_columns", "run_curation_tab", "review_results"

From app.R — Reactive data store (lines 318-330):
- data_store$raw, data_store$clean, data_store$detection, data_store$file_info
- data_store$column_tags, data_store$curation_results, data_store$curation_report, data_store$curation_status

From app.R — Existing outputs used by conditionalPanel (lines 884-971):
- output$has_data (reactive: !is.null(data_store$clean))
- output$tags_applied (reactive: !is.null(data_store$column_tags) && length > 0)
- output$curation_completed (reactive: curation_status == "completed")

From app.R — Existing curation auto-nav (line 1057):
- nav_select("main_tabs", "review_results") — already implemented in curation success handler
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert tabs to nav_panel_hidden and add CSS pulse animation</name>
  <files>app.R</files>
  <action>
  In app.R UI section:

  1. Add CSS for tab pulse animation inside the UI, right after useShinyjs():
     ```r
     tags$head(tags$style("
       @keyframes tab-pulse {
         0% { background-color: transparent; }
         50% { background-color: rgba(0, 123, 255, 0.15); }
         100% { background-color: transparent; }
       }
       .tab-pulse > .nav-link {
         animation: tab-pulse 0.5s ease-in-out 2;
       }
     "))
     ```

  2. Convert 5 tabs from nav_panel() to nav_panel_hidden():
     - "Detection Info" (value = "detection_info") — hidden until upload
     - "Raw Data" (value = "raw_data") — hidden until upload
     - "Tag Columns" (value = "tag_columns") — hidden until upload+detection
     - "Run Curation" (value = "run_curation_tab") — hidden until tags applied
     - "Review Results" (value = "review_results") — hidden until curation complete

     Keep "Data Preview" (value = "data_preview") as nav_panel() — always visible.

     IMPORTANT: nav_panel_hidden() takes `value` as first argument. The title and icon still need to be set so they appear when the tab is shown. The signature is:
     ```r
     nav_panel_hidden(value = "detection_info", title = "Detection Info", icon = bsicons::bs_icon("search"), ...)
     ```
     Keep all existing tab content (children) exactly as-is.

  3. Verify: The Data Preview tab should be the only visible tab on load. All other tabs should be born hidden with no flash.
  </action>
  <verify>
  Run app with: "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838)" and confirm only Data Preview tab is visible on startup. No other tabs flash or appear.
  </verify>
  <done>
  Only Data Preview tab visible on startup. Five other tabs are hidden. CSS pulse animation defined in head. No flash of hidden tabs (UX-01 satisfied).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add reactive tab show/hide observers with pulse effect and cascade reset</name>
  <files>app.R</files>
  <action>
  In app.R server section, add the following observers. Place them after the existing data_store definition and before the file upload handler.

  1. **Helper function for showing tabs with pulse** — define inside server function:
     ```r
     show_tab_with_pulse <- function(tab_value) {
       nav_show("main_tabs", target = tab_value)
       shinyjs::runjs(sprintf("
         var tab = document.querySelector('[data-value=\"%s\"]');
         if (tab) {
           var li = tab.closest('li');
           if (li) {
             li.classList.add('tab-pulse');
             setTimeout(function() { li.classList.remove('tab-pulse'); }, 1200);
           }
         }
       ", tab_value))
     }
     ```

  2. **Show detection/raw/tag tabs after successful upload** — add an observe() that watches data_store$clean:
     ```r
     observe({
       req(data_store$clean)
       show_tab_with_pulse("detection_info")
       show_tab_with_pulse("raw_data")
       show_tab_with_pulse("tag_columns")
     })
     ```
     This fires whenever clean data becomes available (initial upload or re-upload after confirm).

  3. **Show Run Curation tab after tags applied** — modify the existing `observeEvent(input$apply_tags, ...)` handler. After the line that sets `data_store$column_tags <- tags`, add:
     ```r
     show_tab_with_pulse("run_curation_tab")
     ```
     Also: HIDE downstream tabs when tags are re-applied (cascade), since re-applying tags invalidates curation:
     ```r
     nav_hide("main_tabs", target = "review_results")
     data_store$curation_results <- NULL
     data_store$curation_report <- NULL
     data_store$curation_status <- NULL
     ```

  4. **Show Review Results after curation completes** — the existing curation handler already has `nav_select("main_tabs", "review_results")`. Before that line, add:
     ```r
     nav_show("main_tabs", target = "review_results")
     ```
     The nav_select right after will auto-switch to it (existing behavior, per user decision).

  5. **Full reset function** — add a helper inside server:
     ```r
     reset_all_downstream <- function() {
       data_store$column_tags <- NULL
       data_store$curation_results <- NULL
       data_store$curation_report <- NULL
       data_store$curation_status <- NULL
       nav_hide("main_tabs", target = "detection_info")
       nav_hide("main_tabs", target = "raw_data")
       nav_hide("main_tabs", target = "tag_columns")
       nav_hide("main_tabs", target = "run_curation_tab")
       nav_hide("main_tabs", target = "review_results")
       nav_select("main_tabs", "data_preview")
     }
     ```

  6. **Update the existing Reset button handler** (observeEvent(input$reset_btn, ...)) to also call reset_all_downstream() and hide all tabs.

  7. **Back navigation**: No special code needed. Once nav_show() makes a tab visible, it stays visible until nav_hide() is called. User can freely click back to any previously shown tab (UX-02 satisfied).

  IMPORTANT: Do NOT add an observer on data_store$column_tags itself for cascade hiding. That would fire on first-time tag apply and cause Pitfall 6 from research. Instead, cascade hiding is handled inside the apply_tags observer (step 3 above) — which only fires on the Apply Tags button click, distinguishing re-apply from first-apply by checking if Run Curation is already visible.
  </action>
  <verify>
  Run app, upload a CSV file. Verify: Detection Info, Raw Data, Tag Columns tabs appear with brief pulse. Tag columns, click Apply Tags. Verify: Run Curation tab appears with pulse. Run curation. Verify: Review Results appears and auto-switches. Navigate back to Tag Columns — still accessible.
  </verify>
  <done>
  Tabs show/hide reactively based on workflow state. Cascade reset on tag re-apply. Back navigation works. Pulse animation on newly unlocked tabs. Full reset clears all downstream state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add confirmation modal for re-upload and handle cancel/confirm</name>
  <files>app.R</files>
  <action>
  Modify the existing file upload handler (observeEvent(input$file_upload, ...)) in app.R to intercept re-uploads with a confirmation modal.

  1. **Restructure the upload handler**: The current handler processes immediately. Change it to:
     - Check if data already exists (data_store$clean is not NULL)
     - If first upload: process directly (call the existing processing logic)
     - If re-upload: show modal and defer processing

     ```r
     observeEvent(input$file_upload, {
       req(input$file_upload)

       if (!is.null(data_store$clean)) {
         # Re-upload — show confirmation modal
         showModal(modalDialog(
           title = "Replace Current Data?",
           p("Your column tags and curation results will be cleared."),
           p("This action cannot be undone."),
           footer = tagList(
             modalButton("Cancel"),
             actionButton("confirm_reupload", "Replace Data", class = "btn-danger")
           ),
           easyClose = FALSE
         ))
       } else {
         # First upload — process directly
         process_uploaded_file(input$file_upload)
       }
     })
     ```

  2. **Extract upload processing into a function** — Move the existing file processing logic (validation, reading, detection, cleaning, storing) from the current observeEvent into a new function `process_uploaded_file(file_info)` defined inside the server function. This function takes the file_upload reactive value and runs all existing processing.

  3. **Add confirm handler**:
     ```r
     observeEvent(input$confirm_reupload, {
       removeModal()
       reset_all_downstream()
       process_uploaded_file(input$file_upload)
     })
     ```

  4. **Handle cancel** — modalButton("Cancel") automatically dismisses the modal. But the file input widget already shows the new filename. Add to the modal's cancel path: reset the file input display. Since modalButton auto-closes, we need a separate cancel button or handle this via modal close callback. SIMPLEST approach: use a named actionButton for cancel instead of modalButton:
     ```r
     footer = tagList(
       actionButton("cancel_reupload", "Cancel", class = "btn-secondary", `data-bs-dismiss` = "modal"),
       actionButton("confirm_reupload", "Replace Data", class = "btn-danger")
     )
     ```
     Then add:
     ```r
     observeEvent(input$cancel_reupload, {
       removeModal()
       # Reset file input to show previous filename (or clear it)
       shinyjs::reset("file_upload")
     })
     ```
     ALSO: Handle the case where user clicks the X or presses Escape (easyClose should be FALSE to prevent this, forcing explicit choice).

  5. **CRITICAL**: The `input$file_upload` value persists after modal is shown. When confirm_reupload fires, `input$file_upload` still holds the new file data. No need for a pending_upload reactive — just access input$file_upload directly in the confirm handler.

  6. Remove the now-redundant empty-state conditionalPanels inside Tag Columns and Run Curation tabs — they show "Upload data first" messages that are no longer needed since the tabs are completely hidden until prerequisites are met. The content conditionalPanels (condition = "output.has_data", "output.tags_applied", "output.curation_completed") can also be simplified since the tab itself won't be visible without the prerequisite. However, KEEP them as a safety net for the edge case described in CONTEXT.md (deep link/bookmark).
  </action>
  <verify>
  Run app. Upload file. Upload a different file. Verify: modal appears with "Replace Current Data?" warning. Click Cancel — modal closes, original data preserved, file input resets. Upload again, click "Replace Data" — new file processes, all downstream tabs hide, clean slate.
  </verify>
  <done>
  Re-upload shows confirmation modal. Cancel preserves state and resets file input. Confirm triggers full reset then processes new file. easyClose disabled to force explicit choice. Modal wording matches user decision ("column tags and curation results will be cleared").
  </done>
</task>

</tasks>

<verification>
Phase 2 verification checklist:

1. **Startup state (UX-01):** Only "Data Preview" tab visible on app load. No flash of other tabs.
2. **Upload gating (TAB-02):** After uploading a file, Detection Info + Raw Data + Tag Columns tabs appear with pulse.
3. **Tag gating (TAB-03):** After clicking Apply Tags with at least one tag, Run Curation tab appears with pulse.
4. **Curation gating (TAB-04):** After curation completes, Review Results appears and auto-switches.
5. **Back navigation (UX-02):** After reaching Run Curation, user can click back to Tag Columns (tab still visible).
6. **Re-upload modal:** Uploading when data exists shows confirmation. Cancel preserves data. Confirm resets.
7. **Tag cascade:** Re-applying tags hides Run Curation and Review Results, clears curation state.
8. **Reset button:** Full reset hides all tabs, clears all state, returns to Data Preview.
9. **Pulse animation:** Newly shown tabs get brief highlight (~1 second) that fits Flatly theme.
</verification>

<success_criteria>
- All 5 requirements (TAB-02, TAB-03, TAB-04, UX-01, UX-02) are implemented and verifiable
- No flash of hidden tabs on startup
- Tabs appear silently (no toast notifications) with brief pulse animation
- Back navigation works — completed tabs remain accessible
- Re-upload confirmation modal protects against accidental data loss
- Tag changes cascade: downstream tabs hidden, curation state cleared
- Auto-switch to Review Results after curation completes (existing behavior preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/02-gated-navigation/02-01-SUMMARY.md`
</output>
