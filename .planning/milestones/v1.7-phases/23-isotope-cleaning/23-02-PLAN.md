---
phase: 23-isotope-cleaning
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - R/cleaning_pipeline.R
  - R/modules/mod_clean_data.R
  - tests/test_cleaning_pipeline_validation.R
autonomous: true
requirements: [ISOT-01, ISOT-02, ISOT-03, CHIR-01, MANA-01]

must_haves:
  truths:
    - "Chiral protection runs before Step 6a (strip_terminal_enclosures) in both run_cleaning_pipeline() and mod_clean_data.R"
    - "Isotope expansion runs after synonym split and before detect_bare_formulas in both orchestrators"
    - "Multi-analyte flagging runs after isotope expansion in both orchestrators"
    - "Audit trail shows all three new steps when relevant data is present"
    - "Shiny app starts without errors after wiring"
  artifacts:
    - path: "R/cleaning_pipeline.R"
      provides: "Updated run_cleaning_pipeline() with three new steps"
      contains: "protect_chiral_designations"
    - path: "R/modules/mod_clean_data.R"
      provides: "Updated Shiny module with three new steps"
      contains: "protect_chiral_designations"
    - path: "tests/test_cleaning_pipeline_validation.R"
      provides: "Integration test covering isotope expansion through pipeline"
      contains: "isotope"
  key_links:
    - from: "R/cleaning_pipeline.R run_cleaning_pipeline"
      to: "R/cleaning_pipeline.R protect_chiral_designations"
      via: "Function call before strip_terminal_enclosures"
      pattern: "protect_chiral.*strip_terminal_enclosures"
    - from: "R/cleaning_pipeline.R run_cleaning_pipeline"
      to: "R/cleaning_pipeline.R expand_isotope_shortcodes"
      via: "Function call after synonym split, before detect_bare_formulas"
      pattern: "expand_isotope_shortcodes"
    - from: "R/modules/mod_clean_data.R"
      to: "R/cleaning_pipeline.R expand_isotope_shortcodes"
      via: "Function call in cleaning pipeline execution"
      pattern: "expand_isotope_shortcodes"
---

<objective>
Wire the three new cleaning functions into both pipeline orchestrators (run_cleaning_pipeline and mod_clean_data.R) and validate with integration tests and Shiny smoke test.

Purpose: Functions from Plan 01 are standalone — this plan connects them into the actual cleaning flow at the correct pipeline positions per D-13.
Output: Working pipeline with new steps, integration test, confirmed Shiny startup.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-isotope-cleaning/23-CONTEXT.md
@.planning/phases/23-isotope-cleaning/23-01-SUMMARY.md

@R/cleaning_pipeline.R
@R/modules/mod_clean_data.R
@tests/test_cleaning_pipeline_validation.R

<interfaces>
<!-- Pipeline step ordering per D-13 and CONTEXT.md -->

Current name cleaning order in run_cleaning_pipeline() (lines 1452-1520):
```
Step 6a: strip_terminal_enclosures (line 1454)
Step 6b: strip_quality_adjectives (line 1464)
Step 6c: strip_salt_references (line 1469)
Step 6d: strip_terminal_unspecified (line 1474)
Step 6d1: strip_reference_terms (line 1480)
Step 6d2: Final cleanup (line 1486)
Step 6d3: strip_terminal_enclosures AGAIN (line 1497)
Step 6e: split_synonyms (line 1502)
Step 6f: Final cleanup (line 1507)
[then row removal, then return]
```

Required NEW order (per D-13):
```
NEW: protect_chiral_designations (BEFORE Step 6a)
Step 6a: strip_terminal_enclosures (existing)
...existing steps 6b-6f...
NEW: expand_isotope_shortcodes (AFTER Step 6f, before row removal)
NEW: flag_multi_analyte (AFTER isotope expansion)
[then row removal, then return]
```

Note: detect_bare_formulas is called OUTSIDE run_cleaning_pipeline() in mod_clean_data.R at line 245.
So isotope expansion inside run_cleaning_pipeline() naturally runs before detect_bare_formulas.

Current mod_clean_data.R name cleaning order (lines 174-241):
```
strip_terminal_enclosures (line 176)
strip_quality_adjectives (line 185)
strip_salt_references (line 190)
strip_terminal_unspecified (line 195)
strip_reference_terms (line 200)
cleanup (line 207)
strip_terminal_enclosures again (line 218)
split_synonyms (line 223)
final cleanup (line 228)
row removal (line 237)
--- then at line 245: detect_bare_formulas ---
```

Required changes to mod_clean_data.R:
- Insert protect_chiral_designations BEFORE line 176
- Insert expand_isotope_shortcodes + flag_multi_analyte AFTER line 241 (after row removal) and BEFORE line 243 (detect_bare_formulas)

From Plan 01 — function signatures:
```r
protect_chiral_designations(df, name_cols) -> list(cleaned_data, audit_trail)
expand_isotope_shortcodes(df, name_cols) -> list(cleaned_data, audit_trail)
flag_multi_analyte(df, name_cols) -> list(cleaned_data, audit_trail)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire three new steps into run_cleaning_pipeline() and mod_clean_data.R</name>
  <files>R/cleaning_pipeline.R, R/modules/mod_clean_data.R</files>
  <read_first>
    - R/cleaning_pipeline.R (lines 1398-1536 — run_cleaning_pipeline function; understand current step order)
    - R/modules/mod_clean_data.R (lines 130-280 — Shiny module pipeline execution; understand current step order and incProgress calls)
    - .planning/phases/23-isotope-cleaning/23-CONTEXT.md (D-10, D-13 for pipeline ordering)
    - .planning/phases/23-isotope-cleaning/23-01-SUMMARY.md (function signatures from Plan 01)
  </read_first>
  <action>
    **Part A: Update run_cleaning_pipeline() in R/cleaning_pipeline.R**

    Inside `run_cleaning_pipeline()`, within the `if (length(name_cols) > 0)` block:

    1. INSERT chiral protection BEFORE Step 6a (before `enclosure_result <- strip_terminal_enclosures(...)`):
       ```r
       # Step 6-pre: Protect chiral designations (before enclosure stripping)
       chiral_result <- protect_chiral_designations(df_after_multi_cas, name_cols)
       df_after_chiral <- chiral_result$cleaned_data
       audit_combined <- dplyr::bind_rows(audit_combined, chiral_result$audit_trail)
       ```
       Then update Step 6a to use `df_after_chiral` instead of `df_after_multi_cas`:
       ```r
       enclosure_result <- strip_terminal_enclosures(df_after_chiral, name_cols)
       ```

    2. INSERT isotope expansion + multi-analyte flagging AFTER the row removal block (after `df_final <- df_after_synonyms[!all_empty, ]`) but BEFORE the closing `} else {` of the name_cols block:
       ```r
       # Step 7: Expand isotope shortcodes (before bare formula detection)
       isotope_result <- expand_isotope_shortcodes(df_final, name_cols)
       df_final <- isotope_result$cleaned_data
       audit_combined <- dplyr::bind_rows(audit_combined, isotope_result$audit_trail)

       # Step 8: Flag multi-analyte expressions
       multi_result <- flag_multi_analyte(df_final, name_cols)
       df_final <- multi_result$cleaned_data
       audit_combined <- dplyr::bind_rows(audit_combined, multi_result$audit_trail)
       ```

    **Part B: Update mod_clean_data.R**

    Inside the name cleaning block (`if (length(name_cols) > 0)`):

    1. INSERT chiral protection BEFORE the first `strip_terminal_enclosures` call (before line 176):
       ```r
       incProgress(0.04, detail = "Protecting chiral designations...")
       chiral_result <- protect_chiral_designations(df, name_cols)
       df <- chiral_result$cleaned_data
       all_audits[[length(all_audits) + 1]] <- chiral_result$audit_trail
       ```

    2. INSERT isotope expansion + multi-analyte flagging AFTER the row removal block (after `df <- df[!all_empty, ]` at ~line 241) and BEFORE the `detect_bare_formulas` call at ~line 243:
       ```r
       incProgress(0.04, detail = "Expanding isotope shortcodes...")
       isotope_result <- expand_isotope_shortcodes(df, name_cols)
       df <- isotope_result$cleaned_data
       all_audits[[length(all_audits) + 1]] <- isotope_result$audit_trail

       incProgress(0.04, detail = "Flagging multi-analyte expressions...")
       multi_result <- flag_multi_analyte(df, name_cols)
       df <- multi_result$cleaned_data
       all_audits[[length(all_audits) + 1]] <- multi_result$audit_trail
       ```

    **Important:** Adjust existing incProgress values slightly downward so total doesn't exceed 1.0. Reduce some existing 0.08 calls to 0.06 to make room for the 3 new 0.04 calls.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/concert && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"</automated>
  </verify>
  <acceptance_criteria>
    - R/cleaning_pipeline.R run_cleaning_pipeline() contains `chiral_result <- protect_chiral_designations(`
    - R/cleaning_pipeline.R run_cleaning_pipeline() contains `isotope_result <- expand_isotope_shortcodes(`
    - R/cleaning_pipeline.R run_cleaning_pipeline() contains `multi_result <- flag_multi_analyte(`
    - R/cleaning_pipeline.R: protect_chiral_designations call appears BEFORE first strip_terminal_enclosures call within run_cleaning_pipeline
    - R/cleaning_pipeline.R: expand_isotope_shortcodes call appears AFTER split_synonyms call within run_cleaning_pipeline
    - R/modules/mod_clean_data.R contains `chiral_result <- protect_chiral_designations(`
    - R/modules/mod_clean_data.R contains `isotope_result <- expand_isotope_shortcodes(`
    - R/modules/mod_clean_data.R contains `multi_result <- flag_multi_analyte(`
    - R/modules/mod_clean_data.R: protect_chiral_designations appears BEFORE first strip_terminal_enclosures
    - R/modules/mod_clean_data.R: expand_isotope_shortcodes appears BEFORE detect_bare_formulas
    - All existing tests still pass (exit code 0)
  </acceptance_criteria>
  <done>
    All three new cleaning steps are wired into both run_cleaning_pipeline() and mod_clean_data.R at the correct pipeline positions. Chiral protection runs before enclosure stripping, isotope expansion runs after synonym split and before bare formula detection, multi-analyte flagging runs after isotope expansion.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration test and run Shiny smoke test</name>
  <files>tests/test_cleaning_pipeline_validation.R</files>
  <read_first>
    - tests/test_cleaning_pipeline_validation.R (existing integration test pattern — see how it calls run_cleaning_pipeline then detect_bare_formulas)
    - R/cleaning_pipeline.R (updated run_cleaning_pipeline with new steps)
    - R/modules/mod_clean_data.R (updated module)
  </read_first>
  <behavior>
    Integration tests (appended to test_cleaning_pipeline_validation.R):
    - Pipeline with "(+)-catechin" in name column -> audit trail contains step "protect_chiral_designations", value still has catechin, cleaning_flag contains "WARNING: chiral"
    - Pipeline with "u234" in name column -> value becomes "Uranium-234", audit trail contains step "expand_isotope_shortcodes"
    - Pipeline with "radium 226" in name column -> value becomes "Radium-226"
    - Pipeline with "C12H22O11" in name column -> detect_bare_formulas blocks it (proves isotope expansion doesn't corrupt formulas)
    - Pipeline with "nitrate + nitrite" in name column -> cleaning_flag contains "WARNING: potential multi-analyte", value unchanged
    - Pipeline ordering: chiral protection audit entry appears, then enclosure stripping, then isotope expansion
  </behavior>
  <action>
    **Part A: Add integration tests**

    Append to `tests/test_cleaning_pipeline_validation.R` new test_that blocks:

    1. "Pipeline expands isotope shortcodes before bare formula detection":
       ```r
       test_that("Pipeline expands isotope shortcodes", {
         df <- tibble::tibble(analyte = c("u234", "radium 226", "cesium-137", "acetone"))
         tag_map <- list(analyte = "Name")
         result <- run_cleaning_pipeline(df, tag_map)
         cleaned <- result$cleaned_data
         # Check isotope expansion happened
         expect_true(any(cleaned$analyte == "Uranium-234", na.rm = TRUE))
         expect_true(any(cleaned$analyte == "Radium-226", na.rm = TRUE))
         expect_true(any(cleaned$analyte == "Cesium-137", na.rm = TRUE))
         # Check audit trail
         expect_true("expand_isotope_shortcodes" %in% result$audit_trail$step)
       })
       ```

    2. "Pipeline protects chiral designations from enclosure stripping":
       ```r
       test_that("Pipeline protects chiral designations", {
         df <- tibble::tibble(chemical = c("(+)-catechin", "acetone"))
         tag_map <- list(chemical = "Name")
         result <- run_cleaning_pipeline(df, tag_map)
         cleaned <- result$cleaned_data
         # (+)-catechin should still have catechin (not stripped by enclosure removal)
         catechin_row <- cleaned[grepl("catechin", cleaned$chemical, ignore.case = TRUE), ]
         expect_true(nrow(catechin_row) > 0)
         # Check audit trail has chiral step
         expect_true("protect_chiral_designations" %in% result$audit_trail$step)
       })
       ```

    3. "Pipeline flags multi-analyte expressions":
       ```r
       test_that("Pipeline flags multi-analyte expressions", {
         df <- tibble::tibble(analyte = c("nitrate + nitrite", "acetone"))
         tag_map <- list(analyte = "Name")
         result <- run_cleaning_pipeline(df, tag_map)
         cleaned <- result$cleaned_data
         # Value should be unchanged
         multi_row <- cleaned[grepl("nitrate", cleaned$analyte, ignore.case = TRUE), ]
         expect_true(nrow(multi_row) > 0)
         expect_true(grepl("nitrite", multi_row$analyte[1], ignore.case = TRUE))
         # Check flag
         expect_true(any(grepl("multi-analyte", cleaned$cleaning_flag, ignore.case = TRUE), na.rm = TRUE))
       })
       ```

    4. "Carbon backbone formulas survive isotope expansion":
       ```r
       test_that("Carbon backbone formulas not corrupted by isotope expansion", {
         df <- tibble::tibble(chemical = c("C12H22O11"))
         tag_map <- list(chemical = "Name")
         result <- run_cleaning_pipeline(df, tag_map)
         formula_result <- detect_bare_formulas(result$cleaned_data, "chemical")
         # C12H22O11 should be blocked as bare formula (not mangled by isotope expansion)
         expect_true(any(grepl("BLOCK", formula_result$cleaned_data$cleaning_flag, ignore.case = TRUE), na.rm = TRUE))
       })
       ```

    **Part B: Run Shiny smoke test**

    Run the Shiny app startup test per CLAUDE.md guidelines:
    ```bash
    "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" &
    # Wait for "Listening on" or error
    # Kill process
    ```
    If startup fails, fix the error immediately.
  </action>
  <verify>
    <automated>cd C:/Users/sxthi/Documents/concert && "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cleaning_pipeline_validation.R contains `"Pipeline expands isotope shortcodes"`
    - tests/test_cleaning_pipeline_validation.R contains `"Pipeline protects chiral designations"`
    - tests/test_cleaning_pipeline_validation.R contains `"Pipeline flags multi-analyte expressions"`
    - tests/test_cleaning_pipeline_validation.R contains `"Carbon backbone formulas not corrupted"`
    - tests/test_cleaning_pipeline_validation.R contains `"Uranium-234"`
    - All tests in tests/ directory pass (exit code 0)
    - Shiny app starts without error (smoke test passes)
  </acceptance_criteria>
  <done>
    Integration tests confirm the full pipeline correctly orders chiral protection -> name cleaning -> isotope expansion -> multi-analyte flagging -> bare formula detection.
    Carbon backbone formulas survive isotope expansion and are correctly blocked by detect_bare_formulas.
    Shiny app starts without errors.
  </done>
</task>

</tasks>

<verification>
1. Full test suite passes: `Rscript -e "testthat::test_dir('tests')"`
2. Pipeline audit trail shows new steps in correct order
3. Shiny app starts without errors
4. Isotope expansion runs before detect_bare_formulas (verified by integration test)
5. Chiral protection runs before enclosure stripping (verified by integration test)
</verification>

<success_criteria>
- All three new steps wired into both run_cleaning_pipeline() and mod_clean_data.R
- Pipeline ordering per D-13: chiral -> enclosures -> ... -> synonyms -> isotopes -> multi-analyte -> bare formulas
- Integration tests prove correct ordering and no regressions
- Shiny app smoke test passes
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-isotope-cleaning/23-02-SUMMARY.md`
</output>
