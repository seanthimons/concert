---
phase: 08-error-recovery-workflows
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app.R
autonomous: true
requirements:
  - RECV-01
  - RECV-02
  - RECV-03
must_haves:
  truths:
    - "User can click a DTXSID cell on an error-status row and type/paste a DTXSID value"
    - "Manually entered DTXSIDs display a 'manual' badge to distinguish from auto-resolved values"
    - "User can click 'Validate All' button to bulk-validate all queued manual entries"
    - "Progress bar shows count during validation (e.g., 'Validating 3 of 12...')"
    - "Valid entries auto-populate preferredName and set consensus_status to 'manual'"
    - "Invalid entries get red border and summary notification listing failures"
  artifacts:
    - path: "app.R"
      provides: "DT editable cell configuration for error rows, manual queue observer, Validate All handler"
      contains: "manual_queue"
    - path: "app.R"
      provides: "Validate All button in UI"
      contains: "validate_all"
  key_links:
    - from: "app.R"
      to: "R/curation.R"
      via: "validate_manual_dtxsids() called from Validate All observer"
      pattern: "validate_manual_dtxsids"
    - from: "app.R"
      to: "data_store$resolution_state"
      via: "Cell edit observer updates resolution_state and manual_queue"
      pattern: "manual_queue"
---

<objective>
Implement inline DTXSID cell editing for error rows with validation queue and bulk validation flow.

Purpose: Users need a way to manually enter DTXSIDs when auto-curation fails (RECV-01), validate them in batch (RECV-02), and see results populate immediately (RECV-03).
Output: Working manual entry + validation workflow in app.R.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-error-recovery-workflows/08-RESEARCH.md
@.planning/phases/08-error-recovery-workflows/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From R/curation.R (added by Plan 01):
```r
validate_manual_dtxsids <- function(dtxsids, batch_size = 20, delay_sec = 1)
# Returns tibble: searchValue, dtxsid, preferredName, rank, is_valid (logical)
```

From R/consensus.R (modified by Plan 01):
```r
init_resolution_state <- function(df)
# Now also adds .manual_entry column (logical, default FALSE)
```

From app.R (existing - curation_table renderDT at line ~1380):
```r
# Factor levels for consensus_status — MUST be extended:
df$consensus_status <- factor(
  df$consensus_status,
  levels = c("agree", "agree_caveat", "single", "disagree", "error")
)
# Add: "manual", "unresolvable" to levels
```

From app.R (existing - data_store):
```r
data_store <- reactiveValues(
  resolution_state = NULL,  # Main data frame
  dtxsid_cols = NULL,
  column_tags = NULL
)
```

DT editable cell pattern from research:
```r
# DT uses 0-based column indexing for editable config
# input$tableId_cell_edit gives: row (1-based), col (0-based), value
# Use info$value (not df lookup) to get the new value — stale data pitfall
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DT inline editing for error rows and manual entry queue</name>
  <files>app.R</files>
  <action>
Modify the curation_table renderDT and add supporting reactive infrastructure:

**1. Add manual_queue to data_store (near line 369):**
```r
data_store$manual_queue <- list()  # row_idx (string) -> dtxsid (string)
```

**2. Add "Validate All" button to UI:**
In the Review Results tab UI section, add an `actionButton("validate_all", ...)` with an appropriate icon (e.g., bsicons::bs_icon("check2-all")). Place it near the existing action buttons in the Review Results tab. Only show when there are manual entries queued (use `conditionalPanel` or `shinyjs::toggle`).

**3. Extend consensus_status factor levels in renderDT (line ~1386):**
```r
levels = c("agree", "agree_caveat", "single", "disagree", "error", "manual", "unresolvable")
```
Also add "manual" and "unresolvable" to the badge color maps in the JS render callbacks:
- "manual": purple/violet (#6f42c1) — distinguishes from auto-resolved
- "unresolvable": dark red (#721c24) — signals exhausted auto-curation

**4. Make consensus_dtxsid column editable for error/unresolvable rows:**
In the datatable() call, add `editable` parameter:
```r
editable = list(
  target = "cell",
  disable = list(
    columns = setdiff(seq_len(ncol(display_df)) - 1, dtxsid_col_edit_idx)
  )
)
```
Where `dtxsid_col_edit_idx` is the 0-indexed position of consensus_dtxsid column. This makes ONLY the consensus_dtxsid column editable. Non-error rows will have their edits rejected in the observer (checking consensus_status).

**5. Add cell edit observer:**
```r
observeEvent(input$curation_table_cell_edit, {
  info <- input$curation_table_cell_edit
  row_idx <- info$row  # 1-based
  new_value <- trimws(as.character(info$value))

  # Only allow edits on error/unresolvable rows
  current_status <- as.character(data_store$resolution_state$consensus_status[row_idx])
  if (!current_status %in% c("error", "unresolvable")) {
    showNotification("Only error rows can be manually edited", type = "warning")
    return()
  }

  # Basic DTXSID format validation
  if (!grepl("^DTXSID\\d+$", new_value, ignore.case = TRUE)) {
    showNotification(
      paste0("Invalid format: ", new_value, ". Expected: DTXSIDxxxxxxx"),
      type = "warning", duration = 5
    )
    return()
  }

  # Queue for bulk validation
  data_store$manual_queue[[as.character(row_idx)]] <- new_value

  # Update display state immediately (show value + mark as manual)
  updated_df <- data_store$resolution_state
  updated_df$consensus_dtxsid[row_idx] <- new_value
  updated_df$.manual_entry[row_idx] <- TRUE
  data_store$resolution_state <- updated_df

  showNotification(paste0("Row ", row_idx, " queued for validation"), type = "message", duration = 2)
})
```

**6. Update Resolution column rendering for manual/unresolvable statuses:**
In the Resolution sapply block, add handling:
- "manual" status: Show checkmark + DTXSID + preferredName + small "manual" badge
- "unresolvable" status: Show warning icon + "Auto-curation failed" text

**7. Update row background colors:**
Add to the formatStyle styleEqual:
- "manual": light purple background rgba(111, 66, 193, 0.08)
- "unresolvable": light dark-red background rgba(114, 28, 36, 0.12)

**8. Add "manual" badge indicator for .manual_entry rows:**
In the Resolution column rendering, when .manual_entry is TRUE and consensus_dtxsid is not NA, append a small "manual" badge after the DTXSID display (e.g., `<span class="badge bg-secondary ms-1" style="font-size:0.7em;">manual</span>`).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); source('R/consensus.R'); source('R/curation.R'); cat('Sources OK\n'); app_lines <- readLines('app.R'); stopifnot(any(grepl('manual_queue', app_lines))); stopifnot(any(grepl('validate_all', app_lines))); stopifnot(any(grepl('cell_edit', app_lines))); stopifnot(any(grepl('unresolvable', app_lines))); cat('All patterns found\n')"</automated>
  </verify>
  <done>
- consensus_dtxsid column is editable on error/unresolvable rows only
- Cell edit observer validates DTXSID format and queues for bulk validation
- .manual_entry flag set on edited rows
- "manual" badge visible on manually-entered cells
- "manual" and "unresolvable" statuses have badge colors and row backgrounds
- Resolution column handles all status types including manual/unresolvable
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Validate All button handler with progress and failure feedback</name>
  <files>app.R</files>
  <action>
Add the Validate All observer in app.R server section:

```r
observeEvent(input$validate_all, {
  queue <- data_store$manual_queue
  if (length(queue) == 0) {
    showNotification("No manual entries to validate", type = "warning", duration = 3)
    return()
  }

  row_indices <- as.integer(names(queue))
  all_dtxsids <- unlist(queue)
  unique_dtxsids <- unique(all_dtxsids)

  # Disable button during validation
  shinyjs::disable("validate_all")
  on.exit(shinyjs::enable("validate_all"))

  withProgress(message = "Validating manual DTXSIDs...", value = 0, {
    incProgress(0.1, detail = sprintf("Validating %d entries...", length(unique_dtxsids)))

    validation_results <- validate_manual_dtxsids(unique_dtxsids)

    incProgress(0.6, detail = "Updating results...")

    updated_df <- data_store$resolution_state
    n_valid <- 0
    n_invalid <- 0
    invalid_details <- c()

    for (i in seq_along(row_indices)) {
      row_idx <- row_indices[i]
      entered_dtxsid <- queue[[as.character(row_idx)]]

      val_row <- validation_results[validation_results$searchValue == entered_dtxsid, ]

      if (nrow(val_row) > 0 && isTRUE(val_row$is_valid[1])) {
        # Valid: update consensus
        updated_df$consensus_dtxsid[row_idx] <- val_row$dtxsid[1]
        updated_df$consensus_status[row_idx] <- "manual"
        updated_df$consensus_source[row_idx] <- "manual_entry"

        # Store preferredName in manual_preferredName column
        if (!"manual_preferredName" %in% names(updated_df)) {
          updated_df$manual_preferredName <- NA_character_
        }
        updated_df$manual_preferredName[row_idx] <- val_row$preferredName[1]

        n_valid <- n_valid + 1
      } else {
        # Invalid: keep error status, track for feedback
        invalid_details <- c(invalid_details,
          sprintf("Row %d: %s", row_idx, entered_dtxsid))
        n_invalid <- n_invalid + 1
      }
    }

    data_store$resolution_state <- updated_df

    # Clear queue
    data_store$manual_queue <- list()

    incProgress(0.3, detail = "Done")
  })

  # Summary notification
  msg <- sprintf("Validation complete: %d validated, %d failed", n_valid, n_invalid)
  showNotification(msg,
    type = if (n_invalid > 0) "warning" else "message",
    duration = 8
  )

  # Detail notification for failures
  if (n_invalid > 0) {
    showNotification(
      paste("Failed entries:", paste(invalid_details, collapse = "; ")),
      type = "error",
      duration = NULL  # Stays until dismissed
    )
  }

  # Update consensus summary
  # ... update data_store$consensus_summary counts to reflect manual resolutions
})
```

**Also update the Resolution column rendering** to show manual_preferredName for "manual" status rows:
```r
} else if (status == "manual") {
  dtxsid <- df$consensus_dtxsid[i]
  pref_name <- if ("manual_preferredName" %in% names(df)) df$manual_preferredName[i] else NA_character_
  # Fall back to auto preferredName columns if manual not available
  if (is.na(pref_name)) {
    pref_cols <- grep("^preferredName_", names(df), value = TRUE)
    for (pc in pref_cols) {
      if (!is.na(df[[pc]][i])) { pref_name <- df[[pc]][i]; break }
    }
  }
  manual_badge <- '<span class="badge bg-info ms-1" style="font-size:0.7em;">manual</span>'
  if (!is.na(pref_name)) {
    paste0("\u2705 ", htmltools::htmlEscape(dtxsid), " \u2014 ", htmltools::htmlEscape(pref_name), " ", manual_badge)
  } else {
    paste0("\u2705 ", htmltools::htmlEscape(dtxsid), " ", manual_badge)
  }
}
```

**Add "Validate All" button visibility toggle:**
Use `observe()` to show/hide the button based on queue length:
```r
observe({
  shinyjs::toggleState("validate_all", condition = length(data_store$manual_queue) > 0)
})
```

**Add manual_preferredName to always_hidden list** in column visibility section so it doesn't clutter the table.

**Update the consensus summary observer** to count manual resolutions in the summary display.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); source('R/consensus.R'); source('R/curation.R'); cat('Sources OK\n'); app_lines <- readLines('app.R'); stopifnot(any(grepl('validate_manual_dtxsids', app_lines))); stopifnot(any(grepl('manual_preferredName', app_lines))); stopifnot(any(grepl('n_valid.*n_invalid', app_lines))); cat('Validate All handler found\n')"</automated>
  </verify>
  <done>
- "Validate All" button appears when manual entries are queued
- Clicking validates all queued DTXSIDs via CompTox API with progress bar
- Valid entries: preferredName populated, status set to "manual", manual badge shown
- Invalid entries: summary + detail notifications, cells retain error status
- Queue cleared after validation
- Consensus summary updated to reflect manual resolutions
  </done>
</task>

</tasks>

<verification>
1. Source files load without error: `source('R/consensus.R'); source('R/curation.R')`
2. app.R contains manual_queue, validate_all, cell_edit observers
3. consensus_status factor includes "manual" and "unresolvable" levels
4. Badge colors defined for both new statuses
5. Resolution column renders manual and unresolvable rows correctly
</verification>

<success_criteria>
- User can click consensus_dtxsid cell on error rows to enter DTXSID
- Format validation rejects non-DTXSID strings
- "Validate All" button triggers bulk validation with progress
- Valid entries show as "manual" status with preferredName
- Invalid entries get failure notification with details
- Manual badge distinguishes manual from auto-resolved entries
</success_criteria>

<output>
After completion, create `.planning/phases/08-error-recovery-workflows/08-02-SUMMARY.md`
</output>
