---
phase: 08-error-recovery-workflows
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app.R
autonomous: false
requirements:
  - RECV-04
  - RECV-05
must_haves:
  truths:
    - "User can filter table to show only error rows via a dedicated button"
    - "User can select error rows from the filtered view"
    - "User can re-assign column tags via a modal for selected rows"
    - "User can click 'Re-curate' to run the full search pipeline on selected rows"
    - "Re-curated results merge back into the main table preserving row order"
    - "Pinned resolutions on non-selected rows are never touched"
    - "Rows that fail re-curation get 'unresolvable' status"
  artifacts:
    - path: "app.R"
      provides: "Error filter button, row selection, re-tag modal, re-curate handler, merge-back logic"
      contains: "retag_modal"
    - path: "app.R"
      provides: "Filter toggle for error rows"
      contains: "filter_errors"
  key_links:
    - from: "app.R"
      to: "R/curation.R"
      via: "Re-curate calls run_curation_pipeline() on selected subset then merge_retry_results()"
      pattern: "merge_retry_results"
    - from: "app.R"
      to: "R/curation.R"
      via: "Re-curate runs full tier chain on re-tagged subset"
      pattern: "run_curation_pipeline"
---

<objective>
Implement error row selection, re-tag modal, re-curation pipeline execution, and merge-back flow.

Purpose: Users need to recover from curation errors by re-assigning tags and re-running the pipeline on failed rows (RECV-04), with results merging back cleanly (RECV-05).
Output: Complete re-tag and re-curate workflow in app.R.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-error-recovery-workflows/08-RESEARCH.md
@.planning/phases/08-error-recovery-workflows/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be completed first) -->

From R/curation.R (added by Plan 01):
```r
merge_retry_results <- function(original_state, retry_results, selected_row_indices, tags_changed = FALSE)
# Merges retry pipeline results into original state by row index
# Skips .pinned rows, handles new columns if tags_changed, marks unresolvable
```

From R/curation.R (existing):
```r
run_curation_pipeline <- function(clean_data, column_tags, progress_callback = NULL, dedup_only = FALSE)
# Full pipeline: dedup -> search -> map -> consensus -> resolution
# Returns list(results, dedup_summary, search_summary, consensus_summary)
```

From app.R (existing data_store):
```r
data_store <- reactiveValues(
  resolution_state = NULL,   # Main result df
  clean = NULL,              # Original clean data
  column_tags = NULL,        # Named list col -> "Name"|"CASRN"|"Other"
  dtxsid_cols = NULL,        # dtxsid_* column names
  consensus_summary = NULL   # Summary counts
)
```

From app.R (existing run_curation observer pattern, lines ~1184-1288):
# Uses withProgress, calls run_curation_pipeline, updates data_store
# This is the pattern to follow for re-curation

CONTEXT.md locked decisions for this plan:
- Error row selection: Filter view button, select from filtered subset
- Re-tag approach: Bulk re-tag default (modal with column-to-tag mapping), individual override capability
- Re-curate trigger: Explicit "Re-curate" button after user reviews updated tags
- Search pipeline: Full tier chain, no individual tier selection
- Merge style: Silent replace, no before/after comparison
- Pin preservation: Pinned rows NEVER touched
- Re-fail: "unresolvable" status (distinct from "error")
- Row order: Always original upload order
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error filter button, row selection, and re-tag modal</name>
  <files>app.R</files>
  <action>
**1. Add "Show Errors" toggle button to Review Results tab UI:**
Place near existing action buttons. Use `actionButton("filter_errors", "Show Errors", icon = icon("filter"))`. Toggle behavior: first click filters to error/unresolvable rows, second click shows all rows. Track state in `data_store$error_filter_active <- FALSE`.

**2. Implement filter toggle observer:**
```r
observeEvent(input$filter_errors, {
  data_store$error_filter_active <- !data_store$error_filter_active
  # Button label updates to "Show All" when filter is active
  updateActionButton(session, "filter_errors",
    label = if (data_store$error_filter_active) "Show All" else "Show Errors"
  )
})
```

**3. Update curation_table renderDT to respect error filter:**
At the top of the renderDT block, after getting df from resolution_state:
```r
# Apply error filter if active
display_indices <- seq_len(nrow(df))
if (isTRUE(data_store$error_filter_active)) {
  display_indices <- which(df$consensus_status %in% c("error", "unresolvable"))
  df_display <- df[display_indices, ]
} else {
  df_display <- df
}
# Store mapping for row selection (filtered row -> original row)
data_store$display_row_map <- display_indices
```
Use df_display for the datatable() call. CRITICAL: store the mapping so selected rows can be translated back to original indices.

**4. Enable DT row selection when filter is active:**
When error filter is active, add `selection = "multiple"` to datatable() options so users can click to select rows. When filter is off, use `selection = "none"` (default behavior).

**5. Add "Re-tag Selected" button:**
`actionButton("retag_selected", "Re-tag Selected", icon = icon("tags"))`. Show only when error filter is active AND rows are selected. Use observe() to toggle visibility.

**6. Create re-tag modal (showModal):**
When "Re-tag Selected" is clicked, show a modal with:
- Header showing count of selected rows
- For each column in data_store$clean that could be tagged: a selectInput dropdown with options "", "Name", "CASRN", "Other"
- Pre-populate dropdowns with current column_tags values
- "Apply & Re-curate" button (`actionButton("apply_retag", ...)`)
- "Cancel" button

The modal should use the SAME tag assignment pattern as the existing column tagging UI. Read the existing tag UI in app.R to match the pattern.

**7. Capture selected rows in observer:**
```r
observe({
  selected <- input$curation_table_rows_selected
  if (!is.null(selected) && isTRUE(data_store$error_filter_active)) {
    # Map filtered indices back to original indices
    data_store$selected_error_rows <- data_store$display_row_map[selected]
  } else {
    data_store$selected_error_rows <- NULL
  }
})
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "app_lines <- readLines('app.R'); stopifnot(any(grepl('filter_errors', app_lines))); stopifnot(any(grepl('retag_selected', app_lines))); stopifnot(any(grepl('retag_modal\\|showModal\\|apply_retag', app_lines))); stopifnot(any(grepl('display_row_map', app_lines))); cat('Re-tag UI elements found\n')"</automated>
  </verify>
  <done>
- "Show Errors" button toggles error-only view
- Row selection enabled in filtered view
- "Re-tag Selected" button appears when rows are selected
- Modal shows column-to-tag mapping for re-assignment
- Selected row indices correctly mapped from filtered to original positions
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement re-curate pipeline execution and merge-back</name>
  <files>app.R</files>
  <action>
**1. Add "Apply & Re-curate" handler:**
When user clicks apply_retag in the modal:

```r
observeEvent(input$apply_retag, {
  removeModal()

  selected_rows <- data_store$selected_error_rows
  req(selected_rows)

  # Collect new tags from modal inputs
  # Modal inputs named "retag_col_{colname}" -> read each
  original_cols <- names(data_store$clean)
  new_tags <- list()
  for (col in original_cols) {
    tag_val <- input[[paste0("retag_col_", col)]]
    if (!is.null(tag_val) && tag_val != "") {
      new_tags[[col]] <- tag_val
    }
  }

  if (length(new_tags) == 0) {
    showNotification("No columns tagged. Please select at least one tag.", type = "warning")
    return()
  }

  # Check if tags changed from original
  tags_changed <- !identical(sort(names(new_tags)), sort(names(data_store$column_tags))) ||
                  !identical(new_tags[sort(names(new_tags))], data_store$column_tags[sort(names(data_store$column_tags))])

  # Extract subset of clean data for selected rows
  subset_data <- data_store$clean[selected_rows, , drop = FALSE]

  # Run full curation pipeline on subset
  shinyjs::disable("apply_retag")

  withProgress(message = "Re-curating selected rows...", value = 0, {
    retry_result <- tryCatch({
      run_curation_pipeline(
        clean_data = subset_data,
        column_tags = new_tags,
        progress_callback = function(stage, msg) {
          incProgress(0.2, detail = msg)
        }
      )
    }, error = function(e) {
      showNotification(paste("Re-curation failed:", e$message), type = "error", duration = NULL)
      NULL
    })

    if (!is.null(retry_result)) {
      incProgress(0.3, detail = "Merging results...")

      # Merge retry results back into main state
      updated_state <- merge_retry_results(
        original_state = data_store$resolution_state,
        retry_results = retry_result$results,
        selected_row_indices = selected_rows,
        tags_changed = tags_changed
      )

      data_store$resolution_state <- updated_state

      # Update dtxsid_cols if new tag columns were added
      if (tags_changed) {
        data_store$dtxsid_cols <- grep("^dtxsid_", names(updated_state), value = TRUE)
      }

      # Count results
      n_resolved <- sum(updated_state$consensus_status[selected_rows] %in%
        c("agree", "agree_caveat", "single", "manual"), na.rm = TRUE)
      n_still_error <- sum(updated_state$consensus_status[selected_rows] == "unresolvable", na.rm = TRUE)

      showNotification(
        sprintf("Re-curation complete: %d resolved, %d unresolvable", n_resolved, n_still_error),
        type = if (n_still_error > 0) "warning" else "message",
        duration = 8
      )
    }
  })

  # Reset filter and selection state
  data_store$error_filter_active <- FALSE
  data_store$selected_error_rows <- NULL
  updateActionButton(session, "filter_errors", label = "Show Errors")

  # Update consensus summary
  updated_df <- data_store$resolution_state
  data_store$consensus_summary <- list(
    n_agree = sum(updated_df$consensus_status == "agree", na.rm = TRUE),
    n_disagree = sum(updated_df$consensus_status == "disagree", na.rm = TRUE),
    n_agree_caveat = sum(updated_df$consensus_status == "agree_caveat", na.rm = TRUE),
    n_single = sum(updated_df$consensus_status == "single", na.rm = TRUE),
    n_error = sum(updated_df$consensus_status == "error", na.rm = TRUE),
    n_manual = sum(updated_df$consensus_status == "manual", na.rm = TRUE),
    n_unresolvable = sum(updated_df$consensus_status == "unresolvable", na.rm = TRUE)
  )
})
```

**2. Update the Excel export handler:**
Extend the needs_review column logic to also flag "unresolvable" rows:
```r
export_df$needs_review <- export_df$consensus_status %in% c("error", "unresolvable")
```

**3. Ensure re-curated table shows updated data:**
After merge, the renderDT reactive should automatically re-fire since data_store$resolution_state changed. Verify the filter resets to "Show All" so user sees the full updated table.

**4. Add "unresolvable" to the consensus summary display:**
In the summary value boxes or text output, add count of unresolvable and manual rows alongside existing agree/disagree/error counts.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "app_lines <- readLines('app.R'); stopifnot(any(grepl('apply_retag', app_lines))); stopifnot(any(grepl('merge_retry_results', app_lines))); stopifnot(any(grepl('run_curation_pipeline', app_lines))); stopifnot(any(grepl('unresolvable', app_lines))); cat('Re-curate handler found\n')"</automated>
  </verify>
  <done>
- "Apply & Re-curate" runs full pipeline on selected subset
- Results merge back via merge_retry_results preserving pins and row order
- Re-failed rows get "unresolvable" status
- Consensus summary updated with manual and unresolvable counts
- Excel export flags unresolvable rows as needs_review
- Table filter resets after merge to show full updated view
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete error recovery workflow end-to-end</name>
  <files>app.R</files>
  <action>
Human verification checkpoint. Run the app and test:
1. Manual DTXSID entry on error rows (inline cell click)
2. Validate All bulk validation with progress
3. Re-tag and re-curate flow (filter, select, modal, merge)
4. Pin preservation during re-curation
5. Excel export with unresolvable flagging
  </action>
  <what-built>Complete error recovery workflow: inline DTXSID entry + bulk validation + re-tag/re-curate with merge-back</what-built>
  <how-to-verify>
    1. Run the app: `shiny::runApp()`
    2. Upload a test chemical file and run curation to get some error rows
    3. **Manual DTXSID entry test:**
       - Click on the consensus_dtxsid cell of an error row
       - Type a valid DTXSID (e.g., DTXSID7020182 for Acetone)
       - Verify the cell shows the value with a "manual" badge indicator
       - Click "Validate All" button
       - Verify progress bar appears
       - Verify the row status changes to "manual" with purple badge
       - Verify preferredName appears in the Resolution column
    4. **Invalid DTXSID test:**
       - Enter an invalid DTXSID (e.g., "NOTADTXSID")
       - Verify warning notification about invalid format
    5. **Re-tag and re-curate test:**
       - Click "Show Errors" to filter to error rows
       - Select one or more error rows by clicking
       - Click "Re-tag Selected"
       - In the modal, change a column tag assignment
       - Click "Apply & Re-curate"
       - Verify progress indicator during pipeline execution
       - Verify table updates with new results merged back
       - Verify non-selected rows are unchanged
    6. **Pin preservation test:**
       - Resolve a disagree row by pinning a selection
       - Run re-curation on error rows
       - Verify the pinned row is untouched
    7. **Export test:**
       - Download Excel export
       - Verify unresolvable rows have needs_review = TRUE
  </how-to-verify>
  <verify>Human verification of complete error recovery workflow</verify>
  <done>All 7 test scenarios pass visual verification</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Error filter toggles between full table and error-only view
2. Row selection works in filtered view, maps correctly to original indices
3. Re-tag modal shows column-to-tag mapping
4. Re-curation runs full pipeline on subset
5. merge_retry_results correctly updates only selected rows
6. Pinned rows never modified
7. Unresolvable status assigned to re-failed rows
8. All existing tests pass
</verification>

<success_criteria>
- Complete re-tag and re-curate workflow functional end-to-end
- Error filter, selection, modal, pipeline execution, merge-back all working
- Pin preservation verified
- Unresolvable status for re-failed rows
- Excel export updated for new statuses
- User verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-error-recovery-workflows/08-03-SUMMARY.md`
</output>
