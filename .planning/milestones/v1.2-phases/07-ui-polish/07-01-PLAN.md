---
phase: 07-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.R, R/consensus.R]
autonomous: true
requirements: [UIPX-01, UIPX-02, UIPX-03, UIPX-04]

must_haves:
  truths:
    - "User sees only tagged columns and consensus/resolution columns by default in Review Results table"
    - "User can toggle untagged column visibility via a colvis button"
    - "Pipeline internal columns (source_tier_*, searchName_*) never appear in colvis menu"
    - "match_type values render as color-coded badges (green/blue/yellow/red)"
    - "consensus_status values render as color-coded badges (green/orange/gray)"
    - "match_type and consensus_status columns have dropdown filter selectors"
    - "Resolution dropdown for disagree rows shows DTXSID with preferredName"
    - "Resolution dropdown options are sorted by rank (best match first)"
    - "Agree rows show static checkmark with DTXSID and preferredName"
    - "Disagree dropdown includes a None option"
    - "Error (No Match) rows have light pink background highlight"
    - "Excel export includes a needs_review column (TRUE for No Match rows)"
    - "Excel export includes ALL columns including untagged and pipeline internals"
  artifacts:
    - path: "app.R"
      provides: "Updated curation_table renderDT with column visibility tiers, badge rendering, enhanced dropdown, error highlighting, and export flagging"
    - path: "R/consensus.R"
      provides: "Enhanced get_resolution_options returning preferredName and rank metadata"
  key_links:
    - from: "app.R (curation_table)"
      to: "R/consensus.R (get_resolution_options)"
      via: "function call in sapply loop"
      pattern: "get_resolution_options\\(df, i, dtxsid_cols\\)"
    - from: "app.R (curation_table)"
      to: "DT Buttons extension"
      via: "colvis button config"
      pattern: "extend.*colvis"
    - from: "app.R (download handler)"
      to: "data_store$resolution_state"
      via: "needs_review column derivation"
      pattern: "needs_review"
---

<objective>
Implement all UI polish features for the Review Results table: column visibility defaults with colvis toggle, color-coded badges for match_type and consensus_status, enhanced resolution dropdowns with preferredName context, error row highlighting, and Excel export flagging.

Purpose: Reduce cognitive load by hiding irrelevant columns, provide visual scanning cues via badges and row highlighting, give richer context for resolution decisions, and flag error rows in export for downstream processing.
Output: Updated app.R curation_table renderer and download handler, updated R/consensus.R get_resolution_options function.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-polish/07-CONTEXT.md
@.planning/phases/07-ui-polish/07-RESEARCH.md

@app.R
@R/consensus.R

<interfaces>
<!-- Key interfaces the executor needs -->

From R/consensus.R (get_resolution_options, lines 145-158):
```r
get_resolution_options <- function(df, row_idx, dtxsid_cols) {
  # Returns named list: list(col_name = dtxsid_value)
  # Only for rows where consensus_status == "disagree"
  # Filters to columns with non-NA values
}
```

From app.R (curation_table renderDT, lines 1379-1541):
- df = data_store$resolution_state
- dtxsid_cols = data_store$dtxsid_cols
- Column patterns: dtxsid_*, preferredName_*, searchName_*, rank_*, source_tier_*
- Currently hidden: dtxsid_cols, preferredName_*, searchName_*, rank_*, source_tier_*, .pinned
- Currently uses: extensions = 'Buttons', dom = 'Bfrtip', buttons = c('copy', 'csv')
- Currently uses: filter = "top", escape = FALSE, class = 'cell-border stripe hover compact'
- consensus_status already a factor with levels: agree, agree_caveat, single, disagree, error
- match_type derived via sapply, values: "Exact Match", "CAS Lookup", "Starts-With", "No Match"
- Resolution column: HTML select for disagree+unpinned, pin icon for pinned, empty for others
- Row backgrounds via formatStyle on consensus_status
- Cell badge styling via formatStyle on consensus_status (bg color, white text, bold, center, border-radius)

From app.R (download handler, lines 1726-1772):
- export_data = resolution_state minus .pinned
- Writes 3 sheets: Curated Data, Summary, Column Tags
- No current needs_review column

From app.R (data_store, relevant fields):
- data_store$column_tags: named list of column->type mappings (user-assigned tags)
- data_store$clean: cleaned data frame (original columns after detection)
- data_store$resolution_state: full pipeline result with all columns
- data_store$dtxsid_cols: character vector of dtxsid_* column names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Column visibility tiers, colvis button, badges, and error row highlight</name>
  <files>app.R</files>
  <action>
Modify the `output$curation_table <- renderDT({...})` block (starting ~line 1379) as follows:

**1. Three-tier column visibility:**

Replace the current `hidden_cols` logic (lines 1469-1480) with three tiers:

```r
# Tier 1: Always hidden (permanently, excluded from colvis)
always_hidden <- c(
  grep("^source_tier_", names(df), value = TRUE),
  grep("^searchName_", names(df), value = TRUE),
  ".pinned"
)
always_hidden_idx <- which(names(df) %in% always_hidden) - 1

# Tier 2: Hidden by default, toggleable via colvis (untagged original columns)
tagged_col_names <- names(data_store$column_tags)
# Original data columns that exist in df but weren't tagged
all_original_cols <- names(data_store$clean)
untagged_cols <- setdiff(
  all_original_cols[all_original_cols %in% names(df)],
  tagged_col_names
)
untagged_idx <- which(names(df) %in% untagged_cols) - 1

# Combine: both tiers hidden initially
all_hidden_idx <- c(always_hidden_idx, untagged_idx)
```

**2. ColVis button in Buttons config:**

Replace `buttons = c('copy', 'csv')` with:
```r
buttons = list(
  'copy', 'csv',
  list(
    extend = 'colvis',
    text = 'Toggle Columns',
    columns = as.list(untagged_idx)  # Only untagged cols appear in colvis menu
  )
)
```

Update `columnDefs` to use `all_hidden_idx`:
```r
columnDefs = list(
  list(visible = FALSE, targets = as.list(all_hidden_idx))
)
```

**3. Match type as factor (for dropdown filter):**

After the existing match_type derivation (after line 1434), convert to factor:
```r
df$match_type <- factor(df$match_type, levels = c("Exact Match", "CAS Lookup", "Starts-With", "No Match"))
```

**4. Badge HTML rendering for match_type:**

After converting match_type to factor, create a display version with badge HTML:
```r
match_badge_colors <- c(
  "Exact Match" = "#28a745",
  "CAS Lookup" = "#007bff",
  "Starts-With" = "#ffc107",
  "No Match" = "#dc3545"
)
match_badge_text <- c(
  "Exact Match" = "#fff",
  "CAS Lookup" = "#fff",
  "Starts-With" = "#212529",
  "No Match" = "#fff"
)

df$match_type <- sapply(as.character(df$match_type), function(mt) {
  bg <- match_badge_colors[mt] %||% "#6c757d"
  fg <- match_badge_text[mt] %||% "#fff"
  sprintf(
    '<span style="background:%s;color:%s;padding:2px 8px;border-radius:4px;font-weight:600;font-size:0.85em;display:inline-block;">%s</span>',
    bg, fg, htmltools::htmlEscape(mt)
  )
})
```

**IMPORTANT NOTE on badge + filter interaction:** DT's `filter = "top"` reads the underlying data for filter options, NOT the rendered HTML. Since we're overwriting match_type with HTML, the filter will show raw HTML strings. To fix this, keep a separate raw column for filtering:

```r
# BEFORE badge rendering:
df$match_type_raw <- df$match_type  # Keep raw values for filtering

# AFTER badge rendering of match_type:
# We'll use JavaScript columnDefs render instead of overwriting the column.
```

**REVISED APPROACH for match_type badges:** Instead of overwriting the column with HTML (which breaks filters), use a JavaScript `render` callback in `columnDefs`:

```r
match_type_idx <- which(names(df) == "match_type") - 1

# In columnDefs list, add:
list(
  targets = match_type_idx,
  render = JS("function(data, type, row, meta) {
    if (type !== 'display') return data;
    var colors = {
      'Exact Match': '#28a745',
      'CAS Lookup': '#007bff',
      'Starts-With': '#ffc107',
      'No Match': '#dc3545'
    };
    var textColors = { 'Starts-With': '#212529' };
    var bg = colors[data] || '#6c757d';
    var fg = textColors[data] || '#fff';
    return '<span style=\"background:' + bg + ';color:' + fg +
      ';padding:2px 8px;border-radius:4px;font-weight:600;font-size:0.85em;display:inline-block;\">' +
      data + '</span>';
  }")
)
```

This preserves the raw factor data for DT filter dropdowns while rendering badges visually.

**5. Badge rendering for consensus_status:**

Similarly, use a JavaScript render callback for consensus_status instead of the existing formatStyle cell styling (lines 1520-1538):

```r
consensus_idx <- which(names(df) == "consensus_status") - 1

# In columnDefs list, add:
list(
  targets = consensus_idx,
  render = JS("function(data, type, row, meta) {
    if (type !== 'display') return data;
    var colors = {
      'agree': '#28a745',
      'agree_caveat': '#17a2b8',
      'single': '#6c757d',
      'disagree': '#fd7e14',
      'error': '#343a40'
    };
    var bg = colors[data] || '#6c757d';
    return '<span style=\"background:' + bg + ';color:#fff;padding:2px 8px;border-radius:4px;font-weight:600;font-size:0.85em;display:inline-block;\">' +
      data + '</span>';
  }")
)
```

**REMOVE** the existing `formatStyle('consensus_status', ...)` block (lines 1520-1538) that sets cell backgroundColor/color/fontWeight/textAlign/borderRadius/padding. The JS render callback replaces this. **KEEP** the row-level `formatStyle('consensus_status', target = 'row', ...)` block (lines 1504-1518) -- that's for row background colors, not badge styling.

**6. Error row highlight enhancement:**

Update the existing row-level formatStyle (lines 1504-1518) to make error rows more visibly pink. Change the error row background from `"rgba(108, 117, 125, 0.08)"` (gray) to `"rgba(220, 53, 69, 0.12)"` (light pink):

```r
dt <- dt %>% formatStyle(
  'consensus_status',
  target = 'row',
  backgroundColor = styleEqual(
    c("agree", "agree_caveat", "disagree", "single", "error"),
    c(
      "rgba(40, 167, 69, 0.08)",
      "rgba(40, 167, 69, 0.05)",
      "rgba(220, 53, 69, 0.08)",
      "rgba(108, 117, 125, 0.05)",
      "rgba(220, 53, 69, 0.12)"  # Light pink for error/No Match rows
    )
  )
)
```

**Summary of datatable() options changes:**
```r
dt <- datatable(
  display_df,
  options = list(
    pageLength = 25,
    scrollX = TRUE,
    dom = 'Bfrtip',
    buttons = list(
      'copy', 'csv',
      list(
        extend = 'colvis',
        text = 'Toggle Columns',
        columns = as.list(untagged_idx)
      )
    ),
    columnDefs = list(
      list(visible = FALSE, targets = as.list(all_hidden_idx)),
      list(
        targets = match_type_idx,
        render = JS("... match_type badge render ...")
      ),
      list(
        targets = consensus_idx,
        render = JS("... consensus_status badge render ...")
      )
    )
  ),
  extensions = 'Buttons',
  class = 'cell-border stripe hover compact',
  rownames = FALSE,
  filter = "top",
  escape = FALSE
)
```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('R/file_handlers.R'); source('R/data_detection.R'); source('R/consensus.R'); source('R/curation.R'); source('app.R', echo=FALSE, local=new.env())" 2>&1 | head -5</automated>
  </verify>
  <done>
- Untagged columns hidden by default in curation_table
- ColVis button appears with only untagged columns listed
- Pipeline internals (source_tier_*, searchName_*, .pinned) excluded from colvis
- match_type renders as color-coded badges with working dropdown filter
- consensus_status renders as color-coded badges with working dropdown filter
- Error/No Match rows have light pink row background
- Existing row background colors for agree/disagree/single preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolution dropdown enhancement and Excel export flagging</name>
  <files>R/consensus.R, app.R</files>
  <action>
**1. Enhance get_resolution_options() in R/consensus.R:**

Modify `get_resolution_options()` (lines 145-158) to return richer metadata. Instead of returning just `list(col_name = dtxsid_value)`, return a list of lists with dtxsid, preferredName, and rank:

```r
get_resolution_options <- function(df, row_idx, dtxsid_cols) {
  if (df$consensus_status[row_idx] != "disagree") {
    return(list())
  }

  options <- list()
  for (col in dtxsid_cols) {
    val <- df[[col]][row_idx]
    if (!is.na(val)) {
      # Get corresponding preferredName and rank
      pref_col <- sub("^dtxsid_", "preferredName_", col)
      rank_col <- sub("^dtxsid_", "rank_", col)
      pref_name <- if (pref_col %in% names(df)) df[[pref_col]][row_idx] else NA
      rank_val <- if (rank_col %in% names(df)) df[[rank_col]][row_idx] else NA

      options[[col]] <- list(
        dtxsid = val,
        preferredName = pref_name,
        rank = rank_val
      )
    }
  }

  # Sort by rank (lowest/best first), NAs last
  if (length(options) > 1) {
    ranks <- sapply(options, function(o) if (is.na(o$rank)) Inf else as.numeric(o$rank))
    options <- options[order(ranks)]
  }

  options
}
```

**IMPORTANT:** This changes the return type from `list(col = "DTXSID")` to `list(col = list(dtxsid, preferredName, rank))`. Update all consumers.

**2. Update dropdown rendering in app.R curation_table:**

In the Resolution column sapply (starting ~line 1439), update the disagree+unpinned branch to use the new return format:

```r
df$Resolution <- sapply(seq_len(nrow(df)), function(i) {
  status <- as.character(df$consensus_status[i])

  if (status == "agree" || status == "agree_caveat" || status == "single") {
    # Static display with checkmark for agree/agree_caveat/single rows that have a DTXSID
    dtxsid <- df$consensus_dtxsid[i]
    if (!is.na(dtxsid)) {
      # Find preferredName from any available column
      pref_cols <- grep("^preferredName_", names(df), value = TRUE)
      pref_name <- NA
      for (pc in pref_cols) {
        if (!is.na(df[[pc]][i])) { pref_name <- df[[pc]][i]; break }
      }
      if (!is.na(pref_name)) {
        paste0("\u2705 ", htmltools::htmlEscape(dtxsid), " \u2014 ", htmltools::htmlEscape(pref_name))
      } else {
        paste0("\u2705 ", htmltools::htmlEscape(dtxsid))
      }
    } else {
      ""
    }
  } else if (status == "disagree") {
    if (isTRUE(df$.pinned[i])) {
      # Pinned: show pin icon with resolved value and name
      dtxsid <- df$consensus_dtxsid[i]
      pref_cols <- grep("^preferredName_", names(df), value = TRUE)
      pref_name <- NA
      for (pc in pref_cols) {
        if (!is.na(df[[pc]][i])) { pref_name <- df[[pc]][i]; break }
      }
      if (!is.na(pref_name)) {
        paste0('\U0001F4CC ', htmltools::htmlEscape(dtxsid), ' \u2014 ', htmltools::htmlEscape(pref_name))
      } else {
        paste0('\U0001F4CC ', htmltools::htmlEscape(dtxsid))
      }
    } else {
      # Unpinned disagree: dropdown with options
      options <- get_resolution_options(df, i, dtxsid_cols)
      if (length(options) > 0) {
        # Options already sorted by rank from get_resolution_options
        options_html <- paste0(
          sapply(names(options), function(col) {
            opt <- options[[col]]
            label <- if (!is.na(opt$preferredName)) {
              paste0(htmltools::htmlEscape(opt$dtxsid), " \u2014 ", htmltools::htmlEscape(opt$preferredName))
            } else {
              htmltools::htmlEscape(opt$dtxsid)
            }
            paste0('<option value="', htmltools::htmlEscape(col), '">', label, '</option>')
          }),
          collapse = ""
        )
        paste0(
          '<select class="resolve-select form-select form-select-sm" data-row="', i, '">',
          '<option value="">Select...</option>',
          options_html,
          '<option value="__none__">None (skip this row)</option>',
          '</select>'
        )
      } else {
        ""
      }
    }
  } else {
    # error or other status
    ""
  }
})
```

**3. Handle "None" selection in the resolution observer:**

In the `observeEvent(input$resolve_row_choice, ...)` block (~line 1620), add handling for the `__none__` sentinel:

```r
observeEvent(input$resolve_row_choice, {
  req(data_store$resolution_state, data_store$dtxsid_cols)

  row <- input$resolve_row_choice$row
  column <- input$resolve_row_choice$column

  if (column == "__none__") {
    # "None" selected: pin the row with NA consensus_dtxsid
    df <- data_store$resolution_state
    df$.pinned[row] <- TRUE
    # Leave consensus_dtxsid as-is (NA or whatever it was)
    data_store$resolution_state <- df
  } else {
    # Normal resolution: existing logic
    updated_df <- resolve_row(
      data_store$resolution_state,
      row,
      column,
      data_store$dtxsid_cols
    )
    data_store$resolution_state <- updated_df
  }

  # Update consensus summary (existing logic)
  updated_df <- data_store$resolution_state
  data_store$consensus_summary <- list(
    n_agree = sum(updated_df$consensus_status == "agree", na.rm = TRUE),
    n_disagree = sum(updated_df$consensus_status == "disagree" & !isTRUE(updated_df$.pinned), na.rm = TRUE),
    n_agree_caveat = sum(updated_df$consensus_status == "agree_caveat", na.rm = TRUE),
    n_single = sum(updated_df$consensus_status == "single", na.rm = TRUE),
    n_error = sum(updated_df$consensus_status == "error", na.rm = TRUE)
  )
})
```

**4. Update existing tests for get_resolution_options():**

In tests/test_consensus.R, update tests that check the return format. The old format was `list(col = "DTXSID")`, new format is `list(col = list(dtxsid = "DTXSID", preferredName = NA, rank = NA))`. Update assertions:

- "get_resolution_options returns available columns for disagree row" (~line 295): Check `options[[col]]$dtxsid` instead of `options[[col]]`
- "get_resolution_options returns empty for non-disagree row" (~line 314): No change needed (still returns empty list)
- "get_resolution_options excludes NA columns" (~line 328): Check `options[[col]]$dtxsid` instead of `options[[col]]`

**5. Add needs_review column to Excel export:**

In the download handler content function (~line 1726), modify the export_data creation:

```r
export_data <- data_store$resolution_state %>%
  dplyr::mutate(
    needs_review = (consensus_status == "error")
  ) %>%
  dplyr::select(-tidyselect::any_of(".pinned")) %>%
  dplyr::relocate(needs_review, .after = tidyselect::last_col())
```

This adds a logical TRUE/FALSE column. Only "error" (No Match) rows are flagged. Disagree rows are NOT flagged per user decision. The column goes at the end so it doesn't disrupt existing column order.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "source('load_packages.R'); source('R/consensus.R'); testthat::test_file('tests/test_consensus.R')"</automated>
  </verify>
  <done>
- get_resolution_options() returns dtxsid + preferredName + rank metadata per option
- get_resolution_options() sorts options by rank (best first)
- Disagree dropdown shows "DTXSID — preferredName" format
- Disagree dropdown includes "None (skip this row)" option
- Agree/agree_caveat/single rows show static "checkmark DTXSID — preferredName"
- Pinned rows show "pin DTXSID — preferredName"
- "None" selection pins the row without setting a DTXSID
- Excel export includes needs_review column (TRUE for error rows only)
- Existing tests updated and passing for new get_resolution_options return format
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `shiny::runApp()` and upload a test file with multiple tagged columns
2. Verify Review Results table shows:
   - Only tagged columns + consensus/resolution visible by default
   - "Toggle Columns" button in toolbar showing only untagged columns
   - Color-coded match_type badges (green/blue/yellow/red)
   - Color-coded consensus_status badges (green/cyan/orange/gray/dark)
   - Dropdown filters for match_type and consensus_status
   - Error rows with light pink background
   - Disagree dropdowns showing "DTXSID — preferredName" sorted by rank
   - "None" option in disagree dropdowns
   - Agree rows showing checkmark + DTXSID + preferredName
3. Export to Excel and verify:
   - needs_review column present (TRUE for No Match rows, FALSE otherwise)
   - ALL columns included (untagged, tagged, pipeline internals minus .pinned)
4. Run tests: `testthat::test_dir("tests")` passes
</verification>

<success_criteria>
- All 4 requirements (UIPX-01 through UIPX-04) implemented
- Column visibility defaults reduce visual clutter for users
- Badges provide immediate visual scanning of match quality
- Resolution dropdown gives enough context for informed decisions
- Excel export preserves full data with machine-readable error flags
- No regressions in existing functionality (resolution, pinning, priority chain)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish/07-01-SUMMARY.md`
</output>
