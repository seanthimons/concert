---
phase: 16-cleaning-pipeline-bug-fixes
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - tests/test_cleaning_pipeline_validation.R
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "Validation script runs end-to-end and confirms all three fixes work against known-good and known-bad cases"
    - "Known-good chemical names pass through the pipeline without false flags"
    - "Known-bad formulas are correctly caught by the pipeline"
    - "Stop word edge cases are correctly handled in the pipeline"
    - "IUPAC patterns survive the pipeline intact"
  artifacts:
    - path: "tests/test_cleaning_pipeline_validation.R"
      provides: "End-to-end validation test script for all three fixes"
      contains: "run_cleaning_pipeline"
      min_lines: 50
  key_links:
    - from: "tests/test_cleaning_pipeline_validation.R"
      to: "R/cleaning_pipeline.R"
      via: "run_cleaning_pipeline function call"
      pattern: "run_cleaning_pipeline"
---

<objective>
Create a lightweight validation test script that exercises all three bug fixes end-to-end through the full cleaning pipeline, confirming no regressions.

Purpose: VAL-01 requires a validation script that confirms all three fixes against known-good and known-bad cases. Unit tests cover individual functions; this validates the integrated pipeline.
Output: tests/test_cleaning_pipeline_validation.R - runnable validation script.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cleaning-pipeline-bug-fixes/16-01-SUMMARY.md
@R/cleaning_pipeline.R
@R/cleaning_reference.R

<interfaces>
<!-- Pipeline entry point -->
run_cleaning_pipeline(df, tag_map = NULL, reference_lists = NULL)
  Returns: list(cleaned_data, audit_trail, new_tags)

<!-- Reference list structure -->
load_stop_words(cache_dir)
  Returns: tibble(term, source, active)

<!-- The pipeline runs these steps in order for Name columns:
  1. Unicode cleanup
  2. Whitespace trimming
  3. CAS normalization
  4. CAS rescue
  5. Multi-CAS detection
  6. Strip terminal enclosures
  7. Strip quality adjectives
  8. Strip salt references
  9. Strip terminal unspecified
  10. Second enclosure strip
  11. Split synonyms
  12. Final cleanup
  13. Remove empty-name rows

  Then in mod_clean_data.R (not tested here directly):
  14. detect_bare_formulas
  15. flag_reference_matches (functional categories, stop words, block patterns)

  For this validation, we call detect_bare_formulas and flag_reference_matches
  separately after run_cleaning_pipeline, matching the module's execution order.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create end-to-end validation test script (VAL-01)</name>
  <files>tests/test_cleaning_pipeline_validation.R</files>
  <behavior>
    - Test: Pipeline + detect_bare_formulas: "Naphthalene" survives without being flagged as formula
    - Test: Pipeline + detect_bare_formulas: "Sodium chloride" survives without being flagged as formula
    - Test: Pipeline + detect_bare_formulas: "C10H22" IS blocked as bare formula
    - Test: Pipeline + detect_bare_formulas: "NaCl" IS blocked as bare formula
    - Test: Pipeline + detect_bare_formulas: "CaCl2" IS blocked as bare formula
    - Test: Pipeline + flag_reference_matches with stop words: "Naphthalene" is NOT flagged by "na" stop word
    - Test: Pipeline + flag_reference_matches with stop words: "Sodium bicarbonate" is NOT flagged by "na"
    - Test: Pipeline + flag_reference_matches with stop words: "na" IS flagged as exact stop word match
    - Test: Pipeline + flag_reference_matches with stop words: "test" IS flagged as exact stop word match
    - Test: Pipeline: "N,N-Dimethylformamide" passes through synonym split intact (1 row)
    - Test: Pipeline: "xylene, dimethylbenzene, xylol" splits into 3 rows
    - Test: Pipeline: "butane, 2,2-dimethyl" stays intact (existing IUPAC protection)
  </behavior>
  <action>
Create tests/test_cleaning_pipeline_validation.R as a testthat test file that validates all three fixes end-to-end through the actual pipeline functions.

Structure:
1. Source R/cleaning_pipeline.R
2. Create a test dataset with known-good and known-bad cases covering all three bug categories
3. Run through run_cleaning_pipeline() with a tag_map (CASRN + Name columns)
4. Then run detect_bare_formulas() on the pipeline output (mimicking mod_clean_data.R order)
5. Then run flag_reference_matches() with stop words on the output
6. Assert all expected behaviors

Test groups:
- "Validation: Formula detection false positives fixed" - Naphthalene, Sodium chloride survive; C10H22, NaCl, CaCl2 blocked
- "Validation: Stop word matching uses whole-word" - Naphthalene not flagged by "na"; "na" exact match works
- "Validation: IUPAC letter-comma-letter protected" - N,N-Dimethylformamide not split; normal synonyms still split
- "Validation: Full pipeline integration" - Mixed dataset with all three issue types processed correctly in a single pipeline run

The test file should use the same stop_words tibble structure as load_stop_words() returns (tibble with term, source, active columns).

Do NOT source load_packages.R or require the Shiny app to run. Only source R/cleaning_pipeline.R and use testthat + tibble + dplyr + stringr (already loaded by cleaning_pipeline.R).
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/test_cleaning_pipeline_validation.R')"</automated>
  </verify>
  <done>Validation script runs with zero failures. All three fixes confirmed working end-to-end through the full pipeline. Known-good cases pass, known-bad cases are caught.</done>
</task>

</tasks>

<verification>
Run the validation script standalone:

```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/test_cleaning_pipeline_validation.R')"
```

Then run the full test suite to confirm no regressions:

```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"
```

Both pass with zero failures.
</verification>

<success_criteria>
1. Validation test script exists at tests/test_cleaning_pipeline_validation.R
2. Script covers all three fix categories with known-good and known-bad cases
3. All validation tests pass
4. Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/16-cleaning-pipeline-bug-fixes/16-02-SUMMARY.md`
</output>
