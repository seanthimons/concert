---
phase: 11-cas-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - app.R
  - R/modules/mod_clean_data.R
autonomous: false
requirements:
  - UIUX-02
  - UIUX-04

must_haves:
  truths:
    - "User sees Tag Columns tab before Clean Data tab in the navigation"
    - "Clean Data tab is gated behind column tags existing (not behind upload)"
    - "User sees value boxes showing CAS Rescued, CAS Normalized, CAS Invalid, Multi-CAS Flagged, Unicode Cleaned, Fields Trimmed"
    - "User sees step-by-step progress: unicode -> trim -> normalize CAS -> rescue CAS -> detect multi-CAS -> finalize"
    - "User sees multi-CAS flagged rows highlighted in the data table"
    - "User can split a multi-CAS flagged row via a Split button that creates new rows via rbind"
    - "Rescued CAS columns (cas_extract_*) are auto-tagged as CASRN in data_store$column_tags"
  artifacts:
    - path: "app.R"
      provides: "Tab reorder (Tag before Clean), gating logic update, data_store new fields"
      contains: "tag_columns.*clean_data"
    - path: "R/modules/mod_clean_data.R"
      provides: "Value boxes, extended progress, multi-CAS UI, split button, tag merge"
      contains: "value_box"
  key_links:
    - from: "R/modules/mod_clean_data.R"
      to: "R/cleaning_pipeline.R"
      via: "Calls individual pipeline functions with incProgress between each"
      pattern: "normalize_cas_fields|rescue_cas_from_text|detect_multi_cas"
    - from: "R/modules/mod_clean_data.R"
      to: "data_store$column_tags"
      via: "merges new_tags from rescue into column_tags"
      pattern: "column_tags.*new_tags"
    - from: "app.R"
      to: "data_store$column_tags"
      via: "Clean Data tab gated behind column_tags not NULL"
      pattern: "req.*column_tags"
---

<objective>
Wire CAS pipeline into the Shiny UI: reorder tabs (Tag before Clean), update gating logic, replace text summary with value boxes, add step-by-step progress for CAS pipeline, display multi-CAS flagged rows with user-initiated split action, and auto-tag rescued CAS columns.

Purpose: Users can see CAS cleaning results with dashboard-style summary cards and interact with multi-CAS flags.
Output: Updated app.R (tab order + gating) and mod_clean_data.R (value boxes + progress + multi-CAS UI).
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cas-pipeline/11-CONTEXT.md
@.planning/phases/11-cas-pipeline/11-RESEARCH.md
@.planning/phases/11-cas-pipeline/11-01-SUMMARY.md

@app.R
@R/modules/mod_clean_data.R

<interfaces>
<!-- Individual pipeline functions from Plan 01 (called directly for step-by-step progress) -->
```r
# inject_row_lineage(df) -> df with original_row_id column
# clean_unicode_field(x) -> character vector (existing)
# clean_text_field(x) -> character vector (existing)
# build_audit_trail(df_original, df_cleaned, step_name, reason_fn) -> audit tibble (existing)
# normalize_cas_fields(df, tag_map) -> list(cleaned_data, audit_trail)
# rescue_cas_from_text(df, tag_map) -> list(cleaned_data, audit_trail, new_tags)
# detect_multi_cas(df, tag_map) -> df with multi_cas + multi_cas_count columns
# run_cleaning_pipeline(df, tag_map = NULL, reference_lists = NULL) -> list(cleaned_data, audit_trail, new_tags)
```

<!-- data_store fields (from app.R) -->
```r
# data_store$clean — cleaned data from upload module
# data_store$cleaned_data — output of cleaning pipeline
# data_store$cleaning_audit — audit trail tibble
# data_store$column_tags — named list: list(col_name = "CASRN"/"Name"/"Other")
# data_store$reference_lists — reference lists loaded at startup
```

<!-- Value box API (bslib) -->
```r
# bslib::value_box(title, value, showcase = bsicons::bs_icon("name"), theme = "primary")
# Themes: primary, secondary, success, info, warning, danger
# bslib::layout_columns(col_widths = c(4, 4, 4), ...) for responsive grid
```

<!-- Current tab order in app.R navset_underline -->
# Data Preview -> Detection Info -> Raw Data -> Clean Data -> Tag Columns -> Run Curation -> Review Results
# MUST become:
# Data Preview -> Detection Info -> Raw Data -> Tag Columns -> Clean Data -> Run Curation -> Review Results
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder tabs, update gating, and integrate CAS pipeline into mod_clean_data</name>
  <files>app.R, R/modules/mod_clean_data.R</files>
  <action>
    **app.R changes:**

    1. **Reorder navset_underline tabs**: Move Tag Columns BEFORE Clean Data:
       ```
       Data Preview -> Detection Info -> Raw Data -> Tag Columns -> Clean Data -> Run Curation -> Review Results
       ```

    2. **Update gating logic**: Currently Clean Data shows after upload (req(data_store$clean)). Change to:
       - Tag Columns shows after upload: `req(data_store$clean)` -> show tag_columns (add to existing observer that shows detection_info and raw_data)
       - Clean Data shows after tagging: `req(data_store$column_tags)` -> show clean_data
       - Remove clean_data from the current observer that shows it with detection_info and raw_data
       - Add new observer: `observe({ req(data_store$column_tags); show_tab_with_pulse("clean_data") })`

    3. **Update on_cleaning_complete callback**: After cleaning, show run_curation_tab (not tag_columns, since tagging is already done):
       ```r
       mod_clean_data_server("cleaning", data_store,
         on_cleaning_complete = function() {
           show_tab_with_pulse("run_curation_tab")
         }
       )
       ```

    4. **Update on_tags_applied callback**: After tagging, show clean_data (not run_curation):
       ```r
       mod_tag_columns_server("tags", data_store,
         on_tags_applied = function() {
           show_tab_with_pulse("clean_data")
           nav_hide("main_tabs", target = "run_curation_tab", session = session)
           nav_hide("main_tabs", target = "review_results", session = session)
         }
       )
       ```

    5. **Update reset_all_downstream**: Clean Data gating depends on column_tags being reset (already done -- column_tags set to NULL in reset). No change needed but verify.

    **R/modules/mod_clean_data.R changes:**

    1. **Update has_data condition**: Change from checking `data_store$clean` to checking `data_store$column_tags` (since tab is now gated behind tagging):
       ```r
       output$has_data <- reactive({ !is.null(data_store$column_tags) && length(data_store$column_tags) > 0 })
       ```
       Also update empty state message: "Tag your columns first, then run cleaning."

    2. **Replace observeEvent run_cleaning with step-by-step orchestration**: Instead of calling run_cleaning_pipeline() as a batch, call individual pipeline functions with incProgress between each for true step-by-step progress (UIUX-04):
       ```r
       withProgress(message = "Cleaning data...", value = 0, {
         df <- data_store$clean
         tag_map <- data_store$column_tags
         all_audits <- list()

         incProgress(0.10, detail = "Adding row lineage...")
         df <- inject_row_lineage(df)

         incProgress(0.15, detail = "Converting unicode to ASCII...")
         df_before <- df
         df <- dplyr::mutate(df, dplyr::across(where(is.character), clean_unicode_field))
         all_audits[[length(all_audits) + 1]] <- build_audit_trail(df_before, df, "unicode_to_ascii", function(f) paste0("Unicode to ASCII in ", f))

         incProgress(0.15, detail = "Trimming whitespace...")
         df_before <- df
         df <- dplyr::mutate(df, dplyr::across(where(is.character), clean_text_field))
         all_audits[[length(all_audits) + 1]] <- build_audit_trail(df_before, df, "trim_whitespace_punctuation", function(f) paste0("Trim in ", f))

         new_tags <- list()
         if (!is.null(tag_map) && length(tag_map) > 0) {
           incProgress(0.20, detail = "Normalizing CAS-RNs...")
           cas_result <- normalize_cas_fields(df, tag_map)
           df <- cas_result$cleaned_data
           all_audits[[length(all_audits) + 1]] <- cas_result$audit_trail

           incProgress(0.20, detail = "Rescuing CAS from names...")
           rescue_result <- rescue_cas_from_text(df, tag_map)
           df <- rescue_result$cleaned_data
           all_audits[[length(all_audits) + 1]] <- rescue_result$audit_trail
           new_tags <- rescue_result$new_tags

           incProgress(0.10, detail = "Detecting multi-CAS rows...")
           updated_tag_map <- c(tag_map, new_tags)
           df <- detect_multi_cas(df, updated_tag_map)
         }

         incProgress(0.10, detail = "Finalizing...")
         audit_combined <- dplyr::bind_rows(all_audits)
         data_store$cleaned_data <- df
         data_store$cleaning_audit <- audit_combined
         if (length(new_tags) > 0) {
           data_store$column_tags <- c(data_store$column_tags, new_tags)
         }
       })
       ```
       Keep the tryCatch wrapper, shinyjs disable/enable, notification, and callback from existing code.

    3. **Replace text alert summary with value_box() cards**: Replace the current `output$cleaning_summary` renderUI (lines 110-138) with bslib::value_box() layout:
       - Count audit entries by step: normalize_cas, rescue_cas, unicode_to_ascii, trim_whitespace_punctuation
       - Count multi_cas flags from cleaned_data$multi_cas column (if exists)
       - Count CAS invalid: normalize_cas audit entries where new_value is NA or "[NA]"
       - Use bslib::layout_columns with responsive col_widths = c(4, 4, 4) in rows
       - Value box themes and icons:
         - "CAS Rescued" / primary / bs_icon("search")
         - "CAS Normalized" / success / bs_icon("check-circle")
         - "CAS Invalid" / danger / bs_icon("x-circle")
         - "Multi-CAS Flagged" / warning / bs_icon("flag")
         - "Unicode Cleaned" / info / bs_icon("globe")
         - "Fields Trimmed" / info / bs_icon("scissors")
       - When audit has 0 rows, show a single "All Clean" value_box with success theme

    4. **Multi-CAS flagged rows section**: Add UI elements after cleaned_table:
       - In UI: uiOutput(ns("multi_cas_section"))
       - In server: renderUI that shows a card with heading "Multi-CAS Flagged Rows", a DT table filtered to multi_cas == TRUE, and a "Split Selected Row" actionButton
       - Only render when cleaned_data has multi_cas column AND any multi_cas == TRUE
       - Split button: on click, show modal confirmation, then:
         - Get selected row from DT
         - Get all CASRN column values for that row
         - Create N new rows (one per non-NA CAS), each with only that CAS value in the primary CAS column
         - rbind into cleaned_data, remove original multi-CAS row
         - Add audit entry for the split
         - Update data_store$cleaned_data

    5. **Hide internal columns from main DT display**: In output$cleaned_table, use columnDefs to hide original_row_id, multi_cas, multi_cas_count columns:
       ```r
       hidden_cols <- which(names(data_store$cleaned_data) %in% c("original_row_id", "multi_cas", "multi_cas_count")) - 1
       options = list(pageLength = 25, scrollX = TRUE, columnDefs = list(list(visible = FALSE, targets = hidden_cols)))
       ```
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" & sleep 8 && curl -s http://localhost:3838 | head -20 && kill %1 2>/dev/null</automated>
    Shiny smoke test: app starts without error, page loads. Also run full test suite:
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Tab order is Tag Columns before Clean Data
    - Clean Data gated behind column_tags (not upload)
    - Value boxes display CAS pipeline statistics (UIUX-02)
    - Step-by-step progress shows each pipeline stage (UIUX-04)
    - Multi-CAS rows highlighted with split button
    - Rescued CAS columns auto-tagged as CASRN
    - App starts without error (smoke test)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CAS pipeline UI end-to-end</name>
  <files>app.R, R/modules/mod_clean_data.R</files>
  <action>
    Human verification of the complete CAS pipeline UI. No code changes -- this is a visual and functional verification checkpoint.

    The following was built in Task 1:
    - Tab reorder (Tag Columns before Clean Data)
    - Gating update (Clean Data gated behind column tags)
    - Value box summary cards replacing text alert
    - Step-by-step progress indicator for CAS pipeline
    - Multi-CAS flagged rows display with split button
    - Rescued CAS columns auto-tagged as CASRN
  </action>
  <verify>
    Manual verification steps:
    1. Start the app: Run shiny::runApp() in R console
    2. Upload data/chemical_validation_test.csv
    3. Verify tab order: Data Preview, Detection Info, Raw Data, Tag Columns, Clean Data, Run Curation, Review Results
    4. Tag at least one column as CASRN and one as Name/Other
    5. Verify Clean Data tab appears after tagging (not after upload)
    6. Click Run Cleaning -- verify step-by-step progress messages
    7. Verify value boxes appear with correct counts
    8. Check multi-CAS flagged rows section if any exist
    9. Verify rescued CAS columns in cleaned data table
    10. Navigate to Run Curation -- verify rescued CAS columns tagged as CASRN
  </verify>
  <done>User confirms CAS pipeline UI works correctly end-to-end, or describes issues to fix</done>
</task>

</tasks>

<verification>
1. Shiny smoke test passes (app starts without crash)
2. Tab order correct: Tag Columns before Clean Data
3. Clean Data gated behind column tags
4. Value boxes render with correct counts after cleaning
5. Progress indicator shows CAS pipeline steps
6. Multi-CAS rows flagged and splittable
7. Full test suite passes (no regressions)
</verification>

<success_criteria>
- UIUX-02: Value boxes display CAS Rescued, CAS Normalized, CAS Invalid, Multi-CAS Flagged, Unicode Cleaned, Fields Trimmed
- UIUX-04: Step-by-step progress indicator shows each cleaning stage
- Tab reorder: Tag Columns appears before Clean Data in navigation
- Gating: Clean Data requires column tags to exist
- Multi-CAS: Flagged rows displayed with user-initiated split action
- Tag merge: Rescued CAS columns auto-tagged as CASRN for downstream curation
</success_criteria>

<output>
After completion, create `.planning/phases/11-cas-pipeline/11-02-SUMMARY.md`
</output>
