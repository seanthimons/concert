---
phase: 12-name-cleaning
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_name_cleaning.R
  - R/cleaning_pipeline.R
autonomous: true
requirements: [NAME-01, NAME-02, NAME-03, NAME-04]

must_haves:
  truths:
    - "Trailing parentheticals and brackets stripped from chemical names"
    - "Parentheticals containing 'yl' are preserved (except exception words)"
    - "Stripped parenthetical content preserved in formula_extract column"
    - "Quality adjectives, salt references, and 'unspecified' suffixes stripped with audit"
    - "Comma/semicolon-separated synonyms split into separate rows"
    - "IUPAC inverted-name commas (digit-comma-digit) protected from splitting"
    - "Synonym rows get NA for CAS columns"
    - "All transformations logged to audit trail"
  artifacts:
    - path: "tests/test_name_cleaning.R"
      provides: "Test suite for name cleaning functions"
      min_lines: 100
    - path: "R/cleaning_pipeline.R"
      provides: "Name cleaning functions + extended run_cleaning_pipeline"
      contains: "strip_terminal_enclosures"
  key_links:
    - from: "R/cleaning_pipeline.R"
      to: "run_cleaning_pipeline"
      via: "Name cleaning steps called after CAS steps when tag_map has Name columns"
      pattern: "strip_terminal_enclosures|strip_quality_adjectives|split_synonyms"
---

<objective>
Implement name cleaning pipeline functions with TDD: parenthetical stripping (NAME-01), formula extraction from parentheticals (NAME-02), IUPAC-aware synonym splitting (NAME-03), and quality adjective/salt/unspecified stripping (NAME-04).

Purpose: Core data transformation functions that clean chemical names before curation. Synonym splitting is the highest-risk operation (custom IUPAC comma protection, row count changes).
Output: 5+ new functions in R/cleaning_pipeline.R, comprehensive test suite in tests/test_name_cleaning.R
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-name-cleaning/12-CONTEXT.md
@.planning/phases/12-name-cleaning/12-RESEARCH.md
@.planning/phases/11-cas-pipeline/11-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from existing pipeline. Executor should use these directly. -->

From R/cleaning_pipeline.R — pipeline return signature:
```r
# All pipeline functions return this structure:
list(
  cleaned_data = df,        # data.frame with transformations applied
  audit_trail = audit_df,   # tibble: row_id, field, original_value, new_value, step, reason
  new_tags = list()          # named list of new column -> type mappings
)
```

From R/cleaning_pipeline.R — existing functions:
```r
clean_unicode_field <- function(x)          # Character vector -> cleaned vector
clean_text_field <- function(x)             # Character vector -> cleaned vector
build_audit_trail <- function(df_original, df_cleaned, step_name, reason_fn)
inject_row_lineage <- function(df)          # Adds original_row_id column
normalize_cas_fields <- function(df, tag_map)
rescue_cas_from_text <- function(df, tag_map)
detect_multi_cas <- function(df, tag_map)
run_cleaning_pipeline <- function(df, tag_map = NULL, reference_lists = NULL)
```

From R/cleaning_pipeline.R — tag_map structure:
```r
# Named list: column_name -> type ("CASRN", "Name", "Other")
tag_map <- list(cas_number = "CASRN", chemical_name = "Name", description = "Other")
```

From R/cleaning_pipeline.R — run_cleaning_pipeline return:
```r
list(
  cleaned_data = df_final,
  audit_trail = audit_combined,
  new_tags = new_tags
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write name cleaning tests (RED phase)</name>
  <files>tests/test_name_cleaning.R</files>
  <behavior>
    NAME-01 — strip_terminal_enclosures():
    - "Acetone (ACS reagent)" -> "Acetone", stripped="ACS reagent"
    - "dimethyl (methyl)" -> unchanged (has "yl", kept)
    - "compound (high density)" -> "compound" (exception word "density")
    - "Sodium chloride [food grade]" -> "Sodium chloride", stripped="food grade"
    - "ethanol (ethyl alcohol)" -> unchanged (has "yl", kept)
    - "polymer (probably crosslinked)" -> "polymer" (exception word "probably")
    - NA input -> NA output (no crash)
    - "no parens at all" -> unchanged
    - "mid (paren) text" -> unchanged (not terminal)

    NAME-02 — formula extraction preserved:
    - strip_terminal_enclosures on "Sodium chloride (NaCl)" -> stripped content "NaCl" available
    - Verify formula_extract_{source} column created in pipeline integration
    - CAS in parens already handled by Phase 11 extract_cas — no duplication test

    NAME-03 — split_synonyms():
    - "xylene, dimethylbenzene, xylol" -> 3 rows
    - "butane, 2,2-dimethyl" -> 1 row (IUPAC inverted name protected)
    - "acetone; dimethyl ketone" -> 2 rows (semicolon split)
    - "1,4-Dioxane" -> 1 row (digit-comma-digit protected)
    - "2,4-dichlorophenol" -> 1 row (digit-comma-digit protected)
    - Primary name keeps original row_id; synonyms get new rows with original_row_id tracking
    - Synonym rows get NA for CAS columns
    - Empty strings after split are removed
    - Single name (no comma/semicolon) -> 1 row unchanged

    NAME-04 — strip_quality_adjectives(), strip_salt_references(), strip_terminal_unspecified():
    - "technical grade ethanol" -> "ethanol"
    - "Acetone, 99.5% pure" -> "Acetone, 99.5%"
    - "purified water" -> "water"
    - "lead and its salts" -> "lead"
    - "mercury and its inorganic salts" -> "mercury"
    - "compound, unspecified" -> "compound"
    - "chemical - unspecified" -> "chemical"
    - Audit trail entries generated for each removal

    Pipeline integration:
    - run_cleaning_pipeline with tag_map containing Name columns runs name cleaning steps
    - Pipeline step order: CAS steps -> strip parentheticals -> strip adjectives/salts/unspecified -> synonym split -> finalize
    - Name cleaning skipped when no Name columns in tag_map (backward compatible)
  </behavior>
  <action>
Create tests/test_name_cleaning.R with testthat test cases for all name cleaning functions. Source R/cleaning_pipeline.R and required packages. Tests MUST fail (functions don't exist yet).

Structure test file with describe/it blocks:
1. "strip_terminal_enclosures" — parenthetical and bracket stripping with "yl" protection
2. "split_synonyms" — IUPAC-aware comma/semicolon splitting with row expansion
3. "strip_quality_adjectives" — quality word removal with audit
4. "strip_salt_references" — salt pattern removal with audit
5. "strip_terminal_unspecified" — unspecified suffix removal with audit
6. "run_cleaning_pipeline name integration" — full pipeline with name cleaning steps

Use clean_chems.py as reference for expected patterns (term_parenth line 164, drop_text line 495, drop_salts line 557, terminal_unspecified line 348).

For split_synonyms tests: create a small test data.frame with tag_map, verify row count changes, verify CAS NA on synonym rows, verify original_row_id tracking.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/test_name_cleaning.R')" 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists with 30+ test cases covering NAME-01 through NAME-04. All tests FAIL (RED) because functions are not yet implemented. Existing test suite (tests/test_cleaning_pipeline.R, tests/test_cas_pipeline.R) still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement name cleaning functions (GREEN phase)</name>
  <files>R/cleaning_pipeline.R</files>
  <action>
Implement all name cleaning functions in R/cleaning_pipeline.R, appending after detect_multi_cas() and before run_cleaning_pipeline():

1. **strip_terminal_enclosures(df, name_cols)** (NAME-01, NAME-02):
   - For each Name-tagged column, strip terminal `(...)` and `[...]`
   - Protect parentheticals containing "yl" UNLESS content also matches exception words: 'density', 'probably', 'average', 'combination' (from clean_chems.py line 189)
   - Preserve stripped content in `formula_extract_{source}` column (NAME-02)
   - Return list(cleaned_data, audit_trail, new_tags) — new_tags empty (formula_extract columns are informational, not auto-tagged per Claude's discretion)
   - Use stringr for regex, handle NA gracefully

2. **strip_quality_adjectives(df, name_cols)** (NAME-04):
   - Remove quality words: 'pure', 'purif', 'tech', 'grade', 'chemical' (with word boundaries, case-insensitive)
   - Use `\b(pure|purif\w*|tech\w*|grade|chemical)\b` pattern
   - str_squish() after removal to clean whitespace
   - Return list(cleaned_data, audit_trail)

3. **strip_salt_references(df, name_cols)** (NAME-04):
   - Remove "and its (\w+ )?salts" pattern (case-insensitive)
   - str_trim() after removal
   - Return list(cleaned_data, audit_trail)

4. **strip_terminal_unspecified(df, name_cols)** (NAME-04):
   - Remove terminal `[,;-]?\s*unspecified\s*$` pattern (case-insensitive)
   - str_trim() after removal
   - Return list(cleaned_data, audit_trail)

5. **split_synonyms(df, name_cols, tag_map)** (NAME-03):
   - For each Name-tagged column:
     a. Protect digit-comma-digit patterns: replace `(\d+),(\d+)` with `\1@@@\2` (unlikely placeholder)
     b. Split on semicolons first, then commas using tidyr::separate_longer_delim() or equivalent row expansion
     c. Restore protected commas: replace `@@@` back to `,`
     d. Trim whitespace, remove empty strings
     e. Primary name (first) keeps original row; synonyms get new rows
     f. Set all CASRN-tagged columns to NA for synonym rows (synonym_index > 1)
     g. Track with synonym_count and synonym_index columns
   - Build audit trail: log original row "split into N synonyms" AND each new synonym "synonym from row X"
   - Return list(cleaned_data, audit_trail)

6. **Update run_cleaning_pipeline():**
   - After CAS steps (inside `if (!is.null(tag_map))` block), add name cleaning:
     - Get name_cols from tag_map: `names(tag_map)[tag_map == "Name"]`
     - If name_cols has length > 0, run in order:
       a. strip_terminal_enclosures(df, name_cols) — accumulate audit + merge new_tags
       b. strip_quality_adjectives(df, name_cols)
       c. strip_salt_references(df, name_cols)
       d. strip_terminal_unspecified(df, name_cols)
       e. split_synonyms(df, name_cols, tag_map_updated) — MUST be LAST
       f. Final cleanup: str_squish on all name_cols, remove rows where all name_cols are empty/NA
   - Backward compatible: no Name columns in tag_map = no name cleaning

Handle edge cases:
- Empty strings after adjective stripping: set to NA, log "name became empty after cleaning"
- NA inputs throughout: pass through without error
- Single-value names (no comma/semicolon): no split, no audit entry
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_file('tests/test_name_cleaning.R')" 2>&1 | tail -10</automated>
  </verify>
  <done>All name cleaning tests pass (GREEN). Existing test suites (test_cleaning_pipeline.R, test_cas_pipeline.R) still pass with zero regressions. Functions handle NA, empty strings, and edge cases. Pipeline order: CAS -> parentheticals -> adjectives/salts/unspecified -> synonym split -> finalize.</done>
</task>

</tasks>

<verification>
1. All tests in tests/test_name_cleaning.R pass
2. All tests in tests/test_cleaning_pipeline.R pass (no regressions)
3. All tests in tests/test_cas_pipeline.R pass (no regressions)
4. Full suite: "C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "testthat::test_dir('tests')"
</verification>

<success_criteria>
- 5 new functions in R/cleaning_pipeline.R: strip_terminal_enclosures, strip_quality_adjectives, strip_salt_references, strip_terminal_unspecified, split_synonyms
- run_cleaning_pipeline extended with name cleaning steps after CAS steps
- 30+ tests passing in tests/test_name_cleaning.R
- Zero regressions in existing test suites
- Synonym splitting produces correct row expansion with IUPAC protection
- All transformations logged to audit trail
</success_criteria>

<output>
After completion, create `.planning/phases/12-name-cleaning/12-01-SUMMARY.md`
</output>
