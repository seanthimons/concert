---
phase: 09-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/modules/mod_file_upload.R
  - R/modules/mod_data_preview.R
  - R/modules/mod_detection_info.R
  - R/modules/mod_raw_data.R
  - R/modules/mod_tag_columns.R
  - R/modules/mod_run_curation.R
  - R/modules/mod_review_results.R
autonomous: true
requirements:
  - MODL-01
  - MODL-02

must_haves:
  truths:
    - "All 7 module files exist in R/modules/ with mod_X_ui() and mod_X_server() functions"
    - "Each module uses NS() for namespace isolation in UI and moduleServer() in server"
    - "Module server functions accept data_store as parameter and return reactive lists where needed"
    - "Navigation callbacks are accepted as function parameters, not hardcoded"
  artifacts:
    - path: "R/modules/mod_file_upload.R"
      provides: "Sidebar upload controls + detection mode + file processing logic"
      contains: "mod_file_upload_ui"
    - path: "R/modules/mod_data_preview.R"
      provides: "Data Preview tab (summary cards + filtered data table)"
      contains: "mod_data_preview_ui"
    - path: "R/modules/mod_detection_info.R"
      provides: "Detection Info tab (detection metadata display)"
      contains: "mod_detection_info_ui"
    - path: "R/modules/mod_raw_data.R"
      provides: "Raw Data tab (first 20 rows table)"
      contains: "mod_raw_data_ui"
    - path: "R/modules/mod_tag_columns.R"
      provides: "Tag Columns tab (column type tagging UI + apply logic)"
      contains: "mod_tag_columns_ui"
    - path: "R/modules/mod_run_curation.R"
      provides: "Run Curation tab (curation execution + progress + stats)"
      contains: "mod_run_curation_ui"
    - path: "R/modules/mod_review_results.R"
      provides: "Review Results tab (resolution table + validation + error recovery)"
      contains: "mod_review_results_ui"
  key_links:
    - from: "R/modules/mod_file_upload.R"
      to: "R/file_handlers.R"
      via: "calls validate_file(), safely_read_file(), extract_clean_data()"
      pattern: "validate_file|safely_read_file|extract_clean_data"
    - from: "R/modules/mod_run_curation.R"
      to: "R/curation.R"
      via: "calls run_curation_pipeline()"
      pattern: "run_curation_pipeline"
    - from: "R/modules/mod_review_results.R"
      to: "R/consensus.R"
      via: "calls recalc_consensus_summary() and consensus functions"
      pattern: "recalc_consensus_summary|build_consensus"
---

<objective>
Extract all 6 existing tabs and the file upload sidebar into 7 Shiny module files in R/modules/.

Purpose: Create the module files that will replace inline code in app.R. Each module encapsulates one tab's UI and server logic using NS() namespacing and moduleServer(). This is the creation step — app.R is not modified yet (Plan 02 wires everything together).

Output: 7 module files in R/modules/, each containing mod_X_ui() and mod_X_server() functions.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-modularization/09-CONTEXT.md
@.planning/phases/09-modularization/09-RESEARCH.md
@app.R

<interfaces>
<!-- Current app.R structure that modules are extracted from -->

From app.R (lines 46-177) — Sidebar UI to extract into mod_file_upload:
- fileInput("file_upload", ...) — file upload control
- uiOutput("file_info") — file metadata display
- sliderInput("preview_rows", ...) — preview row count
- radioButtons("detection_mode", ...) — auto/manual detection
- conditionalPanel for manual_header_row numericInput
- actionButton("reset_btn", ...) — reset button
- Card "Column Selection" with checkboxGroupInput("selected_columns", ...) and select/deselect all buttons

From app.R (lines 184-197) — Data Preview tab UI:
- uiOutput("summary_cards") + DTOutput("data_table")

From app.R (lines 200-206) — Detection Info tab UI:
- uiOutput("detection_details")

From app.R (lines 209-219) — Raw Data tab UI:
- h4("Raw File Contents...") + DTOutput("raw_table")

From app.R (lines 222-256) — Tag Columns tab UI:
- conditionalPanel(!output.has_data) empty state
- conditionalPanel(output.has_data) with apply_tags button + uiOutput("column_tagging_ui")

From app.R (lines 259-296) — Run Curation tab UI:
- conditionalPanel(output.tags_applied) with curation_summary, run_curation button, priority_controls, curation_progress, curation_stats

From app.R (lines 298-387) — Review Results tab UI:
- conditionalPanel(output.curation_completed) with curation_table, validate_all, filter_errors, retag buttons

From app.R (lines 371-386) — recalc_consensus_summary() helper:
- Standalone function used only by Review Results — move into mod_review_results.R

From R/file_handlers.R — Functions used by upload module:
- validate_file(file_path, file_name)
- safely_read_file(file_path, file_name)
- extract_clean_data(raw_df, detection_result)
- handle_merged_cells(df)
- calculate_smart_preview_rows(n_rows)
- format_file_size(size_bytes)

From R/data_detection.R — Functions used by upload module:
- detect_data_start(raw_df, mode, manual_row)

From R/curation.R — Functions used by run_curation module:
- run_curation_pipeline(clean_data, column_tags, ...)

From R/consensus.R — Functions used by review_results module:
- build_consensus_table(results, ...)
- Various consensus helper functions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upload, data preview, detection info, and raw data modules</name>
  <files>
    R/modules/mod_file_upload.R
    R/modules/mod_data_preview.R
    R/modules/mod_detection_info.R
    R/modules/mod_raw_data.R
  </files>
  <action>
Create R/modules/ directory. Then create 4 module files by extracting code from app.R:

**mod_file_upload.R** — Extract sidebar UI (lines 84-177) and server logic (lines 493-740, 998-1070):
- `mod_file_upload_ui(id)`: Wrap all sidebar content in tagList with ns() on all input IDs (file_upload, preview_rows, detection_mode, manual_header_row, reset_btn, selected_columns, select_all_cols, deselect_all_cols). Add `ns = ns` to conditionalPanel for manual_header_row. The Column Selection conditionalPanel uses `output.has_data` — this needs ns-aware JavaScript: `condition = paste0("output['", ns("has_data"), "']")`.
- `mod_file_upload_server(id, data_store)`: Contains file upload processing (the `process_uploaded_file` internal function from lines 493-641), file_upload observeEvent (line 644), re-upload modal (cancel_reupload/confirm_reupload observers lines 666-684), detection mode observer (line 686), file_info renderUI (line 741), reset_btn observer (line 998), column selection logic (lines 1022-1070), has_data reactive output (line 1069).
- Returns a reactive list with: `file_processed`, `raw_data`, `clean_data`, `detection_info`, `file_info`, `preview_rows` (reactive wrapping input$preview_rows).
- The upload module WRITES to data_store directly for: raw, clean, detection, file_info, selected_columns — because it owns these fields (orchestration exception per Serapeum: upload is the single writer for these fields).
- Column selection (select_all, deselect_all, selected_columns observer) stays in this module since it's sidebar UI.

**mod_data_preview.R** — Extract Data Preview tab UI (lines 184-197) and server (lines 765-851):
- `mod_data_preview_ui(id)`: uiOutput(ns("summary_cards")) + div(DTOutput(ns("data_table")))
- `mod_data_preview_server(id, data_store)`: Contains summary_cards renderUI (line 765) and data_table renderDT (line 812). Needs to read data_store$clean, data_store$detection, data_store$file_info, data_store$selected_columns.
- The filtered_data reactive (lines 1057-1067) belongs here since it filters for display. Accept a `preview_rows` reactive argument from app.R (forwarded from upload module return).
- No return value needed.

**mod_detection_info.R** — Extract Detection Info tab UI (lines 200-206) and server (lines 852-977):
- `mod_detection_info_ui(id)`: uiOutput(ns("detection_details"))
- `mod_detection_info_server(id, data_store)`: Contains detection_details renderUI (line 852). Reads data_store$detection.
- No return value needed.

**mod_raw_data.R** — Extract Raw Data tab UI (lines 209-219) and server (line 979-996):
- `mod_raw_data_ui(id)`: h4(...) + div(DTOutput(ns("raw_table")))
- `mod_raw_data_server(id, data_store)`: Contains raw_table renderDT (line 979). Reads data_store$raw.
- No return value needed.

**Key patterns for ALL modules:**
- Use `ns <- NS(id)` at top of UI function
- Wrap ALL input/output IDs with `ns()` in UI
- Use `moduleServer(id, function(input, output, session) { ... })` in server
- Use `req()` guards on data_store fields before accessing
- Keep all existing CSS classes, Bootstrap styling, and conditional panels intact
- For conditionalPanel that reference `output.X`, use ns-aware condition: `condition = paste0("output['", ns("X"), "']")`
  </action>
  <verify>
    <automated>ls R/modules/mod_file_upload.R R/modules/mod_data_preview.R R/modules/mod_detection_info.R R/modules/mod_raw_data.R && grep -c "mod_.*_ui\|mod_.*_server\|moduleServer\|NS(id)" R/modules/mod_file_upload.R R/modules/mod_data_preview.R R/modules/mod_detection_info.R R/modules/mod_raw_data.R</automated>
  </verify>
  <done>4 module files exist in R/modules/ with proper mod_X_ui() and mod_X_server() functions using NS() and moduleServer(). Each module's UI wraps all IDs with ns(). Each module's server reads from data_store and uses req() guards.</done>
</task>

<task type="auto">
  <name>Task 2: Create tag columns, run curation, and review results modules</name>
  <files>
    R/modules/mod_tag_columns.R
    R/modules/mod_run_curation.R
    R/modules/mod_review_results.R
  </files>
  <action>
Create 3 module files by extracting the complex interactive tab code from app.R:

**mod_tag_columns.R** — Extract Tag Columns tab UI (lines 222-256) and server (lines 1074-1180):
- `mod_tag_columns_ui(id)`: Both conditionalPanels (empty state + tagging interface). NS-wrap: column_tagging_ui output, apply_tags button, has_data conditionalPanel condition. Use `condition = paste0("output['", ns("has_data"), "']")` pattern.
- `mod_tag_columns_server(id, data_store, on_tags_applied = NULL)`: Contains column_tagging_ui renderUI (line 1077), apply_tags observeEvent (line 1121). Accept `on_tags_applied` callback function for navigation after tags applied.
- The apply_tags handler writes to data_store$column_tags (this module owns that field).
- Also contains: has_data reactive output (if not in upload module — check if tag_columns needs its own), tags_applied reactive output (line 1178).
- When apply_tags succeeds, call `on_tags_applied()` if not NULL.
- Returns list with `tags_applied` reactive for app.R to observe.

**mod_run_curation.R** — Extract Run Curation tab UI (lines 259-296) and server (lines 1183-1420):
- `mod_run_curation_ui(id)`: conditionalPanel(output.tags_applied) wrapping all curation controls. NS-wrap: curation_summary, run_curation button, curation_progress, curation_stats, priority_controls outputs, tags_applied conditionalPanel.
- `mod_run_curation_server(id, data_store, on_curation_complete = NULL)`: Contains curation_summary renderUI (line 1184), run_curation observeEvent (line 1227), curation_completed reactive output (line 1335), curation_progress renderUI (line 1341), curation_stats renderUI (line 1379).
- Accept `on_curation_complete` callback for showing review_results tab after curation finishes.
- The run_curation handler writes to data_store curation fields (curation_results, curation_report, curation_status, dedup_preview, consensus_data, consensus_summary, resolution_state, dtxsid_cols, priority_order).
- Contains tags_applied reactive output (mirrors the tag module's state check on data_store$column_tags).
- Returns list with `curation_completed` reactive.

**mod_review_results.R** — Extract Review Results tab UI (lines 298-387) and server (lines 1422-2272):
- Move `recalc_consensus_summary()` helper (lines 371-386) INTO this module file as a module-internal function (not exported). It's only used here.
- `mod_review_results_ui(id)`: conditionalPanel(output.curation_completed) wrapping the entire review interface. NS-wrap: curation_table, validate_all, filter_errors, retag_selected, apply_retag_trigger buttons, and all output IDs.
- `mod_review_results_server(id, data_store)`: Contains ALL review results server logic (lines 1422-2272): curation_table renderDT (line 1422), priority_controls renderUI (line 1731), priority up/down observers (lines 1779-1804), cell_edit observer (line 1807), resolve_row_choice observer (line 1855), validate_all observer (line 1905), apply_priority observer (line 1990), error recovery observers (lines 2107-2272: filter_errors, retag_selected, apply_retag_trigger).
- Contains curation_completed reactive output.
- This is the largest module (~850 lines). That's acceptable for now — further decomposition would be premature.

**Key patterns for ALL modules:**
- The resolve-select JavaScript in app.R (lines 63-71) sets `Shiny.setInputValue('resolve_row_choice', ...)`. In the module, this input needs namespace-aware JavaScript. Update the JS in the module's UI to use `Shiny.setInputValue(ns('resolve_row_choice'), ...)` — but since ns() is R-side, generate the JS string with the namespaced ID: `tags$script(HTML(sprintf("$(document).on('change', '.resolve-select', function() { var row = $(this).data('row'); var column = $(this).val(); if (column && column !== '') { Shiny.setInputValue('%s', {row: row, column: column}, {priority: 'event'}); } });", ns("resolve_row_choice"))))`.
- For the retag modal (if any modal uses hardcoded IDs), ensure all modal input IDs are ns-wrapped.
- conditionalPanel conditions referencing outputs must use ns-aware pattern.
  </action>
  <verify>
    <automated>ls R/modules/mod_tag_columns.R R/modules/mod_run_curation.R R/modules/mod_review_results.R && grep -c "mod_.*_ui\|mod_.*_server\|moduleServer\|NS(id)" R/modules/mod_tag_columns.R R/modules/mod_run_curation.R R/modules/mod_review_results.R</automated>
  </verify>
  <done>3 module files exist in R/modules/ with proper mod_X_ui() and mod_X_server() functions. Review results module contains recalc_consensus_summary() as internal function. All JavaScript Shiny.setInputValue calls use namespaced IDs. All conditionalPanel conditions are ns-aware.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. All 7 files exist: `ls R/modules/mod_*.R` shows 7 files
2. Each file contains both UI and server functions: `grep -l "mod_.*_ui.*<-.*function\|mod_.*_server.*<-.*function" R/modules/mod_*.R`
3. All use NS() and moduleServer(): `grep -l "NS(id)\|moduleServer" R/modules/mod_*.R` shows 7 files
4. No module imports from app.R (modules are standalone): `grep -l "source.*app.R" R/modules/mod_*.R` returns empty
</verification>

<success_criteria>
- 7 module files in R/modules/ directory
- Each file has mod_X_ui(id) and mod_X_server(id, data_store, ...) functions
- All UI IDs wrapped with ns()
- All conditionalPanel conditions are ns-aware
- JavaScript Shiny.setInputValue calls use namespaced IDs
- recalc_consensus_summary() moved into mod_review_results.R
- Module server functions use moduleServer() pattern
- Navigation callbacks accepted as function parameters
</success_criteria>

<output>
After completion, create `.planning/phases/09-modularization/09-01-SUMMARY.md`
</output>
