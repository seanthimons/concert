---
phase: 15-post-curation-qc
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - R/modules/mod_review_results.R
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: ["POST-01", "POST-02"]
must_haves:
  truths:
    - "App starts without error after icon fix"
    - "Re-run QC button renders and is clickable in Review Results tab"
    - "POST-01 is documented as satisfied by existing pipeline per user decision"
  artifacts:
    - path: "R/modules/mod_review_results.R"
      provides: "Fixed icon on Re-run QC button"
      contains: "icon"
    - path: ".planning/REQUIREMENTS.md"
      provides: "POST-01 marked complete with rationale"
      contains: "Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 15 success criteria updated to reflect POST-01 coverage"
  key_links:
    - from: "R/modules/mod_review_results.R"
      to: "shiny icon wrapper"
      via: "icon wrapper instead of bsicons bs_icon"
      pattern: "icon.*arrows"
---

<objective>
Fix two gaps from Phase 15 verification: (1) a blocker where bsicons::bs_icon() in an actionButton crashes the app on startup, and (2) document that POST-01 (CAS re-validation) is satisfied by the existing pipeline per the user's locked decision in CONTEXT.md.

Purpose: Unblock the app from crashing and close out POST-01 requirement traceability.
Output: Working Re-run QC button, updated REQUIREMENTS.md and ROADMAP.md reflecting POST-01 coverage.
</objective>

<execution_context>
@C:/Users/sxthi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sxthi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-post-curation-qc/15-CONTEXT.md
@.planning/phases/15-post-curation-qc/15-VERIFICATION.md

Key context: CONTEXT.md locked decision states "CAS Re-Validation: Not Needed — POST-01 is already satisfied by the existing pipeline: pre-curation CAS cleaning (Phase 11) validates user-uploaded CAS values, and CompTox API returns are authoritative (server-side validated, never ships invalid CAS-RNs)."

<interfaces>
From R/modules/mod_review_results.R line 91-96 (CURRENT - broken):
```r
actionButton(
  ns("rerun_qc"),
  "Re-run QC",
  icon = bsicons::bs_icon("arrow-repeat"),
  class = "btn-sm btn-outline-secondary"
)
```

Other working actionButtons in same module use icon() wrapper:
```r
icon = icon("tags")
icon = icon("check")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix icon crash and verify app starts</name>
  <files>R/modules/mod_review_results.R</files>
  <action>
Fix the icon validation error on line 94 of R/modules/mod_review_results.R.

Change:
```r
icon = bsicons::bs_icon("arrow-repeat"),
```
to:
```r
icon = icon("arrows-rotate"),
```

Note: Using `icon("arrows-rotate")` which is the Font Awesome 6 equivalent of "arrow-repeat". The `icon()` wrapper from Shiny is required for actionButton icons — `bsicons::bs_icon()` returns an HTML tag that fails Shiny's `validateIcon()` check.

After making the change, run the Shiny smoke test to verify the app starts without error:
```bash
"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)"
```
Wait for "Listening on" message, then kill the process. If the icon name is invalid, try alternatives: `icon("rotate")`, `icon("redo")`, or `icon("refresh")`.
  </action>
  <verify>
    <automated>"C:\Program Files\R\R-4.5.1\bin\Rscript.exe" -e "shiny::runApp('app.R', port=3838, launch.browser=FALSE)" starts successfully (shows "Listening on" without error)</automated>
  </verify>
  <done>App starts without error, Re-run QC button renders with a valid icon in Review Results tab</done>
</task>

<task type="auto">
  <name>Task 2: Document POST-01 as satisfied by existing pipeline</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Update REQUIREMENTS.md and ROADMAP.md to reflect the user's locked decision that POST-01 is already satisfied by the existing pipeline (Phase 11 CAS validation + CompTox API server-side validation). Per user decision in CONTEXT.md — this is NOT implementing new code, it is documenting coverage.

In .planning/REQUIREMENTS.md:
1. Change POST-01 line from `- [ ] **POST-01**: User can see resolved CAS-RNs re-validated after curation, with any invalid CAS flagged` to `- [x] **POST-01**: User can see resolved CAS-RNs re-validated after curation, with any invalid CAS flagged`
2. In the Traceability table, change POST-01 status from `Pending` to `Complete (covered by Phase 11 CAS validation + CompTox API server-side validation)`

In .planning/ROADMAP.md:
1. Update Phase 15 success criteria #2 to clarify coverage: Change "User can see resolved CAS-RNs from CompTox re-validated after curation, with any invalid CAS flagged in Review Results table" to "User can see resolved CAS-RNs validated (pre-curation by Phase 11 CAS pipeline + CompTox API server-side validation; no additional post-curation re-validation needed per user decision)"
2. Add plan 15-03 to the Plans list under Phase 15
  </action>
  <verify>
    <automated>grep -c "POST-01.*Complete\|POST-01.*\[x\]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>POST-01 marked complete in REQUIREMENTS.md with rationale. ROADMAP.md success criteria clarified. Traceability table updated.</done>
</task>

</tasks>

<verification>
1. App starts without error (smoke test passes)
2. POST-01 is marked [x] complete in REQUIREMENTS.md
3. ROADMAP.md Phase 15 success criteria #2 reflects existing pipeline coverage
4. All existing tests still pass: `Rscript -e "source('load_packages.R'); testthat::test_dir('tests')"`
</verification>

<success_criteria>
- App starts without crashing (icon fix resolves the blocker)
- Re-run QC button is functional with valid icon
- POST-01 requirement closed with documentation per user's locked CONTEXT.md decision
- No regressions in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/15-post-curation-qc/15-03-SUMMARY.md`
</output>
