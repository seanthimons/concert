# Requirements: v1.8 R Package Migration

**Milestone goal:** Convert ChemReg into a proper R package so the cleaning and curation pipeline can be used headlessly from scripts without launching the Shiny UI.

**Source:** R_PACKAGE_MIGRATION.md analysis + milestone scoping (2026-04-13)

---

## v1 Requirements

### Package Scaffolding

- [ ] **PKG-01**: Developer can run `library(chemreg)` after `devtools::install()` without errors
- [ ] **PKG-02**: `DESCRIPTION` file exists with correct Imports (dplyr, purrr, stringr, stringi, tibble, tidyr, readxl, readr, rio, janitor, rlang, tidyselect, fs, here, ComptoxR) and Suggests (shiny, bslib, bsicons, reactable, reactable.extras, shinyjs, writexl, testthat)
- [ ] **PKG-03**: `NAMESPACE` is generated by roxygen2 (all exported functions have `@export` tags, `devtools::document()` succeeds)

### Source File Cleanup

- [ ] **SRC-01**: `R/cleaning_pipeline.R` has no bare `library()` calls and all function calls use `::` notation or `@importFrom`
- [ ] **SRC-02**: `R/cleaning_reference.R` has no bare `library()` calls and all function calls use `::` notation or `@importFrom`
- [ ] **SRC-03**: `R/consensus.R` has no bare `library()` calls and all function calls use `::` notation or `@importFrom`
- [ ] **SRC-04**: Package passes `devtools::check()` with no errors (warnings about non-standard files are acceptable)

### App Relocation

- [ ] **APP-01**: Shiny app lives at `inst/app/app.R` (moved from project root)
- [ ] **APP-02**: `R/run_app.R` exports `run_app()` that launches the app via `system.file("app", package = "chemreg")`
- [ ] **APP-03**: Reference cache loaded via `system.file("extdata", "reference_cache", package = "chemreg")` — no `here::here()` calls remain in package source files
- [ ] **APP-04**: `inst/app/app.R` has no `source()` loop (functions come from package namespace in installed mode; source loop only needed in dev mode)
- [ ] **APP-05**: User can launch the app via `chemreg::run_app()` after package install

### Headless Pipeline

- [ ] **HDL-01**: `curate_headless(input_path, output_path, tag_map, skip_flags)` is an exported function in the package
- [ ] **HDL-02**: `curate_headless()` runs the full pipeline: file read → frontmatter detect → clean data → cleaning pipeline → dedup → CompTox search → consensus → XLSX export
- [ ] **HDL-03**: `curate_headless()` produces a valid XLSX output when run against `uncurated_sswqs.csv`
- [ ] **HDL-04**: `curate_headless()` returns a list with `data` and `audit_trail` invisibly

### Test Migration

- [ ] **TST-01**: `tests/testthat/` directory structure exists with `tests/testthat.R` runner (`library(testthat); library(chemreg); test_check("chemreg")`)
- [ ] **TST-02**: All existing test files renamed from `tests/test_*.R` → `tests/testthat/test-*.R` (dash convention)
- [ ] **TST-03**: Pre-existing failure in `test_cleaning_reference.R` (expects 3 keys from `load_all_reference_lists`, gets 4 including `strip_terms`) is fixed
- [ ] **TST-04**: `devtools::test()` passes with all tests green

---

## Future Requirements

*(Deferred from v1.8 scope)*

- Vignette: Getting Started guide for headless usage
- `pkgdown` documentation site
- CI/CD: GitHub Actions for R CMD check on push
- Parallel / batched headless processing for large inventories

---

## Out of Scope

*(Explicitly excluded from v1.8)*

- Converting to a CRAN-ready package (not the goal — internal/dev use)
- Splitting into multiple packages (chemreg.core + chemreg.shiny)
- Breaking changes to existing Shiny workflow or module APIs

---

## Traceability

*(Filled by roadmapper)*

| REQ-ID | Phase |
|--------|-------|
| PKG-01 | TBD |
| PKG-02 | TBD |
| PKG-03 | TBD |
| SRC-01 | TBD |
| SRC-02 | TBD |
| SRC-03 | TBD |
| SRC-04 | TBD |
| APP-01 | TBD |
| APP-02 | TBD |
| APP-03 | TBD |
| APP-04 | TBD |
| APP-05 | TBD |
| HDL-01 | TBD |
| HDL-02 | TBD |
| HDL-03 | TBD |
| HDL-04 | TBD |
| TST-01 | TBD |
| TST-02 | TBD |
| TST-03 | TBD |
| TST-04 | TBD |
