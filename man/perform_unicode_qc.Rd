% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_pipeline.R
\name{perform_unicode_qc}
\alias{perform_unicode_qc}
\title{Perform post-curation Unicode QC (read-only detection)}
\usage{
perform_unicode_qc(df)
}
\arguments{
\item{df}{Dataframe to scan (typically post-curation resolution_state)}
}
\value{
List with: rows_with_non_ascii (integer), row_indices (integer vector),
unhandled_chars (named list keyed by "U+XXXX")
}
\description{
Scans a dataframe for non-ASCII characters without modifying the data.
This is a QC function for post-curation detection of Unicode that may
not have been handled by ComptoxR::clean_unicode.
}
\details{
Returns a report of:
\itemize{
\item How many rows contain non-ASCII characters
\item Which row indices have non-ASCII
\item What specific Unicode characters were found (with codepoints and counts)
}
}
\examples{
df <- tibble::tibble(name = c("acetone", "\u03B1-tocopherol"))
result <- perform_unicode_qc(df)
result$rows_with_non_ascii  # => 1
result$row_indices  # => c(2)
result$unhandled_chars[["U+03B1"]]$count  # => 1
}
