% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curation.R
\name{enrich_synonyms}
\alias{enrich_synonyms}
\title{Fetch and cache CompTox synonym data for a set of DTXSIDs}
\usage{
enrich_synonyms(dtxsids, existing_cache = NULL)
}
\arguments{
\item{dtxsids}{Character vector of DTXSIDs to enrich with synonyms}

\item{existing_cache}{Optional tibble from a previous enrichment call.
If it already has a \code{synonyms} column, DTXSIDs already cached will
not be re-fetched. If the \code{synonyms} column is absent, all DTXSIDs
are treated as needing fetch.}
}
\value{
Named list with:
\itemize{
\item cache: tibble with all original columns plus a \code{synonyms} column
(pipe-joined string per DTXSID; NA_character_ if API returned no synonyms)
\item failed_dtxsids: character vector of DTXSIDs that could not be fetched
}
}
\description{
Extends the enrichment cache with a \code{synonyms} column containing pipe-joined
synonym strings from all CompTox synonym tiers. Follows the same incremental
caching pattern as \code{enrich_candidates()}: only fetches DTXSIDs not
already present in the cache with a \code{synonyms} column.
}
