% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_pipeline.R
\name{normalize_cas_fields}
\alias{normalize_cas_fields}
\title{Normalize CAS fields using ComptoxR}
\usage{
normalize_cas_fields(df, tag_map)
}
\arguments{
\item{df}{Dataframe with CAS columns}

\item{tag_map}{Named list mapping column names to types ("CASRN", "Name", "Other")}
}
\value{
List with cleaned_data (tibble), audit_trail (tibble), and new_tags (generated CASRN columns)
}
\description{
Applies ComptoxR::as_cas() to all CASRN-tagged columns:
\itemize{
\item Converts unformatted CAS (e.g., "67641") to standard format ("67-64-1")
\item Converts placeholder text ("no cas", "n/a", "proprietary", "-") to NA
\item Validates checksums and sets invalid CAS to NA
}
}
\examples{
df <- tibble::tibble(cas = c("67641", "no cas", "67-64-2"))
tag_map <- list(cas = "CASRN")
normalize_cas_fields(df, tag_map)
}
