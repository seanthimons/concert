% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_cas.R
\name{is_cas}
\alias{is_cas}
\title{Checks if a string is a syntactically and algorithmically valid CASRN}
\usage{
is_cas(x)
}
\arguments{
\item{x}{A character vector of potential CASRN strings.}
}
\value{
A logical vector of the same length as \code{x}. Returns \code{TRUE} for
each valid CASRN, \code{FALSE} for invalid ones, and \code{NA} for \code{NA} inputs.
}
\description{
A valid CAS Registry Number (CASRN) is a string in the format "dd...d-dd-d",
containing 5 to 10 digits in total. The final digit is a check-digit
calculated using a specific algorithm. This function validates both the
format and the check-digit calculation.
}
\examples{
is_cas("50-00-0")  # Valid
is_cas("50-00-1")  # Invalid check digit
is_cas("7732-18-5") # Valid
is_cas("100-42-5")  # Valid
is_cas("not-a-casrn")
is_cas(c("50-00-0", "50-00-1", NA, "7732-18-5"))
}
