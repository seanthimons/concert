% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_pipeline.R
\name{run_cleaning_pipeline}
\alias{run_cleaning_pipeline}
\title{Run complete cleaning pipeline with audit trail tracking}
\usage{
run_cleaning_pipeline(
  df,
  tag_map = NULL,
  reference_lists = NULL,
  use_dedup = TRUE
)
}
\arguments{
\item{df}{Dataframe to clean}

\item{tag_map}{Optional named list mapping column names to types ("CASRN", "Name", "Other")}

\item{reference_lists}{Optional list of reference data (unused in Phase 11, reserved for future)}

\item{use_dedup}{Logical. When TRUE (default), uses dedup_step() wrappers
and pre-check predicates for performance optimization. Set to FALSE for
benchmark comparison against the non-dedup baseline path.}
}
\value{
List with cleaned_data (tibble), audit_trail (tibble), and new_tags (list)
}
\description{
Orchestrates multiple cleaning steps:
0. Row lineage injection (always)
\enumerate{
\item Unicode to ASCII conversion
\item Whitespace and punctuation artifact stripping
\item CAS normalization (if tag_map provided)
\item CAS rescue from text (if tag_map provided)
\item Multi-CAS detection (if tag_map provided)
}
}
\details{
Each step generates an audit trail. Final audit trail combines all changes.
}
\examples{
df <- tibble::tibble(chemical_name = c("  acetone  ", "cafe\u0301"))
result <- run_cleaning_pipeline(df)
result$cleaned_data  # => tibble with cleaned values
result$audit_trail   # => tibble with change records

# With CAS processing
df <- tibble::tibble(cas = c("67641", "no cas"), name = c("acetone", "ethanol 64-17-5"))
tag_map <- list(cas = "CASRN", name = "Name")
result <- run_cleaning_pipeline(df, tag_map)
result$new_tags  # => list(cas_extract_name = "CASRN")
}
