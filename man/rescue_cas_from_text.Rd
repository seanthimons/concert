% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_pipeline.R
\name{rescue_cas_from_text}
\alias{rescue_cas_from_text}
\title{Rescue CAS-RNs from non-CASRN text columns}
\usage{
rescue_cas_from_text(df, tag_map)
}
\arguments{
\item{df}{Dataframe with tagged columns}

\item{tag_map}{Named list mapping column names to types ("CASRN", "Name", "Other")}
}
\value{
List with cleaned_data, audit_trail, and new_tags (mapping cas_extract columns to "CASRN")
}
\description{
Uses extract_cas() to find CAS-RNs embedded in Name/Other columns.
Extracted CAS values are placed in new \verb{cas_extract_\{source\}} columns.
Source text is stripped of the CAS pattern.
}
\examples{
df <- tibble::tibble(name = c("acetone (67-64-1)", "water"))
tag_map <- list(name = "Name")
rescue_cas_from_text(df, tag_map)
}
