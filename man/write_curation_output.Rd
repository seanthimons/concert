% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_helpers.R
\name{write_curation_output}
\alias{write_curation_output}
\title{Write Curation Output in a Requested Format}
\usage{
write_curation_output(
  path,
  format = c("xlsx", "csv", "parquet"),
  sheets = NULL,
  toxval_tibble = NULL
)
}
\arguments{
\item{path}{Destination file path.}

\item{format}{One of "xlsx", "csv", or "parquet". "xlsx" writes the full
multi-sheet workbook and requires \code{sheets}. "csv"/"parquet" write the flat
ToxVal table and require \code{toxval_tibble}.}

\item{sheets}{Named list of data frames from \code{\link[=build_export_sheets]{build_export_sheets()}}. Used
only when \code{format = "xlsx"}.}

\item{toxval_tibble}{Flat 56-column ToxVal tibble from
\code{\link[=map_to_toxval_schema]{map_to_toxval_schema()}}. Used only when \code{format} is "csv" or "parquet".}
}
\value{
The \code{path}, invisibly.
}
\description{
Single dispatch point for writing CONCERT export output. Keeps the
"format -> writer + what content" decision in one place so the Shiny app
and the headless pipeline cannot drift.
}
