% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curate_iterate.R
\name{curate_iterate}
\alias{curate_iterate}
\title{Run one iteration of the agent curation loop}
\usage{
curate_iterate(
  decisions_path,
  out_dir = dirname(decisions_path),
  verbose = TRUE
)
}
\arguments{
\item{decisions_path}{Path to a decisions file. Create one with
\code{\link[=curate_decisions_template]{curate_decisions_template()}}.}

\item{out_dir}{Output directory. Defaults to the decisions file directory.}

\item{verbose}{Logical. Print pipeline messages.}
}
\value{
Invisibly, a list with \code{done}, \code{pending}, and \code{state}.
}
\description{
Sources \code{decisions.R}, runs the staged headless pipeline with a curation
cache, and writes \code{status.md}, \code{pending.csv}, and \code{replay.R} next to it.
When no rows are pending it also writes the curated workbook (and ToxVal
files when \code{harmonize} is set). Re-run after editing \code{decisions.R} until
\code{status.md} reports \code{done: TRUE}.
}
