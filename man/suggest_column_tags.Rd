% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto_tag_columns.R
\name{suggest_column_tags}
\alias{suggest_column_tags}
\title{Suggest Column Tags From Header Names}
\usage{
suggest_column_tags(col_names)
}
\arguments{
\item{col_names}{Character vector of column names (post-\code{clean_names()}).}
}
\value{
A named list, one element per input column (1:1, order preserved),
whose value is a tag type (e.g. \code{"CASRN"}, \code{"Name"}, \code{"Result"}) or \code{""}
when no confident match is found. Returns an empty named list for
zero-length input. Returned as a list (not an atomic vector) so callers can
safely use \code{suggestions[[col]] \%||\% ""} without a subscript-out-of-bounds
error on missing keys.
}
\description{
Heuristically guesses a tag type for each column from its (already
\code{janitor::clean_names()}-normalized) header name. This powers the
"suggest, don't auto-apply" pre-fill in the Tag Columns step: the returned
suggestions seed the dropdowns, but the user always reviews and applies.
}
\details{
The matcher is \strong{name-only} (no cell-value sampling) and \strong{precision-first}:
it matches on whole tokens, prefers the most specific (longest) keyword
phrase, drops dangerous generic tokens (bare \code{name}, \code{date}, \code{value}, \code{id},
\code{exposure}), and emits at most one suggestion for the singular chemical tags
(\code{Name}, \code{CASRN}).

Every emitted value is a member of the \code{classify_tags()} taxonomy
(\code{R/tag_helpers.R}); unmatched columns return \code{""}.
}
\examples{
suggest_column_tags(c("cas_number", "chemical_name", "supplier_name"))
# $cas_number    -> "CASRN"
# $chemical_name -> "Name"
# $supplier_name -> ""   (generic 'name' without a chemical qualifier)

}
