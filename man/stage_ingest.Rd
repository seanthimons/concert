% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curate_stages.R
\name{stage_ingest}
\alias{stage_ingest}
\title{Stage 1: read a file, detect frontmatter, and validate the tag map}
\usage{
stage_ingest(
  input_path,
  tag_map,
  header_row = NULL,
  reference_lists = NULL,
  reference_list_snapshot = NULL,
  activate_all_references = FALSE,
  site_manifest = NULL,
  site_alias_map = NULL
)
}
\arguments{
\item{input_path}{Character. Path to the input file (CSV, XLSX, or XLS).}

\item{tag_map}{Named list mapping cleaned column names to tag roles. Keys
must match column names \emph{after} \code{janitor::clean_names()} normalization.
Values are one of \code{"Name"}, \code{"CASRN"}, \code{"Other"}, \code{"Result"}, \code{"Numeric"}, \code{"Unit"},
\code{"Qualifier"}, \code{"ReportingLimit"}, \code{"Uncertainty"},
\code{"UncertaintyCoverage"}, etc. Example:
\code{list(chemical_name = "Name", cas_number = "CASRN", result = "Result", unit = "Unit")}.}

\item{header_row}{Integer or NULL. If NULL (default), frontmatter detection
runs automatically. If an integer, that row number is used as the header
row (manual override).}

\item{reference_lists}{Named list of reference data. If NULL (default), the
package-bundled reference cache is loaded automatically. If provided, must
contain keys: \code{stop_words}, \code{functional_categories}, \code{block_patterns},
\code{strip_terms}, \code{isotope_lookup}.}

\item{reference_list_snapshot}{Optional compact replay snapshot generated by
\code{generate_concert_script()}. When supplied, package defaults are loaded
without local \code{user_reference_lists.rds} overrides, verified against the
snapshot hashes, and merged with snapshot overrides.}

\item{activate_all_references}{Logical. If TRUE, activates all cleaning
reference-list rows for this run only.}

\item{site_manifest}{Optional curated Dataset Context site manifest to include
in the workbook export.}

\item{site_alias_map}{Optional Dataset Context raw-label alias map to include
in the workbook export.}
}
\value{
A curation state list. See \code{\link[=curate_headless]{curate_headless()}} for the composed run.
}
\description{
Stage 1: read a file, detect frontmatter, and validate the tag map
}
