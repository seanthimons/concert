% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curate_stages.R
\name{stage_harmonize}
\alias{stage_harmonize}
\title{Stage 5: run numeric, unit, media, and ToxVal harmonization}
\usage{
stage_harmonize(
  state,
  harmonize = FALSE,
  unit_map = NULL,
  unit_map_snapshot = NULL,
  corrections = NULL,
  media_map = NULL,
  media_map_snapshot = NULL,
  media = NULL,
  source_name = NULL
)
}
\arguments{
\item{state}{State list from \code{\link[=stage_review]{stage_review()}}.}

\item{harmonize}{Logical. If TRUE, runs numeric parsing, unit harmonization,
and ToxVal schema mapping after curation. Default FALSE for backward compat.}

\item{unit_map}{Tibble with unit conversion mappings, or NULL (default) to
load from package cache via load_unit_map().}

\item{unit_map_snapshot}{Optional compact replay snapshot from
\code{generate_concert_script()}; reconstructs the effective unit map from
package defaults plus embedded deltas. Mutually exclusive with \code{unit_map}.}

\item{corrections}{Tibble with pattern/replacement columns for one-off
corrections, or NULL (default) to load from package cache.}

\item{media_map}{Optional media harmonization map passed to
\code{harmonize_media()}.}

\item{media_map_snapshot}{Optional compact replay snapshot from
\code{generate_concert_script()}; reconstructs the effective media map from
package defaults plus embedded deltas. Mutually exclusive with \code{media_map}.}

\item{media}{Character. Media context for ppb/ppm routing: "aqueous", "air",
or "solid". NULL (default) uses aqueous assumption.}

\item{source_name}{Optional dataset identifier for ToxVal \code{source}. Defaults
to the input filename stem.}
}
\value{
The state with \code{harmonize}, \code{harmonization_refs},
\code{harmonization_runtime_result}, \code{toxval_output}, \code{harmonize_audit}, and
\code{detection_results} added. When \code{harmonize = FALSE} only \code{harmonize} is set.
}
\description{
Stage 5: run numeric, unit, media, and ToxVal harmonization
}
