% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cleaning_pipeline.R
\name{resolve_review_row}
\alias{resolve_review_row}
\title{Resolve one flagged review row (multi-analyte and/or multi-CAS)}
\usage{
resolve_review_row(df, name_cols, row_index, spec, cas_cols = character(0))
}
\arguments{
\item{df}{Cleaned data frame.}

\item{name_cols}{Character vector of Name-tagged column names.}

\item{row_index}{One-based row position in \code{df}.}

\item{spec}{List describing the disposition:
\itemize{
\item \code{name_action}: \code{"split"}, \code{"keep"}, or \code{"rename"}.
\item \code{name_parts}: split parts or rename value (character or newline/\verb{;} text).
For split, \code{NULL}/empty falls back to \code{suggest_multi_analyte_parts()}.
\item \code{cas_parts}: CAS values to assign (character or newline/\verb{;} text). Ignored
unless \code{pairing = "position"}.
\item \code{pairing}: \code{"position"} pairs name/CAS parts one-to-one (counts must match,
or one side has length 1); \code{"broadcast"} (default) leaves CAS columns intact.
}}

\item{cas_cols}{Character vector of CASRN-tagged column names.}
}
\value{
List with \code{cleaned_data} and \code{audit_trail}.
}
\description{
Single coordinated engine behind \code{\link[=resolve_multi_analyte_row]{resolve_multi_analyte_row()}}. Splits the
selected Name value and, when \code{pairing = "position"}, assigns CAS values from
\code{spec$cas_parts} across the resulting rows so a row flagged for both reasons
cannot be split twice into garbage.
}
