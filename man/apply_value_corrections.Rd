% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value_corrections.R
\name{apply_value_corrections}
\alias{apply_value_corrections}
\title{Apply dataset-specific value corrections before cleaning}
\usage{
apply_value_corrections(df, value_corrections = NULL)
}
\arguments{
\item{df}{Data frame after frontmatter extraction and \code{janitor::clean_names()}.}

\item{value_corrections}{Data frame with columns \code{column}, \code{pattern},
\code{replacement}, and optional \code{match_mode} (one of \code{"regex"} (default),
\code{"literal_exact"}, or \code{"literal_word"}). Rows are applied in order.}
}
\value{
List with \code{cleaned_data} and \code{audit_trail} (step \code{value_correction}).
}
\description{
Rewrites cell values in named columns using a small pattern/replacement
table. This is the escape hatch for dataset quirks the reference lists
cannot express: a "Total " prefix on every analyte, a known-bad CAS, a
unit typo. Runs before the cleaning pipeline so every downstream step sees
the corrected value.
}
