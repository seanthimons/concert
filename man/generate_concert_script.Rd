% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/code_generation.R
\name{generate_concert_script}
\alias{generate_concert_script}
\title{Generate a CONCERT replay script}
\usage{
generate_concert_script(
  input_path,
  output_path,
  tag_map,
  header_row,
  review_overrides = NULL,
  wqx_threshold = 0.85,
  starts_with = FALSE,
  harmonize = FALSE,
  media = NULL,
  unit_map = NULL,
  corrections = NULL,
  media_map = NULL,
  format = "parquet",
  source_name = NULL,
  reference_lists = NULL,
  activate_all_references = FALSE,
  site_manifest = NULL,
  site_alias_map = NULL
)
}
\arguments{
\item{input_path}{Character input file path shown at the top of the script.}

\item{output_path}{Character output XLSX path shown at the top of the script.}

\item{tag_map}{Named list of chemical, numeric, metadata, and study tags.}

\item{header_row}{Detected header row to pin for replay.}

\item{review_overrides}{Optional content-match override spec from
\code{build_review_overrides()} to embed as generated per-column
\code{dplyr::rows_update()} override tables.}

\item{wqx_threshold}{WQX fuzzy match threshold.}

\item{starts_with}{Logical. Enables CompTox starts-with fallback search.}

\item{harmonize}{Logical. Re-run harmonization during replay.}

\item{media}{Optional dataset-wide media fallback.}

\item{unit_map}{Optional effective unit harmonization map. When
\code{harmonize = TRUE} it is snapshotted against package defaults so only user
deltas (plus a baseline hash) are embedded.}

\item{corrections}{Optional effective numeric corrections table to embed when
\code{harmonize = TRUE}.}

\item{media_map}{Optional effective media harmonization map. When
\code{harmonize = TRUE} it is snapshotted against package defaults so only user
deltas (plus a baseline hash) are embedded.}

\item{format}{ToxVal output format for harmonized headless runs.}

\item{source_name}{Optional source name for ToxVal mapping.}

\item{reference_lists}{Optional current cleaning reference lists to snapshot
for portable replay.}

\item{activate_all_references}{Logical. Replays the preflight setting that
activates all cleaning reference-list rows for the run.}

\item{site_manifest}{Optional curated Dataset Context site manifest to embed
in the replay script.}

\item{site_alias_map}{Optional Dataset Context raw-label alias map to embed
in the replay script.}
}
\value{
Complete R script as a character scalar.
}
\description{
Generate a CONCERT replay script
}
